# Phase 32.1: Output Schema Validation Gap - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-28
**Phase:** 32.1-output-schema-validation-gap
**Areas discussed:** Documentation location, D-08 update, Existing test overlap, jsonschema dependency

---

## Documentation Location

| Option | Description | Selected |
|--------|-------------|----------|
| CLAUDE.md only | Add imperative rules for future agents. Architecture.md already has the taxonomy. | ✓ |
| Both CLAUDE.md + architecture.md | CLAUDE.md gets rules, architecture.md gets a "Testing Invariants" section | |
| Architecture.md only | Relies on agents reading it proactively | |

**User's choice:** CLAUDE.md only
**Notes:** Agents always see CLAUDE.md. Architecture.md already has the naming taxonomy — no need to duplicate.

---

## D-08 Update

| Option | Description | Selected |
|--------|-------------|----------|
| Update architecture.md | Fix outdated references to interval=1 omission | |
| No update needed | — | ✓ |

**User's choice:** After review, architecture.md was found to be accurate — it says `interval` has `(default: 1)` and examples show interval explicitly. Nothing says "omitted when 1 in output." The outdated references were in Phase 32's CONTEXT.md and old model docstrings, not architecture.md. No update needed.

---

## Existing Test Overlap

| Option | Description | Selected |
|--------|-------------|----------|
| New test file + migrate | Create test_output_schema.py, move existing TestSerializationSchema there | ✓ |
| Expand in test_models.py | Add to existing class | |

**User's choice:** New file + migrate. User said "if it's going to blow up, create a new file — I just want things consistent." Confirmed it will blow up (6 tools with fixtures, naming tests, etc.)

---

## jsonschema Dependency

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit dev dep | Add to pyproject.toml dev group | ✓ |
| Transitive is fine | Rely on fastmcp/mcp chain | |

**User's choice:** Explicit dev dep
**Notes:** No extended discussion needed.

---

## Naming Convention Enforcement

Not presented as a formal gray area — emerged from conversation about the `FrequencySpec` naming violation in Phase 32. User initially asked whether it should be programmatic or documentation-only. After seeing a code sketch of the test, user decided to keep both: programmatic test + CLAUDE.md documentation rule.

**User's choice:** Both layers — programmatic test in test_output_schema.py + CLAUDE.md rules

---

## Phase Scope Clarification

User clarified mid-discussion that the `_FrequencyBase` serializer fix is NOT part of Phase 32.1 — they did it themselves before this phase starts. Phase 32.1 is purely about testing safeguards and documentation.

---

## Claude's Discretion

- Fixture construction strategy
- Schema-vs-data test structure (parametrized vs per-tool)
- How to extract outputSchema from FastMCP
- EXEMPT list for naming convention test
- CLAUDE.md rule wording

## Deferred Ideas

- Old test names still reference `FrequencySpec` — user will fix separately
- `cWQ63CD2pjo` impossible-state task — existing fail-fast is correct, not a schema concern
