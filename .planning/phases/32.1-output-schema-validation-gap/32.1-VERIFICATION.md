---
phase: 32.1-output-schema-validation-gap
verified: 2026-03-28T17:00:00Z
status: passed
score: 10/10 must-haves verified
re_verification:
  previous_status: passed
  previous_score: 5/5
  note: "Previous verification covered SC-1 through SC-5 only. This verification adds gap-closure must-haves (Plans 02 + 03) and confirms full phase completion."
  gaps_closed:
    - "from_completion + catchUpAutomatically=true maps to schedule='from_completion' without error"
    - "WeeklyFrequency bare serializes as {type:'weekly', interval:1} with no onDays field"
    - "WeeklyOnDaysFrequency uses type='weekly_on_days' with required on_days"
    - "Frequency union has 9 branches (was 8)"
    - "_derive_schedule logic extracted to single location (DRY)"
  gaps_remaining: []
  regressions: []
---

# Phase 32.1: Output Schema Validation Gap — Full Verification Report

**Phase Goal:** Add systemic test safeguards ensuring serialized tool output validates against MCP outputSchema, model naming conventions are programmatically enforced, and future agents have clear rules to follow. Additionally close two UAT gaps: (1) from_completion + catchUpAutomatically=true crash fix, (2) WeeklyFrequency onDays:null elimination via model split.
**Verified:** 2026-03-28
**Status:** passed
**Re-verification:** Yes — previous verification (Plans 01 only, SC-1..SC-5). This covers Plans 02 + 03 gap closures.

---

## Goal Achievement

### Observable Truths (SC-1..SC-5 from original phase, plus 5 gap-closure must-haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | All 6 MCP tools' serialized output validates against outputSchema via jsonschema | VERIFIED | `test_tool_output_validates_against_schema` passes for all 6 tools |
| SC-2 | Fixtures include DailyFrequency, WeeklyFrequency(onDays), MonthlyDayOfWeekFrequency(on), MonthlyDayInMonthFrequency(onDates), EndByDate, EndByOccurrences | VERIFIED | `_make_repetition_rule_dict` covers all required variants; `test_repetition_rule_variants_validate[weekly_bare]`, `[weekly_with_days]`, `[monthly_day_of_week]`, `[monthly_day_in_month]` all pass |
| SC-3 | No union branch degrades to `{"type":"object","additionalProperties":true}` | VERIFIED | `TestUnionRegressionGuard` — 3 tests; branch count guard now asserts == 9 |
| SC-4 | models/ has no write-side suffixes; contracts/ uses recognized suffixes | VERIFIED | `TestNamingConvention` — 2 package-scanning tests, both pass |
| SC-5 | CLAUDE.md directs agents to read naming taxonomy and run schema tests | VERIFIED | `## Model Conventions` section in CLAUDE.md with both directives |
| GC-1 | from_completion + catchUpAutomatically=true maps to schedule='from_completion' without error | VERIFIED | `derive_schedule("from_completion", True) == "from_completion"` — no ValueError; `TestDeriveSchedule::test_from_completion_with_catch_up_true` and `TestAdaptRepetitionRule::test_from_completion_with_catch_up_true` both pass |
| GC-2 | _derive_schedule logic exists in exactly one location (DRY) | VERIFIED | `src/omnifocus_operator/rrule/schedule.py` is the only definition; `grep "def _derive_schedule" src/` returns no matches; adapter.py and hybrid.py both import `derive_schedule` from `omnifocus_operator.rrule` |
| GC-3 | Bare weekly repetition rule serializes as {type:'weekly', interval:1} with no onDays field | VERIFIED | `WeeklyFrequency().model_dump(by_alias=True)` == `{'interval': 1, 'type': 'weekly'}` — confirmed in spot-check; `test_repetition_rule_variants_validate[weekly_bare]` passes |
| GC-4 | Weekly with specific days uses type 'weekly_on_days' and onDays is required (not optional) | VERIFIED | `WeeklyOnDaysFrequency` at line 64 of repetition_rule.py has `type: Literal["weekly_on_days"]` and `on_days: list[str]` (no default, required); `test_repetition_rule_variants_validate[weekly_with_days]` passes |
| GC-5 | Frequency union has 9 branches with const discriminator on each | VERIFIED | Union in repetition_rule.py has 9 members; `TypeAdapter(Frequency).json_schema()` returns 9 `$defs` entries; `TestUnionRegressionGuard::test_frequency_branches_have_properties_and_const` asserts `len(defs) == 9` and passes |

**Score:** 10/10 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_output_schema.py` | Schema validation, union regression guard, naming convention tests | VERIFIED | 18 tests pass (up from 16; weekly_bare + weekly_on_days added to parametrize) |
| `src/omnifocus_operator/rrule/schedule.py` | Single canonical derive_schedule function | VERIFIED | 29 lines; `derive_schedule(schedule_type, catch_up) -> str`; from_completion always returns "from_completion" regardless of catch_up |
| `src/omnifocus_operator/models/repetition_rule.py` | WeeklyFrequency (bare) + WeeklyOnDaysFrequency (9-branch union) | VERIFIED | WeeklyFrequency at line 60 has no on_days field; WeeklyOnDaysFrequency at line 64 has required on_days; Frequency union at lines 89-100 has 9 members |
| `src/omnifocus_operator/rrule/parser.py` | WEEKLY branch returns WeeklyOnDaysFrequency when BYDAY present | VERIFIED | Lines 101-105: `if "BYDAY" in parts` returns `WeeklyOnDaysFrequency`, else `WeeklyFrequency` |
| `src/omnifocus_operator/rrule/builder.py` | Handles both WeeklyFrequency and WeeklyOnDaysFrequency | VERIFIED | `_TYPE_TO_FREQ` has `"weekly_on_days": "WEEKLY"` at line 48; isinstance check uses `WeeklyOnDaysFrequency` at line 89 |
| `src/omnifocus_operator/bridge/adapter.py` | Calls shared derive_schedule (no local definition) | VERIFIED | Imports `derive_schedule` from `omnifocus_operator.rrule` at line 14; no `_derive_schedule` definition present |
| `src/omnifocus_operator/repository/hybrid.py` | Calls shared derive_schedule (no local definition) | VERIFIED | Imports `derive_schedule` from `omnifocus_operator.rrule` at line 30; no `_derive_schedule` definition present |
| `CLAUDE.md` | Agent rules for naming taxonomy and schema tests | VERIFIED | `## Model Conventions` section at lines 35-38 |
| `pyproject.toml` | jsonschema as dev dependency | VERIFIED | `jsonschema>=4.23.0` in `[dependency-groups] dev` |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `adapter.py` | `rrule/schedule.py` | `from omnifocus_operator.rrule import derive_schedule` | VERIFIED | Line 14 import confirmed; line 134 call site: `derive_schedule(schedule_mapped, catch_up)` |
| `repository/hybrid.py` | `rrule/schedule.py` | `from omnifocus_operator.rrule import derive_schedule` | VERIFIED | Line 30 import confirmed; line 229 call site: `derive_schedule(schedule_type, catch_up)` |
| `rrule/__init__.py` | `rrule/schedule.py` | `from omnifocus_operator.rrule.schedule import derive_schedule` | VERIFIED | Line 11 export confirmed; `derive_schedule` in `__all__` at line 13 |
| `rrule/parser.py` | `models/repetition_rule.py` | `import WeeklyOnDaysFrequency` | VERIFIED | Line 27 import confirmed; used at line 104 in WEEKLY branch |
| `rrule/builder.py` | `models/repetition_rule.py` | `import WeeklyOnDaysFrequency` | VERIFIED | Line 18 import confirmed; used at line 89 in isinstance check |
| `tests/test_output_schema.py` | Frequency union | `len(defs) == 9` assertion | VERIFIED | Line 332: `assert len(defs) == 9` passes |

---

## Data-Flow Trace (Level 4)

Not applicable — this phase produces test infrastructure, model definitions, and shared utilities. No components render dynamic data.

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 18 schema/convention tests pass | `uv run pytest tests/test_output_schema.py -v --no-cov` | 18 passed | PASS |
| derive_schedule gap-closure tests pass | `uv run pytest tests/test_rrule.py::TestDeriveSchedule tests/test_adapter.py -x -q --no-cov` | 190 passed (full modules) | PASS |
| WeeklyFrequency bare has no onDays field | `WeeklyFrequency().model_dump(by_alias=True)` | `{'interval': 1, 'type': 'weekly'}` — no onDays key | PASS |
| Frequency union has exactly 9 branches | `TypeAdapter(Frequency).json_schema()` | 9 `$defs` entries | PASS |
| No local `_derive_schedule` in src | `grep "def _derive_schedule" src/` | No matches | PASS |
| Full test suite — no regressions | `uv run pytest -x -q --no-cov` | 862 passed | PASS |

---

## Requirements Coverage

No formal requirement IDs assigned to this phase (inserted gap-closure phase). All success criteria from ROADMAP.md satisfied as documented in SC-1..SC-5 above. Gap-closure must-haves from Plans 02 and 03 satisfied as documented in GC-1..GC-5.

---

## Anti-Patterns Found

None.

- No `_derive_schedule` duplication in src (grep clean)
- No `WeeklyFrequency(on_days=...)` calls remaining anywhere in tests or src
- No placeholder returns, TODO/FIXME comments, or stub patterns in any modified file
- `on_days` field in `WeeklyOnDaysFrequency` is required (not `list[str] | None = None`)

---

## Human Verification Required

None. All success criteria and gap-closure must-haves are mechanically verifiable and confirmed via test execution and direct behavioral spot-checks.

---

## Gaps Summary

No gaps. Phase 32.1 is fully complete across all three plans:

- **Plan 01 (SC-1..SC-5):** Output schema validation, union regression guard, naming conventions, CLAUDE.md agent rules — all confirmed by 18 passing tests in `tests/test_output_schema.py`.
- **Plan 02 (GC-1..GC-2):** `from_completion + catchUpAutomatically=true` no longer crashes. `derive_schedule` extracted to `rrule/schedule.py`, both read paths import from shared location. `TestDeriveSchedule` (4 cases) + adapter integration test confirm behavior.
- **Plan 03 (GC-3..GC-5):** `WeeklyFrequency` bare serializes without `onDays` field. `WeeklyOnDaysFrequency` added with `type='weekly_on_days'` and required `on_days`. Frequency union expanded to 9 branches. Schema regression guard updated to assert 9 branches.

Full test suite: 862 tests pass, zero regressions.

---

_Verified: 2026-03-28_
_Verifier: Claude (gsd-verifier)_
