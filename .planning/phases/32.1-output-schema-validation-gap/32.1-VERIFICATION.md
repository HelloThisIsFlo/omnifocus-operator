---
phase: 32.1-output-schema-validation-gap
verified: 2026-03-28T14:30:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 32.1: Output Schema Validation Gap — Verification Report

**Phase Goal:** Add systemic test safeguards ensuring serialized tool output validates against MCP outputSchema, model naming conventions are programmatically enforced, and future agents have clear rules to follow
**Verified:** 2026-03-28
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Success Criteria SC-1 through SC-5)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | All 6 MCP tools' serialized output validates against outputSchema via jsonschema | VERIFIED | `TestSchemaValidation::test_tool_output_validates_against_schema` passes for all 6 tools |
| SC-2 | Fixtures include DailyFrequency, WeeklyFrequency(onDays), MonthlyDayOfWeekFrequency(on), MonthlyDayInMonthFrequency(onDates), EndByDate, EndByOccurrences | VERIFIED | All 4 frequency variants and both end conditions present in `_make_repetition_rule_dict`; 4 parametrized `test_repetition_rule_variants_validate` and `test_end_conditions_validate` all pass |
| SC-3 | No union type branch in Frequency or EndCondition degrades to `{"type":"object","additionalProperties":true}` | VERIFIED | `TestUnionRegressionGuard` — 3 tests covering Frequency (8 branches), EndCondition (2 branches), and broad catch-all across all tool schemas |
| SC-4 | models/ has no write-side suffixes; contracts/ uses recognized suffixes | VERIFIED | `TestNamingConvention` — 2 tests using `pkgutil.walk_packages` + `inspect.getmembers`; both pass against current codebase |
| SC-5 | CLAUDE.md directs agents to read naming taxonomy before creating models and to run schema tests after modifying output models | VERIFIED | `## Model Conventions` section present at line 35 of CLAUDE.md with both required rules |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_output_schema.py` | All schema validation, union regression guard, and naming convention tests | VERIFIED | 441 lines, contains `TestSchemaValidation`, `TestUnionRegressionGuard`, `TestNamingConvention`; all 16 tests pass |
| `pyproject.toml` | jsonschema as explicit dev dependency | VERIFIED | Line 27: `"jsonschema>=4.23.0,"` in `[dependency-groups] dev` |
| `CLAUDE.md` | Agent rules for naming taxonomy and schema tests | VERIFIED | `## Model Conventions` section at lines 35-38 with `docs/architecture.md` reference and `tests/test_output_schema.py` invocation |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `tests/test_output_schema.py` | `src/omnifocus_operator/models/` | `TypeAdapter.json_schema(mode='serialization')` + `pydantic_core.to_jsonable_python` + `jsonschema.validate` | VERIFIED | All three call patterns confirmed present in file; 11 `jsonschema.validate` calls execute without error |
| `tests/test_output_schema.py` | `fastmcp.utilities.json_schema` | `compress_schema(prune_titles=True)` | VERIFIED | Imported at line 19; used at lines 60 and 68 in `get_output_schema` and `get_wrapped_output_schema` |
| `tests/test_output_schema.py` | `src/omnifocus_operator/models/` and `contracts/` | `pkgutil.walk_packages` + `inspect.getmembers` | VERIFIED | `_scan_package_models` at lines 391-407; called by both naming convention tests |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase produces test infrastructure and documentation, not components that render dynamic data.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 16 schema/convention tests pass | `uv run pytest tests/test_output_schema.py -v --no-cov` | 16 passed in 2.23s | PASS |
| No regression in test_models.py after migration | `uv run pytest tests/test_models.py --no-cov` | 83 passed | PASS |
| Full suite still green | `uv run pytest -x -q --no-cov` | 846 passed in 11.58s | PASS |
| `TestSerializationSchema` fully removed from test_models.py | `grep -c "TestSerializationSchema" tests/test_models.py` | 0 | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SC-1 | 32.1-01-PLAN.md | All 6 tools' serialized output validates against outputSchema via jsonschema | SATISFIED | `test_tool_output_validates_against_schema` parametrized over all 6 tools, all pass |
| SC-2 | 32.1-01-PLAN.md | Fixtures cover DailyFrequency, WeeklyFrequency(onDays), MonthlyDayOfWeekFrequency(on), MonthlyDayInMonthFrequency(onDates), EndByDate, EndByOccurrences | SATISFIED | All variants confirmed in `_make_repetition_rule_dict` at lines 83-121; tested individually |
| SC-3 | 32.1-01-PLAN.md | No union branch degrades to bare `{type:object, additionalProperties:true}` | SATISFIED | 3 guard tests in `TestUnionRegressionGuard`, all pass |
| SC-4 | 32.1-01-PLAN.md | Naming conventions enforced programmatically | SATISFIED | 2 tests in `TestNamingConvention` using package scanning |
| SC-5 | 32.1-01-PLAN.md | CLAUDE.md contains agent rules | SATISFIED | `## Model Conventions` section present with both required directives |

---

### Anti-Patterns Found

None.

All test implementations are fully wired:
- Schema generation uses real `TypeAdapter` + `compress_schema`
- Serialization uses real `pydantic_core.to_jsonable_python`
- Validation uses real `jsonschema.validate` (not Pydantic validation)
- Naming convention checks use real package introspection

No `TODO`, `FIXME`, placeholder returns, or empty handlers found in the new file.

---

### Human Verification Required

None. All five success criteria are mechanically verifiable and confirmed via test execution.

---

### Gaps Summary

No gaps. All 5 success criteria are satisfied:

- SC-1: 6-tool parametrized schema validation in `TestSchemaValidation`
- SC-2: All 4 required frequency types and both end conditions present as fixtures and tested individually
- SC-3: Three-layer union regression guard in `TestUnionRegressionGuard`
- SC-4: Package-scanning naming convention enforcement in `TestNamingConvention`
- SC-5: `## Model Conventions` section in CLAUDE.md with explicit taxonomy and test-running directives

The phase goal is fully achieved: the codebase now has systemic test safeguards that will catch `@model_serializer` regressions, naming violations, and schema drift before they ship.

---

_Verified: 2026-03-28_
_Verifier: Claude (gsd-verifier)_
