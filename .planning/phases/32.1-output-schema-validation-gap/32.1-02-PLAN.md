---
phase: 32.1-output-schema-validation-gap
plan: 02
type: execute
wave: 1
depends_on: ["32.1-01"]
files_modified:
  - src/omnifocus_operator/rrule/schedule.py
  - src/omnifocus_operator/rrule/__init__.py
  - src/omnifocus_operator/bridge/adapter.py
  - src/omnifocus_operator/repository/hybrid.py
  - tests/test_adapter.py
  - tests/test_rrule.py
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "from_completion + catchUpAutomatically=true maps to schedule='from_completion' without error"
    - "_derive_schedule logic exists in exactly one location (DRY)"
  artifacts:
    - path: "src/omnifocus_operator/rrule/schedule.py"
      provides: "Single canonical _derive_schedule function"
      exports: ["derive_schedule"]
    - path: "src/omnifocus_operator/bridge/adapter.py"
      provides: "Adapter calls shared derive_schedule"
    - path: "src/omnifocus_operator/repository/hybrid.py"
      provides: "Hybrid repo calls shared derive_schedule"
  key_links:
    - from: "src/omnifocus_operator/bridge/adapter.py"
      to: "src/omnifocus_operator/rrule/schedule.py"
      via: "import derive_schedule"
      pattern: "from omnifocus_operator\\.rrule.*import.*derive_schedule"
    - from: "src/omnifocus_operator/repository/hybrid.py"
      to: "src/omnifocus_operator/rrule/schedule.py"
      via: "import derive_schedule"
      pattern: "from omnifocus_operator\\.rrule.*import.*derive_schedule"
---

<objective>
Fix blocker: from_completion + catchUpAutomatically=true crashes the server instead of mapping to from_completion schedule. Extract duplicated _derive_schedule to a shared module and remove the incorrect ValueError.

Purpose: OmniFocus UI legitimately produces this state (catchUpAutomatically has no effect in from_completion context). The server must not crash on valid real-world data.
Output: Single derive_schedule function in rrule/schedule.py, both read paths (adapter + hybrid) use it, tests cover the from_completion + catch_up=true case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-output-schema-validation-gap/32.1-01-SUMMARY.md

<interfaces>
<!-- Current _derive_schedule signature (identical in both files): -->
From src/omnifocus_operator/bridge/adapter.py (lines 95-106):
```python
def _derive_schedule(schedule_type: str, catch_up: bool) -> str:
    """Derive 3-value schedule from scheduleType + catchUpAutomatically (D-06)."""
    if schedule_type == "from_completion" and catch_up:
        raise ValueError(
            "from_completion + catchUpAutomatically=true is an impossible state. "
            "This indicates data corruption in the OmniFocus database."
        )
    if schedule_type == "from_completion":
        return "from_completion"
    if catch_up:
        return "regularly_with_catch_up"
    return "regularly"
```

From src/omnifocus_operator/rrule/__init__.py:
```python
from omnifocus_operator.rrule.builder import build_rrule
from omnifocus_operator.rrule.parser import parse_end_condition, parse_rrule

__all__ = ["build_rrule", "parse_end_condition", "parse_rrule"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract derive_schedule to rrule/schedule.py and fix from_completion logic</name>
  <files>src/omnifocus_operator/rrule/schedule.py, src/omnifocus_operator/rrule/__init__.py, src/omnifocus_operator/bridge/adapter.py, src/omnifocus_operator/repository/hybrid.py</files>
  <behavior>
    - derive_schedule("from_completion", False) -> "from_completion"
    - derive_schedule("from_completion", True) -> "from_completion" (NO error — catch_up is irrelevant for from_completion)
    - derive_schedule("regularly", True) -> "regularly_with_catch_up"
    - derive_schedule("regularly", False) -> "regularly"
  </behavior>
  <action>
    1. Create `src/omnifocus_operator/rrule/schedule.py` with a public `derive_schedule(schedule_type: str, catch_up: bool) -> str` function. Logic: if schedule_type is "from_completion", always return "from_completion" regardless of catch_up value. Otherwise, return "regularly_with_catch_up" if catch_up else "regularly".
    2. Update `src/omnifocus_operator/rrule/__init__.py` to export `derive_schedule` from the new module.
    3. In `src/omnifocus_operator/bridge/adapter.py`: remove the local `_derive_schedule` function (lines 95-106). Import `derive_schedule` from `omnifocus_operator.rrule`. Update the call site at line 148 from `_derive_schedule(schedule_mapped, catch_up)` to `derive_schedule(schedule_mapped, catch_up)`.
    4. In `src/omnifocus_operator/repository/hybrid.py`: remove the local `_derive_schedule` function (lines 217-228). Import `derive_schedule` from `omnifocus_operator.rrule`. Update the call site at line 243 from `_derive_schedule(schedule_type, catch_up)` to `derive_schedule(schedule_type, catch_up)`.
    5. Ensure neither adapter.py nor hybrid.py contains any `_derive_schedule` definition after the change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rrule.py tests/test_adapter.py -x -q --no-cov</automated>
  </verify>
  <done>derive_schedule lives in exactly one place (rrule/schedule.py), from_completion + catch_up=True returns "from_completion" without error, both adapter and hybrid repo import from shared location.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add from_completion + catch_up=true tests for both read paths</name>
  <files>tests/test_rrule.py, tests/test_adapter.py</files>
  <behavior>
    - test_from_completion_with_catch_up_true: derive_schedule("from_completion", True) == "from_completion"
    - test_from_completion_with_catch_up_false: derive_schedule("from_completion", False) == "from_completion"
    - test_adapter_from_completion_catch_up_true: bridge task with FromCompletion + catchUpAutomatically=true adapts to schedule="from_completion" (no error)
  </behavior>
  <action>
    1. In `tests/test_rrule.py`, add a `TestDeriveSchedule` class that imports `derive_schedule` from `omnifocus_operator.rrule` and tests all 4 combinations: (from_completion, False), (from_completion, True), (regularly, False), (regularly, True). The from_completion + True case is the critical regression test — it must NOT raise.
    2. In `tests/test_adapter.py`, add `test_from_completion_with_catch_up_true` to `TestAdaptRepetitionRule`. Construct a task with `scheduleType: "FromCompletion"` and `catchUpAutomatically: True`. After adapt_snapshot, verify `rule["schedule"] == "from_completion"` (no error raised).
  </action>
  <verify>
    <automated>uv run pytest tests/test_rrule.py::TestDeriveSchedule tests/test_adapter.py::TestAdaptRepetitionRule::test_from_completion_with_catch_up_true -x -v --no-cov</automated>
  </verify>
  <done>from_completion + catch_up=True is explicitly tested in both the unit function and the adapter integration path. Full suite passes.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_rrule.py tests/test_adapter.py -x -v --no-cov` — all pass including new tests
- `grep -rn "_derive_schedule" src/` returns only `rrule/schedule.py` — no duplicates
- `uv run pytest -x -q --no-cov` — full suite green, no regressions
</verification>

<success_criteria>
- from_completion + catchUpAutomatically=true maps to "from_completion" schedule without error on both adapter and hybrid read paths
- _derive_schedule exists in exactly one file (rrule/schedule.py), not duplicated in adapter.py or hybrid.py
- Explicit tests cover the from_completion + catch_up=True case
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/32.1-output-schema-validation-gap/32.1-02-SUMMARY.md`
</output>
