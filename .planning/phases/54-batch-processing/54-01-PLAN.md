---
phase: 54-batch-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/config.py
  - src/omnifocus_operator/contracts/use_cases/add/tasks.py
  - src/omnifocus_operator/contracts/use_cases/edit/tasks.py
  - src/omnifocus_operator/service/service.py
  - src/omnifocus_operator/service/domain.py
  - src/omnifocus_operator/server/handlers.py
  - tests/test_service.py
  - tests/test_service_domain.py
  - tests/test_models.py
  - tests/test_preferences_warnings_surfacing.py
  - tests/test_server.py
  - tests/test_output_schema.py
autonomous: true
requirements:
  - BATCH-01
  - BATCH-04
  - BATCH-05
  - BATCH-06

must_haves:
  truths:
    - "AddTaskResult and EditTaskResult use status field instead of success field"
    - "Both result models have id/name/error as optional fields"
    - "MAX_BATCH_SIZE = 50 exists in config.py"
    - "All existing tests pass after mechanical migration"
  artifacts:
    - path: "src/omnifocus_operator/config.py"
      provides: "MAX_BATCH_SIZE constant"
      contains: "MAX_BATCH_SIZE"
    - path: "src/omnifocus_operator/contracts/use_cases/add/tasks.py"
      provides: "Rewritten AddTaskResult with status field"
      contains: "status: Literal"
    - path: "src/omnifocus_operator/contracts/use_cases/edit/tasks.py"
      provides: "Rewritten EditTaskResult with status field"
      contains: "status: Literal"
  key_links:
    - from: "src/omnifocus_operator/service/service.py"
      to: "contracts/use_cases/add/tasks.py"
      via: "AddTaskResult construction"
      pattern: "AddTaskResult\\(.*status="
    - from: "src/omnifocus_operator/service/service.py"
      to: "contracts/use_cases/edit/tasks.py"
      via: "EditTaskResult construction"
      pattern: "EditTaskResult\\(.*status="
---

<objective>
Rewrite AddTaskResult and EditTaskResult from `success: bool` to `status: Literal["success", "error", "skipped"]` with optional id/name/error fields, add MAX_BATCH_SIZE config constant, and mechanically migrate all production code constructors and test assertions across the codebase.

Purpose: Foundation for batch processing — every subsequent plan depends on the new model shape. Must be atomic (model + all constructors + all assertions) to keep the test suite green.
Output: Updated models, config constant, green test suite with new field names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-batch-processing/54-CONTEXT.md
@.planning/phases/54-batch-processing/54-RESEARCH.md

@src/omnifocus_operator/config.py
@src/omnifocus_operator/contracts/use_cases/add/tasks.py
@src/omnifocus_operator/contracts/use_cases/edit/tasks.py
@src/omnifocus_operator/service/service.py
@src/omnifocus_operator/service/domain.py
@src/omnifocus_operator/server/handlers.py
@docs/model-taxonomy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MAX_BATCH_SIZE config constant and rewrite result models</name>
  <files>
    src/omnifocus_operator/config.py,
    src/omnifocus_operator/contracts/use_cases/add/tasks.py,
    src/omnifocus_operator/contracts/use_cases/edit/tasks.py
  </files>
  <read_first>
    src/omnifocus_operator/config.py,
    src/omnifocus_operator/contracts/use_cases/add/tasks.py,
    src/omnifocus_operator/contracts/use_cases/edit/tasks.py,
    docs/model-taxonomy.md
  </read_first>
  <action>
**config.py** — Add `MAX_BATCH_SIZE: int = 50` alongside `DEFAULT_LIST_LIMIT`:
```python
# -- Batch processing ----------------------------------------------------------
# Maximum items accepted in a single add_tasks or edit_tasks call.
MAX_BATCH_SIZE: int = 50
```
Place it immediately after the `DEFAULT_LIST_LIMIT` block (after line 15).

**contracts/use_cases/add/tasks.py** — Rewrite `AddTaskResult`:
- Add `from typing import Literal` import (merge with existing imports)
- Replace the entire class body:
```python
class AddTaskResult(OmniFocusBaseModel):
    __doc__ = ADD_TASK_RESULT_DOC

    status: Literal["success", "error", "skipped"]
    id: str | None = None
    name: str | None = None
    error: str | None = None
    warnings: list[str] | None = None
```
- Remove `success: bool` entirely — it is replaced by `status`
- `id` changes from `str` (required) to `str | None = None` (optional) per BATCH-05
- `name` changes from `str` (required) to `str | None = None` (optional) per BATCH-05

**contracts/use_cases/edit/tasks.py** — Rewrite `EditTaskResult`:
- `Literal` is already imported (used for lifecycle) — no new import needed
- Replace the entire class body:
```python
class EditTaskResult(OmniFocusBaseModel):
    __doc__ = EDIT_TASK_RESULT_DOC

    status: Literal["success", "error", "skipped"]
    id: str | None = None
    name: str | None = None
    error: str | None = None
    warnings: list[str] | None = None
```
- Same changes as AddTaskResult: `success: bool` removed, `id`/`name` made optional, `error` added

Keep AddTaskResult and EditTaskResult as separate models (same shape, different names) per existing naming convention. Do NOT merge into a shared BatchItemResult.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && python3 -c "
from omnifocus_operator.config import MAX_BATCH_SIZE
from omnifocus_operator.contracts.use_cases.add.tasks import AddTaskResult
from omnifocus_operator.contracts.use_cases.edit.tasks import EditTaskResult
assert MAX_BATCH_SIZE == 50
r = AddTaskResult(status='success', id='x', name='y')
assert r.status == 'success'
assert not hasattr(r, 'success') or 'success' not in r.model_fields
r2 = AddTaskResult(status='error', error='bad')
assert r2.id is None
assert r2.name is None
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - config.py contains `MAX_BATCH_SIZE: int = 50`
    - AddTaskResult in contracts/use_cases/add/tasks.py has `status: Literal["success", "error", "skipped"]` and does NOT have `success: bool`
    - EditTaskResult in contracts/use_cases/edit/tasks.py has `status: Literal["success", "error", "skipped"]` and does NOT have `success: bool`
    - Both result models have `id: str | None = None`, `name: str | None = None`, `error: str | None = None`, `warnings: list[str] | None = None`
    - AddTaskResult can be constructed with `status="error"` and no id/name (they default to None)
    - EditTaskResult can be constructed with `status="skipped"` and no name (defaults to None)
  </acceptance_criteria>
  <done>MAX_BATCH_SIZE = 50 in config, both result models use status Literal instead of success bool, all fields optional except status</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all production code constructors from success=True to status="success"</name>
  <files>
    src/omnifocus_operator/service/service.py,
    src/omnifocus_operator/service/domain.py,
    src/omnifocus_operator/server/handlers.py
  </files>
  <read_first>
    src/omnifocus_operator/service/service.py,
    src/omnifocus_operator/service/domain.py,
    src/omnifocus_operator/server/handlers.py
  </read_first>
  <action>
Mechanical replacement across all production code files that construct AddTaskResult or EditTaskResult.

**service/service.py** — Two construction sites:
1. `_AddTaskPipeline._delegate()` (line ~653): Change `success=True` to `status="success"`:
```python
return AddTaskResult(
    status="success", id=repo_result.id, name=repo_result.name, warnings=all_warnings or None
)
```
2. `_EditTaskPipeline._delegate()` (line ~977): Change `success=True` to `status="success"`:
```python
return EditTaskResult(
    status="success",
    id=repo_result.id,
    name=repo_result.name,
    warnings=self._all_warnings or None,
)
```

**service/domain.py** — Three construction sites in `detect_early_return()` (lines ~972, ~978, ~991):
Change every `success=True` to `status="success"` in the three `EditTaskResult(...)` constructor calls within that method. All three follow the same pattern — just replace the keyword argument.

**server/handlers.py** — One debug log line:
Line ~175 uses `results[0].success` — change to `results[0].status`:
```python
logger.debug(
    "server.edit_tasks: returning id=%s, status=%s, warnings=%s",
    results[0].id,
    results[0].status,
    results[0].warnings,
)
```
This is the ONLY change in handlers.py for this plan. Do NOT modify handler logic, guards, or control flow — that is Plan 02's scope.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && grep -rn "success=True" src/omnifocus_operator/service/ src/omnifocus_operator/server/ && echo "FAIL: success=True still present" || echo "OK: no success=True in production code"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "success=True" src/omnifocus_operator/service/service.py` returns no results
    - `grep -rn "success=True" src/omnifocus_operator/service/domain.py` returns no results
    - `grep -rn "results\[0\].success" src/omnifocus_operator/server/handlers.py` returns no results
    - service.py line ~653 contains `status="success"` instead of `success=True`
    - service.py line ~977 contains `status="success"` instead of `success=True`
    - domain.py has zero occurrences of `success=True`
    - handlers.py debug log references `results[0].status` not `results[0].success`
  </acceptance_criteria>
  <done>All production code constructors of AddTaskResult/EditTaskResult use status="success" instead of success=True. Zero remaining references to success=True in src/.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate all test assertions from success to status</name>
  <files>
    tests/test_service.py,
    tests/test_service_domain.py,
    tests/test_models.py,
    tests/test_preferences_warnings_surfacing.py,
    tests/test_server.py,
    tests/test_output_schema.py
  </files>
  <read_first>
    tests/test_service.py,
    tests/test_service_domain.py,
    tests/test_models.py,
    tests/test_preferences_warnings_surfacing.py,
    tests/test_server.py,
    tests/test_output_schema.py
  </read_first>
  <action>
Mechanical search-and-replace across all test files. This is a high-volume change (~110 locations) but each replacement is identical.

**Pattern 1: Pydantic model assertions** (test_service.py ~100 lines, test_models.py ~5, test_preferences_warnings_surfacing.py ~2, test_service_domain.py ~3):
Replace every occurrence of:
```python
assert result.success is True
```
with:
```python
assert result.status == "success"
```
Also handle variants like `assert parent_result.success is True` → `assert parent_result.status == "success"` and `assert child_result.success is True` → `assert child_result.status == "success"`.

**Pattern 2: Dict-based assertions** (test_server.py ~20 lines):
Replace every occurrence of:
```python
assert items[0]["success"] is True
```
with:
```python
assert items[0]["status"] == "success"
```
Also handle `edit_result.structured_content["result"][0]["success"]` → `edit_result.structured_content["result"][0]["status"]`.
And `assert item["success"] is True` → `assert item["status"] == "success"`.

**Pattern 3: Schema property assertions** (test_server.py lines ~369, ~372):
Replace:
```python
assert "success" in add_props
```
with:
```python
assert "status" in add_props
```
And same for `edit_props`.

**Pattern 4: Output schema fixtures** (test_output_schema.py lines ~244, ~245, ~345, ~359):
Replace every `success=True` in AddTaskResult/EditTaskResult constructor calls with `status="success"`:
- Line ~244: `_ADD_TASK_RESULT = AddTaskResult(status="success", id="new-task-001", name="Created Task")`
- Line ~245: `_EDIT_TASK_RESULT = EditTaskResult(status="success", id="task-001", name="Edited Task", warnings=[])`
- Line ~345: `AddTaskResult(status="success", id="task-with-warnings", ...)`
- Line ~359: `AddTaskResult(status="success", id="task-no-warnings", name="No Warnings")`

After all replacements, run the full test suite to verify nothing was missed.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\.success" tests/` returns zero results related to AddTaskResult/EditTaskResult (may have other unrelated .success if any)
    - `grep -rn 'success=True' tests/test_output_schema.py` returns no results
    - `grep -rn '"success" in' tests/test_server.py` returns no results
    - `grep -rn 'items\[0\]\["success"\]' tests/test_server.py` returns no results
    - `uv run pytest -x -q` passes with zero failures
    - All ~110 assertion sites use `result.status == "success"` or `items[0]["status"] == "success"`
  </acceptance_criteria>
  <done>All test files migrated from success to status. Full test suite passes green. Zero remaining references to the old success field in test assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan is a mechanical model refactor — no new inputs, no new endpoints, no behavioral changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | T (Tampering) | AddTaskResult/EditTaskResult | accept | Outbound-only models — constructed by server, not parsed from agent input. No new attack surface. |
</threat_model>

<verification>
1. `uv run pytest -x -q` — full suite green
2. `grep -rn "success=True" src/` — zero hits
3. `grep -rn "\.success is True" tests/` — zero hits related to result models
4. `python3 -c "from omnifocus_operator.config import MAX_BATCH_SIZE; assert MAX_BATCH_SIZE == 50"`
</verification>

<success_criteria>
- MAX_BATCH_SIZE = 50 exists in config.py
- AddTaskResult and EditTaskResult use `status: Literal["success", "error", "skipped"]` with optional id/name/error fields
- All production constructors use `status="success"`
- All test assertions reference `status` not `success`
- Full test suite passes (2086+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/54-batch-processing/54-01-SUMMARY.md`
</output>
