---
phase: 54-batch-processing
plan: 03
type: execute
wave: 3
depends_on: ["54-01", "54-02"]
files_modified:
  - tests/test_server.py
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02
  - BATCH-03
  - BATCH-04
  - BATCH-05
  - BATCH-06
  - BATCH-07
  - BATCH-08

must_haves:
  truths:
    - "Submitting 2+ items to add_tasks processes all items regardless of failures"
    - "Submitting 2+ items to edit_tasks stops at first failure and skips the rest"
    - "Skipped items have status skipped with warning referencing failing item"
    - "Error items have Task N: prefix in error message"
    - "50 items accepted, 51 items rejected at schema level"
    - "Empty items array rejected at schema level"
    - "Same-task edits in a batch each see the prior item's result"
  artifacts:
    - path: "tests/test_server.py"
      provides: "Batch scenario tests for add_tasks and edit_tasks"
      contains: "class TestAddTasksBatch"
  key_links:
    - from: "tests/test_server.py"
      to: "src/omnifocus_operator/server/handlers.py"
      via: "MCP client tool calls"
      pattern: "call_tool.*add_tasks|call_tool.*edit_tasks"
---

<objective>
Add comprehensive batch processing test scenarios for both add_tasks (best-effort) and edit_tasks (fail-fast), rewrite old single-item constraint tests, and verify all BATCH requirement behaviors end-to-end through the MCP client.

Purpose: Tests are the behavioral contract for batch processing — they verify every BATCH requirement through the actual tool interface. Without these, batch semantics are unverified.
Output: Complete test coverage for batch scenarios in test_server.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-batch-processing/54-CONTEXT.md
@.planning/phases/54-batch-processing/54-RESEARCH.md
@.planning/phases/54-batch-processing/54-01-SUMMARY.md
@.planning/phases/54-batch-processing/54-02-SUMMARY.md

@tests/test_server.py
@src/omnifocus_operator/server/handlers.py
@src/omnifocus_operator/config.py

<interfaces>
<!-- After Plan 01 + 02, the result models and handler behavior are: -->

AddTaskResult fields: status (Literal["success","error","skipped"]), id (str|None), name (str|None), error (str|None), warnings (list[str]|None)
EditTaskResult fields: identical to AddTaskResult

add_tasks behavior: best-effort — all items processed, individual failures produce status="error" with error="Task N: {message}"
edit_tasks behavior: fail-fast — stops at first error, remaining items get status="skipped" with warnings=["Skipped: task {N} failed"]

MAX_BATCH_SIZE = 50 enforced via Pydantic max_length (or manual check — see Plan 02)
min_length=1 enforced — empty array rejected

Per-item catch scope: ToolError, ValueError only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: add_tasks batch test scenarios (best-effort)</name>
  <files>tests/test_server.py</files>
  <read_first>
    tests/test_server.py,
    src/omnifocus_operator/server/handlers.py,
    src/omnifocus_operator/config.py,
    src/omnifocus_operator/contracts/use_cases/add/tasks.py
  </read_first>
  <behavior>
    - Test: 2 items both succeed → result is array of 2 items, both status="success", both have id and name
    - Test: 3 items, item 2 fails (e.g. service raises ToolError) → [success, error, success] — item 3 still processed (best-effort)
    - Test: 2 items both fail → [error, error]
    - Test: error item has "Task N:" prefix in error string (1-indexed)
    - Test: error item has id=None (AddTaskCommand has no id field — no OmniFocus ID exists for failed creates)
    - Test: error item has name=None
    - Test: success item has id and name populated
    - Test: warnings present on success items when service produces warnings
    - Test: 50 items accepted (no validation error — just verify the call doesn't raise)
    - Test: 51 items rejected with validation error (schema-level maxItems)
    - Test: 0 items rejected with validation error (schema-level minItems)
  </behavior>
  <action>
Add a new test class `TestAddTasksBatch` in `tests/test_server.py`, placed after the existing `TestAddTasks` class. Use the same test infrastructure (fixtures, `call_tool_mcp`, `InMemoryBridge` or `SimulatorBridge` patterns) as the existing `TestAddTasks` tests.

**Rewrite the old constraint tests** — find and rewrite these existing tests within `TestAddTasks`:
- `test_add_tasks_single_item_constraint` (line ~667) — was testing 2 items raises error. Now: 2 items should SUCCEED. Move this test logic into the new batch class as `test_batch_all_succeed`.
- `test_add_tasks_empty_array` (line ~675) — keep as validation error test but update the match string from "exactly 1 item" to whatever the new Pydantic validation error says for `min_length=1` (likely something like "List should have at least 1 item" or similar). Or keep the test but adjust the assertion.

**To simulate per-item failures:** The add_tasks handler catches `ToolError` and `ValueError` from `service.add_task()`. To trigger a per-item failure, use an invalid parent reference (e.g., `parent: "nonexistent-project-id"`) which will cause the service to raise a `ToolError` for name resolution failure. Alternatively, use a tag name that doesn't exist. The `InMemoryBridge` or test fixtures should support this pattern — look at existing tests for how they trigger service errors.

**For the 50-item acceptance test:** Create a list of 50 minimal items `[{"name": f"Task {i}"} for i in range(50)]` and verify the call returns 50 results without raising.

**For the 51-item rejection test:** Create 51 items and verify the call raises a `ToolError` (from ValidationReformatterMiddleware catching the Pydantic validation error). The exact error message depends on how Pydantic formats `max_length` violations — match on a substring like "50" or "most" or check the test output.

**Result field presence checks (BATCH-05):**
For a successful add result: assert `item["status"] == "success"`, `item["id"]` is a non-empty string, `item["name"]` is a non-empty string, `"error" not in item or item["error"] is None`.
For a failed add result: assert `item["status"] == "error"`, `"id" not in item or item["id"] is None`, `"name" not in item or item["name"] is None`, `item["error"]` starts with "Task ".
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_server.py -x -q -k "batch" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_server.py contains class `TestAddTasksBatch` or equivalent batch test methods within TestAddTasks
    - Test exists that submits 2+ items and verifies all get processed (best-effort)
    - Test exists that submits items where one fails and verifies others still succeed
    - Test exists that verifies "Task N:" prefix on error messages (1-indexed)
    - Test exists that verifies error items have id=None and name=None for add_tasks
    - Test exists that verifies success items have id and name populated
    - Test exists that 50 items are accepted without error
    - Test exists that 51 items are rejected with validation error
    - Test exists that 0 items are rejected with validation error
    - `uv run pytest tests/test_server.py -x -q -k "batch or empty_array or single_item"` passes
  </acceptance_criteria>
  <done>Complete test coverage for add_tasks batch behavior: best-effort semantics, per-item error handling, field presence per status, batch size limits (0, 50, 51).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: edit_tasks batch test scenarios (fail-fast)</name>
  <files>tests/test_server.py</files>
  <read_first>
    tests/test_server.py,
    src/omnifocus_operator/server/handlers.py,
    src/omnifocus_operator/contracts/use_cases/edit/tasks.py
  </read_first>
  <behavior>
    - Test: 2 items both succeed → [success, success]
    - Test: 3 items, item 1 fails → [error, skipped, skipped] — fail-fast
    - Test: 3 items, item 2 fails → [success, error, skipped] — first committed, rest skipped
    - Test: skipped items have warnings array with message "Skipped: task N failed" (N = 1-indexed index of failing item)
    - Test: skipped items have id populated (from command.id input)
    - Test: skipped items have name=None (never processed by service)
    - Test: error items have id populated (from command.id input — EditTaskCommand always has id)
    - Test: error items have "Task N:" prefix in error string
    - Test: 50 items accepted
    - Test: 51 items rejected with validation error
    - Test: 0 items rejected with validation error
    - Test: same-task edits — 2 edits to same task ID, both succeed (BATCH-08: sequential processing means second sees first's result)
  </behavior>
  <action>
Add a new test class `TestEditTasksBatch` in `tests/test_server.py`, placed after the existing `TestEditTasks` class.

**Rewrite the old constraint tests** — find and rewrite these existing tests within `TestEditTasks`:
- `test_edit_tasks_rejects_multi_item_array` (line ~746) — was testing 2+ items raises error. Now: 2 items should process with fail-fast semantics. Move into batch class.
- `test_edit_tasks_rejects_empty_array` (line ~741) — update match string from "exactly 1 item" to new Pydantic min_length error message.

**To simulate per-item failures:** Use an invalid task ID (e.g., `id: "nonexistent-task-id"`) which will cause the service to raise `ToolError("Task not found: ...")`. First create real tasks (via add_tasks), then submit a batch where one item references a bad ID.

**For the fail-fast behavior test:**
1. Create 2 tasks via add_tasks (to get valid IDs)
2. Submit edit_tasks with 3 items: [valid-id-1, bad-id, valid-id-2]
3. Assert results: [status=success, status=error, status=skipped]
4. Assert skipped item's warnings contains "Skipped: task 2 failed"
5. Assert skipped item's id == valid-id-2 (from command input)

**For same-task edit test (BATCH-08):**
1. Create a task via add_tasks
2. Submit edit_tasks with 2 items both targeting the same task ID (e.g., first sets name="A", second sets flagged=true)
3. Assert both succeed — the second edit sees the first's changes because processing is sequential and the service re-fetches task state

**Result field presence checks (BATCH-05 for edit):**
For error edit result: assert `item["id"]` is populated (from command.id), `"name" not in item or item["name"] is None`, `item["error"]` starts with "Task ".
For skipped edit result: assert `item["id"]` is populated (from command.id), `"name" not in item or item["name"] is None`, `item["warnings"]` is a list containing the skip message.

**For batch size tests:** Same pattern as Task 1 — 50 items accepted (use valid task IDs or accept that some will fail but the batch itself isn't rejected), 51 items rejected at schema level, 0 items rejected.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_server.py -x -q -k "EditTasksBatch or edit_tasks_batch" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_server.py contains class `TestEditTasksBatch` or equivalent batch test methods
    - Test exists that submits 3 items where item 2 fails → [success, error, skipped]
    - Test exists that submits 3 items where item 1 fails → [error, skipped, skipped]
    - Test exists that verifies skipped items have `warnings` containing "Skipped: task N failed"
    - Test exists that verifies skipped items have `id` populated from command input
    - Test exists that verifies error items have `id` populated from command input
    - Test exists that verifies "Task N:" prefix on error messages (1-indexed)
    - Test exists for same-task edits (BATCH-08) — both succeed sequentially
    - Test exists that 50 items accepted, 51 rejected, 0 rejected
    - `uv run pytest tests/test_server.py -x -q` passes (full server test suite green)
    - `uv run pytest -x -q` passes (full project test suite green)
  </acceptance_criteria>
  <done>Complete test coverage for edit_tasks batch behavior: fail-fast semantics, skip messages with failing item reference, id presence on error/skip items, same-task sequential edits, batch size limits. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — tests operate against InMemoryBridge/SimulatorBridge per SAFE-01.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-06 | E (Elevation) | test_server.py | accept | Tests use InMemoryBridge per SAFE-01 — no risk of touching real data |
</threat_model>

<verification>
1. `uv run pytest tests/test_server.py -x -q -k "batch"` — all batch tests pass
2. `uv run pytest tests/test_server.py -x -q` — full server test suite green
3. `uv run pytest -x -q` — full project test suite green (2086+ tests)
4. `grep -c "def test_.*batch\|def test_.*batch" tests/test_server.py` — at least 15 new test methods
</verification>

<success_criteria>
- All BATCH-01 through BATCH-08 requirements have corresponding test coverage
- add_tasks batch tests: all-succeed, mixed failures (best-effort), all-fail, field presence per status, 50/51/0 limits
- edit_tasks batch tests: all-succeed, fail-fast (item 1/2 fails), skip messages with item reference, id on skipped, same-task edits, 50/51/0 limits
- Old constraint tests (single_item_constraint, rejects_multi_item_array) rewritten for new batch behavior
- Full test suite passes green
</success_criteria>

<output>
After completion, create `.planning/phases/54-batch-processing/54-03-SUMMARY.md`
</output>
