---
phase: 37.1-fix-effectivecompletiondate-availability-ghost-tasks
verified: 2026-04-04T23:30:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 37.1: Fix effectiveCompletionDate Availability Ghost Tasks — Verification Report

**Phase Goal:** Fix tasks with effectiveDateCompleted/effectiveDateHidden set (inherited from completed/dropped parent) but no own dateCompleted/dateHidden from appearing as available — use effective columns in both Python mappers and SQL filter clauses.
**Verified:** 2026-04-04T23:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Task with effectiveDateCompleted (no own dateCompleted) maps to availability "completed" | VERIFIED | `test_task_availability_completed_by_effective` at line 564; call site at hybrid.py:357 uses `row["effectiveDateCompleted"]` |
| 2 | Task with effectiveDateHidden (no own dateHidden) maps to availability "dropped" | VERIFIED | `test_task_availability_dropped_by_effective` at line 575; call site at hybrid.py:358 uses `row["effectiveDateHidden"]` |
| 3 | Same fix applied symmetrically to projects | VERIFIED | `test_project_availability_completed_by_effective` (line 807) and `test_project_availability_dropped_by_effective` (line 818); project call site at hybrid.py:397-398 uses effective columns |
| 4 | SQL availability clauses use effective columns, excluding ghost tasks from default queries | VERIFIED | All 8 clause entries in `_TASK_AVAILABILITY_CLAUSES` and `_PROJECT_AVAILABILITY_CLAUSES` use `t.effectiveDateCompleted` / `t.effectiveDateHidden`; `test_list_tasks_default_excludes_ghost_completed` at line 1847 confirms ghost exclusion |
| 5 | Full test suite passes with no regressions | VERIFIED | `uv run pytest -x -q` — 1528 passed, 98.26% coverage |
| 6 | Priority chain order (dropped > completed > blocked > available) unchanged | VERIFIED | `_map_task_availability` at hybrid.py:186-192 and `_map_project_availability` at hybrid.py:199-207 — logic unchanged, only call-site column names changed |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/omnifocus_operator/repository/hybrid/hybrid.py` | Fixed call sites using effective date columns | VERIFIED | Lines 357-358 use `row["effectiveDateCompleted"]` / `row["effectiveDateHidden"]` for task; lines 397-398 same for project |
| `src/omnifocus_operator/repository/hybrid/query_builder.py` | Fixed SQL clauses using effective date columns | VERIFIED | `_TASK_AVAILABILITY_CLAUSES` (lines 67-73) and `_PROJECT_AVAILABILITY_CLAUSES` (lines 79-89) all use `t.effectiveDateCompleted` / `t.effectiveDateHidden` — 8 occurrences |
| `tests/test_hybrid_repository.py` | 5 new test methods for effective-only scenarios | VERIFIED | All 5 methods exist and are substantive: `test_task_availability_completed_by_effective` (564), `test_task_availability_dropped_by_effective` (575), `test_project_availability_completed_by_effective` (807), `test_project_availability_dropped_by_effective` (818), `test_list_tasks_default_excludes_ghost_completed` (1847) |
| `tests/test_query_builder.py` | SQL clause assertions updated to effective column names | VERIFIED | 8 assertions reference `effectiveDateCompleted` / `effectiveDateHidden` (lines 66-67, 157-158, 168, 173, 351-352, 363, 368) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `hybrid.py:_map_task_availability` | `hybrid.py:_map_task_row call site` | `row["effectiveDateCompleted"]` passed as `date_completed` param | WIRED | Line 355-359 confirmed: `date_completed=row["effectiveDateCompleted"]`, `date_hidden=row["effectiveDateHidden"]` |
| `query_builder.py:_TASK_AVAILABILITY_CLAUSES` | `query_builder.py:build_list_tasks_sql` | static dict lookup | WIRED | Confirmed: `_TASK_AVAILABILITY_CLAUSES` used at line 148 in `build_list_tasks_sql`; all entries use `t.effectiveDateCompleted` |

### Data-Flow Trace (Level 4)

Not applicable — this phase fixes column references in availability mappers and SQL filter clauses, not data rendering in user-facing components. The "data" is the `availability` string field on Task/Project models; its source (SQLite columns) is directly verified via grep.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Effective-date tests pass | `uv run pytest tests/test_hybrid_repository.py -k "effective" -x -q` | 6 passed | PASS |
| Availability tests (all) pass | `uv run pytest tests/test_hybrid_repository.py -k "availability" -x -q` | 21 passed | PASS |
| Query builder assertions pass | `uv run pytest tests/test_query_builder.py -x -q` | 53 passed | PASS |
| Full suite clean | `uv run pytest -x -q` | 1528 passed, 98.26% coverage | PASS |

### Requirements Coverage

Phase 37.1 uses D-01 through D-06 from CONTEXT.md (implementation decisions), not IDs from REQUIREMENTS.md. These are phase-local decisions that fix a bug introduced before the v1.3 requirements were authored. No REQUIREMENTS.md IDs map to Phase 37.1 in the traceability table.

| Decision | Description | Status | Evidence |
|----------|-------------|--------|----------|
| D-01 | `_map_task_availability` uses `effectiveDateCompleted` for "completed" check | SATISFIED | hybrid.py:357 `row["effectiveDateCompleted"]` at call site |
| D-02 | `_map_task_availability` uses `effectiveDateHidden` for "dropped" check | SATISFIED | hybrid.py:358 `row["effectiveDateHidden"]` at call site |
| D-03 | Priority chain (dropped > completed > blocked > available) unchanged | SATISFIED | hybrid.py:186-192 logic structure unchanged |
| D-04 | `_TASK_AVAILABILITY_CLAUSES` uses `effectiveDateCompleted` / `effectiveDateHidden` | SATISFIED | query_builder.py lines 67-73, 4 entries confirmed |
| D-05 | `_PROJECT_AVAILABILITY_CLAUSES` also updated to effective columns | SATISFIED | query_builder.py lines 79-89, 4 entries confirmed |
| D-06 | Fix pre-aligns with v1.3.2 "all date filters use effective values" direction | SATISFIED | Both mapper call sites and SQL clauses now uniformly use effective columns |

### Anti-Patterns Found

No anti-patterns found. The changes are precise column name substitutions with real test coverage. No TODOs, stubs, placeholder returns, or hardcoded empty values were introduced.

### Human Verification Required

None. All behavioral claims are fully verifiable through the test suite.

## Gaps Summary

No gaps. All 6 observable truths are verified, all 4 artifacts are substantive and wired, all 6 implementation decisions are satisfied, and the full test suite passes clean.

---

_Verified: 2026-04-04T23:30:00Z_
_Verifier: Claude (gsd-verifier)_
