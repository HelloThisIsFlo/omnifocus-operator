---
phase: 37-server-registration-and-integration-was-phase-38
plan: 03
type: execute
wave: 1
depends_on: ["37-01", "37-02"]
files_modified:
  - src/omnifocus_operator/config.py
  - src/omnifocus_operator/contracts/use_cases/list/tasks.py
  - src/omnifocus_operator/contracts/use_cases/list/projects.py
  - src/omnifocus_operator/contracts/use_cases/list/tags.py
  - src/omnifocus_operator/contracts/use_cases/list/folders.py
  - src/omnifocus_operator/contracts/use_cases/list/perspectives.py
  - src/omnifocus_operator/contracts/use_cases/list/_validators.py
  - src/omnifocus_operator/repository/hybrid/hybrid.py
  - src/omnifocus_operator/repository/bridge_only/bridge_only.py
  - src/omnifocus_operator/service/service.py
  - src/omnifocus_operator/agent_messages/descriptions.py
autonomous: true
gap_closure: true
requirements: [INFRA-05, DOC-10, DOC-11]

must_haves:
  truths:
    - "Calling list_tasks({}) returns at most 50 items by default, not 2174"
    - "Calling list_tags({}) returns at most 50 items by default"
    - "Calling list_folders({}) returns at most 50 items by default"
    - "Calling list_perspectives({}) returns at most 50 items by default"
    - "All 5 list tools report accurate total_count and has_more when limit applies"
    - "Tool descriptions document the default pagination limit"
    - "Tool descriptions document entity-reference filter resolution cascade"
    - "Query model Field(description=...) explains resolution behavior for project, tags, folder fields"
  artifacts:
    - path: "src/omnifocus_operator/config.py"
      provides: "DEFAULT_LIST_LIMIT constant"
      contains: "DEFAULT_LIST_LIMIT"
    - path: "src/omnifocus_operator/contracts/use_cases/list/tags.py"
      provides: "limit/offset fields on ListTagsQuery and ListTagsRepoQuery"
      contains: "limit"
    - path: "src/omnifocus_operator/contracts/use_cases/list/folders.py"
      provides: "limit/offset fields on ListFoldersQuery and ListFoldersRepoQuery"
      contains: "limit"
    - path: "src/omnifocus_operator/contracts/use_cases/list/perspectives.py"
      provides: "limit/offset fields on ListPerspectivesQuery and ListPerspectivesRepoQuery"
      contains: "limit"
  key_links:
    - from: "src/omnifocus_operator/contracts/use_cases/list/tasks.py"
      to: "src/omnifocus_operator/config.py"
      via: "default limit import"
      pattern: "DEFAULT_LIST_LIMIT"
    - from: "src/omnifocus_operator/repository/hybrid/hybrid.py"
      to: "ListRepoResult"
      via: "has_more calculation for tags/folders/perspectives"
      pattern: "has_more.*limit"
    - from: "src/omnifocus_operator/service/service.py"
      to: "ListTagsRepoQuery"
      via: "passes limit/offset through to repo"
      pattern: "limit.*query.limit"
---

<objective>
Close two UAT gaps: (1) add default pagination limit=50 to all 5 list tools so unbounded queries don't blow agent token budgets, and (2) document entity-reference filter resolution cascade in tool descriptions and query model Field descriptions.

Purpose: Gap 1 caused 1.8M character responses in production. Gap 2 causes agent confusion when substring matching returns unexpected results.
Output: All 5 list tools paginated by default, resolution cascade documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/omnifocus_operator/config.py
@src/omnifocus_operator/contracts/use_cases/list/tasks.py
@src/omnifocus_operator/contracts/use_cases/list/projects.py
@src/omnifocus_operator/contracts/use_cases/list/tags.py
@src/omnifocus_operator/contracts/use_cases/list/folders.py
@src/omnifocus_operator/contracts/use_cases/list/perspectives.py
@src/omnifocus_operator/contracts/use_cases/list/_validators.py
@src/omnifocus_operator/contracts/use_cases/list/common.py
@src/omnifocus_operator/repository/hybrid/hybrid.py
@src/omnifocus_operator/repository/bridge_only/bridge_only.py
@src/omnifocus_operator/service/service.py
@src/omnifocus_operator/agent_messages/descriptions.py

<interfaces>
<!-- Key contracts the executor needs -->

From config.py (currently):
```python
FUZZY_MATCH_MAX_SUGGESTIONS: int = 3
FUZZY_MATCH_CUTOFF: float = 0.6
```

From _validators.py:
```python
def validate_offset_requires_limit(limit: int | None, offset: int | None) -> None
```

From common.py:
```python
class ListRepoResult[T](OmniFocusBaseModel):
    items: list[T]
    total: int
    has_more: bool
```

ListTasksQuery and ListProjectsQuery already have limit/offset fields (default None).
ListTagsQuery, ListFoldersQuery, ListPerspectivesQuery have NO limit/offset fields.

Service list_tags/list_folders/list_perspectives are inline pass-throughs that construct RepoQuery from Query fields.
Service list_tasks/list_projects are pipelines that pass limit/offset through to RepoQuery.

HybridRepo tags/folders/perspectives use fetch-all + Python filter, hardcode has_more=False.
BridgeOnlyRepo tags/folders/perspectives also fetch-all + Python filter, hardcode has_more=False.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add default pagination to all 5 list query models and implement slicing in repos</name>
  <files>
    src/omnifocus_operator/config.py,
    src/omnifocus_operator/contracts/use_cases/list/tasks.py,
    src/omnifocus_operator/contracts/use_cases/list/projects.py,
    src/omnifocus_operator/contracts/use_cases/list/tags.py,
    src/omnifocus_operator/contracts/use_cases/list/folders.py,
    src/omnifocus_operator/contracts/use_cases/list/perspectives.py,
    src/omnifocus_operator/contracts/use_cases/list/_validators.py,
    src/omnifocus_operator/repository/hybrid/hybrid.py,
    src/omnifocus_operator/repository/bridge_only/bridge_only.py,
    src/omnifocus_operator/service/service.py
  </files>
  <behavior>
    - Test: ListTasksQuery() (no args) has limit == 50 (from DEFAULT_LIST_LIMIT)
    - Test: ListProjectsQuery() (no args) has limit == 50
    - Test: ListTagsQuery() (no args) has limit == 50
    - Test: ListFoldersQuery() (no args) has limit == 50
    - Test: ListPerspectivesQuery() (no args) has limit == 50
    - Test: ListTasksQuery(limit=None) explicitly sets limit to None (agent can override default to get all)
    - Test: ListTagsQuery(limit=10, offset=5) accepted; ListTagsQuery(offset=5) rejected (offset requires limit)
    - Test: Repo list_tags with 100 items and limit=10 returns 10 items, total=100, has_more=True
    - Test: Repo list_tags with 5 items and limit=50 returns 5 items, total=5, has_more=False
    - Test: Repo list_folders with limit=10, offset=5 returns correct slice
    - Test: Repo list_perspectives with limit and offset works correctly
    - Test: Cross-path equivalence still holds (existing tests pass)
  </behavior>
  <action>
    **config.py**: Add `DEFAULT_LIST_LIMIT: int = 50` constant.

    **All 5 Query models** (tasks, projects, tags, folders, perspectives): Change `limit` default from `None` to `DEFAULT_LIST_LIMIT`. For tags/folders/perspectives, ADD `limit: int | None` and `offset: int | None` fields. Add the `validate_offset_requires_limit` model_validator to tags/folders/perspectives (same pattern as tasks/projects already use).

    **All 5 RepoQuery models**: Same changes -- add limit/offset to tags/folders/perspectives RepoQuery, change default to `DEFAULT_LIST_LIMIT` on all 5.

    **Service pass-throughs** (list_tags, list_folders, list_perspectives in service.py): Pass limit and offset through when constructing RepoQuery from Query.

    **HybridRepo** (_list_tags_sync, _list_folders_sync, _list_perspectives_sync): After filtering, compute `total = len(filtered)`. Apply offset slice (`filtered = filtered[offset:]` if offset). Apply limit slice (`filtered = filtered[:limit]` if limit). Compute `has_more = len(remaining_after_offset) > limit`. Return ListRepoResult with correct total and has_more.

    **BridgeOnlyRepo** (list_tags, list_folders, list_perspectives): Same Python-side pagination slicing as hybrid.

    **Important**: The agent can explicitly pass `limit=None` to get all results. The default is 50 but it's overridable. Existing query builder for tasks/projects already handles limit/offset in SQL -- those paths only need the default change.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/.claude/worktrees/agent-c743ac7c && uv run pytest tests/ -x -q --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 5 QueryModels default limit to 50
    - Tags/folders/perspectives have limit+offset fields with offset-requires-limit validation
    - Repos compute correct total, has_more, and slice for tags/folders/perspectives
    - Service passes limit/offset through for tags/folders/perspectives
    - All existing tests pass (cross-path equivalence, integration, etc.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Document default pagination and entity-reference resolution cascade in tool descriptions and Field descriptions</name>
  <files>
    src/omnifocus_operator/agent_messages/descriptions.py,
    src/omnifocus_operator/contracts/use_cases/list/tasks.py,
    src/omnifocus_operator/contracts/use_cases/list/projects.py
  </files>
  <action>
    **descriptions.py**: 

    1. Update LIST_TASKS_TOOL_DOC to mention default limit=50 and that `limit=null` returns all. Add a line about the project and tags filters: "project and tags accept an ID or name. Names use case-insensitive substring matching; multiple matches return all. Unrecognized names are skipped with a warning."

    2. Update LIST_PROJECTS_TOOL_DOC similarly: mention default limit=50, `limit=null` for all. Add: "folder accepts an ID or name. Names use case-insensitive substring matching; multiple matches return all. Unrecognized names are skipped with a warning."

    3. Update LIST_TAGS_TOOL_DOC, LIST_FOLDERS_TOOL_DOC, LIST_PERSPECTIVES_TOOL_DOC: replace "No pagination" with documentation of default limit=50, limit/offset support, `limit=null` for all.

    4. Update LIST_TAGS_QUERY_DOC, LIST_FOLDERS_QUERY_DOC, LIST_PERSPECTIVES_QUERY_DOC: add "and paginate" (e.g., "Filter and paginate tags.") to match tasks/projects pattern.

    5. Add new description constants for entity-reference fields:
       - PROJECT_FILTER_DESC: "Project ID or name. Names use case-insensitive substring matching -- if multiple projects match, tasks from all are included."
       - TAGS_FILTER_DESC: "Tag names or IDs (OR logic). Names use case-insensitive substring matching."
       - FOLDER_FILTER_DESC: "Folder ID or name. Names use case-insensitive substring matching -- if multiple folders match, projects from all are included."

    **tasks.py**: Add `Field(description=PROJECT_FILTER_DESC)` to the `project` field on ListTasksQuery. Add `Field(description=TAGS_FILTER_DESC)` to the `tags` field. Remove the inline comments that previously served this purpose.

    **projects.py**: Add `Field(description=FOLDER_FILTER_DESC)` to the `folder` field on ListProjectsQuery. Remove the inline comment.

    Run `uv run pytest tests/test_output_schema.py -x -q` after changes to verify serialized output still validates.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/.claude/worktrees/agent-c743ac7c && uv run pytest tests/test_output_schema.py tests/ -x -q --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <done>
    - LIST_TASKS_TOOL_DOC and LIST_PROJECTS_TOOL_DOC mention default limit and entity-reference resolution behavior
    - LIST_TAGS/FOLDERS/PERSPECTIVES_TOOL_DOC document pagination (limit/offset, default 50)
    - project, tags, folder fields on query models have Field(description=...) explaining resolution cascade
    - test_output_schema passes
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q` -- full suite passes
2. `uv run pytest tests/test_output_schema.py -x -q` -- schema validation passes
3. Instantiate `ListTasksQuery()` in Python REPL and confirm `limit == 50`
4. Instantiate `ListTagsQuery()` and confirm `limit == 50`, `offset == None`
5. Confirm `ListTagsQuery(offset=5)` raises ValidationError (offset requires limit)
</verification>

<success_criteria>
- All 5 list tools default to limit=50, preventing unbounded responses
- Tags, folders, perspectives support limit/offset pagination with correct has_more/total
- Entity-reference filter resolution documented in tool descriptions and Field descriptions
- Full test suite passes including cross-path equivalence and output schema
</success_criteria>

<output>
After completion, create `.planning/phases/37-server-registration-and-integration-was-phase-38/37-03-SUMMARY.md`
</output>
