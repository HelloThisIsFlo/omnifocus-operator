---
phase: 36.1-migrate-write-tools-to-typed-params-with-validation-middleware
plan: 02
type: execute
wave: 2
depends_on: [36.1-01]
files_modified:
  - tests/test_server.py
  - tests/test_middleware.py
autonomous: true
requirements: [WRIT-01, WRIT-02, WRIT-04, WRIT-05, WRIT-10, WRIT-11]

must_haves:
  truths:
    - "add_tasks inputSchema contains 52+ fields with types and enums"
    - "edit_tasks inputSchema contains 61+ fields with types and enums"
    - "If FastMCP moves validation outside middleware chain, canary test fails with explanatory message"
    - "_format_validation_errors produces 'Task N: field' format for list[Model] loc tuples"
    - "UNSET sentinel errors are filtered out by ctx-class check, not string matching"
    - "Validation ToolErrors appear in captured logs via logging middleware"
  artifacts:
    - path: "tests/test_server.py"
      provides: "Schema richness tests, canary test"
      contains: "test_canary_middleware_catches_validation_error"
    - path: "tests/test_middleware.py"
      provides: "Unit tests for middleware helpers and logging integration"
      contains: "test_format_validation_errors"
  key_links:
    - from: "tests/test_server.py"
      to: "src/omnifocus_operator/middleware.py"
      via: "canary test verifies middleware catches ValidationError"
      pattern: "test_canary.*middleware"
    - from: "tests/test_middleware.py"
      to: "src/omnifocus_operator/middleware.py"
      via: "unit tests for _format_validation_errors, _clean_loc, _strip_items_prefix, _extract_item_index"
      pattern: "from omnifocus_operator.middleware import"
---

<objective>
Add schema richness tests (WRIT-01, WRIT-02), a canary test (WRIT-11), dedicated unit tests for middleware formatting helpers (WRIT-04, WRIT-05), and a logging integration test (WRIT-10).

Purpose: Lock in the migration outcomes with automated regression guards. Schema tests prove agents see rich inputSchema. Canary test future-proofs against FastMCP changes. Helper unit tests cover the critical behavioral contracts of error formatting, UNSET filtering, and location prefix logic independently of integration tests.

Output: New tests in test_server.py (schema + canary) and test_middleware.py (helper units + logging).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-migrate-write-tools-to-typed-params-with-validation-middleware/36.1-CONTEXT.md
@.planning/phases/36.1-migrate-write-tools-to-typed-params-with-validation-middleware/36.1-RESEARCH.md
@.planning/phases/36.1-migrate-write-tools-to-typed-params-with-validation-middleware/36.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these to write tests -->

From src/omnifocus_operator/middleware.py (created by Plan 01):
```python
class ValidationReformatterMiddleware(Middleware):
    async def on_call_tool(self, context: MiddlewareContext, call_next: Any) -> Any:
        try:
            return await call_next(context)
        except ValidationError as exc:
            messages = _format_validation_errors(exc)
            raise ToolError("; ".join(messages) or INVALID_INPUT) from None

def _clean_loc(loc: tuple[str | int, ...]) -> tuple[str | int, ...]: ...
def _strip_items_prefix(loc: tuple[str | int, ...]) -> tuple[str | int, ...]: ...
def _extract_item_index(loc: tuple[str | int, ...]) -> int | None: ...
def _format_validation_errors(exc: ValidationError) -> list[str]: ...
```

From src/omnifocus_operator/server.py (updated by Plan 01):
```python
async def add_tasks(items: list[AddTaskCommand], ctx: Context) -> list[AddTaskResult]: ...
async def edit_tasks(items: list[EditTaskCommand], ctx: Context) -> list[EditTaskResult]: ...
```

From tests/conftest.py (updated by Plan 01):
```python
# server fixture registers middleware:
srv.add_middleware(ValidationReformatterMiddleware())
srv.add_middleware(ToolLoggingMiddleware(logging.getLogger(...)))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema richness tests and canary test</name>
  <files>tests/test_server.py</files>
  <read_first>
    - tests/test_server.py (understand existing test class structure, find where to add new tests)
    - src/omnifocus_operator/contracts/use_cases/add/tasks.py (AddTaskCommand — count the fields for expected minimum)
    - src/omnifocus_operator/contracts/use_cases/edit/tasks.py (EditTaskCommand — count the fields for expected minimum)
    - src/omnifocus_operator/middleware.py (ValidationReformatterMiddleware — understand what the canary guards)
  </read_first>
  <action>
**Schema richness tests (WRIT-01, WRIT-02):**

Add a new test class (or add to existing schema/tool-listing class) with two tests that verify inputSchema field counts. Use `client.list_tools()` to get the tool list, find the `add_tasks` and `edit_tasks` tools, and inspect their `inputSchema`.

1. `test_add_tasks_schema_has_rich_input_schema` — WRIT-01:
   - Call `client.list_tools()`, find tool where `tool.name == "add_tasks"`
   - Get `tool.inputSchema` (a dict). Count total leaf properties recursively by walking `properties` dicts and `$defs` refs. Alternative simpler approach: convert the schema dict to JSON string and count unique property keys — or just count the top-level `items` array's inner `$ref` resolved properties.
   - Simplest reliable approach: serialize `tool.inputSchema` to JSON string, assert `len(json_str) > 500` (rich schema is large) AND assert specific key fields are present: check that `"name"`, `"dueDate"`, `"flagged"`, `"estimatedMinutes"`, `"repetitionRule"`, `"tags"` all appear in the JSON string.
   - Also assert NO snake_case leakage (WRIT-03): assert `"due_date"` NOT in JSON string, assert `"defer_date"` NOT in JSON string, assert `"estimated_minutes"` NOT in JSON string.

2. `test_edit_tasks_schema_has_rich_input_schema` — WRIT-02:
   - Same approach for `edit_tasks`
   - Assert `"id"`, `"name"`, `"dueDate"`, `"flagged"`, `"estimatedMinutes"`, `"repetitionRule"`, `"actions"` all appear in JSON string.
   - Also assert NO snake_case leakage (WRIT-03): same checks as above.

**Canary test (WRIT-11, D-11):**

3. `test_canary_middleware_catches_validation_error`:
   - **Docstring** (MUST be included — D-11 requires clear explanation):
     ```
     Guard: FastMCP validation must happen inside call_next() so middleware can catch it.

     This test verifies that ValidationReformatterMiddleware catches ValidationError
     raised by FastMCP's internal type_adapter.validate_python() during tool dispatch.
     If this test fails after a FastMCP upgrade, it means validation moved BEFORE the
     middleware chain -- the middleware architecture needs rethinking.

     What to do if this fails:
     1. Check FastMCP changelog for validation pipeline changes
     2. The middleware approach may no longer work -- validation errors will bypass it
     3. Options: move validation back to handlers, or hook into FastMCP's new pipeline
     ```
   - Send an invalid `add_tasks` call via `client.call_tool("add_tasks", {"items": [{"bogus": "field"}]})` — this should raise `ToolError` (not raw `ValidationError` or unhandled exception)
   - Assert `pytest.raises(ToolError)` catches it
   - Assert the error message contains `"Task 1:"` (proving the middleware reformatted it, not raw Pydantic output)
   - Assert the error message does NOT contain `"validation error"` or `"ValidationError"` (no Pydantic internals leaking)

**Run full suite to confirm no regressions.**
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_server.py -x -q -k "rich_input_schema or canary_middleware" && uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_server.py contains `test_add_tasks_schema_has_rich_input_schema`
    - test_server.py contains `test_edit_tasks_schema_has_rich_input_schema`
    - test_server.py contains `test_canary_middleware_catches_validation_error`
    - Canary test docstring contains "What to do if this fails"
    - Canary test asserts ToolError (not ValidationError)
    - Canary test asserts error message contains "Task 1:"
    - Schema tests assert presence of `"dueDate"` and absence of `"due_date"` in inputSchema JSON
    - `uv run pytest -x -q` exits 0
  </acceptance_criteria>
  <done>Schema richness tests prove add_tasks has 52+ fields and edit_tasks has 61+ fields in inputSchema with camelCase aliases, canary test guards middleware architecture, full suite green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests for middleware helpers and logging integration</name>
  <files>tests/test_middleware.py</files>
  <read_first>
    - src/omnifocus_operator/middleware.py (read _format_validation_errors, _clean_loc, _strip_items_prefix, _extract_item_index — understand exact signatures and behavior)
    - src/omnifocus_operator/agent_messages/errors.py (INVALID_INPUT, UNKNOWN_FIELD constants — needed for expected output assertions)
  </read_first>
  <behavior>
    **_format_validation_errors (WRIT-04 — "Task N:" format):**
    - Test: single error with loc `("items", 0, "name")` produces `["Task 1: ..."]` (idx+1 prefix)
    - Test: single error with loc `("items", 2, "dueDate")` produces `["Task 3: ..."]` (correct index)
    - Test: error with no `items` prefix in loc (e.g., `("name",)`) produces message WITHOUT "Task N:" prefix
    - Test: `extra_forbidden` error type with loc `("items", 0, "bogusField")` produces `"Task 1: Unknown field 'bogusField'"` (using UNKNOWN_FIELD template)

    **UNSET filtering (WRIT-05):**
    - Test: error with `ctx={"class": "_Unset"}` is filtered out (not in returned list)
    - Test: error with msg containing "_Unset" but ctx WITHOUT `class: "_Unset"` is NOT filtered (ctx-based, not string-based — per D-08a)
    - Test: mix of UNSET and real errors returns only the real errors

    **_strip_items_prefix:**
    - Test: `("items", 0, "name")` returns `("name",)`
    - Test: `("items", 1, "dueDate", "nested")` returns `("dueDate", "nested")`
    - Test: `("name",)` returns `("name",)` (no items prefix — unchanged)
    - Test: `()` returns `()` (empty — unchanged)

    **_extract_item_index:**
    - Test: `("items", 0, "name")` returns `0`
    - Test: `("items", 5, "field")` returns `5`
    - Test: `("name",)` returns `None`
    - Test: `()` returns `None`

    **_clean_loc:**
    - Test: verify it strips `items` and integer parts as designed (check existing behavior from server.py)

    **Logging integration (WRIT-10):**
    - Test: trigger a validation error through the full middleware stack, assert the ToolError message appears in captured log output (use `caplog` fixture or similar)
  </behavior>
  <action>
Create `tests/test_middleware.py` with dedicated unit tests for the middleware helpers.

**Unit tests for helpers** — import helpers directly from `omnifocus_operator.middleware`:

1. For `_format_validation_errors`: construct `ValidationError` instances using Pydantic's programmatic API. Create a simple Pydantic model for testing (e.g., `class _TestModel(BaseModel): name: str; model_config = ConfigDict(extra="forbid")`), then call `_TestModel.model_validate(...)` in a try/except to capture a real `ValidationError`. Pass it to `_format_validation_errors` and assert output strings.

   Alternatively, if constructing real `ValidationError` is awkward, use `pydantic_core.InitErrorDetails` to build synthetic errors — but real `ValidationError` from `model_validate` is more realistic and preferred.

   Key cases for WRIT-04:
   - Validate `{"items": [{"bogus": "x"}]}` against a `list[_TestModel]` wrapper to get `items.0.bogus` loc — assert output contains `"Task 1: Unknown field 'bogus'"`
   - Validate `{"items": [{}, {}, {"bogus": "x"}]}` to get `items.2.bogus` loc — assert output contains `"Task 3:"`

2. For WRIT-05 (UNSET filtering): Create a scenario where `_Unset` ctx-class errors appear. Since constructing these synthetically is complex, an alternative is to directly test the filtering logic by mocking `exc.errors()` to return a list with a `ctx: {"class": "_Unset"}` entry. Verify it's excluded from output.

3. For `_strip_items_prefix` and `_extract_item_index`: direct unit tests with tuple inputs as described in `<behavior>`.

4. For `_clean_loc`: direct unit tests verifying the cleaning behavior.

**Logging integration test (WRIT-10):**

5. Create an async test that:
   - Sets up a minimal FastMCP server with both middlewares registered
   - Sends an invalid tool call that triggers ValidationError
   - Uses `caplog` at DEBUG level to capture log output from `omnifocus_operator.server` logger
   - Asserts that the log contains the ToolError message (proving logging middleware saw the reformatted error)

**Run tests to confirm green.**
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_middleware.py -x -q -v && uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_middleware.py exists with unit tests for _format_validation_errors, _strip_items_prefix, _extract_item_index, _clean_loc
    - At least one test asserts "Task 1:" prefix in formatted output (WRIT-04)
    - At least one test asserts "Task 3:" prefix for index 2 (WRIT-04)
    - At least one test asserts UNSET ctx-class errors are filtered out (WRIT-05)
    - At least one test asserts UNSET filtering uses ctx, not string matching (WRIT-05)
    - At least one test asserts ToolError appears in captured logs (WRIT-10)
    - `uv run pytest tests/test_middleware.py -x -q` exits 0
    - `uv run pytest -x -q` exits 0
  </acceptance_criteria>
  <done>Middleware helpers have dedicated unit tests covering "Task N:" format (WRIT-04), ctx-based UNSET filtering (WRIT-05), and logging integration (WRIT-10). All tests green.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_server.py -x -q -k "rich_input_schema"` — schema richness tests pass
2. `uv run pytest tests/test_server.py -x -q -k "canary"` — canary test passes
3. `uv run pytest tests/test_middleware.py -x -q` — all middleware helper unit tests pass
4. `uv run pytest -x -q` — full suite green
5. `grep "test_canary_middleware_catches_validation_error" tests/test_server.py` — canary test exists
6. `grep "test_format_validation_errors" tests/test_middleware.py` — helper unit tests exist
7. `grep "due_date" tests/test_server.py` — snake_case check present in schema tests
</verification>

<success_criteria>
- add_tasks inputSchema contains all AddTaskCommand fields with camelCase aliases (WRIT-01, WRIT-03)
- edit_tasks inputSchema contains all EditTaskCommand fields with camelCase aliases (WRIT-02, WRIT-03)
- Canary test verifies middleware catches ValidationError from call_next() with clear failure guidance (WRIT-11)
- _format_validation_errors unit tests verify "Task N:" format for list[Model] loc tuples (WRIT-04)
- UNSET filtering unit tests verify ctx-based filtering, not string matching (WRIT-05)
- Logging integration test verifies ToolError appears in captured logs (WRIT-10)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/36.1-migrate-write-tools-to-typed-params-with-validation-middleware/36.1-02-SUMMARY.md`
</output>
