---
phase: 36.1-migrate-write-tools-to-typed-params-with-validation-middleware
plan: 02
type: execute
wave: 2
depends_on: [36.1-01]
files_modified:
  - tests/test_server.py
autonomous: true
requirements: [WRIT-01, WRIT-02, WRIT-11]

must_haves:
  truths:
    - "add_tasks inputSchema contains 52+ fields with types and enums"
    - "edit_tasks inputSchema contains 61+ fields with types and enums"
    - "If FastMCP moves validation outside middleware chain, canary test fails with explanatory message"
  artifacts:
    - path: "tests/test_server.py"
      provides: "Schema richness tests, canary test"
      contains: "test_canary_middleware_catches_validation_error"
  key_links:
    - from: "tests/test_server.py"
      to: "src/omnifocus_operator/middleware.py"
      via: "canary test verifies middleware catches ValidationError"
      pattern: "test_canary.*middleware"
---

<objective>
Add schema richness tests (WRIT-01, WRIT-02) verifying inputSchema field counts, and a canary test (WRIT-11) that detects if a FastMCP upgrade moves validation outside the middleware chain.

Purpose: Lock in the migration outcomes with automated regression guards. The schema tests prove agents see rich inputSchema. The canary test future-proofs against FastMCP internal changes.

Output: New tests in test_server.py proving schema richness and middleware architecture assumptions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-migrate-write-tools-to-typed-params-with-validation-middleware/36.1-CONTEXT.md
@.planning/phases/36.1-migrate-write-tools-to-typed-params-with-validation-middleware/36.1-RESEARCH.md
@.planning/phases/36.1-migrate-write-tools-to-typed-params-with-validation-middleware/36.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these to write tests -->

From src/omnifocus_operator/middleware.py (created by Plan 01):
```python
class ValidationReformatterMiddleware(Middleware):
    async def on_call_tool(self, context: MiddlewareContext, call_next: Any) -> Any:
        try:
            return await call_next(context)
        except ValidationError as exc:
            messages = _format_validation_errors(exc)
            raise ToolError("; ".join(messages) or INVALID_INPUT) from None
```

From src/omnifocus_operator/server.py (updated by Plan 01):
```python
async def add_tasks(items: list[AddTaskCommand], ctx: Context) -> list[AddTaskResult]: ...
async def edit_tasks(items: list[EditTaskCommand], ctx: Context) -> list[EditTaskResult]: ...
```

From tests/conftest.py (updated by Plan 01):
```python
# server fixture registers middleware:
srv.add_middleware(ValidationReformatterMiddleware())
srv.add_middleware(ToolLoggingMiddleware(logging.getLogger(...)))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema richness tests and canary test</name>
  <files>tests/test_server.py</files>
  <read_first>
    - tests/test_server.py (understand existing test class structure, find where to add new tests)
    - src/omnifocus_operator/contracts/use_cases/add/tasks.py (AddTaskCommand — count the fields for expected minimum)
    - src/omnifocus_operator/contracts/use_cases/edit/tasks.py (EditTaskCommand — count the fields for expected minimum)
    - src/omnifocus_operator/middleware.py (ValidationReformatterMiddleware — understand what the canary guards)
  </read_first>
  <action>
**Schema richness tests (WRIT-01, WRIT-02):**

Add a new test class (or add to existing schema/tool-listing class) with two tests that verify inputSchema field counts. Use `client.list_tools()` to get the tool list, find the `add_tasks` and `edit_tasks` tools, and inspect their `inputSchema`.

1. `test_add_tasks_schema_has_rich_input_schema` — WRIT-01:
   - Call `client.list_tools()`, find tool where `tool.name == "add_tasks"`
   - Get `tool.inputSchema` (a dict). Count total leaf properties recursively by walking `properties` dicts and `$defs` refs. Alternative simpler approach: convert the schema dict to JSON string and count unique property keys — or just count the top-level `items` array's inner `$ref` resolved properties.
   - Simplest reliable approach: serialize `tool.inputSchema` to JSON string, assert `len(json_str) > 500` (rich schema is large) AND assert specific key fields are present: check that `"name"`, `"dueDate"`, `"flagged"`, `"estimatedMinutes"`, `"repetitionRule"`, `"tags"` all appear in the JSON string.
   - Also assert NO snake_case leakage (WRIT-03): assert `"due_date"` NOT in JSON string, assert `"defer_date"` NOT in JSON string, assert `"estimated_minutes"` NOT in JSON string.

2. `test_edit_tasks_schema_has_rich_input_schema` — WRIT-02:
   - Same approach for `edit_tasks`
   - Assert `"id"`, `"name"`, `"dueDate"`, `"flagged"`, `"estimatedMinutes"`, `"repetitionRule"`, `"actions"` all appear in JSON string.
   - Also assert NO snake_case leakage (WRIT-03): same checks as above.

**Canary test (WRIT-11, D-11):**

3. `test_canary_middleware_catches_validation_error`:
   - **Docstring** (MUST be included — D-11 requires clear explanation):
     ```
     Guard: FastMCP validation must happen inside call_next() so middleware can catch it.

     This test verifies that ValidationReformatterMiddleware catches ValidationError
     raised by FastMCP's internal type_adapter.validate_python() during tool dispatch.
     If this test fails after a FastMCP upgrade, it means validation moved BEFORE the
     middleware chain -- the middleware architecture needs rethinking.

     What to do if this fails:
     1. Check FastMCP changelog for validation pipeline changes
     2. The middleware approach may no longer work -- validation errors will bypass it
     3. Options: move validation back to handlers, or hook into FastMCP's new pipeline
     ```
   - Send an invalid `add_tasks` call via `client.call_tool("add_tasks", {"items": [{"bogus": "field"}]})` — this should raise `ToolError` (not raw `ValidationError` or unhandled exception)
   - Assert `pytest.raises(ToolError)` catches it
   - Assert the error message contains `"Task 1:"` (proving the middleware reformatted it, not raw Pydantic output)
   - Assert the error message does NOT contain `"validation error"` or `"ValidationError"` (no Pydantic internals leaking)

**Run full suite to confirm no regressions.**
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_server.py -x -q -k "rich_input_schema or canary_middleware" && uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_server.py contains `test_add_tasks_schema_has_rich_input_schema`
    - test_server.py contains `test_edit_tasks_schema_has_rich_input_schema`
    - test_server.py contains `test_canary_middleware_catches_validation_error`
    - Canary test docstring contains "What to do if this fails"
    - Canary test asserts ToolError (not ValidationError)
    - Canary test asserts error message contains "Task 1:"
    - Schema tests assert presence of `"dueDate"` and absence of `"due_date"` in inputSchema JSON
    - `uv run pytest -x -q` exits 0
  </acceptance_criteria>
  <done>Schema richness tests prove add_tasks has 52+ fields and edit_tasks has 61+ fields in inputSchema with camelCase aliases, canary test guards middleware architecture, full suite green</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_server.py -x -q -k "rich_input_schema"` — schema richness tests pass
2. `uv run pytest tests/test_server.py -x -q -k "canary"` — canary test passes
3. `uv run pytest -x -q` — full suite green
4. `grep "test_canary_middleware_catches_validation_error" tests/test_server.py` — canary test exists
5. `grep "due_date" tests/test_server.py` — snake_case check present in schema tests
</verification>

<success_criteria>
- add_tasks inputSchema contains all AddTaskCommand fields with camelCase aliases (WRIT-01, WRIT-03)
- edit_tasks inputSchema contains all EditTaskCommand fields with camelCase aliases (WRIT-02, WRIT-03)
- Canary test verifies middleware catches ValidationError from call_next() with clear failure guidance (WRIT-11)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/36.1-migrate-write-tools-to-typed-params-with-validation-middleware/36.1-02-SUMMARY.md`
</output>
