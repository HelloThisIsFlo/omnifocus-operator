---
status: complete
phase: 36.1-migrate-write-tools-to-typed-params-with-validation-middleware
source: [36.1-01-SUMMARY.md, 36.1-02-SUMMARY.md]
started: 2026-03-31T21:00:00Z
updated: 2026-03-31T21:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. add_tasks validation error formatting
expected: Call add_tasks with invalid input (e.g., bad field type or missing required field). Error message shows "Task 1:" prefix identifying the failing item, with clean agent-readable description. No Pydantic internals exposed.
result: pass

### 2. edit_tasks validation error formatting
expected: Call edit_tasks with invalid input (e.g., unrecognized field). Error message shows "Task N:" prefix and UNKNOWN_FIELD guidance. No raw Pydantic output.
result: pass

### 3. Schema exposes camelCase fields
expected: Inspect the inputSchema for add_tasks and edit_tasks. All property names should be camelCase (e.g., dueDate, estimatedMinutes, addTags). No snake_case property keys (due_date, estimated_minutes, add_tags).
result: pass

### 4. _Unset sentinel filtering
expected: Call add_tasks or edit_tasks omitting optional fields. If validation fails, the error message should NOT mention "_Unset" or sentinel values for fields that were simply not provided.
result: pass

### 5. Behavioral preservation
expected: Call add_tasks with valid input to create a task, and edit_tasks with valid input to modify it. Both operations succeed exactly as before the migration — no regressions.
result: pass

### 6. Middleware canary guards architecture
expected: A canary test exists in the test suite that will fail with clear guidance if FastMCP ever moves validation outside the middleware call_next() boundary. Run the test suite and confirm the canary test passes.
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
