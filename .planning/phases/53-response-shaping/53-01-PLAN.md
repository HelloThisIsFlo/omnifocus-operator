---
phase: 53-response-shaping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/models/common.py
  - src/omnifocus_operator/models/task.py
  - src/omnifocus_operator/agent_messages/descriptions.py
  - src/omnifocus_operator/repository/hybrid/hybrid.py
  - src/omnifocus_operator/repository/bridge_only/bridge_only.py
  - tests/conftest.py
  - tests/doubles/bridge.py
  - tests/test_cross_path_equivalence.py
  - tests/test_hybrid_repository.py
  - tests/test_models.py
  - tests/test_service.py
  - tests/test_list_pipelines.py
  - tests/test_server.py
  - tests/test_adapter.py
  - tests/test_stateful_bridge.py
  - tests/test_query_builder.py
autonomous: true
requirements:
  - RENAME-01

must_haves:
  truths:
    - "All model fields that were effective_* are now inherited_*"
    - "model_dump(by_alias=True) produces inheritedDueDate, inheritedFlagged, etc. instead of effective*"
    - "All tests pass with the new field names"
    - "Golden master JSON files are NOT modified (bridge format unchanged)"
    - "Bridge-format factories in conftest (make_task_dict) still use effective* keys"
    - "Model-format factories in conftest (make_model_task_dict) use inherited* keys"
  artifacts:
    - path: "src/omnifocus_operator/models/common.py"
      provides: "ActionableEntity with inherited_* fields"
      contains: "inherited_flagged"
    - path: "src/omnifocus_operator/models/task.py"
      provides: "Task with inherited_completion_date"
      contains: "inherited_completion_date"
    - path: "src/omnifocus_operator/agent_messages/descriptions.py"
      provides: "INHERITED_* description constants"
      contains: "INHERITED_FLAGGED"
  key_links:
    - from: "src/omnifocus_operator/models/common.py"
      to: "src/omnifocus_operator/agent_messages/descriptions.py"
      via: "Field(description=INHERITED_FLAGGED)"
      pattern: "INHERITED_FLAGGED"
    - from: "src/omnifocus_operator/repository/hybrid/hybrid.py"
      to: "models"
      via: "mapper dicts mapping bridge keys to model field names"
      pattern: "inherited_"
---

<objective>
Rename all `effective_*` fields to `inherited_*` across the entire codebase.

Purpose: Clearer agent-facing semantics -- "inherited" communicates that the value comes from the parent hierarchy, which is what the field actually represents. Pre-release, no compatibility concern (per D-01).

Output: All 6 effective fields renamed at the Python level. The `to_camel` alias generator automatically produces `inheritedDueDate` etc. in serialized output. Golden master JSON untouched (bridge format).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-response-shaping/53-CONTEXT.md
@.planning/phases/53-response-shaping/53-RESEARCH.md

@src/omnifocus_operator/models/common.py
@src/omnifocus_operator/models/task.py
@src/omnifocus_operator/agent_messages/descriptions.py
@src/omnifocus_operator/repository/hybrid/hybrid.py
@src/omnifocus_operator/repository/bridge_only/bridge_only.py

<interfaces>
<!-- Key types the executor needs to understand -->

From models/common.py (ActionableEntity) — fields to rename:
  effective_flagged -> inherited_flagged
  effective_due_date -> inherited_due_date
  effective_defer_date -> inherited_defer_date
  effective_planned_date -> inherited_planned_date
  effective_drop_date -> inherited_drop_date

From models/task.py (Task) — field to rename:
  effective_completion_date -> inherited_completion_date

From descriptions.py — constants to rename:
  EFFECTIVE_FLAGGED -> INHERITED_FLAGGED
  EFFECTIVE_DUE_DATE -> INHERITED_DUE_DATE
  EFFECTIVE_DEFER_DATE -> INHERITED_DEFER_DATE
  EFFECTIVE_PLANNED_DATE -> INHERITED_PLANNED_DATE
  EFFECTIVE_DROP_DATE -> INHERITED_DROP_DATE
  EFFECTIVE_COMPLETION_DATE -> INHERITED_COMPLETION_DATE

From models/base.py:
  OmniFocusBaseModel uses `alias_generator=to_camel` from pydantic
  This means inherited_due_date -> inheritedDueDate automatically
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename effective_* to inherited_* in production code</name>
  <files>
    src/omnifocus_operator/models/common.py,
    src/omnifocus_operator/models/task.py,
    src/omnifocus_operator/agent_messages/descriptions.py,
    src/omnifocus_operator/repository/hybrid/hybrid.py,
    src/omnifocus_operator/repository/bridge_only/bridge_only.py
  </files>
  <read_first>
    src/omnifocus_operator/models/common.py,
    src/omnifocus_operator/models/task.py,
    src/omnifocus_operator/agent_messages/descriptions.py,
    src/omnifocus_operator/repository/hybrid/hybrid.py,
    src/omnifocus_operator/repository/bridge_only/bridge_only.py,
    src/omnifocus_operator/models/base.py,
    src/omnifocus_operator/service/domain.py,
    src/omnifocus_operator/service/service.py
  </read_first>
  <action>
Per RENAME-01 and D-01: Full Python rename of 6 fields at the model level, plus description constants.

**models/common.py (ActionableEntity)** — rename 5 fields:
- `effective_flagged` -> `inherited_flagged`
- `effective_due_date` -> `inherited_due_date`
- `effective_defer_date` -> `inherited_defer_date`
- `effective_planned_date` -> `inherited_planned_date`
- `effective_drop_date` -> `inherited_drop_date`
- Update Field descriptions: `description=EFFECTIVE_FLAGGED` -> `description=INHERITED_FLAGGED`, etc.
- Update imports from descriptions.py to use new constant names

**models/task.py (Task)** — rename 1 field:
- `effective_completion_date` -> `inherited_completion_date`
- Update Field description: `description=EFFECTIVE_COMPLETION_DATE` -> `description=INHERITED_COMPLETION_DATE`
- Update import from descriptions.py

**agent_messages/descriptions.py** — rename 6 constants:
- `EFFECTIVE_FLAGGED` -> `INHERITED_FLAGGED`
- `EFFECTIVE_DUE_DATE` -> `INHERITED_DUE_DATE`
- `EFFECTIVE_DEFER_DATE` -> `INHERITED_DEFER_DATE`
- `EFFECTIVE_PLANNED_DATE` -> `INHERITED_PLANNED_DATE`
- `EFFECTIVE_DROP_DATE` -> `INHERITED_DROP_DATE`
- `EFFECTIVE_COMPLETION_DATE` -> `INHERITED_COMPLETION_DATE`
- Keep the description TEXT as-is for now (Phase 53 Plan 05 will update the content to D-11 verbatim text)

**repository/hybrid/hybrid.py** — update mapper dicts:
- All references to `effective_flagged`, `effective_due_date`, etc. in field mapping dicts change to `inherited_*` equivalents
- These are the model-side target names. The bridge-side source keys (`effectiveFlagged` etc.) do NOT change.

**repository/bridge_only/bridge_only.py** — update field maps:
- Same pattern: model-side target names change from `effective_*` to `inherited_*`
- Bridge-side source keys remain unchanged

**CRITICAL: Do NOT rename:**
- Local variables in `service/domain.py` and `service/service.py` that happen to use `effective_` as a generic English word (e.g., `effective_dates`, `effective_type`). These are NOT model field references. Per Pitfall 6 in RESEARCH.md.
- Anything in `bridge/bridge.js` (OmniFocus API field names)
- Anything in golden master JSON files (bridge-format snapshots)
- Bridge-format factory keys in conftest.py (`make_task_dict` uses bridge-format camelCase keys which are OmniFocus API names)
- `tests/golden_master/normalize.py` PRESENCE_CHECK fields (bridge-format keys)
- Anything in `simulator/data.py` (bridge-format fixture data)
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run python -c "from omnifocus_operator.models.common import ActionableEntity; print([f for f in ActionableEntity.model_fields if 'inherited' in f])"</automated>
  </verify>
  <acceptance_criteria>
    - models/common.py contains `inherited_flagged: bool` and `inherited_due_date`, `inherited_defer_date`, `inherited_planned_date`, `inherited_drop_date` fields
    - models/task.py contains `inherited_completion_date` field
    - descriptions.py contains `INHERITED_FLAGGED =`, `INHERITED_DUE_DATE =`, `INHERITED_DEFER_DATE =`, `INHERITED_PLANNED_DATE =`, `INHERITED_DROP_DATE =`, `INHERITED_COMPLETION_DATE =`
    - descriptions.py does NOT contain `EFFECTIVE_FLAGGED`, `EFFECTIVE_DUE_DATE`, etc. (old names removed)
    - models/common.py imports `INHERITED_FLAGGED`, `INHERITED_DUE_DATE`, etc. from descriptions
    - hybrid.py maps bridge keys to `inherited_*` model field names (not `effective_*`)
    - bridge_only.py maps bridge keys to `inherited_*` model field names (not `effective_*`)
    - service/domain.py and service/service.py local variables named `effective_*` are NOT renamed
    - No golden master JSON files are modified
    - No bridge.js files are modified
  </acceptance_criteria>
  <done>All 6 effective_* model fields renamed to inherited_* in production code. Imports and mapper dicts updated. Service-layer local variables and bridge-format files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update all test files for inherited rename</name>
  <files>
    tests/conftest.py,
    tests/doubles/bridge.py,
    tests/test_cross_path_equivalence.py,
    tests/test_hybrid_repository.py,
    tests/test_models.py,
    tests/test_service.py,
    tests/test_list_pipelines.py,
    tests/test_server.py,
    tests/test_adapter.py,
    tests/test_stateful_bridge.py,
    tests/test_query_builder.py
  </files>
  <read_first>
    tests/conftest.py,
    tests/doubles/bridge.py,
    tests/test_cross_path_equivalence.py,
    tests/test_hybrid_repository.py,
    tests/test_models.py,
    tests/test_service.py,
    tests/test_list_pipelines.py,
    tests/test_server.py,
    tests/test_adapter.py,
    tests/test_stateful_bridge.py,
    tests/test_query_builder.py,
    tests/golden_master/normalize.py
  </read_first>
  <action>
Update all test files for the `effective_*` -> `inherited_*` rename. Two categories of changes:

**Category A: Model-format references (MUST rename)**
These reference Pydantic model fields or their camelCase aliases in model-format dicts:

1. **conftest.py** — `make_model_task_dict()` and similar model-format factories:
   - `"effectiveFlagged"` -> `"inheritedFlagged"`
   - `"effectiveDueDate"` -> `"inheritedDueDate"`
   - `"effectiveDeferDate"` -> `"inheritedDeferDate"`
   - `"effectivePlannedDate"` -> `"inheritedPlannedDate"`
   - `"effectiveDropDate"` -> `"inheritedDropDate"`
   - `"effectiveCompletionDate"` -> `"inheritedCompletionDate"`

2. **doubles/bridge.py** (InMemoryBridge) — if it builds model-format dicts, rename those. If it builds bridge-format dicts, leave unchanged.

3. **test_cross_path_equivalence.py** — heavy assertions on field names. Rename all model-format field name references.

4. **test_hybrid_repository.py** — mapper output assertions change from `effective_*` to `inherited_*`.

5. **test_models.py** — model field name assertions.

6. **test_service.py** — service pipeline output assertions referencing model fields.

7. **test_list_pipelines.py** — query field mapping references.

8. **test_server.py** — integration assertions on serialized output (camelCase).

9. **test_adapter.py** — adapter mapping assertions (target changes from `effective_*` to `inherited_*`).

10. **test_stateful_bridge.py** — bridge output assertions.

11. **test_query_builder.py** — SQL column references that map to model fields.

**Category B: Bridge-format references (do NOT rename)**
- `make_task_dict()` in conftest.py (bridge-format factory) — keys like `"effectiveFlagged"` stay
- `normalize.py` PRESENCE_CHECK — bridge-format keys stay
- Any assertion checking raw bridge output format

**How to distinguish:** Bridge-format dicts have keys matching OmniFocus JS API (`effectiveFlagged`). Model-format dicts/assertions reference Python field names (`effective_flagged` or `inherited_flagged`) or model-dump aliases that should now be `inheritedFlagged`.

The key discriminator: if the dict is being passed TO Pydantic model creation or comparing against model output, it's model-format (rename). If it's comparing against raw bridge/adapter input, it's bridge-format (keep).

After all renames, run the full test suite.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -x -q` exits 0 (all tests pass)
    - conftest.py `make_model_task_dict` contains `"inheritedFlagged"` (not `"effectiveFlagged"`)
    - conftest.py `make_task_dict` (bridge-format) still contains `"effectiveFlagged"` (unchanged)
    - golden_master/normalize.py is NOT modified
    - No test file contains the string `effective_flagged` in a model-field context (search for `\.effective_flagged` or `["effective_flagged"]` should return 0 hits in production code)
    - `uv run pytest tests/test_output_schema.py -x -q` exits 0 (output schema validation still passes)
  </acceptance_criteria>
  <done>All tests pass with inherited_* field names. Model-format references updated, bridge-format references preserved. Output schema validation passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure rename refactoring, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | Golden master JSON | accept | Rename is Python-side only. Bridge-format JSON files are not model output. Verified in RESEARCH.md A3. |
</threat_model>

<verification>
- `uv run pytest tests/ -q` — full suite green
- `uv run pytest tests/test_output_schema.py -x -q` — output schema still valid
- `grep -r "effective_flagged" src/` returns 0 hits (or only in service local variables)
- `grep -r "EFFECTIVE_FLAGGED" src/` returns 0 hits
</verification>

<success_criteria>
All 6 effective_* fields renamed to inherited_* across models, descriptions, mappers, and tests. Full test suite passes. Golden master and bridge-format files untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/53-response-shaping/53-01-SUMMARY.md`
</output>
