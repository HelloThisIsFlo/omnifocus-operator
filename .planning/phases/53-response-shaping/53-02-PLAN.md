---
phase: 53-response-shaping
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - src/omnifocus_operator/server.py
  - src/omnifocus_operator/server/__init__.py
  - src/omnifocus_operator/server/handlers.py
  - src/omnifocus_operator/server/lifespan.py
  - tests/test_descriptions.py
autonomous: true
requirements:
  - FSEL-13

must_haves:
  truths:
    - "server.py no longer exists as a single file — it is a server/ package"
    - "create_server() is importable from omnifocus_operator.server"
    - "All 11 tool handlers live in server/handlers.py"
    - "app_lifespan lives in server/lifespan.py"
    - "All existing tests pass without changes to test imports"
  artifacts:
    - path: "src/omnifocus_operator/server/__init__.py"
      provides: "create_server() export + package marker"
      contains: "create_server"
    - path: "src/omnifocus_operator/server/handlers.py"
      provides: "All 11 tool handler functions"
      contains: "_register_tools"
    - path: "src/omnifocus_operator/server/lifespan.py"
      provides: "app_lifespan context manager"
      contains: "app_lifespan"
  key_links:
    - from: "src/omnifocus_operator/server/__init__.py"
      to: "src/omnifocus_operator/server/handlers.py"
      via: "import _register_tools"
      pattern: "from omnifocus_operator.server.handlers import"
    - from: "src/omnifocus_operator/server/__init__.py"
      to: "src/omnifocus_operator/server/lifespan.py"
      via: "import app_lifespan"
      pattern: "from omnifocus_operator.server.lifespan import"
---

<objective>
Restructure `server.py` into a `server/` package with separate modules for handlers, lifespan, and the projection module placeholder.

Purpose: Creates the package structure needed for `server/projection.py` (Plan 03). Separation of concerns — handlers, lifespan, and projection each get their own module. Per D-02.

Output: `server/` package with `__init__.py` (create_server + exports), `handlers.py` (all 11 tool handlers), `lifespan.py` (app_lifespan context manager). Old `server.py` removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-response-shaping/53-CONTEXT.md

@src/omnifocus_operator/server.py

<interfaces>
<!-- Current server.py exports -->
From server.py:
  __all__ = ["create_server"]
  def create_server() -> FastMCP: ...
  async def app_lifespan(app: FastMCP) -> AsyncIterator[dict[str, object]]: ...
  def _register_tools(mcp: FastMCP) -> None: ...

<!-- External consumers -->
From __main__.py or entry point:
  from omnifocus_operator.server import create_server

From tests/test_server.py:
  from omnifocus_operator.server import create_server (or similar)

From tests/test_descriptions.py:
  _SERVER_PATH = _SRC_ROOT / "server.py"  # MUST update to server/handlers.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split server.py into server/ package</name>
  <files>
    src/omnifocus_operator/server/__init__.py,
    src/omnifocus_operator/server/handlers.py,
    src/omnifocus_operator/server/lifespan.py,
    src/omnifocus_operator/server.py
  </files>
  <read_first>
    src/omnifocus_operator/server.py,
    src/omnifocus_operator/__main__.py,
    src/omnifocus_operator/__init__.py,
    tests/test_descriptions.py
  </read_first>
  <action>
Per D-02: Moderate split of `server.py` into a `server/` package.

**Step 1: Create `server/` directory**
Create `src/omnifocus_operator/server/` directory.

**Step 2: Create `server/lifespan.py`**
Move the `app_lifespan` async context manager function from `server.py` to `server/lifespan.py`. Include only the imports it needs (logging, asynccontextmanager, TYPE_CHECKING for AsyncIterator, FastMCP).

**Step 3: Create `server/handlers.py`**
Move the `_register_tools` function and all 11 tool handler definitions from `server.py` to `server/handlers.py`. Include all imports needed by handlers:
- FastMCP, Context, ToolAnnotations
- All description imports (GET_ALL_TOOL_DOC, LIST_TASKS_TOOL_DOC, etc.)
- All contract imports (AddTaskCommand, EditTaskCommand, ListTasksQuery, etc.)
- All model imports (AllEntities, Task, Project, Tag, Folder, Perspective, ListResult)
- Error message imports (ADD_TASKS_BATCH_LIMIT, EDIT_TASKS_BATCH_LIMIT)
- logging

**Step 4: Create `server/__init__.py`**
This is the public API of the package:
```python
"""Server package -- FastMCP server for OmniFocus Operator."""

from omnifocus_operator.middleware import (
    ToolLoggingMiddleware,
    ValidationReformatterMiddleware,
)
from omnifocus_operator.server.handlers import _register_tools
from omnifocus_operator.server.lifespan import app_lifespan

from fastmcp import FastMCP
import logging

__all__ = ["create_server"]

logger = logging.getLogger(__name__)

def create_server() -> FastMCP:
    """Create and return a configured FastMCP server instance."""
    mcp = FastMCP("omnifocus-operator", lifespan=app_lifespan)
    _register_tools(mcp)
    mcp.add_middleware(ValidationReformatterMiddleware())
    mcp.add_middleware(ToolLoggingMiddleware(logger))
    return mcp
```

**Step 5: Delete old `server.py`**
Use `git rm src/omnifocus_operator/server.py` to remove the old file.

**Key constraint:** `from omnifocus_operator.server import create_server` must still work after the change. Python resolves `server` as a package and finds `create_server` in `__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run python -c "from omnifocus_operator.server import create_server; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/omnifocus_operator/server.py` does NOT exist (deleted)
    - `src/omnifocus_operator/server/__init__.py` exists and contains `def create_server`
    - `src/omnifocus_operator/server/handlers.py` exists and contains `def _register_tools`
    - `src/omnifocus_operator/server/lifespan.py` exists and contains `async def app_lifespan`
    - `from omnifocus_operator.server import create_server` works (import test passes)
    - handlers.py contains all 11 `@mcp.tool` decorated functions
  </acceptance_criteria>
  <done>server.py split into server/ package with __init__.py, handlers.py, lifespan.py. Old file deleted via git rm.</done>
</task>

<task type="auto">
  <name>Task 2: Update test paths and verify full suite</name>
  <files>
    tests/test_descriptions.py
  </files>
  <read_first>
    tests/test_descriptions.py,
    tests/test_server.py,
    tests/test_output_schema.py
  </read_first>
  <action>
Per Pitfall 7 in RESEARCH.md: The AST enforcement test in `test_descriptions.py` hardcodes the path to `server.py`. After the restructure, tools live in `server/handlers.py`.

**test_descriptions.py:**
- Find `_SERVER_PATH` (or similar) that points to `"server.py"`. Update to point to `"server/handlers.py"`.
- Find `_CONSUMER_MODULES` or similar list that includes the server module path. Update to reference `server/handlers.py`.
- The AST test scans for `@mcp.tool()` decorators to verify description constants are used. After restructure, decorators are in `handlers.py`.

**test_server.py** — should NOT need changes. It imports `create_server` from `omnifocus_operator.server`, which still works via `__init__.py`. Verify by running.

**test_output_schema.py** — should NOT need changes. It tests output schema validation which doesn't depend on file structure. Verify by running.

After updates, run the full test suite.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -x -q` exits 0 (all tests pass)
    - test_descriptions.py references `server/handlers.py` (not `server.py`) for AST scanning
    - `uv run pytest tests/test_descriptions.py -x -q` exits 0
    - `uv run pytest tests/test_output_schema.py -x -q` exits 0
    - `uv run pytest tests/test_server.py -x -q` exits 0
  </acceptance_criteria>
  <done>All tests pass against the new server/ package structure. AST enforcement test updated to scan handlers.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure structural refactoring, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02 | Tampering | Import resolution | accept | Python package __init__.py re-exports maintain backward compatibility. Tests verify. |
</threat_model>

<verification>
- `uv run pytest tests/ -q` — full suite green
- `from omnifocus_operator.server import create_server` — import works
- `ls src/omnifocus_operator/server/` shows `__init__.py`, `handlers.py`, `lifespan.py`
- `test -f src/omnifocus_operator/server.py` returns false (old file gone)
</verification>

<success_criteria>
server.py successfully split into server/ package. All tests pass. Import paths maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/53-response-shaping/53-02-SUMMARY.md`
</output>
