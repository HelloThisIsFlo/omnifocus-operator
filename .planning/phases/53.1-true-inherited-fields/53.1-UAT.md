---
status: complete
phase: 53.1-true-inherited-fields
source: 53.1-01-SUMMARY.md, 53.1-02-SUMMARY.md
started: 2026-04-15T00:00:00Z
updated: 2026-04-15T12:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Project output has no inherited fields
expected: Call `get_project` on any project. The response should contain NO `inheritedFlagged`, `inheritedDueDate`, `inheritedDeferDate`, `inheritedPlannedDate`, `inheritedDropDate`, or `inheritedCompletionDate` fields. Projects can no longer have inherited fields — they were structurally removed.
result: pass

### 2. Inbox task: self-echoed inherited fields stripped
expected: Call `get_task` on a task that sits in the Inbox (no parent task, no project). Even if OmniFocus internally echoes values like `inheritedFlagged: false` or `inheritedDueDate` matching the task's own `dueDate`, these self-echoed values should NOT appear in the response.
result: pass

### 3. True inheritance from project: flagged
expected: Find or create a task inside a flagged project (the task itself is NOT flagged). Call `get_task` on that task. The response SHOULD include `inheritedFlagged: true` — this is a truly inherited value from the containing project.
result: pass

### 4. True inheritance from parent task: dates
expected: Find a subtask whose parent task has a due date, but the subtask itself does NOT have its own due date set. Call `get_task` on the subtask. The response SHOULD include `inheritedDueDate` matching the parent's due date — this is truly inherited.
result: pass

### 5. get_all applies true inheritance to tasks
expected: Call `get_all`. Examine tasks in the response. Tasks should reflect true inheritance: self-echoed inherited fields stripped, only truly inherited values present. Projects in the response should have NO inherited fields at all.
result: pass

### 6. list_tasks applies true inheritance
expected: Call `list_tasks` (with any filter). Tasks in the response should reflect true inheritance — same behavior as get_all and get_task. Self-echoed values stripped, truly inherited values present.
result: issue
reported: "inheritedDueDate shows OmniFocus effective value (min of own + ancestor) instead of the actual ancestor's value. When task's own dueDate is sooner than ancestor's, inheritedDueDate equals dueDate — misleading because it looks like a self-echo but is actually 'confirmed inheritance with wrong value'."
severity: major

## Summary

total: 6
passed: 5
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "inheritedDueDate should show the actual ancestor's due date, not OmniFocus's effective (min) resolution"
  status: failed
  reason: "User reported: walk confirms inheritance correctly but passes through OmniFocus's effectiveDueDate value instead of reconstructing the ancestor's actual value. When task's own dueDate (2026-05-01) is sooner than project's (2036-03-01), inheritedDueDate shows 2026-05-01 (= own) instead of 2036-03-01 (= ancestor's). Applies to all 6 inherited field pairs."
  severity: major
  test: 6
  root_cause: "_walk_one confirms/strips inherited fields based on ancestor presence but does not replace the VALUE — it keeps OmniFocus's effective resolution. Should compute the actual ancestor value from the task_map/project_map."
  artifacts:
    - path: "src/omnifocus_operator/service/domain.py"
      issue: "_walk_one checks ancestor presence but keeps OF effective value instead of computing ancestor value"
  missing:
    - "When confirming inheritance, compute the actual ancestor value (soonest due date from ancestor chain for dates, any-True for flags) instead of passing through OmniFocus's effective value"
  debug_session: ""
