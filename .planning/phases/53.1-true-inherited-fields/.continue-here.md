---
context: phase
phase: 53.1-true-inherited-fields
task: Empirical replication of inheritance semantics
total_tasks: N/A (UAT session, not plan execution)
status: in_progress
last_updated: 2026-04-15T16:30:00.000Z
---

## Critical Anti-Patterns

_None identified._

<current_state>
Phase 53.1 (True Inherited Fields) — full replication of inheritance semantics across 3 sessions.

Session 3 (current) replicated due/defer/planned/flagged from scratch with fresh hierarchies.
Also ran extensive drop/completion lifecycle experiments, discovering auto-completion behavior.
Planned date design discussion completed — first-found confirmed as correct both empirically
and conceptually. Documentation updated (descriptions.py, omnifocus-concepts.md, FINDINGS.md).

**Current position:** 4 of 6 fields fully replicated. Two remaining tests needed:
1. Drop date: rule out MAX (need reverse-order drop test)
2. Completion date: test with distinct timestamps
Both can be done with ONE fresh hierarchy (Option 3 from session discussion).
</current_state>

<completed_work>

### Session 3 (2026-04-15, current)

1. **Replicated due date (min)** — fresh 5-level hierarchy, all 5 levels match. CONFIRMED.
2. **Replicated defer date (max)** — same hierarchy with defer dates. L3 decisive. CONFIRMED.
3. **Replicated planned date (first-found)** — two rounds including 2027→2037 swap.
   Plus UI confirmation (inherited shown in gray). CONFIRMED.
4. **Replicated flagged (OR)** — project unflagged, L3 flagged. L1/L2 false, L3/L4/L5 true.
   Decisively proves downward-only propagation. CONFIRMED.
5. **Planned date design discussion** — concluded first-found is conceptually correct:
   - Planned date = "when I'll engage with this," NOT "when it'll be done"
   - Min would collapse phased schedules to the project root date
   - First-found preserves user's phased scheduling intent
   - Decision: match OmniFocus semantics (first-found)
   - Updated descriptions.py, omnifocus-concepts.md, FINDINGS.md
6. **Drop date partial replication** — 5-level hierarchy with blockers, top-down drops.
   Min ruled out (tasks switched to nearer ancestor's timestamp). Max vs first-found NOT
   YET distinguished (all drops were top-down, so nearer = later in all cases).
7. **Auto-completion lifecycle discovery (Finding 7)** — major new finding:
   - Auto-complete requires ALL children to have own lifecycle status
   - "Effectively dropped" (inherited) doesn't count as "done"
   - Auto-complete always produces "completed", even when all children are dropped
   - Auto-complete ERASES existing dropDate (replaced with completionDate)
   - Cascades upward (completing one blocker can chain-complete multiple ancestors)
8. **Committed** planned date docs: `bffdd4e5`

### Sessions 1-2 (prior)

- Original empirical testing with 5-level hierarchy
- 4 rounds of drop testing (Tests 1-4, ruled out both max and min for drop)
- Completion date test (inconclusive — all timestamps identical)
- Gap closure plan 53.1-03 (fixed _walk_one for dueDate)

</completed_work>

<remaining_work>

### Immediate next step: One fresh hierarchy for two tests

Create a 4-level hierarchy with blocker siblings in `🧪 GM-TestProject-Dated`:

```
UAT-Test-Root
├── UAT-Test-Root-Blocker
└── UAT-Test-Mid
    ├── UAT-Test-Mid-Blocker
    └── UAT-Test-Inner
        ├── UAT-Test-Inner-Blocker
        └── UAT-Test-Leaf
```

**Test A: Rule out MAX for drop date (REVERSE drop order)**

1. Drop Mid FIRST (nearer to Leaf) → timestamp T1
2. Wait ~5 seconds
3. Drop Root SECOND (further from Leaf) → timestamp T2 (T2 > T1)
4. Check Leaf's effectiveDropDate:
   - T1 (Mid's) → first-found CONFIRMED
   - T2 (Root's) → max (would contradict session 1 tests)
5. Run script filtering `UAT-Test-*` tasks for drop/completion dates

**Test B: Completion date inheritance with distinct timestamps**

May need a separate fresh hierarchy (since drop test marks tasks as dropped).
Protocol: complete tasks at different levels manually with pauses between each.
Think through auto-completion cascades before executing.

### After replication complete

1. **Fix `_walk_one` in `service/domain.py`** — three code paths:
   - `min` for `due_date` (already correct)
   - `max` for `defer_date` (currently min — BUG)
   - `first-found` for `planned_date`, `drop_date`, `completion_date` (currently min — BUG)
   - `any-True` for `flagged` (already correct)
2. **Write tests** (TDD RED → GREEN)
3. **Update UAT file** (53.1-UAT.md)
4. **Architecture documentation** for inheritance semantics
5. **Clean up test tasks** from OmniFocus

</remaining_work>

<decisions_made>

1. **Three semantic families confirmed:**
   - Constraint (min/max): due and defer — hierarchy imposes limits
   - Override (first-found): planned, drop, completion — events/intentions, nearest ancestor wins
   - Boolean OR: flagged — attention signal propagates

2. **Planned date design decision:** Match OmniFocus (first-found). Planned date = scoped intent
   ("when I'll engage with this"), not a completion target. Min would destroy phased scheduling.

3. **Empirical approach:** All semantics derived from live OmniFocus testing, not documentation.

4. **Replication standard:** User wants ALL findings independently replicated in session 3.
   Do NOT rely on session 1/2 findings alone. Each field must be confirmed with fresh data.

</decisions_made>

<blockers>

- **Drop date max not yet ruled out independently.** Session 1 tests ruled it out, but user
  wants independent replication. Fresh hierarchy with reverse drop order is the next step.
- **Completion date semantics unconfirmed.** Need distinct timestamps.

</blockers>

## Required Reading (in order)

1. `.research/deep-dives/omnifocus-inheritance-semantics/FINDINGS.md` — THE critical document.
   Contains all raw test data, verification tables, OmniJS scripts, confidence levels,
   auto-completion findings, and the planned date design discussion.
2. `src/omnifocus_operator/service/domain.py` lines 224-274 — current `_walk_one` implementation
3. `tests/test_service_domain.py` — existing tests for computed ancestor values (from plan 53.1-03)

## Infrastructure State

- `🧪 GM-TestProject-Dated` project (id: `e-OCRt-smkH`) — project is UNFLAGGED (changed in session 3)
- `UAT-Due-L1` through `L5` still present (dates cleared, L3 flagged)
- `UAT-Drop-A` through `E` + blockers still present (various completed/dropped states)
- All old session 1 tasks were deleted by user at start of session 3
- **Re-flag the project** (`flagged: true`) before any non-inheritance testing

## OmniJS Script for drop/completion testing

```javascript
(() => {
    var lines = [];
    var fmt = function(dt) { return dt ? dt.toISOString().slice(0, 19) : "—"; };
    var tasks = flattenedTasks.filter(function(t) { return t.name.startsWith("UAT-Test-"); });
    tasks.sort(function(a, b) { return a.name.localeCompare(b.name); });
    tasks.forEach(function(t) {
        lines.push("=== " + t.name + " ===");
        lines.push("  dropDate:             " + fmt(t.dropDate));
        lines.push("  effectiveDropDate:    " + fmt(t.effectiveDropDate));
        lines.push("  completionDate:       " + fmt(t.completionDate));
        lines.push("  effectiveCompletion:  " + fmt(t.effectiveCompletionDate));
        lines.push("");
    });
    console.log(lines.join("\n"));
})();
```

<next_action>
1. Read FINDINGS.md "Next Steps" section for the full test protocol
2. Create fresh hierarchy (UAT-Test-Root/Mid/Inner/Leaf + blockers)
3. Execute Test A: reverse-order drop (Mid first, Root second)
4. Execute Test B: completion with distinct timestamps
5. Then fix _walk_one and write tests
</next_action>
