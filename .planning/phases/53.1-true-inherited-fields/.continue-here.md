---
context: phase
phase: 53.1-true-inherited-fields
task: Empirical replication complete, implementation next
total_tasks: N/A (UAT session, not plan execution)
status: in_progress
last_updated: 2026-04-15T18:05:46.198Z
---

## Critical Anti-Patterns

_None identified._

<current_state>
Phase 53.1 (True Inherited Fields) — empirical study COMPLETE across 3 sessions.
All 6 inherited fields confirmed. Two small optional gaps remain (see remaining_work).
Next step: decide on gaps, then implement _walk_one fix.
</current_state>

<completed_work>

### Session 3b (2026-04-15, current session)

1. **Drop date REPLICATED** — reverse-order drop test (Mid first, Root second) ruled out max.
   Bonus: dropped Inner (nearer) → Leaf's effective changed, proving live re-walk. Commit: e10e8eba.
2. **Completion date HIGH confidence** — max ruled out via distinct-timestamp test (Leaf→Mid→Root).
   Drop/completion parity test confirmed identical behavior. Un-drop/re-drop proved no latching.
3. **"Derived, not stored" insight** documented with two proofs:
   - Proof 1: nearer ancestor changes effective (drop test)
   - Proof 2: own value is first stop in walk, not latched (un-drop/re-drop cycle)
4. **Architecture.md restructured** — clearly separates:
   - Our `inherited*` (ancestor walk, own never involved)
   - OmniFocus `effective*` (own + ancestors merged) — context only
   - We REPLACE effective with inherited, not "split"
5. **FINDINGS.md fully updated** — status "All 6 Fields Confirmed", completion date HIGH,
   per-field implementation table, updated next steps.
6. **FIXME comments** added to domain.py and descriptions.py linking to FINDINGS.md.

### Sessions 1-3a (prior)

- Due, defer, planned, flagged: all REPLICATED with fresh hierarchies
- Drop date: min ruled out (session 2), auto-completion behavior discovered (Finding 7)
- Planned date design discussion: first-found = scoped intent, not completion target
- Documentation updates across multiple commits

</completed_work>

<remaining_work>

### Two optional gaps (human-only tests)

1. **Min not independently ruled out for completion date.**
   Currently inferred from family consistency (min ruled out for drop, same override family).
   To test: reverse-order completion — complete Root first (T1), wait, complete Mid second (T2).
   If Inner-Blocker shows T2 (Mid's, nearer) → first-found. If T1 (Root's, earlier) → min.
   **Decision needed: is family consistency sufficient, or do we want independent proof?**

2. **Project-to-task lifecycle inheritance untested.**
   All lifecycle tests were task-to-task. `_walk_one` includes the containing project as
   final ancestor, so the same rule should apply, but never empirically tested by dropping
   or completing a project. The date/flag tests DID include the project as an ancestor.
   **Decision needed: is the structural argument sufficient?**

### Implementation (after gaps decided)

1. Fix `_walk_one` in `service/domain.py` — three code paths:
   - `min` for `due_date` (already correct)
   - `max` for `defer_date` (currently min — BUG)
   - `first-found` for `planned_date`, `drop_date`, `completion_date` (currently min — BUG)
   - `any-True` for `flagged` (already correct)
2. Write tests (TDD RED → GREEN)
3. Update UAT file
4. Clean up test tasks from OmniFocus

</remaining_work>

<decisions_made>

All 6 fields confirmed (see FINDINGS.md for full evidence):

| Field | Rule | Confidence |
|-------|------|-----------|
| Due date | min (cascading minimum) | REPLICATED |
| Defer date | max (cascading maximum) | REPLICATED |
| Planned date | first-found (override) | REPLICATED |
| Flagged | any-True (OR) | REPLICATED |
| Drop date | first-found (override) | REPLICATED |
| Completion date | first-found (override) | HIGH (max ruled out, family consistency for min) |

Key insights:
- Three semantic families: constraint (min/max), override (first-found), boolean OR
- Effective values are derived, not stored — live tree walk, no latching
- Own value = first stop in the walk — not "history remembering"
- Our `inherited*` replaces OmniFocus `effective*` — ancestor-only, no self-echo

</decisions_made>

<blockers>

None. All empirical work complete. Implementation is straightforward.

</blockers>

## Required Reading (in order)

1. `.research/deep-dives/omnifocus-inheritance-semantics/FINDINGS.md` — THE critical document.
   All raw data, verification tables, proofs, confidence levels. Status: "All 6 Fields Confirmed."
2. `docs/architecture.md` "True Inheritance Principle" section — our design (inherited* vs effective*)
3. `src/omnifocus_operator/service/domain.py` lines 220-274 — current `_walk_one` with FIXME comments

## Infrastructure State

- `🧪 GM-TestProject-Dated` project (id: `e-OCRt-smkH`) — project is **UNFLAGGED** (changed during
  flagged testing in session 2). Re-flag before non-inheritance work.
- Test hierarchies still in OmniFocus: UAT-Due-L*, UAT-Drop-*, UAT-Test-*, UAT-Comp-*
  (all in various completed/dropped states — cleanup needed)

<next_action>
1. Decide on two optional gaps: (a) reverse-order completion test for min, (b) project-level lifecycle test
2. If testing: create hierarchy, run tests (human-only)
3. If skipping: proceed to _walk_one implementation
4. Fix _walk_one, write tests, update UAT, clean up test tasks
</next_action>
