---
context: phase
phase: 53.1-true-inherited-fields
task: Empirical study complete, implementation next
total_tasks: N/A (UAT session, not plan execution)
status: paused
last_updated: 2026-04-15T18:44:36.816Z
---

## Critical Anti-Patterns

_None identified._

<current_state>
Phase 53.1 (True Inherited Fields) — empirical study COMPLETE across 4 sessions.
All 6 inherited fields confirmed at REPLICATED confidence. Both optional gaps closed.
Next step: decide whether to use /gsd-plan-phase or go straight to implementation.
</current_state>

<completed_work>

### Session 4 (2026-04-15, current session)

1. **Completion min independently ruled out** — reverse-order completion test (UAT-CompMin-*).
   Completed Root first (T1=18:38:31), then Mid 30s later (T2=18:39:01).
   Inner showed Mid's T2 (nearer), not Root's T1 (earlier). First-found confirmed.
   Completion date upgraded from HIGH to REPLICATED.
2. **Project-to-task lifecycle confirmed** — dropped `🧪 GM-TestProject`, all 3 tasks
   (Parent, Child, Sibling) inherited effectiveDropDate=18:36:10 from project.
   Nested Child (2 levels deep) confirms walk goes Child→Parent→Project.
3. **FINDINGS.md updated** — Finding 6 upgraded to REPLICATED, Finding 8 added (project lifecycle),
   test artifacts and next steps sections updated.

### Sessions 1-3b (prior)

- Due, defer, planned, flagged: all REPLICATED with fresh hierarchies
- Drop date: min + max independently ruled out, first-found confirmed
- Completion date: max ruled out (session 3), min ruled out (session 4)
- Auto-completion lifecycle discovery (Finding 7)
- Planned date design discussion: first-found = scoped intent, not completion target
- "Derived, not stored" insight documented with two proofs
- Architecture.md restructured (inherited* vs effective*)
- FIXME comments added to domain.py and descriptions.py

</completed_work>

<remaining_work>

### Implementation

1. Fix `_walk_one` in `service/domain.py` — three code paths:
   - `min` for `due_date` (already correct)
   - `max` for `defer_date` (currently min — BUG)
   - `first-found` for `planned_date`, `drop_date`, `completion_date` (currently min — BUG)
   - `any-True` for `flagged` (already correct)
2. Write tests (TDD RED → GREEN)
3. Update UAT file
4. Clean up test tasks from OmniFocus (human-only)

</remaining_work>

<decisions_made>

All 6 fields confirmed (see FINDINGS.md for full evidence):

| Field | Rule | Confidence |
|-------|------|-----------|
| Due date | min (cascading minimum) | REPLICATED |
| Defer date | max (cascading maximum) | REPLICATED |
| Planned date | first-found (override) | REPLICATED |
| Flagged | any-True (OR) | REPLICATED |
| Drop date | first-found (override) | REPLICATED |
| Completion date | first-found (override) | REPLICATED |

Additional confirmations:
- Project-to-task lifecycle inheritance: REPLICATED (project drop propagates to all descendants)
- Three semantic families: constraint (min/max), override (first-found), boolean OR
- Effective values are derived, not stored — live tree walk, no latching
- Our `inherited*` replaces OmniFocus `effective*` — ancestor-only, no self-echo

</decisions_made>

<blockers>

None. All empirical work complete. Implementation is straightforward.

</blockers>

## Required Reading (in order)

1. `.research/deep-dives/omnifocus-inheritance-semantics/FINDINGS.md` — THE critical document.
   All raw data, verification tables, proofs, confidence levels. Status: "All 6 Fields Confirmed."
2. `docs/architecture.md` "True Inheritance Principle" section — our design (inherited* vs effective*)
3. `src/omnifocus_operator/service/domain.py` lines 220-274 — current `_walk_one` with FIXME comments

## Infrastructure State

- `🧪 GM-TestProject-Dated` project (id: `e-OCRt-smkH`) — project is **UNFLAGGED** (changed during
  flagged testing in session 2). Re-flag before non-inheritance work.
- `🧪 GM-TestProject` — was dropped during session 4 testing. **Verify it's restored to Active.**
- Test hierarchies still in OmniFocus: UAT-Due-L*, UAT-Drop-*, UAT-Test-*, UAT-Comp-*,
  UAT-CompMin-*, UAT-ProjLC-* (all in various completed/dropped states — cleanup needed)

<next_action>
1. Decide: /gsd-plan-phase for _walk_one implementation, or go straight to coding
2. Fix _walk_one (3 code paths: min, max, first-found + any-True)
3. Write tests (TDD RED → GREEN)
4. Update UAT file
5. Clean up test tasks from OmniFocus (human-only)
</next_action>
