---
context: phase
phase: 53.1-true-inherited-fields
task: UAT re-verification after gap closure
total_tasks: N/A (UAT session, not plan execution)
status: in_progress
last_updated: 2026-04-15T15:14:09.617Z
---

## Critical Anti-Patterns

_None identified._

<current_state>
Phase 53.1 (True Inherited Fields) — UAT re-verification after gap closure plan 53.1-03.

The original UAT found 1 issue: `inheritedDueDate` showed OmniFocus's effective value
instead of the computed ancestor value. Gap closure plan 53.1-03 fixed this for `dueDate`
using `min` semantics for all date fields. During re-verification, we discovered that
`min` is only correct for `dueDate`. Three other field types need different aggregation.

**Current position:** We have completed empirical testing of all 6 inherited field pairs.
We discovered 4 bugs in `_walk_one`. We have NOT yet:
1. Fixed the code
2. Discussed the planned date semantics (user wanted to discuss this specifically)
3. Run a completion date test with distinct timestamps
4. Created the architecture documentation
5. Updated the UAT file
6. Cleaned up test tasks from OmniFocus
</current_state>

<completed_work>

### Empirical testing (comprehensive)

1. **Created 5-level test hierarchy** (UAT-Deep-L1 through L5) in GM-TestProject-Dated
   with dates/flags at different levels, gaps, and edge cases (own date sooner than ancestor)

2. **Ran OmniJS scripts** in OmniFocus console to read `own` vs `effective` values for
   due, defer, planned, flagged at all 5 levels

3. **Derived OmniFocus semantics** from raw data:
   - Due: cascading min (tightest deadline) — **confirmed, 5/5 levels match**
   - Defer: cascading max (latest block) — **confirmed, 5/5 levels match**
   - Planned: simple override (own ?? parent_eff) — **confirmed, ruled out both min AND max**
   - Flagged: any-True / OR — **consistent but all-true due to project flag**

4. **Tested drop date** with 4 test rounds:
   - Tests 1-2: without blockers — discovered auto-completion side effect, ruled out max
   - Tests 3-4: with blocker siblings — DECISIVE: Test 3 ruled out max, Test 4 ruled out min.
     Only first-found/override matches both.

5. **Tested completion date** with 1 test:
   - Completed mid-level task, observed cascade behavior
   - Key finding: children do NOT get own completionDate (only effectiveCompletionDate)
   - All timestamps identical (auto-completion cascade), can't determine aggregation

6. **Documented all findings** in `.research/deep-dives/omnifocus-inheritance-semantics/`
</completed_work>

<remaining_work>

### Must do (bugs to fix)

1. **Fix `_walk_one` in `service/domain.py`** — needs three code paths:
   - `min` for `due_date` (already correct)
   - `max` for `defer_date` (currently min — BUG)
   - `first-found` for `planned_date`, `drop_date`, `completion_date` (currently min — BUG)
   - `any-True` for `flagged` (already correct)

2. **Write tests** for the new semantics (TDD RED → GREEN)

3. **Update UAT file** (53.1-UAT.md) — re-test 6 needs updating, plus new gaps for defer/planned/drop/completion

### Should do

4. **Discuss planned date semantics** — user specifically wanted to discuss how to handle
   `inheritedPlannedDate` before implementing. The override/first-found semantics mean
   showing the nearest ancestor's planned date, which is different from "aggregate across chain."

5. **Run completion date test with blocker siblings** to get distinct timestamps and
   confirm first-found (currently low confidence)

6. **Create architecture documentation** — user wants a dedicated doc (like the repetition
   rule doc) for inheritance semantics. See existing docs for format reference.

7. **Clean up test tasks** from OmniFocus (UAT-Deep-*, UAT-Inheritance-*, UAT-Lifecycle-*,
   UAT-Drop*) — all in GM-TestProject-Dated
</remaining_work>

<decisions_made>

1. **Three semantic families identified:**
   - Constraint (min/max): due and defer — hierarchy imposes limits
   - Override (first-found): planned, drop, completion — events/intentions, nearest ancestor wins
   - Boolean OR: flagged — attention signal propagates

2. **Empirical approach:** All semantics derived from live OmniFocus testing, not documentation.
   OmniJS scripts used to read both direct and effective values at each hierarchy level.

3. **Deep-dive documentation created** at `.research/deep-dives/omnifocus-inheritance-semantics/`
   with comprehensive findings, raw data, verification tables, and confidence levels.

4. **User's assessment:** User said findings are NOT 100% confirmed yet. Confidence levels
   assigned: high for due/defer/planned/flagged, medium for drop, low for completion.
</decisions_made>

<blockers>

- **Completion date semantics unconfirmed:** Need test with blocker siblings to get distinct
  timestamps. Not a blocker for fixing the other 3 fields, but should be resolved before
  claiming full completion.
</blockers>

## Required Reading (in order)

1. `.research/deep-dives/omnifocus-inheritance-semantics/FINDINGS.md` — THE critical document.
   Contains all raw test data, verification tables, OmniJS scripts, confidence levels.
   Read this THOROUGHLY before making any code changes.
2. `src/omnifocus_operator/service/domain.py` lines 224-274 — current `_walk_one` implementation
3. `tests/test_service_domain.py` — existing tests for computed ancestor values (from plan 53.1-03)
4. `.planning/phases/53.1-true-inherited-fields/53.1-UAT.md` — UAT state with gap diagnosis

## Infrastructure State

- Test tasks remain in OmniFocus `🧪 GM-TestProject-Dated` project
- Some are completed/dropped from testing — won't interfere with normal usage

## Pre-Execution Critique Required

- Before implementing fixes, **discuss with user** how `inheritedPlannedDate` should work
  from the agent's perspective. The "override" semantics mean we show the nearest ancestor's
  planned date — is this the most useful value for an AI agent?
- Also confirm user is satisfied with confidence levels before coding.

<context>
We started this session to re-verify phase 53.1 after gap closure plan 53.1-03. The gap
closure fixed `inheritedDueDate` to show actual ancestor values instead of OF effective values,
using `min` for all dates. During re-verification, I flagged that `min` might be wrong for
defer dates (should be max). The user then asked us to empirically determine the correct
semantics for ALL inherited fields.

We built a 5-level hierarchy, ran OmniJS scripts, and methodically determined each field's
aggregation strategy. The user then requested drop/completion testing, which required
creating tasks and having the user manually drop/complete them via the UI (because dropDate
is read-only in OmniJS). After 4 rounds of drop testing (including the critical blocker-
sibling tests that distinguished first-found from min), we have high confidence on 4 fields
and medium/low on 2.

The user paused because the context window was getting full. They want to:
1. Resume and discuss planned date handling
2. Optionally run the completion test
3. Fix the code
4. Document in architecture
</context>

<next_action>
Start with: Read `.research/deep-dives/omnifocus-inheritance-semantics/FINDINGS.md` thoroughly.
Then ask the user:
1. Do you want to run the completion date test with blocker siblings first?
2. Ready to discuss `inheritedPlannedDate` semantics?
3. Or proceed straight to fixing `_walk_one`?
</next_action>
