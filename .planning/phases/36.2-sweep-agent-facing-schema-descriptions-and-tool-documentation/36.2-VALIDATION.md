---
phase: 36.2
slug: sweep-agent-facing-schema-descriptions-and-tool-documentation
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-01
validated: 2026-04-01
---

# Phase 36.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | pyproject.toml |
| **Quick run command** | `uv run pytest tests/test_output_schema.py -x -q` |
| **Full suite command** | `uv run pytest -x -q` |
| **Estimated runtime** | ~20 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_output_schema.py -x -q`
- **After every plan wave:** Run `uv run pytest -x -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 36.2-01-T1 | 01 | 1 | DOC-01, DOC-03 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ✅ green |
| 36.2-01-T2 | 01 | 1 | DOC-04, DOC-05, DOC-09 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ✅ green |
| 36.2-02-T1 | 02 | 1 | DOC-01, DOC-08 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ✅ green |
| 36.2-02-T2 | 02 | 1 | DOC-05, DOC-09 | schema+full | `uv run pytest -x -q` | ✅ | ✅ green |
| 36.2-03-T1 | 03 | 2 | DOC-02, DOC-06 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ✅ green |
| 36.2-03-T2 | 03 | 2 | DOC-07, DOC-08 | schema+full | `uv run pytest -x -q` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. `test_output_schema.py` validates serialized output against MCP outputSchema — this catches Field description additions that break schema structure. Full suite (1383 tests, 98% coverage) validates no behavioral regressions.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tool descriptions appear correctly in MCP tools/list | DOC-02 | Requires running MCP server and inspecting tools/list response | Start server, call tools/list, verify docstrings match approved text |
| Field descriptions visible in JSON Schema | DOC-03, DOC-05 | Requires inspecting model_json_schema() output | Run `python -c "from omnifocus_operator.contracts.use_cases.add.tasks import AddTaskCommand; import json; print(json.dumps(AddTaskCommand.model_json_schema(), indent=2))"` and verify description fields |
| Description content accuracy (text fidelity) | DOC-01 through DOC-09 | Documentation text content cannot be validated by structure tests | Visually inspect docstrings and Field descriptions against CONTEXT.md approved text |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 20s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** complete

---

## Validation Audit 2026-04-01

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

**Verification summary:**
- All 1383 tests pass (98% coverage)
- 23 output schema tests pass
- Runtime docstring checks: all agent-visible docstrings clean of banned strings
- Runtime Field description checks: all expected descriptions present
- Tool docstring checks: camelCase notes, response hints, patch contract, tag resolution, partial rep rule, lifecycle drop — all verified
