---
phase: 36.2
slug: sweep-agent-facing-schema-descriptions-and-tool-documentation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-01
---

# Phase 36.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | pyproject.toml |
| **Quick run command** | `uv run pytest tests/test_output_schema.py -x -q` |
| **Full suite command** | `uv run pytest -x -q` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_output_schema.py -x -q`
- **After every plan wave:** Run `uv run pytest -x -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 36.2-01-01 | 01 | 1 | DOC-01, DOC-02 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ⬜ pending |
| 36.2-01-02 | 01 | 1 | DOC-03, DOC-12 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ⬜ pending |
| 36.2-01-03 | 01 | 1 | DOC-04, DOC-05, DOC-11 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ⬜ pending |
| 36.2-02-01 | 02 | 1 | DOC-06 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ⬜ pending |
| 36.2-02-02 | 02 | 1 | DOC-07, DOC-08 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ⬜ pending |
| 36.2-02-03 | 02 | 1 | DOC-09, DOC-10, DOC-13, DOC-14, DOC-15 | schema | `uv run pytest tests/test_output_schema.py -x -q` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. `test_output_schema.py` validates serialized output against MCP outputSchema expectations — this catches Field description additions that break schema structure.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tool descriptions appear correctly in MCP tools/list | DOC-03 | Requires running MCP server and inspecting tools/list response | Start server, call tools/list, verify docstrings match approved text |
| Field descriptions visible in JSON Schema | DOC-04 | Requires inspecting model_json_schema() output | Run `python -c "from omnifocus_operator.contracts.use_cases.add.tasks import AddTaskCommand; import json; print(json.dumps(AddTaskCommand.model_json_schema(), indent=2))"` and verify description fields |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
