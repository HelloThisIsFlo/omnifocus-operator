---
status: complete
phase: 36.4-reserve-literal-and-annotated-types-for-contract-models-only
source: 36.4-01-SUMMARY.md, 36.4-02-SUMMARY.md, 36.4-03-SUMMARY.md
started: 2026-04-02T12:00:00Z
updated: 2026-04-02T12:30:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Type aliases live in contracts/, not models/
expected: FrequencyType, DayCode, OnDate, DayName are importable from contracts/shared/repetition_rule.py. models/repetition_rule.py does NOT export these names (no public aliases).
result: pass

### 2. Core model fields use plain str, not Literal
expected: Frequency.type is annotated as `str`. OrdinalWeekday day fields (monday through sunday) are annotated as `str | None`. No private _FrequencyType or _DayName aliases exist in models/repetition_rule.py.
result: pass

### 3. JSON Schema: interval and occurrences advertise minimum: 1
expected: Running `FrequencyAddSpec.model_json_schema()` shows `interval` with `minimum: 1`. `EndByOccurrences.model_json_schema()` shows `occurrences` with `minimum: 1`. Same for `FrequencyEditSpec.interval`.
result: pass

### 4. AST enforcement test catches Literal/Annotated in models/
expected: `pytest tests/test_type_boundary.py -x -q` passes. The test scans all models/ files and would fail if any field annotation uses Literal[] or Annotated[] directly (with one known exception: EndByOccurrences.occurrences).
result: pass

### 5. AST enforcement detects alias-based evasion
expected: The enforcement test also detects module-level aliases that resolve to Literal[...] or Annotated[...]. If someone adds `_Foo = Literal["a", "b"]` and uses it on a field, the test catches it.
result: pass

### 6. model-taxonomy.md documents the type boundary convention
expected: docs/model-taxonomy.md contains a "Type constraint boundary" section explaining: Literal/Annotated go on contracts/, plain types on models/, with the exception policy for agent-facing core models.
result: issue
reported: "EndCondition models (EndByDate, EndByOccurrences) are reused directly on contract fields instead of having spec versions in contracts/. The doc codifies an exception for this, but the right fix is to follow the convention: create EndByDateSpec/EndByOccurrencesSpec in contracts/, remove Annotated from EndByOccurrences in models/, remove the exception from the doc and enforcement test."
severity: major

### 7. Full test suite passes
expected: `uv run pytest -x -q` — all tests pass (1422+), no regressions from the type boundary changes.
result: pass

## Summary

total: 7
passed: 6
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Type boundary is absolute — zero Literal/Annotated in models/, no exceptions"
  status: failed
  reason: "EndCondition models (EndByDate, EndByOccurrences) from models/ reused directly on contract input fields. Core models leaking into input schema violates the type boundary convention."
  severity: major
  test: 6
  root_cause: "EndCondition union imported from models/ onto contract fields — no spec equivalents exist in contracts/"
  artifacts:
    - path: "src/omnifocus_operator/models/repetition_rule.py"
      issue: "EndByOccurrences.occurrences uses Annotated[int, Field(ge=1)] — should be plain int"
    - path: "src/omnifocus_operator/contracts/shared/repetition_rule.py"
      issue: "RepetitionRuleAddSpec.end and RepetitionRuleEditSpec.end use EndCondition from models/ directly"
    - path: "tests/test_type_boundary.py"
      issue: "_EXCEPTIONS set contains EndByOccurrences.occurrences — should be empty"
  missing:
    - "Create EndByDateSpec/EndByOccurrencesSpec in contracts/ with Annotated constraints"
    - "Use EndConditionSpec union on contract fields"
    - "Revert EndByOccurrences.occurrences to plain int in models/"
    - "Empty the enforcement test exception list"
  note: "Doc update (model-taxonomy.md) already completed — exception removed, callouts added. Remaining work is structural only."
  debug_session: ""
