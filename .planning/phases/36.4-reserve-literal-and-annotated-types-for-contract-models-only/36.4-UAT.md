---
status: complete
phase: 36.4-reserve-literal-and-annotated-types-for-contract-models-only
source: [36.4-01-SUMMARY.md, 36.4-02-SUMMARY.md, 36.4-03-SUMMARY.md, 36.4-04-SUMMARY.md]
started: 2026-04-02T14:00:00Z
updated: 2026-04-02T14:55:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Core models — plain types with runtime validators
expected: Look at models/repetition_rule.py. Frequency.type is `str`, OrdinalWeekday fields are `str | None`, EndByOccurrences.occurrences is `int`. No Literal import. Validation via runtime validators. Does this read clearly as "dumb domain objects"?
result: pass

### 2. Contract models — rich types for agent schema
expected: Look at contracts/shared/repetition_rule.py. The mirror image: FrequencyAddSpec.type is `FrequencyType` (Literal[...]), interval is `Annotated[int, Field(ge=1)]`, OrdinalWeekdaySpec fields are `DayName` (Literal[...]), EndByOccurrencesSpec.occurrences is `Annotated[int, Field(ge=1)]`. Type aliases defined at the top of this file. Does the split make sense — schema richness lives here, not in models/?
result: pass

### 3. Shared validation — one source of truth
expected: Both core and contract models delegate to the same standalone validation functions at the top of models/repetition_rule.py: validate_interval(), normalize_day_codes(), normalize_day_name(), check_at_most_one_ordinal(), validate_on_dates(). Contracts import these rather than duplicating. Does this feel right — single source for rules, but each side annotates types differently?
result: pass

### 4. Duck-typed dispatch in build_rrule
expected: Look at rrule/builder.py lines 107-110. End condition dispatch uses `hasattr(end, "occurrences")` / `hasattr(end, "date")` instead of isinstance. This lets it accept both core EndByDate/EndByOccurrences AND contract EndByDateSpec/EndByOccurrencesSpec without coupling. Does this duck-typing feel appropriate here, or would you prefer a protocol?
result: issue
reported: "Spec types leak past service boundary into builder. Fix: convert specs to core models at service boundary in payload.py, restore isinstance dispatch. Captured in todo: 2026-04-02-convert-specs-to-core-models-at-service-boundary.md"
severity: major

### 5. AST enforcement test
expected: Look at tests/test_type_boundary.py. It walks the AST of every file in models/, checks class field annotations for Literal/Annotated (including via module-level aliases). _EXCEPTIONS set is empty — zero exemptions. Does this guardian test make sense? Is it clear what it catches?
result: pass

### 6. model-taxonomy.md — Type constraint boundary section
expected: Look at the "Type constraint boundary" section in docs/model-taxonomy.md (line 188+). Convention table shows contracts/ gets Literal/Annotated, models/ gets plain types. Explains why (construction sites vs agent schemas). Notes that output schema doesn't matter today because MCP clients don't expose it. Does this rationale hold up? Anything you'd add or challenge?
result: pass

## Summary

total: 6
passed: 5
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Lower layers (builder, payload) only see core models, not contract specs"
  status: deferred
  reason: "User reported: Spec types leak past service boundary. hasattr duck-typing in builder.py is a symptom, not the fix."
  severity: major
  test: 4
  root_cause: "Contract spec types (EndConditionSpec, FrequencyAddSpec) passed directly to build_rrule and payload builder instead of being converted to core models at the service boundary"
  artifacts:
    - path: "src/omnifocus_operator/rrule/builder.py"
      issue: "Union signature and hasattr dispatch instead of isinstance"
    - path: "src/omnifocus_operator/service/payload.py"
      issue: "Union type annotations accepting both spec and core models"
  missing:
    - "Convert specs to core models in payload.py before calling build_rrule"
    - "Restore isinstance dispatch in builder.py"
    - "Remove union signatures from builder and payload"
  deferred_to: "todo: 2026-04-02-convert-specs-to-core-models-at-service-boundary.md"
