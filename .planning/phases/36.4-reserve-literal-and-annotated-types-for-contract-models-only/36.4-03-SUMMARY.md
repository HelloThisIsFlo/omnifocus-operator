---
phase: 36.4-reserve-literal-and-annotated-types-for-contract-models-only
plan: 03
subsystem: models, tests
tags: [type-boundary, enforcement, cleanup]
dependency_graph:
  requires: [36.4-01, 36.4-02]
  provides: [clean-model-fields, alias-detection]
  affects: [models/repetition_rule.py, tests/test_output_schema.py, tests/test_type_boundary.py]
tech_stack:
  patterns: [AST-alias-resolution, module-scope-alias-detection]
key_files:
  modified:
    - src/omnifocus_operator/models/repetition_rule.py
    - tests/test_output_schema.py
    - tests/test_type_boundary.py
decisions:
  - Private Literal aliases removed from models/ -- plain str with runtime validators
  - AST enforcement extended to detect module-level aliases resolving to Literal/Annotated
metrics:
  duration: 2min
  completed: 2026-04-02
  tasks_completed: 2
  tasks_total: 2
  files_modified: 3
---

# Phase 36.4 Plan 03: Close Verification Gaps Summary

Remove private Literal aliases from models/, change field annotations to plain types, and strengthen AST enforcement to detect alias-based evasion of the type boundary convention.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 | 4273ee1 | Remove private Literal aliases and change core model fields to plain types |
| 2 | a352503 | Strengthen AST enforcement to detect private Literal aliases on class fields |

## What Changed

### Task 1: Remove private aliases, plain-type fields
- Deleted `_FrequencyType` and `_DayName` Literal alias definitions
- Changed `Frequency.type` from `_FrequencyType` to `str`
- Changed all 6 `OrdinalWeekday` fields from `_DayName | None` to `str | None`
- Removed `Literal` from typing import (kept `Annotated` for `EndByOccurrences`)
- Updated `test_output_schema.py`: renamed `test_frequency_is_flat_model_with_type_enum` to `test_frequency_is_flat_model_with_type_field`, replaced enum assertions with string-type assertions
- Updated OrdinalWeekday schema assertions: string+null anyOf branches instead of enum

### Task 2: Strengthen AST enforcement
- Added `_resolve_module_literal_aliases()` -- scans module-level `ast.Assign`/`ast.AnnAssign` for `Literal[...]` or `Annotated[...]` RHS
- Extended `_find_constraint_types()` with `module_aliases` parameter to flag `alias:_Foo` names
- Updated violation messages to distinguish direct vs alias usage

## Deviations from Plan

None -- plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED
