---
phase: 36.4-reserve-literal-and-annotated-types-for-contract-models-only
verified: 2026-04-02T00:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification: true
  previous_status: gaps_found
  previous_score: 5/7
  gaps_closed:
    - "Core model Frequency.type is str, not FrequencyType (TYPE-02)"
    - "Core model OrdinalWeekday fields are str | None, not DayName | None (TYPE-03)"
    - "AST enforcement test detects private Literal aliases on class fields (TYPE-07 gap closed)"
  gaps_remaining: []
  regressions: []
human_verification: []
---

# Phase 36.4: Reserve Literal/Annotated Types for Contract Models Only — Verification Report

**Phase Goal:** Move Literal/Annotated type aliases from models/ to contracts/, update core model fields to plain types, add Annotated constraints to contract fields missing schema richness, document and enforce the convention.
**Verified:** 2026-04-02T00:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure plan 36.4-03

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | FrequencyType, DayCode, OnDate, DayName type aliases are defined in contracts/, not models/ | VERIFIED | All four aliases present in `contracts/shared/repetition_rule.py` lines 44-57; absent from `models/repetition_rule.py` (no definition, no `__all__` entry) |
| 2 | Core model Frequency.type is plain str, not FrequencyType | VERIFIED | `models/repetition_rule.py` line 184: `type: str`. Schema check: `Frequency.model_json_schema()['properties']['type']` == `{'title': 'Type', 'type': 'string'}` — no `enum` key |
| 3 | Core model OrdinalWeekday fields (first–last) are str \| None, not DayName \| None | VERIFIED | `models/repetition_rule.py` lines 120-125: all six fields annotated `str \| None = None`. Schema check: `OrdinalWeekday.model_json_schema()['properties']['first']` == `{'anyOf': [{'type': 'string'}, {'type': 'null'}], ...}` |
| 4 | Contract models FrequencyAddSpec.interval and EndByOccurrences.occurrences advertise minimum: 1 in JSON Schema | VERIFIED | `FrequencyAddSpec.model_json_schema()['properties']['interval']['minimum'] == 1`; `EndByOccurrences.model_json_schema()['properties']['occurrences']['minimum'] == 1` |
| 5 | models/repetition_rule.py has no Literal import and no Literal/Annotated on class fields | VERIFIED | `Literal` not present in any import or field annotation; `Annotated` import retained only for `EndByOccurrences.occurrences` (documented exception). Grep for `_FrequencyType\|_DayName`: no matches in file. |
| 6 | docs/model-taxonomy.md documents the Literal/Annotated convention | VERIFIED | Section "## Type constraint boundary" at line 183; contains rationale, convention table, and exception policy |
| 7 | AST enforcement test detects both direct Literal/Annotated and private module-level aliases used on class fields | VERIFIED | `tests/test_type_boundary.py` contains `_resolve_module_literal_aliases` (lines 28-53) and `module_aliases` parameter in `_find_constraint_types` (line 58). Test passes on clean codebase; would detect any `_Foo = Literal[...]` used as a field annotation. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/omnifocus_operator/contracts/shared/repetition_rule.py` | FrequencyType, DayCode, OnDate, DayName definitions + Annotated constraints on interval | VERIFIED | All four aliases at lines 44-57; `FrequencyAddSpec.interval: Annotated[int, Field(ge=1, default=1)]` at line 108 |
| `src/omnifocus_operator/models/repetition_rule.py` | Core models with genuinely plain types; no Literal import | VERIFIED | `Frequency.type: str`; all six `OrdinalWeekday` fields `str \| None`; no `_FrequencyType`/`_DayName`; `Literal` absent from imports |
| `docs/model-taxonomy.md` | Convention documentation for Literal/Annotated boundary | VERIFIED | "## Type constraint boundary" section with rationale, table, and exception policy |
| `tests/test_type_boundary.py` | Strengthened AST enforcement detecting private Literal aliases | VERIFIED | Contains `_resolve_module_literal_aliases`, `module_aliases` parameter, alias detection via `alias:` prefix in violation messages |
| `tests/test_output_schema.py` | Updated assertions accepting string type (not enum) for Frequency.type and OrdinalWeekday fields | VERIFIED | `test_frequency_is_flat_model_with_type_field` asserts `type_prop.get("type") == "string"`; OrdinalWeekday loop checks `"string" in type_branches` and `"null" in type_branches` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `tests/test_type_boundary.py` | `src/omnifocus_operator/models/` | AST scanning with `_resolve_module_literal_aliases` | VERIFIED | `_MODELS_DIR` set to `models/`; scans all `.py` files via `rglob`; resolves module-level aliases before checking field annotations |
| `contracts/shared/repetition_rule.py` | `models/repetition_rule.py` | imports shared validators (no type aliases imported from models) | VERIFIED | contracts imports `check_at_most_one_ordinal`, `check_frequency_cross_type_fields`, `normalize_day_codes`, `normalize_day_name`, `validate_interval`, `validate_on_dates` — no type aliases round-tripped back |

### Data-Flow Trace (Level 4)

Not applicable — this phase produces no UI components or data pipelines. All artifacts are model definitions, documentation, and enforcement tests.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Frequency.type schema is plain string (no enum) | `Frequency.model_json_schema()['properties']['type']` | `{'title': 'Type', 'type': 'string'}` — no `enum` key | PASS |
| OrdinalWeekday.first schema is str \| None | `OrdinalWeekday.model_json_schema()['properties']['first']` | `{'anyOf': [{'type': 'string'}, {'type': 'null'}], ...}` | PASS |
| FrequencyAddSpec.interval advertises minimum: 1 | `FrequencyAddSpec.model_json_schema()['properties']['interval']['minimum']` | `1` | PASS |
| EndByOccurrences.occurrences advertises minimum: 1 | `EndByOccurrences.model_json_schema()['properties']['occurrences']['minimum']` | `1` | PASS |
| AST enforcement test passes | `uv run pytest tests/test_type_boundary.py -x -q --no-cov` | 1 passed | PASS |
| Output schema test passes | `uv run pytest tests/test_output_schema.py -x -q --no-cov` | 25 passed | PASS |
| Full test suite passes | `uv run pytest tests/ -x -q --no-cov` | 1422 passed | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| TYPE-01 | 36.4-01 | FrequencyType, DayCode, OnDate, DayName defined in contracts/, not models/ | SATISFIED | All four aliases in `contracts/shared/repetition_rule.py` lines 44-57; absent from `models/__all__` |
| TYPE-02 | 36.4-01/03 | Core model Frequency.type uses plain str, not FrequencyType Literal | SATISFIED | `Frequency.type: str` at line 184; schema emits `{'type': 'string'}` with no `enum` |
| TYPE-03 | 36.4-01/03 | Core model OrdinalWeekday fields use str \| None, not DayName \| None | SATISFIED | All six fields annotated `str \| None = None` at lines 120-125 |
| TYPE-04 | 36.4-01 | _VALID_DAY_NAMES is a plain set literal, not derived from DayName.__args__ | SATISFIED | Lines 57-67: plain set literal with 9 values; no `__args__` reference anywhere in file |
| TYPE-05 | 36.4-01 | FrequencyAddSpec.interval and EndByOccurrences.occurrences advertise minimum: 1 via Annotated | SATISFIED | Both confirmed at `minimum: 1` via `model_json_schema()` |
| TYPE-06 | 36.4-02 | docs/model-taxonomy.md documents the Literal/Annotated convention | SATISFIED | "## Type constraint boundary" section at line 183; includes rationale, table, exception policy |
| TYPE-07 | 36.4-02/03 | AST enforcement test scans models/ for Literal/Annotated field annotations (including private aliases) | SATISFIED | `_resolve_module_literal_aliases` resolves module-scope aliases; `_find_constraint_types` flags both direct and alias usage; `_EXCEPTIONS` tracks documented exceptions |

### Anti-Patterns Found

None. Previous gaps (private `_FrequencyType`/`_DayName` Literal aliases on class fields, enforcement test blind spot) are fully resolved.

### Human Verification Required

None — all verification performed programmatically.

### Gaps Summary

No gaps. All seven TYPE requirements are satisfied. The three previously-failing items (TYPE-02, TYPE-03, and the TYPE-07 enforcement blind spot) are resolved by plan 36.4-03:

- `_FrequencyType` and `_DayName` private aliases deleted from `models/repetition_rule.py`
- `Frequency.type` changed to plain `str`; all six `OrdinalWeekday` fields changed to `str | None`
- `Literal` removed from `models/repetition_rule.py` imports
- `test_output_schema.py` updated to assert `type == "string"` rather than `enum` presence
- `test_type_boundary.py` strengthened with `_resolve_module_literal_aliases` to detect private aliases on class fields

The full test suite (1422 tests) passes.

---

_Verified: 2026-04-02T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
