---
phase: 45-date-models-resolution
plan: 04
type: execute
wave: 1
depends_on: [45-01, 45-03]
files_modified:
  - src/omnifocus_operator/agent_messages/errors.py
  - src/omnifocus_operator/contracts/use_cases/list/_date_filter.py
  - src/omnifocus_operator/contracts/use_cases/list/_enums.py
  - src/omnifocus_operator/service/resolve_dates.py
  - tests/test_date_filter_contracts.py
  - tests/test_date_filter_constants.py
  - tests/test_resolve_dates.py
autonomous: true
gap_closure: true
requirements: [DATE-05, RESOLVE-06]

must_haves:
  truths:
    - "DateFilter(this='2w') raises error that only mentions bare unit chars (d/w/m/y), not count+unit like '2w'"
    - "resolve_date_filter accepts DueSoonSetting enum instead of raw interval/granularity ints"
    - "DueSoonSetting enum has exactly 7 members matching OmniFocus discrete settings"
  artifacts:
    - path: "src/omnifocus_operator/agent_messages/errors.py"
      provides: "DATE_FILTER_INVALID_THIS_UNIT constant"
      contains: "DATE_FILTER_INVALID_THIS_UNIT"
    - path: "src/omnifocus_operator/contracts/use_cases/list/_enums.py"
      provides: "DueSoonSetting enum"
      contains: "class DueSoonSetting"
    - path: "src/omnifocus_operator/service/resolve_dates.py"
      provides: "DueSoonSetting-based resolve signature"
      contains: "due_soon_setting"
  key_links:
    - from: "src/omnifocus_operator/contracts/use_cases/list/_date_filter.py"
      to: "src/omnifocus_operator/agent_messages/errors.py"
      via: "err.DATE_FILTER_INVALID_THIS_UNIT"
      pattern: "DATE_FILTER_INVALID_THIS_UNIT"
    - from: "src/omnifocus_operator/service/resolve_dates.py"
      to: "src/omnifocus_operator/contracts/use_cases/list/_enums.py"
      via: "DueSoonSetting import"
      pattern: "DueSoonSetting"
---

<objective>
Close two UAT gaps from Phase 45: (1) the `this` field error message incorrectly reuses the duration error constant, and (2) `resolve_date_filter` exposes raw SQLite interval/granularity ints instead of a domain enum.

Purpose: Agent receives correct educational guidance when `this` is misused; domain API is clean with no leaked storage format.
Output: Fixed error constant, new DueSoonSetting enum, updated resolver signature, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-date-models-resolution/45-UAT.md
@.planning/phases/45-date-models-resolution/45-01-SUMMARY.md
@.planning/phases/45-date-models-resolution/45-03-SUMMARY.md

@src/omnifocus_operator/agent_messages/errors.py
@src/omnifocus_operator/contracts/use_cases/list/_date_filter.py
@src/omnifocus_operator/contracts/use_cases/list/_enums.py
@src/omnifocus_operator/service/resolve_dates.py
@tests/test_date_filter_contracts.py
@tests/test_date_filter_constants.py
@tests/test_resolve_dates.py
@tests/test_errors.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/omnifocus_operator/contracts/use_cases/list/_date_filter.py:
```python
# Line 41-49: _validate_this_unit currently uses err.DATE_FILTER_INVALID_DURATION
@field_validator("this", mode="after")
@classmethod
def _validate_this_unit(cls, v: str | None) -> str | None:
    if v is None:
        return v
    if not _THIS_UNIT_PATTERN.match(v):
        raise ValueError(err.DATE_FILTER_INVALID_DURATION.format(value=v))  # <-- BUG
    return v
```

From src/omnifocus_operator/agent_messages/errors.py:
```python
# Line 147-150: Duration error mentions counts and '2w' example — wrong for 'this'
DATE_FILTER_INVALID_DURATION = (
    "Invalid duration '{value}' -- use a number followed by d/w/m/y (e.g. '3d', '2w', 'm'). "
    "Count defaults to 1 when omitted, so 'w' means '1w'."
)
```

From src/omnifocus_operator/service/resolve_dates.py:
```python
# Lines 21-28: Current signature with raw ints
def resolve_date_filter(
    value: StrEnum | DateFilter,
    field_name: str,
    now: datetime,
    *,
    week_start: int = 0,
    due_soon_interval: int | None = None,     # <-- raw SQLite int
    due_soon_granularity: int | None = None,   # <-- raw SQLite int
) -> tuple[datetime | None, datetime | None]:
```

From src/omnifocus_operator/contracts/use_cases/list/_enums.py:
```python
class DueDateShortcut(StrEnum):
    OVERDUE = "overdue"
    SOON = "soon"
    TODAY = "today"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DATE_FILTER_INVALID_THIS_UNIT error constant and fix _validate_this_unit</name>
  <files>
    src/omnifocus_operator/agent_messages/errors.py
    src/omnifocus_operator/contracts/use_cases/list/_date_filter.py
    tests/test_date_filter_contracts.py
    tests/test_date_filter_constants.py
  </files>
  <action>
    1. In `errors.py`, add a new constant `DATE_FILTER_INVALID_THIS_UNIT` after `DATE_FILTER_INVALID_DURATION` (around line 151). The message must:
       - Accept a `{value}` placeholder
       - Only mention bare unit chars: d, w, m, y
       - NOT mention counts, NOT mention '2w' or '3d' examples
       - Explain that 'this' means "the current period" and accepts only a single unit character
       - Example wording: `"Invalid period unit '{value}' for 'this' -- use one of: d (day), w (week), m (month), y (year)."`

    2. In `_date_filter.py` line 49, change `err.DATE_FILTER_INVALID_DURATION` to `err.DATE_FILTER_INVALID_THIS_UNIT`.

    3. In `test_date_filter_contracts.py`, update `TestDateFilterDuration::test_this_only_accepts_single_unit` (line 174-177): change the `match` pattern from `"Invalid duration"` to match the new error message (e.g., `"period unit"` or whatever the new constant contains).

    4. In `test_date_filter_constants.py`, add a test for the new constant:
       - Add `DATE_FILTER_INVALID_THIS_UNIT` to the import list in `test_error_constants_importable`
       - Add assertion for it
       - Add a new test `test_this_unit_error_format_placeholder` that calls `.format(value="2w")` and asserts the value appears and no mention of counts

    5. The `test_errors.py` consolidation test should pass automatically since `_date_filter.py` is already in `_ERROR_CONSUMERS` and will now reference the new constant.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_date_filter_contracts.py::TestDateFilterDuration::test_this_only_accepts_single_unit tests/test_date_filter_constants.py tests/test_errors.py::TestErrorConsolidation::test_all_error_constants_referenced_in_consumers -x -q</automated>
  </verify>
  <done>DateFilter(this="2w") raises error mentioning only bare unit chars (d/w/m/y), not duration format with counts. New constant is tested and referenced in consolidation.</done>
</task>

<task type="auto">
  <name>Task 2: Create DueSoonSetting enum and refactor resolve_date_filter signature</name>
  <files>
    src/omnifocus_operator/contracts/use_cases/list/_enums.py
    src/omnifocus_operator/contracts/use_cases/list/__init__.py
    src/omnifocus_operator/service/resolve_dates.py
    tests/test_resolve_dates.py
  </files>
  <action>
    1. In `_enums.py`, add a `DueSoonSetting` enum (not a StrEnum -- this is an internal config enum, not agent-facing input). It should:
       - Have exactly 7 members: `TODAY`, `TWENTY_FOUR_HOURS`, `TWO_DAYS`, `THREE_DAYS`, `FOUR_DAYS`, `FIVE_DAYS`, `ONE_WEEK`
       - Each member stores `(interval_seconds, granularity)` as its value (use a tuple)
       - Mapping: TODAY=(86400,1), TWENTY_FOUR_HOURS=(86400,0), TWO_DAYS=(172800,1), THREE_DAYS=(259200,1), FOUR_DAYS=(345600,1), FIVE_DAYS=(432000,1), ONE_WEEK=(604800,1)
       - Add properties `interval` and `granularity` that unpack the tuple
       - Only TWENTY_FOUR_HOURS has granularity=0 (rolling); all others are calendar-aligned (granularity=1)
       - Import the DUE_SOON_SETTING_DOC description if one exists, otherwise add a docstring directly explaining the 7 OmniFocus settings

    2. In `__init__.py`, add `DueSoonSetting` to the public exports.

    3. In `resolve_dates.py`:
       - Replace `due_soon_interval: int | None = None` and `due_soon_granularity: int | None = None` with `due_soon_setting: DueSoonSetting | None = None` in the `resolve_date_filter` signature
       - Update the docstring accordingly (remove interval/granularity docs, add due_soon_setting doc)
       - Update `_resolve_shortcut` signature the same way
       - In the "soon" branch of `_resolve_shortcut`, change the None check from checking both interval and granularity to checking `due_soon_setting is None`
       - Update the error message for missing config to reference `due_soon_setting`
       - Update `_compute_soon_threshold` call to unpack from the enum: `due_soon_setting.interval`, `due_soon_setting.granularity`
       - Keep `_compute_soon_threshold` signature as `(now, interval, granularity)` -- it's a pure internal helper

    4. In `test_resolve_dates.py`:
       - Import `DueSoonSetting` from `_enums`
       - Update `TestSoonShortcut`:
         - `test_soon_calendar_aligned`: replace `due_soon_interval=172800, due_soon_granularity=1` with `due_soon_setting=DueSoonSetting.TWO_DAYS`
         - `test_soon_rolling`: replace `due_soon_interval=86400, due_soon_granularity=0` with `due_soon_setting=DueSoonSetting.TWENTY_FOUR_HOURS`
         - `test_soon_without_config_raises`: no change needed (still passes no config)
         - `test_soon_with_interval_only_raises`: REMOVE this test (raw int params no longer exist)
         - `test_soon_with_granularity_only_raises`: REMOVE this test (raw int params no longer exist)
       - Add new tests:
         - `test_soon_today_setting`: passes `DueSoonSetting.TODAY` and asserts calendar-aligned (midnight + 86400s = next midnight)
         - `test_soon_one_week_setting`: passes `DueSoonSetting.ONE_WEEK` and asserts calendar-aligned (midnight + 604800s)
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_resolve_dates.py -x -q</automated>
  </verify>
  <done>resolve_date_filter takes DueSoonSetting enum instead of raw ints. All 7 OmniFocus due-soon settings representable. Tests use enum values, not raw interval/granularity numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced -- these are internal domain model changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-gc-01 | I (Info Disclosure) | DueSoonSetting enum | accept | Enum values are OmniFocus's published preference options, not secrets |
</threat_model>

<verification>
- `uv run pytest tests/test_date_filter_contracts.py tests/test_date_filter_constants.py tests/test_resolve_dates.py tests/test_errors.py -x -q` -- all pass
- `uv run pytest -x -q` -- full suite green
- `uv run mypy src/omnifocus_operator/service/resolve_dates.py src/omnifocus_operator/contracts/use_cases/list/_enums.py --strict` -- no errors
- Verify `DateFilter(this="2w")` error message does NOT contain "2w" as a valid example
</verification>

<success_criteria>
- DateFilter(this="2w") error mentions only bare unit chars, not count+unit format
- resolve_date_filter signature uses DueSoonSetting enum, not raw ints
- DueSoonSetting has exactly 7 members with correct (interval, granularity) mappings
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/45-date-models-resolution/45-04-SUMMARY.md`
</output>
