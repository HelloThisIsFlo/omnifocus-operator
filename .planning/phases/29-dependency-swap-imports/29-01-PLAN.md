---
phase: 29-dependency-swap-imports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/omnifocus_operator/server.py
  - src/omnifocus_operator/__main__.py
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - DEP-04

must_haves:
  truths:
    - "Server starts and all 6 tools are functional with fastmcp>=3.1.1 as the sole declared dependency"
    - "No mcp.server.fastmcp imports remain anywhere in src/"
    - "All tool handlers use ctx.lifespan_context (not ctx.request_context.lifespan_context)"
    - "pyproject.toml declares fastmcp>=3.1.1 and does not declare mcp>=1.26.0"
  artifacts:
    - path: "pyproject.toml"
      provides: "fastmcp>=3.1.1 dependency declaration"
      contains: "fastmcp>=3.1.1"
    - path: "src/omnifocus_operator/server.py"
      provides: "Migrated imports, Context type, lifespan shorthand"
      contains: "from fastmcp import"
    - path: "src/omnifocus_operator/__main__.py"
      provides: "TODO comment for Phase 31 logging redesign"
      contains: "TODO(Phase 31)"
  key_links:
    - from: "src/omnifocus_operator/server.py"
      to: "fastmcp"
      via: "import statement"
      pattern: "from fastmcp import FastMCP, Context"
    - from: "src/omnifocus_operator/server.py"
      to: "mcp.types"
      via: "ToolAnnotations import (no fastmcp equivalent)"
      pattern: "from mcp\\.types import ToolAnnotations"
---

<objective>
Swap the runtime dependency from `mcp>=1.26.0` to `fastmcp>=3.1.1` and migrate all src/ imports to native FastMCP v3 patterns.

Purpose: Foundation for the entire v1.2.2 milestone -- nothing else can proceed until the server runs on fastmcp.
Output: Server running on fastmcp>=3.1.1 with all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-dependency-swap-imports/29-CONTEXT.md
@.planning/phases/29-dependency-swap-imports/29-RESEARCH.md

@src/omnifocus_operator/server.py
@src/omnifocus_operator/__main__.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependency swap and uv sync</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current state -- dependency on line 8, spike group lines 28-31)
    - .planning/phases/29-dependency-swap-imports/29-RESEARCH.md (verified changes in "pyproject.toml Changes" section)
  </read_first>
  <action>
Edit pyproject.toml to perform two changes:

1. **Line 8 -- Replace the runtime dependency:**
   Change `"mcp>=1.26.0",` to `"fastmcp>=3.1.1",`

2. **Lines 28-31 -- Delete the spike dependency group entirely (per D-13):**
   Remove these 4 lines:
   ```
   # Spike: FastMCP v3 migration exploration (see .research/deep-dives/fastmcp-spike/)
   spike = [
       "fastmcp>=3",
   ]
   ```

After editing, run `uv sync` to resolve the new dependency tree and update the lockfile.

Then run `uv run python -c "import fastmcp; print(fastmcp.__version__)"` to confirm fastmcp is importable.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && grep 'fastmcp>=3.1.1' pyproject.toml && ! grep 'mcp>=1.26.0' pyproject.toml && ! grep -A2 'spike = \[' pyproject.toml && uv run python -c "import fastmcp; print(fastmcp.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml line 8 contains `"fastmcp>=3.1.1",`
    - pyproject.toml does NOT contain `"mcp>=1.26.0"`
    - pyproject.toml does NOT contain `spike = [` (spike group deleted)
    - pyproject.toml does NOT contain `"fastmcp>=3",` (spike dep deleted)
    - `uv sync` completes successfully
    - `uv run python -c "import fastmcp"` exits 0
  </acceptance_criteria>
  <done>pyproject.toml declares fastmcp>=3.1.1 as the sole runtime dependency, spike group is deleted, uv sync has resolved the new dependency tree</done>
</task>

<task type="auto">
  <name>Task 2: Migrate server.py imports, Context types, and lifespan shorthand</name>
  <files>src/omnifocus_operator/server.py, src/omnifocus_operator/__main__.py</files>
  <read_first>
    - src/omnifocus_operator/server.py (current state -- all lines to understand full context of imports, Context usage, lifespan_context usage)
    - src/omnifocus_operator/__main__.py (current state -- logging comment on line 15)
    - .planning/phases/29-dependency-swap-imports/29-RESEARCH.md (exact before/after for each change)
    - .research/deep-dives/fastmcp-spike/experiments/01_import.py (import migration reference)
  </read_first>
  <action>
### server.py -- 4 categories of changes:

**A. Import migration (line 16, per D-01 and D-09):**
Change:
```python
from mcp.server.fastmcp import Context, FastMCP
```
To:
```python
from fastmcp import FastMCP, Context
```

Keep line 17 UNCHANGED -- `from mcp.types import ToolAnnotations` stays because fastmcp does not re-export it. Per D-09 this is the one acceptable `mcp.*` import.

Keep `from typing import TYPE_CHECKING, Any` UNCHANGED on line 14 -- `Any` is still used in `dict[str, Any]` on lines 179 and 236.

**B. Context type simplification (6 occurrences, per D-10):**
Change every `Context[Any, Any, Any]` to `Context` (FastMCP v3 Context is NOT generic -- subscripting raises TypeError):
- Line 115: `async def get_all(ctx: Context[Any, Any, Any])` -> `async def get_all(ctx: Context)`
- Line 137: `async def get_task(id: str, ctx: Context[Any, Any, Any])` -> `async def get_task(id: str, ctx: Context)`
- Line 150: `async def get_project(id: str, ctx: Context[Any, Any, Any])` -> `async def get_project(id: str, ctx: Context)`
- Line 163: `async def get_tag(id: str, ctx: Context[Any, Any, Any])` -> `async def get_tag(id: str, ctx: Context)`
- Line 180: `ctx: Context[Any, Any, Any],` -> `ctx: Context,`
- Line 237: `ctx: Context[Any, Any, Any],` -> `ctx: Context,`

**C. Lifespan shorthand (6 occurrences, per D-03):**
Change every `ctx.request_context.lifespan_context` to `ctx.lifespan_context`:
- Line 123: `ctx.request_context.lifespan_context["service"]` -> `ctx.lifespan_context["service"]`
- Line 142: same change
- Line 155: same change
- Line 168: same change
- Line 209: same change
- Line 277: same change

**D. TODO comment for deferred cleanup (per D-12):**
Add a `# TODO(Phase 30):` comment near the `from mcp.types import ToolAnnotations` line to note it remains because fastmcp doesn't re-export it:
```python
from mcp.types import ToolAnnotations  # TODO(Phase 30): no fastmcp equivalent; revisit if fastmcp adds re-export
```

### __main__.py -- 1 change:

**Replace the misleading logging comment (line 15, per D-12):**
Change:
```python
# Log to file -- stdio_server() hijacks stderr, so file is the reliable path.
```
To:
```python
# TODO(Phase 31): Redesign logging -- stderr is NOT hijacked (spike exp 03 proved
# the misdiagnosis). Phase 31 should add dual-handler (StreamHandler + FileHandler),
# proper namespace, the works. See CONTEXT.md deferred ideas.
```

### After all changes, run full test suite:
```bash
uv run pytest tests/test_server.py -x --no-cov
uv run pytest
```
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && grep 'from fastmcp import FastMCP, Context' src/omnifocus_operator/server.py && grep 'from mcp.types import ToolAnnotations' src/omnifocus_operator/server.py && ! grep 'from mcp.server.fastmcp' src/omnifocus_operator/server.py && ! grep 'Context\[Any, Any, Any\]' src/omnifocus_operator/server.py && ! grep 'request_context.lifespan_context' src/omnifocus_operator/server.py && grep 'ctx.lifespan_context' src/omnifocus_operator/server.py && grep 'TODO(Phase 31)' src/omnifocus_operator/__main__.py && uv run pytest tests/test_server.py -x --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - server.py line 16 contains `from fastmcp import FastMCP, Context`
    - server.py line 17 contains `from mcp.types import ToolAnnotations`
    - server.py does NOT contain `from mcp.server.fastmcp`
    - server.py does NOT contain `Context[Any, Any, Any]` (0 occurrences)
    - server.py does NOT contain `request_context.lifespan_context` (0 occurrences)
    - server.py contains exactly 6 occurrences of `ctx.lifespan_context`
    - server.py contains `from typing import TYPE_CHECKING, Any` (Any still needed)
    - __main__.py contains `TODO(Phase 31)` comment
    - __main__.py does NOT contain `stdio_server() hijacks stderr`
    - `uv run pytest tests/test_server.py -x --no-cov` exits 0
    - `uv run pytest` exits 0 (full suite green)
  </acceptance_criteria>
  <done>All src/ imports use fastmcp, Context type annotations are plain (not generic), lifespan shorthand is used everywhere, TODO comments mark deferred cleanup, full test suite passes</done>
</task>

</tasks>

<verification>
Phase-level verification for Plan 01:

1. `grep -c 'from mcp.server.fastmcp' src/omnifocus_operator/server.py` returns 0
2. `grep -c 'from fastmcp import' src/omnifocus_operator/server.py` returns 1
3. `grep -c 'Context\[Any, Any, Any\]' src/omnifocus_operator/server.py` returns 0
4. `grep -c 'request_context.lifespan_context' src/omnifocus_operator/server.py` returns 0
5. `grep -c 'ctx.lifespan_context' src/omnifocus_operator/server.py` returns 6
6. `grep 'fastmcp>=3.1.1' pyproject.toml` succeeds
7. `grep 'mcp>=1.26.0' pyproject.toml` fails (removed)
8. `grep 'TODO(Phase 31)' src/omnifocus_operator/__main__.py` succeeds
9. `uv run pytest` exits 0
</verification>

<success_criteria>
- Server runs on fastmcp>=3.1.1 with all 6 tools functional (verified by existing test suite)
- Zero `mcp.server.fastmcp` imports in src/
- All Context annotations are plain `Context` (not generic)
- All lifespan access uses shorthand `ctx.lifespan_context`
- pyproject.toml declares fastmcp>=3.1.1 (not mcp>=1.26.0)
- Spike dependency group deleted from pyproject.toml
- TODO comments mark Phase 30 and Phase 31 deferred work
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/29-dependency-swap-imports/29-01-SUMMARY.md`
</output>
