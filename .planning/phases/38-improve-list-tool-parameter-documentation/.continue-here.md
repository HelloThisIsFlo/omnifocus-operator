---
context: phase
phase: 38-improve-list-tool-parameter-documentation
task: N/A (no formal plan — direct editing)
status: in_progress
last_updated: 2026-04-05T11:04:15.616Z
---

<current_state>
Phase 38 is being done WITHOUT the full GSD pipeline (no planner/executor/verifier). The user decided
the phase is pure string work — the discussion IS the value, and code changes are made directly.

First commit is done (48a676e): 12 Field(description=...) additions across all list query models,
5 reworked list tool descriptions (output-focused), 3 updated enum docstrings, folder hierarchy note,
perspectives built-in caveat.

The user was about to iterate on more changes — specifically mentioned:
1. Task tool description should mention hierarchy (parent field) like we did for folders
2. Reviewing the v1.3.1 milestone spec (MILESTONE-v1.3.1.md) which significantly changes Task output
   (new `project` field, `parent` becomes tagged object, `inInbox` removed from output)
3. General iteration on descriptions with fresh eyes

The user also mentioned wanting to cancel/remove this phase from the roadmap afterward since
there's no need for the full GSD ceremony for string changes.
</current_state>

<completed_work>

## Committed (48a676e)

### New constants in descriptions.py
- FLAGGED_FILTER_DESC: "true = flagged only, false = unflagged only, null/omit = skip filter."
- IN_INBOX_FILTER_DESC: "true = Inbox tasks only (not assigned to a project), false = non-Inbox only, null/omit = skip filter."
- ESTIMATED_MINUTES_MAX_DESC: "Include tasks with estimate <= this value (minutes). Tasks with no estimate are excluded."
- LIMIT_DESC: "Max items to return. Pass null to return all."
- OFFSET_DESC: "Skip this many items. Requires limit to be set."
- REVIEW_DUE_WITHIN_DESC: 'Review due within this window. "now" or "N<unit>" (unit: d/w/m/y). Examples: "1w", "2m".'
- FOLDER_PARENT_DESC: "Parent folder ID, or null for top-level folders."
- PERSPECTIVES_BUILTIN_NOTE (with TODO(v1.5) comment)

### Updated constants
- AVAILABILITY_DOC: "Can this be worked on?"
- TAG_AVAILABILITY_DOC: "Is this tag active? blocked = on hold, always blocks tagged tasks. dropped = hidden from hierarchy, blocks tasks only if their sole tag."
- FOLDER_AVAILABILITY_DOC: "Is this folder active?"

### Reworked 5 list tool descriptions
All slimmed to focus on output fields (since outputSchema not visible to agents).
Removed redundant input field docs that are now on Field(description=...).

### Query model files (5 files)
All bare parameters now have Field(default=..., description=CONSTANT).

### Folder model
parent field wrapped with FOLDER_PARENT_DESC.
</completed_work>

<remaining_work>

## Still under discussion
- Task hierarchy mention in list_tasks tool description (similar to folder hierarchy note)
- Possible forward-compatibility considerations for v1.3.1 changes
- Any other description tweaks the user spots during review
- Run test suite to verify (uv run pytest tests/test_output_schema.py -x -q)
- Move todo from pending to done after all iterations complete
</remaining_work>

<decisions_made>

- Field-level descriptions preferred over tool-level for boolean tristates (only 2 booleans, each has own semantics)
- "null/omit = skip filter" pattern for boolean filters
- Capitalize "Inbox" as OmniFocus concept
- reviewDueWithin uses "N<unit>" format notation
- Availability field descriptions NOT needed — user fixed default_factory->default so defaults visible in schema
- Tool descriptions reworked to: purpose + cross-cutting behavior + output field guide (since no outputSchema)
- PERSPECTIVES_BUILTIN_NOTE as separate grep-able constant with TODO(v1.5)
- Phase done WITHOUT GSD pipeline — direct editing, cancel phase after
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. This file
2. `src/omnifocus_operator/agent_messages/descriptions.py` — all the constants we added/changed
3. `.research/updated-spec/MILESTONE-v1.3.1.md` — v1.3.1 changes that affect Task output fields
4. `.planning/todos/pending/2026-04-05-improve-list-tool-parameter-documentation.md` — original todo with 4 items

<context>
This was a collaborative phrasing session — user wanted to lock in exact description text through
discussion rather than delegating to planner/executor. The conversation spanned two days (user
reviewed overnight). All 4 todo items are addressed in the first commit. The user wants to continue
iterating on additional improvements they noticed, particularly around task hierarchy documentation
and forward-compatibility with v1.3.1.
</context>

<next_action>
Start with: Ask the user what they wanted to discuss about task hierarchy in the list_tasks tool
description (they mentioned this before pausing). Also run the output schema test to verify current
changes don't break anything.
</next_action>
