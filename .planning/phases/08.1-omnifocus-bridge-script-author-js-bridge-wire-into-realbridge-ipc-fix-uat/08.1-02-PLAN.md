---
phase: 08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/repository/_repository.py
  - src/omnifocus_operator/simulator/__main__.py
  - uat/test_read_only.py
  - tests/test_bridge.py
  - tests/test_ipc_engine.py
  - tests/test_repository.py
  - tests/test_service.py
  - tests/test_simulator_integration.py
autonomous: true
requirements:
  - RENAME-01

must_haves:
  truths:
    - "No reference to 'dump_all' exists in production code or tests"
    - "All tests pass after the rename"
    - "The operation string 'snapshot' is used everywhere dump_all was"
  artifacts:
    - path: "src/omnifocus_operator/repository/_repository.py"
      provides: "Repository using 'snapshot' operation"
      contains: "snapshot"
    - path: "src/omnifocus_operator/simulator/__main__.py"
      provides: "Simulator dispatching on snapshot"
  key_links:
    - from: "src/omnifocus_operator/repository/_repository.py"
      to: "bridge.send_command"
      via: "send_command('snapshot')"
      pattern: "send_command.*snapshot"
---

<objective>
Rename the `dump_all` operation to `snapshot` across the entire codebase.

Purpose: Align the operation name with the `DatabaseSnapshot` model name for clearer semantics. The rename is a prerequisite for the IPC protocol changes in Plan 03, which builds the new envelope format around the `snapshot` operation.

Output: All production code and tests use `"snapshot"` instead of `"dump_all"`. Zero grep matches for `dump_all` in `src/` and `tests/`.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat/08.1-CONTEXT.md

<interfaces>
<!-- All files containing "dump_all" that need renaming -->

Production code (1 file):
- src/omnifocus_operator/repository/_repository.py:84 — `await self._bridge.send_command("dump_all")`

Simulator:
- src/omnifocus_operator/simulator/__main__.py:89-90 — parses dispatch and processes operations (no literal "dump_all" but the operation routing is implicit)

UAT:
- uat/test_read_only.py:29,31 — `"dump_all"` in print message and send_command call

Test files (many references — see grep output):
- tests/test_bridge.py — ~30 references in error construction and send_command calls
- tests/test_ipc_engine.py — 0 direct "dump_all" references (uses "test_op" etc.)
- tests/test_repository.py — ~8 references in operation assertions and error construction
- tests/test_service.py — 1 reference in error construction
- tests/test_simulator_integration.py — ~10 references in send_command calls

NOTE: InMemoryBridge ignores the operation string, so no changes needed there.
NOTE: _real.py has no literal "dump_all" — it receives operation as a parameter.
NOTE: _simulator.py has no literal "dump_all" — it inherits from RealBridge.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename dump_all to snapshot in production code and UAT</name>
  <files>src/omnifocus_operator/repository/_repository.py, uat/test_read_only.py</files>
  <action>
Update all `"dump_all"` string literals to `"snapshot"` in production code:

1. **`src/omnifocus_operator/repository/_repository.py`** (line 84):
   - Change: `await self._bridge.send_command("dump_all")` -> `await self._bridge.send_command("snapshot")`

2. **`uat/test_read_only.py`**:
   - Line 29: `"Sending dump_all command..."` -> `"Sending snapshot command to OmniFocus..."`
   - Line 31: `await bridge.send_command("dump_all")` -> `await bridge.send_command("snapshot")`

No other production files contain literal `"dump_all"`. The bridge implementations (`_real.py`, `_simulator.py`, `_in_memory.py`) receive operation as a parameter — they don't hardcode it.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && ! grep -r "dump_all" src/ uat/ --include="*.py" && echo "PASS: no dump_all in src/ or uat/"</automated>
  </verify>
  <done>Zero grep matches for "dump_all" in `src/` and `uat/` Python files.</done>
</task>

<task type="auto">
  <name>Task 2: Rename dump_all to snapshot in all test files</name>
  <files>tests/test_bridge.py, tests/test_repository.py, tests/test_service.py, tests/test_simulator_integration.py</files>
  <action>
Replace all `"dump_all"` string literals with `"snapshot"` in test files. This is a mechanical find-and-replace operation.

Files and approximate change counts:

1. **`tests/test_bridge.py`** (~30 replacements):
   - All `BridgeError("dump_all", ...)` -> `BridgeError("snapshot", ...)`
   - All `BridgeTimeoutError("dump_all", ...)` -> `BridgeTimeoutError("snapshot", ...)`
   - All `BridgeConnectionError("dump_all", ...)` -> `BridgeConnectionError("snapshot", ...)`
   - All `BridgeProtocolError("dump_all", ...)` -> `BridgeProtocolError("snapshot", ...)`
   - All `send_command("dump_all")` -> `send_command("snapshot")`
   - All `BridgeCall(operation="dump_all", ...)` -> `BridgeCall(operation="snapshot", ...)`
   - All assertion strings containing `dump_all` -> `snapshot`
   - Rename test: `test_first_call_uses_dump_all_operation` -> `test_first_call_uses_snapshot_operation` (if it exists in this file)

2. **`tests/test_repository.py`** (~8 replacements):
   - `test_first_call_uses_dump_all_operation` -> `test_first_call_uses_snapshot_operation`
   - All `BridgeError("dump_all", ...)` -> `BridgeError("snapshot", ...)`
   - Operation assertions: `== "dump_all"` -> `== "snapshot"`

3. **`tests/test_service.py`** (~1 replacement):
   - `BridgeError("dump_all", ...)` -> `BridgeError("snapshot", ...)`

4. **`tests/test_simulator_integration.py`** (~10 replacements):
   - All `send_command("dump_all")` -> `send_command("snapshot")`
   - `test_round_trip_dump_all` -> `test_round_trip_snapshot`
   - Docstring: `send_command('dump_all')` -> `send_command('snapshot')`

After replacements, verify no remaining `dump_all` references exist in tests/ (except possibly in comments/docstrings explaining the rename, which should also be updated).

Run full pytest suite to confirm all tests still pass with the renamed operation.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && ! grep -r "dump_all" tests/ --include="*.py" && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>
    - Zero grep matches for "dump_all" in `tests/` Python files
    - All pytest tests pass with the renamed operation string
  </done>
</task>

</tasks>

<verification>
```bash
# No dump_all anywhere in src/, tests/, or uat/
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator
grep -r "dump_all" src/ tests/ uat/ --include="*.py" | wc -l  # Expect: 0

# Full test suite passes
uv run pytest tests/ -x
```
</verification>

<success_criteria>
- `grep -r "dump_all" src/ tests/ uat/ --include="*.py"` returns zero matches
- All existing tests pass with the operation renamed to "snapshot"
- No behavioral changes — only the operation string changed
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat/08.1-02-SUMMARY.md`
</output>
