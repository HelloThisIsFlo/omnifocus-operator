# Phase 08.1: OmniFocus Bridge Script - Context

**Gathered:** 2026-03-02
**Status:** Ready for planning

## Phase Boundary

Author the OmniFocus-side JavaScript bridge script that executes inside OmniFocus's Omni Automation runtime. Wire it into `_trigger_omnifocus()` via the URL scheme. Update the IPC protocol (request envelope, operation naming). Fix the UAT script and re-run against live OmniFocus to complete the Phase 8 checkpoint.



## Implementation Decisions

### Bridge folder structure

- npm project in `bridge/` at project root — devDependencies only (test runner), zero production deps
- Canonical `bridge.js` lives at `src/omnifocus_operator/bridge/bridge.js` (inside the Python package)
- `bridge/` npm project references the canonical file for testing via relative path (`../src/omnifocus_operator/bridge/bridge.js`)
- Python loads `bridge.js` via `importlib.resources` at runtime — PyPI-compatible
- RealBridge reads and caches the script once at `__init__()` time (IPC dir doesn't change during a session)

### JS testing strategy

- Test runner: **Vitest** (one devDependency in `bridge/package.json`)
- Scope: Pure logic only — argument parsing, dispatch routing, response envelope shaping, error handling
- OmniFocus API calls (`flattenedTasks`, `FileWrapper`, etc.) are mocked at the boundary, not deeply simulated
- Script uses **dual-format**: exports internal functions for testability via `module.exports`, production entry point wraps in IIFE
- Unified test command: a top-level `Makefile` runs both `uv run pytest` and `cd bridge && npm test`

### Argument protocol design

- **Template injection**: Python reads `bridge.js`, replaces `__IPC_DIR__` placeholder with the actual IPC directory path, URL-encodes the result as `script=` parameter
- **Argument is a file prefix only**: `arg={pid}_{uuid}` — the script uses this to construct request/response filenames
- URL format: `omnifocus:///omnijs-run?script={encoded_customized_script}&arg={pid}_{uuid}`
- The script reads `{IPC_DIR}/{argument}.request.json`, processes the operation, writes `{IPC_DIR}/{argument}.response.json`
- The `::::` delimiter is removed from the dispatch flow — no longer needed since UUID is in the filename
- **OmniFocus runtime contract**: The `arg=` URL parameter is exposed as the global variable `argument` inside the executed script (Omni Automation convention)
- `_trigger_omnifocus()` method signature changes — no longer receives `dispatch: str`, instead takes the file prefix and encoded script. `SimulatorBridge._trigger_omnifocus()` override must be updated to match.

### Request/response envelope

- **Request file** (clean JSON): `{"operation": "snapshot", "params": {}}` — no more `{"dispatch": "uuid::::op"}` format
- **Response file** (unchanged): `{"success": true, "data": {...}}` or `{"success": false, "error": "message"}`
- `send_command()`'s existing `params` argument (currently unused) will be forwarded into the request envelope
- This requires updating `_write_request()` in `_real.py` and the simulator's `_handle_request()` to use the new envelope

### Operation naming

- Rename `dump_all` → `snapshot` across the codebase (~60+ references in: repository, service, bridge, simulator, tests, UAT). Planning docs retain the historical name.
- Only one operation for now: `snapshot`. Design for extensibility but only implement snapshot.
- Aligns with the existing `DatabaseSnapshot` model name
- `InMemoryBridge` ignores the operation string — no changes needed there

### Error handling in JS

- Always write a response file on error: `{"success": false, "error": "message"}` — Python never times out for catchable failures
- Also log errors to OmniFocus developer console (`console.error`) for debugging
- Covers: can't read request file, unknown operation, OmniFocus API exceptions

### File I/O in OmniFocus

- Use `FileWrapper.withContents()` for writing (proven by research script)
- Use `URL.fromPath()` for path construction
- Atomic writes via FileWrapper's built-in atomic option



## Specific Ideas

- The research script (`.research/operatorBridgeScript.js`) shows the OmniFocus API surface: `flattenedTasks`, `flattenedProjects`, `flattenedTags`, `flattenedFolders`, `Perspective.all`, `Task.Status.*`, `FileWrapper`, `URL.fromPath()`
- The simulator (`simulator/__main__.py`) is the Python reference implementation of the IPC protocol — the JS bridge mirrors its behavior
- Script should be an IIFE in production: `(function() { ... })()` — self-contained, no global pollution
- Template placeholder `__IPC_DIR__` must be a string that won't appear in normal JS code



## Existing Code Insights

### Reusable Assets

- `_real.py:129-162`: `_trigger_omnifocus()` — needs `script=` parameter added, `arg=` changed to file prefix
- `_real.py:164-175`: `_write_request()` — needs envelope changed from `{"dispatch": ...}` to `{"operation": ..., "params": ...}`
- `_real.py:101-127`: `send_command()` — dispatch format changes, `::::` delimiter removed
- `simulator/__main__.py:76-128`: `_handle_request()` — reference for request processing flow, needs envelope update
- `.research/operatorBridgeScript.js`: OmniFocus API usage reference (data mapping, FileWrapper, helpers)
- `uat/test_read_only.py`: UAT script — needs `dump_all` → `snapshot` rename, otherwise looks ready

### Established Patterns

- Atomic file writes via `.tmp` + `os.replace()` on Python side — JS side uses `FileWrapper` atomic option
- Request/response file naming: `{pid}_{uuid}.request.json` / `{pid}_{uuid}.response.json`
- `_validate_response()` already expects `{success, data, error}` envelope — no changes needed there
- `pragma: no cover` on `_trigger_omnifocus()` — SAFE-01 prevents automated testing of production trigger

### Integration Points

- `_trigger_omnifocus()` in `_real.py` — main wiring point for the `script=` parameter
- `RealBridge.__init__()` — where script loading + template injection happens
- `bridge/_factory.py` — factory already handles RealBridge creation, may need IPC dir awareness for template
- `pyproject.toml` — needs package-data configuration to include `bridge.js`
- `importlib.resources` — new import in `_real.py` for loading the JS file
- `_simulator.py:26`: `SimulatorBridge._trigger_omnifocus()` — inherits from `RealBridge`, override signature must match parent changes



## Deferred Ideas

- Write operations (create task, complete task, etc.) — future milestones, but the `{"operation": ..., "params": {}}` envelope is designed for extensibility
- Script versioning / compatibility checks — not needed for single-operation phase
- Hot-reload of bridge script during development — nice-to-have, not essential



---

*Phase: 08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat*
*Context gathered: 2026-03-02*