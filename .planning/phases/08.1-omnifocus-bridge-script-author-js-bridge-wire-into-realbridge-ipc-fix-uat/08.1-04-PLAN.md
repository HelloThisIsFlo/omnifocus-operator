---
phase: 08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat
plan: 04
type: execute
wave: 3
depends_on:
  - "08.1-03"
files_modified:
  - Makefile
  - .gitignore
  - uat/test_read_only.py
autonomous: false
requirements:
  - MAKEFILE-01
  - IPC-PROTO-02

must_haves:
  truths:
    - "`make test` runs both Python pytest and JS Vitest successfully"
    - "UAT script uses the new IPC protocol and can be run against live OmniFocus"
  artifacts:
    - path: "Makefile"
      provides: "Unified test command"
      contains: "pytest"
    - path: "uat/test_read_only.py"
      provides: "Updated UAT script for manual validation"
      contains: "snapshot"
  key_links:
    - from: "Makefile"
      to: "uv run pytest"
      via: "test target"
      pattern: "uv run pytest"
    - from: "Makefile"
      to: "bridge/npm test"
      via: "test target"
      pattern: "npm test"
---

<objective>
Create the unified Makefile test command and fix the UAT script for end-to-end validation against live OmniFocus.

Purpose: Provide a single `make test` command that runs both Python and JS test suites. Then validate the complete pipeline works against real OmniFocus via the UAT checkpoint.

Output: Working Makefile + human-verified end-to-end pipeline.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat/08.1-CONTEXT.md
@.planning/phases/08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat/08.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Makefile and update .gitignore</name>
  <files>Makefile, .gitignore</files>
  <action>
**1. Create `Makefile` at project root:**

```makefile
.PHONY: test test-python test-js lint typecheck

test: test-python test-js
	@echo "All tests passed."

test-python:
	uv run pytest tests/ -x

test-js:
	cd bridge && npm test

lint:
	uv run ruff check src/ tests/
	uv run ruff format --check src/ tests/

typecheck:
	uv run mypy src/
```

**2. Update `.gitignore`** — ensure `bridge/node_modules/` is excluded. Check if there's already a `node_modules` entry. If not, add:
```
# JS bridge test dependencies
bridge/node_modules/
```

**3. Run `make test`** to verify both suites execute and pass.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && make test</automated>
  </verify>
  <done>
    - `Makefile` exists at project root with `test`, `test-python`, `test-js` targets
    - `make test` runs both pytest and Vitest successfully
    - `bridge/node_modules/` is in .gitignore
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — Validate end-to-end pipeline against live OmniFocus</name>
  <files>uat/test_read_only.py</files>
  <action>
This is a human verification checkpoint. The complete end-to-end pipeline has been built:
- JS bridge script (bridge.js) runs inside OmniFocus via `omnifocus:///omnijs-run?script=...&arg=...`
- RealBridge loads bridge.js, injects IPC directory, URL-encodes it, passes via URL scheme
- Bridge script reads request file, collects OmniFocus data, writes response file
- Python side polls for response and parses it

All automated tests pass (`make test`). This validates the real OmniFocus interaction that SAFE-01 prevents from being automated.

**Steps for human:**

1. Ensure OmniFocus is running with at least one task visible.

2. Run the UAT script:
   ```bash
   cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator
   uv run python uat/test_read_only.py
   ```

3. Expected output shows snapshot with Tasks > 0, Projects > 0, Perspectives > 0, and "UAT PASSED".

4. If errors: check OmniFocus Automation Console, verify IPC directory, check template injection.

5. If `Data.fromURL()` fails in OmniFocus JS, report the error for fallback approach.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && test -f uat/test_read_only.py && grep -q "snapshot" uat/test_read_only.py && echo "UAT script ready for manual execution"</automated>
  </verify>
  <done>
    - UAT script runs successfully against live OmniFocus
    - Snapshot data returned with all 5 entity types populated
    - Human approves: "approved"
  </done>
</task>

</tasks>

<verification>
```bash
# Unified test command works
make test

# UAT script exists and is ready
test -f uat/test_read_only.py
grep -q "snapshot" uat/test_read_only.py
```
</verification>

<success_criteria>
- `make test` runs both Python and JS test suites and all pass
- UAT script successfully communicates with live OmniFocus
- Full snapshot data returned with tasks, projects, tags, folders, perspectives
- End-to-end pipeline validated: Python -> file IPC -> bridge.js in OmniFocus -> response -> Python
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat/08.1-04-SUMMARY.md`
</output>
