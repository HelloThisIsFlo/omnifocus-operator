---
phase: 05-service-layer-and-mcp-server
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/service/__init__.py
  - src/omnifocus_operator/service/_service.py
  - src/omnifocus_operator/repository/_mtime.py
  - src/omnifocus_operator/repository/__init__.py
  - tests/test_service.py
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "OperatorService.get_all_data() delegates to repository.get_snapshot() and returns a DatabaseSnapshot"
    - "ConstantMtimeSource always returns 0 (no cache invalidation) for InMemoryBridge usage"
    - "Bridge factory creates InMemoryBridge for 'inmemory', raises NotImplementedError for 'simulator' and 'real' with clear messages"
    - "Bridge factory raises ValueError for unknown bridge types"
  artifacts:
    - path: "src/omnifocus_operator/service/__init__.py"
      provides: "Public re-exports for service package"
      exports: ["OperatorService"]
    - path: "src/omnifocus_operator/service/_service.py"
      provides: "OperatorService thin passthrough to repository"
      contains: "class OperatorService"
    - path: "src/omnifocus_operator/repository/_mtime.py"
      provides: "ConstantMtimeSource added alongside existing MtimeSource and FileMtimeSource"
      contains: "class ConstantMtimeSource"
    - path: "tests/test_service.py"
      provides: "Unit tests for OperatorService, ConstantMtimeSource, bridge factory"
  key_links:
    - from: "src/omnifocus_operator/service/_service.py"
      to: "src/omnifocus_operator/repository/_repository.py"
      via: "constructor injection of OmniFocusRepository"
      pattern: "self._repository"
---

<objective>
Create the OperatorService (thin passthrough to repository), ConstantMtimeSource (for InMemoryBridge usage), and bridge factory function. These are the building blocks that the MCP server (Plan 02) will wire together.

Purpose: Establishes the service layer (ARCH-01 middle layer) and the bridge injection mechanism (ARCH-02) as independently tested units before MCP integration.
Output: `service/` package with OperatorService, ConstantMtimeSource in `repository/_mtime.py`, bridge factory, and comprehensive unit tests.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-service-layer-and-mcp-server/05-CONTEXT.md
@.planning/phases/05-service-layer-and-mcp-server/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/omnifocus_operator/repository/_repository.py:
```python
class OmniFocusRepository:
    def __init__(self, bridge: Bridge, mtime_source: MtimeSource) -> None: ...
    async def get_snapshot(self) -> DatabaseSnapshot: ...
    async def initialize(self) -> None: ...
```

From src/omnifocus_operator/repository/_mtime.py:
```python
class MtimeSource(Protocol):
    async def get_mtime_ns(self) -> int: ...

class FileMtimeSource:
    def __init__(self, path: str) -> None: ...
    async def get_mtime_ns(self) -> int: ...
```

From src/omnifocus_operator/bridge/_protocol.py:
```python
class Bridge(Protocol):
    async def send_command(self, operation: str, params: dict[str, Any] | None = None) -> dict[str, Any]: ...
```

From src/omnifocus_operator/bridge/_in_memory.py:
```python
class InMemoryBridge:
    def __init__(self, data: dict[str, Any] | None = None) -> None: ...
    async def send_command(self, operation: str, params: dict[str, Any] | None = None) -> dict[str, Any]: ...
```

From src/omnifocus_operator/models/_snapshot.py:
```python
class DatabaseSnapshot(OmniFocusBaseModel):
    tasks: list[Task]
    projects: list[Project]
    tags: list[Tag]
    folders: list[Folder]
    perspectives: list[Perspective]
```

From src/omnifocus_operator/repository/__init__.py:
```python
__all__ = ["FileMtimeSource", "MtimeSource", "OmniFocusRepository"]
```

From tests/conftest.py:
```python
def make_snapshot_dict(**overrides: Any) -> dict[str, Any]: ...
```

Established patterns:
- Package-per-concern with __init__.py re-exports and __all__
- Private modules with underscore prefix (_service.py, _mtime.py)
- TYPE_CHECKING imports for ruff TC compliance
- `from __future__ import annotations` in all modules
- Protocol-based typing (structural subtyping, not inheritance)
- FakeMtimeSource pattern from tests/test_repository.py for test doubles
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OperatorService, ConstantMtimeSource, and bridge factory (TDD)</name>
  <files>
    src/omnifocus_operator/service/__init__.py,
    src/omnifocus_operator/service/_service.py,
    src/omnifocus_operator/repository/_mtime.py,
    src/omnifocus_operator/repository/__init__.py,
    tests/test_service.py
  </files>
  <behavior>
    OperatorService:
    - get_all_data() calls repository.get_snapshot() and returns the DatabaseSnapshot
    - get_all_data() raises whatever the repository raises (fail-fast passthrough)

    ConstantMtimeSource:
    - get_mtime_ns() always returns 0
    - Satisfies the MtimeSource protocol

    Bridge factory (create_bridge function):
    - "inmemory" -> returns InMemoryBridge(data={empty collections})
    - "simulator" -> raises NotImplementedError with message mentioning Phase 7
    - "real" -> raises NotImplementedError with message mentioning Phase 8 and suggesting OMNIFOCUS_BRIDGE=inmemory
    - unknown string -> raises ValueError with message listing valid options
  </behavior>
  <action>
    **RED phase:**
    Write tests/test_service.py with:

    1. `TestOperatorService` class:
       - `test_get_all_data_returns_snapshot`: Create InMemoryBridge with make_snapshot_dict(), FakeMtimeSource, OmniFocusRepository, OperatorService. Call get_all_data(), assert returns DatabaseSnapshot with expected entity counts.
       - `test_get_all_data_delegates_to_repository`: Verify bridge.call_count == 1 after get_all_data() (proves delegation path).
       - `test_get_all_data_propagates_errors`: Configure bridge with set_error(BridgeError(...)), assert get_all_data() raises BridgeError (fail-fast passthrough).

    2. `TestConstantMtimeSource` class:
       - `test_always_returns_zero`: Call get_mtime_ns() twice, both return 0.
       - `test_satisfies_mtime_protocol`: Assert ConstantMtimeSource instance passes isinstance check with MtimeSource protocol (runtime_checkable or structural check).

    3. `TestCreateBridge` class (for the factory function):
       - `test_inmemory_returns_inmemory_bridge`: Assert return is InMemoryBridge.
       - `test_simulator_raises_not_implemented`: Assert raises NotImplementedError, match "Phase 7".
       - `test_real_raises_not_implemented`: Assert raises NotImplementedError, match "Phase 8", match "OMNIFOCUS_BRIDGE=inmemory".
       - `test_unknown_raises_value_error`: Assert raises ValueError, match "Unknown bridge type".

    Import from stubs that don't exist yet. Run `uv run pytest tests/test_service.py -x` -- all tests MUST FAIL. Commit: `test(05-01): add failing tests for OperatorService, ConstantMtimeSource, bridge factory`

    **GREEN phase:**
    1. Create `src/omnifocus_operator/service/_service.py`:
       - `from __future__ import annotations` + TYPE_CHECKING imports for OmniFocusRepository, DatabaseSnapshot
       - `OperatorService` class with `__init__(self, repository: OmniFocusRepository)` storing `self._repository`
       - `async def get_all_data(self) -> DatabaseSnapshot:` delegates to `self._repository.get_snapshot()`

    2. Create `src/omnifocus_operator/service/__init__.py`:
       - Re-export `OperatorService` from `_service`
       - Include `__all__ = ["OperatorService"]`

    3. Add `ConstantMtimeSource` to `src/omnifocus_operator/repository/_mtime.py`:
       - Simple class: `async def get_mtime_ns(self) -> int: return 0`
       - Docstring: "MtimeSource that always returns 0 -- no cache invalidation for InMemoryBridge usage."

    4. Update `src/omnifocus_operator/repository/__init__.py`:
       - Add `ConstantMtimeSource` to imports and `__all__`

    5. Add `create_bridge` factory function. Place it in `src/omnifocus_operator/server/_factory.py` (will be part of the server package that Plan 02 creates, but the factory is independently testable):
       - Actually, since server/ package doesn't exist yet and we want to avoid creating it partially, place the factory in `src/omnifocus_operator/bridge/_factory.py` -- it's a bridge concern (selecting which bridge to create).
       - `def create_bridge(bridge_type: str) -> Bridge:` using match/case
       - For "inmemory": return `InMemoryBridge(data={"tasks": [], "projects": [], "tags": [], "folders": [], "perspectives": []})` -- empty collections per research recommendation
       - For "simulator": `raise NotImplementedError("SimulatorBridge not yet implemented (Phase 7)")`
       - For "real": `raise NotImplementedError("RealBridge not yet implemented (Phase 8). Set OMNIFOCUS_BRIDGE=inmemory for development.")`
       - Default: `raise ValueError(f"Unknown bridge type: {bridge_type!r}. Use: inmemory, simulator, real")`
       - Update `src/omnifocus_operator/bridge/__init__.py` to re-export `create_bridge`

    Run `uv run pytest tests/test_service.py -x` -- all tests MUST PASS. Then run full suite `uv run pytest tests/ -x`. Commit: `feat(05-01): implement OperatorService, ConstantMtimeSource, bridge factory`

    **REFACTOR phase (if needed):**
    Run `uv run ruff check src/ tests/` and `uv run mypy src/`. Fix any issues. If changes needed, commit: `refactor(05-01): lint and type fixes`
  </action>
  <verify>
    <automated>uv run pytest tests/test_service.py -x -q && uv run pytest tests/ -x -q && uv run ruff check src/ tests/ && uv run mypy src/</automated>
  </verify>
  <done>
    - OperatorService delegates get_all_data() to repository.get_snapshot() (passthrough verified)
    - ConstantMtimeSource always returns 0
    - Bridge factory correctly routes inmemory/simulator/real/unknown with appropriate errors
    - All tests pass including full suite, linting, and type checking
    - service/ package follows established package-per-concern pattern
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_service.py -x -v` -- all OperatorService, ConstantMtimeSource, and bridge factory tests pass
2. `uv run pytest tests/ -x -q` -- full test suite passes (no regressions)
3. `uv run ruff check src/ tests/` -- no lint errors
4. `uv run mypy src/` -- no type errors
5. `from omnifocus_operator.service import OperatorService` -- import works
6. `from omnifocus_operator.repository import ConstantMtimeSource` -- import works
7. `from omnifocus_operator.bridge import create_bridge` -- import works
</verification>

<success_criteria>
- OperatorService is a thin passthrough that delegates to OmniFocusRepository
- ConstantMtimeSource satisfies MtimeSource protocol and always returns 0
- Bridge factory handles all 4 cases (inmemory, simulator, real, unknown) correctly
- All tests pass, linting clean, types check
</success_criteria>

<output>
After completion, create `.planning/phases/05-service-layer-and-mcp-server/05-01-SUMMARY.md`
</output>
