---
phase: 17-task-lifecycle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/service.py
  - tests/test_service.py
autonomous: true
gap_closure: true
requirements: [LIFE-04, LIFE-05]

must_haves:
  truths:
    - "No-op lifecycle produces only the no-op warning, not an additional 'No changes specified' warning"
    - "Drop warning on repeating tasks mentions next occurrence creation and OmniFocus UI requirement for full sequence drop"
    - "Same-container move warning explains API limitation and suggests before/after workaround"
  artifacts:
    - path: "src/omnifocus_operator/service.py"
      provides: "Fixed empty-edit guard, updated warning texts"
    - path: "tests/test_service.py"
      provides: "Tests covering all three gap fixes"
  key_links:
    - from: "src/omnifocus_operator/service.py"
      to: "tests/test_service.py"
      via: "test assertions on warning text"
      pattern: "No changes specified|already a child|occurrence was skipped"
---

<objective>
Fix three UAT-reported minor issues in service.py: (1) suppress spurious "No changes specified" warning on no-op lifecycle, (2) improve repeating task drop warning text, (3) improve same-container move warning text.

Purpose: Close UAT gaps from phase 17 verification.
Output: Updated service.py with fixes and corresponding test updates.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-task-lifecycle/17-UAT.md

<interfaces>
<!-- Key code locations the executor needs -->

From src/omnifocus_operator/service.py:

Line 159: `lifecycle_handled = False` -- set to True at line 174 when lifecycle action is present
Line 285: `if len(payload) == 1:` -- empty-edit guard, returns early with "No changes specified" warning
Line 338-343: Same-container move warning text
Line 420: Repeating task drop warning text
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix no-op lifecycle spurious warning</name>
  <files>src/omnifocus_operator/service.py, tests/test_service.py</files>
  <behavior>
    - Test: No-op lifecycle (complete already-completed task, lifecycle only, no field edits) should NOT contain "No changes specified" in warnings
    - Test: Genuine empty edit (no lifecycle, no fields) STILL produces "No changes specified" warning
  </behavior>
  <action>
    In `service.py` line 285, change `if len(payload) == 1:` to `if len(payload) == 1 and not lifecycle_handled:`.

    This fixes the bug where a no-op lifecycle (e.g., complete an already-completed task) doesn't add `lifecycle` to the payload (since `should_call=False`), causing `len(payload)==1` to trigger the spurious "No changes specified" warning even though the user did specify an action.

    In `tests/test_service.py`, update the existing test `test_lifecycle_noop_suppresses_status_warning` (line 1643) to ALSO assert that "No changes specified" is NOT in the warnings. Add a new test `test_lifecycle_noop_no_spurious_empty_edit_warning` that sends lifecycle="complete" on an already-completed task and asserts: (a) success is True, (b) has the no-op warning ("already complete"), (c) does NOT have "No changes specified" warning.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && python -m pytest tests/test_service.py -k "lifecycle_noop" -x -v</automated>
  </verify>
  <done>No-op lifecycle only produces the no-op warning, not the "No changes specified" warning. Existing empty-edit test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update warning texts for repeating drop and same-container move</name>
  <files>src/omnifocus_operator/service.py, tests/test_service.py</files>
  <action>
    Two text updates in `service.py`:

    1. **Line 420** (repeating task drop warning): Change from:
       `"Repeating task -- this occurrence was skipped."`
       To:
       `"Repeating task -- this occurrence was skipped, next occurrence created. To drop the entire repeating sequence, this must be done in the OmniFocus UI. Confirm with user if this was their intent."`

    2. **Lines 339-343** (same-container move warning): Change from:
       `"Task is already a child of this parent. The move was applied, but the server cannot yet confirm whether the task's position within the parent changed."`
       To:
       `"Task is already in this container. OmniFocus API limitation: 'beginning'/'ending' moves within the same container may not change position. Workaround: use 'before' or 'after' with a sibling task ID to control ordering."`

    Update any existing tests that assert on these exact warning strings. The repeating drop warning test is `test_lifecycle_drop_repeating_task_warning` (line 1547). The same-container move test is likely in the move/no-op detection section -- search for "already a child" in test_service.py and update the assertion string.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && python -m pytest tests/test_service.py -k "repeating or same_container or move" -x -v</automated>
  </verify>
  <done>Both warning texts updated. All tests pass with new wording.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && python -m pytest tests/test_service.py -x -v
```
All service tests pass, including lifecycle and move tests.

```bash
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && python -m pytest -x
```
Full test suite passes.
</verification>

<success_criteria>
- No-op lifecycle (complete/drop on already-completed/dropped task) produces ONLY the no-op warning
- Repeating task drop warning mentions next occurrence, OmniFocus UI, and user confirmation
- Same-container move warning explains API limitation and suggests before/after workaround
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/17-task-lifecycle/17-03-SUMMARY.md`
</output>
