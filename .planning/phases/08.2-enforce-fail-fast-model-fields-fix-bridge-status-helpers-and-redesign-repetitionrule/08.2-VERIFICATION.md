---
phase: 08.2-enforce-fail-fast-model-fields-fix-bridge-status-helpers-and-redesign-repetitionrule
verified: 2026-03-06T02:17:00Z
status: passed
score: 37/37 must-haves verified
must_haves:
  truths:
    # Plan 01 (Bridge JS)
    - "Bridge ts() throws on unknown TaskStatus instead of returning null"
    - "Bridge ps(), gs(), fs() resolvers return correct strings for all known values and throw on unknown"
    - "Bridge rst(), adk() resolvers return correct strings for all RepetitionScheduleType and AnchorDateKey values and throw on unknown"
    - "Bridge rr() extracts all 4 RepetitionRule fields (ruleString, scheduleType, anchorDateKey, catchUpAutomatically)"
    - "Bridge serializes tags as {id, name} objects instead of bare name strings"
    - "Bridge adds url field to all 4 entity types (tasks, projects, tags, folders)"
    - "Bridge adds active, effectiveActive, added, modified to projects via p.task.*"
    - "Bridge adds childrenAreMutuallyExclusive to tags"
    - "Bridge removes assignedContainer from tasks"
    - "Bridge perspective uses p.id.primaryKey instead of p.identifier, removes builtin"
    - "Bridge uses p.task.taskStatus for project taskStatus"
    - "All Vitest tests pass with updated mocks and assertions"
    # Plan 02 (Python models)
    - "EntityStatus enum is deleted and replaced by per-entity enums (ProjectStatus, TagStatus, FolderStatus)"
    - "ProjectStatus has 4 members: Active, OnHold, Done, Dropped"
    - "TagStatus has 3 members: Active, OnHold, Dropped"
    - "FolderStatus has 2 members: Active, Dropped"
    - "ScheduleType and AnchorDateKey enums exist with correct members"
    - "RepetitionRule has 4 required fields with typed enums"
    - "TagRef model exists with id and name fields"
    - "OmniFocusEntity has url, added, modified, active, effective_active fields"
    - "Task, Project, Tag, Folder all inherit shared fields from OmniFocusEntity"
    - "Project has status: ProjectStatus (required), plus active, effective_active, added, modified (inherited)"
    - "Tag has status: TagStatus (required), children_are_mutually_exclusive: bool, url (inherited)"
    - "Folder has status: FolderStatus (required), url (inherited)"
    - "ActionableEntity tags field is list[TagRef] not list[str]"
    - "Task no longer has assigned_container field"
    - "Perspective builtin is a computed_field derived from id is None"
    - "All new types are in __init__.py namespace, model_rebuild, and __all__"
    # Plan 03 (Tests)
    - "All conftest factory functions produce bridge-format dicts matching the new bridge.js output shape"
    - "All model tests pass with updated field counts, enum types, and TagRef format"
    - "New test classes exist for ProjectStatus, TagStatus, FolderStatus, ScheduleType, AnchorDateKey enums"
    - "RepetitionRule tests validate 4 required typed fields"
    - "TagRef model has its own test"
    - "Perspective tests validate computed builtin field"
    - "Seed data in _factory.py is valid for the new models"
    - "Full pytest suite passes (models, repository, service, server tests)"
    - "Full Vitest suite still passes"
  artifacts:
    - path: "src/omnifocus_operator/bridge/bridge.js"
      provides: "Updated bridge with per-entity resolvers, field changes, TagRef format"
    - path: "bridge/tests/bridge.test.js"
      provides: "Updated Vitest tests covering all new resolvers and output shape changes"
    - path: "src/omnifocus_operator/models/_enums.py"
      provides: "Per-entity status enums + RepetitionRule enums"
    - path: "src/omnifocus_operator/models/_common.py"
      provides: "Redesigned RepetitionRule (4 fields) + new TagRef model"
    - path: "src/omnifocus_operator/models/_base.py"
      provides: "Restructured hierarchy with shared fields on OmniFocusEntity"
    - path: "src/omnifocus_operator/models/__init__.py"
      provides: "Updated namespace with all new types, model_rebuild calls, exports"
    - path: "src/omnifocus_operator/models/_task.py"
      provides: "Task model with inherited lifecycle fields, no assigned_container"
    - path: "src/omnifocus_operator/models/_project.py"
      provides: "Project with ProjectStatus, required review fields, inherited lifecycle"
    - path: "src/omnifocus_operator/models/_tag.py"
      provides: "Tag with TagStatus, children_are_mutually_exclusive, inherited lifecycle"
    - path: "src/omnifocus_operator/models/_folder.py"
      provides: "Folder with FolderStatus, inherited lifecycle"
    - path: "src/omnifocus_operator/models/_perspective.py"
      provides: "Perspective with computed builtin field"
    - path: "tests/conftest.py"
      provides: "Updated factory functions matching new bridge output shape"
    - path: "tests/test_models.py"
      provides: "Updated model tests with new enum tests, field count checks, TagRef assertions"
    - path: "src/omnifocus_operator/bridge/_factory.py"
      provides: "Updated InMemoryBridge seed data matching new model requirements"
    - path: "src/omnifocus_operator/simulator/_data.py"
      provides: "Updated simulator snapshot with all required fields"
  key_links:
    - from: "bridge.js ts()"
      to: "Task.Status constants"
      via: "=== comparison with throw on unknown"
    - from: "bridge.js ps()"
      to: "Project.Status constants"
      via: "=== comparison with throw on unknown"
    - from: "_enums.py ProjectStatus"
      to: "_project.py Project.status"
      via: "TYPE_CHECKING import + model_rebuild namespace"
    - from: "_common.py TagRef"
      to: "_base.py ActionableEntity.tags"
      via: "TYPE_CHECKING import + model_rebuild namespace"
    - from: "__init__.py _ns dict"
      to: "all model_rebuild() calls"
      via: "_types_namespace parameter"
    - from: "conftest.py make_task_dict()"
      to: "Task.model_validate()"
      via: "bridge-format dict"
    - from: "_factory.py seed data"
      to: "DatabaseSnapshot.model_validate()"
      via: "InMemoryBridge response"
---

# Phase 08.2: Enforce fail-fast model fields, fix bridge status helpers, and redesign RepetitionRule - Verification Report

**Phase Goal:** Full alignment of the bridge script (JS) and Python models/enums with the empirically-verified BRIDGE-SPEC -- per-entity status resolvers, RepetitionRule redesign with 4 fields, fail-fast on all required fields, TagRef format, url fields, and model hierarchy restructuring

**Verified:** 2026-03-06T02:17:00Z
**Status:** PASSED
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Bridge ts() throws on unknown TaskStatus | VERIFIED | `throw new Error("Unknown TaskStatus: " + String(s))` at bridge.js:39; Vitest test "ts() throws on unknown status" passes |
| 2 | Bridge ps(), gs(), fs() resolvers return correct strings and throw on unknown | VERIFIED | ps() at L42-48, gs() at L50-55, fs() at L57-61; Vitest "per-entity status resolvers" block (6 tests) passes |
| 3 | Bridge rst(), adk() resolvers correct and throw on unknown | VERIFIED | rst() at L63-68, adk() at L70-75; Vitest "repetition resolvers" block (4 tests) passes |
| 4 | Bridge rr() extracts all 4 RepetitionRule fields | VERIFIED | rr() at L77-85 returns ruleString, scheduleType via rst(), anchorDateKey via adk(), catchUpAutomatically |
| 5 | Bridge tags as {id, name} objects | VERIFIED | bridge.js:145-147 `{ id: g.id.primaryKey, name: g.name }`; Vitest asserts `[{ id: "tag-001", name: "tag1" }]` |
| 6 | Bridge url field on all 4 entity types | VERIFIED | `t.url.toString()` at L116, `p.url.toString()` at L155, `g.url.toString()` at L198, `f.url.toString()` at L214 |
| 7 | Bridge project fields from p.task.* | VERIFIED | `p.task.taskStatus` at L158, `p.task.active` at L159, `p.task.effectiveActive` at L160, `p.task.added` at L161, `p.task.modified` at L162 |
| 8 | Bridge childrenAreMutuallyExclusive on tags | VERIFIED | bridge.js:205 `childrenAreMutuallyExclusive: g.childrenAreMutuallyExclusive` |
| 9 | Bridge removes assignedContainer from tasks | VERIFIED | No `assignedContainer` in task mapping (L112-148); Vitest: `expect(result.tasks[0]).not.toHaveProperty("assignedContainer")` |
| 10 | Bridge perspective id via p.id.primaryKey, no builtin | VERIFIED | `id: p.id ? p.id.primaryKey : null` at L226; Vitest: `not.toHaveProperty("builtin")` |
| 11 | Bridge project taskStatus via p.task.taskStatus | VERIFIED | `taskStatus: ts(p.task.taskStatus)` at L158 |
| 12 | All Vitest tests pass | VERIFIED | 26 tests passed (ran live) |
| 13 | EntityStatus deleted, replaced by per-entity enums | VERIFIED | No `EntityStatus` found anywhere in src/ or tests/; ProjectStatus, TagStatus, FolderStatus exist in _enums.py |
| 14 | ProjectStatus has 4 members | VERIFIED | _enums.py L27-33: ACTIVE, ON_HOLD, DONE, DROPPED |
| 15 | TagStatus has 3 members | VERIFIED | _enums.py L36-40: ACTIVE, ON_HOLD, DROPPED |
| 16 | FolderStatus has 2 members | VERIFIED | _enums.py L44-47: ACTIVE, DROPPED |
| 17 | ScheduleType and AnchorDateKey enums exist | VERIFIED | _enums.py L51-56 (3 members) and L59-64 (3 members) |
| 18 | RepetitionRule has 4 required typed fields | VERIFIED | _common.py L35-38: rule_string: str, schedule_type: ScheduleType, anchor_date_key: AnchorDateKey, catch_up_automatically: bool |
| 19 | TagRef model exists with id and name | VERIFIED | _common.py L19-23: TagRef(OmniFocusBaseModel) with id: str, name: str |
| 20 | OmniFocusEntity has url, added, modified, active, effective_active | VERIFIED | _base.py L35-48: 7 fields (id, name, url, added, modified, active, effective_active) |
| 21 | Task, Project, Tag, Folder inherit from OmniFocusEntity | VERIFIED | Task inherits via ActionableEntity; Project via ActionableEntity; Tag and Folder directly from OmniFocusEntity |
| 22 | Project has status: ProjectStatus (required) + inherited lifecycle | VERIFIED | _project.py L29: `status: ProjectStatus`; lifecycle fields inherited from OmniFocusEntity via ActionableEntity |
| 23 | Tag has status: TagStatus (required) + children_are_mutually_exclusive | VERIFIED | _tag.py L24-26: status: TagStatus, allows_next_action: bool, children_are_mutually_exclusive: bool |
| 24 | Folder has status: FolderStatus (required) | VERIFIED | _folder.py L24: `status: FolderStatus` |
| 25 | ActionableEntity tags is list[TagRef] | VERIFIED | _base.py L87: `tags: list[TagRef] = []` |
| 26 | Task has no assigned_container field | VERIFIED | _task.py has only status, in_inbox, project, parent. No `assigned_container` anywhere in src/ |
| 27 | Perspective builtin is computed_field | VERIFIED | _perspective.py L27-31: `@computed_field` + `@property` def builtin returning `self.id is None` |
| 28 | All new types in __init__.py namespace | VERIFIED | __init__.py L36-52: _ns dict has all types; L53-59: model_rebuild calls; L61-80: __all__ includes all new exports |
| 29 | Conftest factories produce valid bridge-format dicts | VERIFIED | Factory functions updated with url, lifecycle fields, TagRef format, no assignedContainer, no builtin on perspective |
| 30 | All model tests pass with updated assertions | VERIFIED | 177 pytest tests passed (ran live) |
| 31 | New enum test classes exist | VERIFIED | test_models.py has TestProjectStatus, TestTagStatus, TestFolderStatus, TestScheduleType, TestAnchorDateKey classes |
| 32 | RepetitionRule tests validate 4 required typed fields | VERIFIED | TestRepetitionRule class with full_round_trip, missing_field_rejected, invalid_schedule_type_rejected tests |
| 33 | TagRef model has its own test | VERIFIED | TestTagRef class with tag_ref_round_trip test |
| 34 | Perspective tests validate computed builtin | VERIFIED | TestPerspectiveModel checks model_fields==2, model_computed_fields==1, builtin_null_id test |
| 35 | Seed data in _factory.py valid for new models | VERIFIED | _factory.py InMemoryBridge seed data includes all required fields; validates against DatabaseSnapshot (tested live) |
| 36 | Full pytest suite passes | VERIFIED | 177 tests passed, 98.43% coverage (ran live) |
| 37 | Full Vitest suite passes | VERIFIED | 26 tests passed (ran live) |

**Score:** 37/37 truths verified

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `src/omnifocus_operator/bridge/bridge.js` | VERIFIED | 292 lines, 8 enum resolvers (ts/ps/gs/fs/rst/adk) + rr, all entity mappings updated, exports include all new functions |
| `bridge/tests/bridge.test.js` | VERIFIED | 557 lines, 26 tests in 6 describe blocks covering all resolvers and output shapes |
| `src/omnifocus_operator/models/_enums.py` | VERIFIED | 65 lines, 6 StrEnum classes, no EntityStatus |
| `src/omnifocus_operator/models/_common.py` | VERIFIED | 49 lines, TagRef + RepetitionRule (4 fields) + ReviewInterval |
| `src/omnifocus_operator/models/_base.py` | VERIFIED | 88 lines, OmniFocusEntity with 7 fields, ActionableEntity with list[TagRef] tags |
| `src/omnifocus_operator/models/__init__.py` | VERIFIED | 81 lines, all types in _ns, model_rebuild calls, complete __all__ |
| `src/omnifocus_operator/models/_task.py` | VERIFIED | 38 lines, 4 own fields, no assigned_container |
| `src/omnifocus_operator/models/_project.py` | VERIFIED | 43 lines, ProjectStatus, required review fields |
| `src/omnifocus_operator/models/_tag.py` | VERIFIED | 27 lines, TagStatus, children_are_mutually_exclusive |
| `src/omnifocus_operator/models/_folder.py` | VERIFIED | 25 lines, FolderStatus |
| `src/omnifocus_operator/models/_perspective.py` | VERIFIED | 31 lines, @computed_field builtin |
| `tests/conftest.py` | VERIFIED | 190 lines, 6 factory functions with correct field counts (32/36/11/9/2) |
| `tests/test_models.py` | VERIFIED | 825 lines, comprehensive tests for all enums, models, and field counts |
| `src/omnifocus_operator/bridge/_factory.py` | VERIFIED | 180 lines, InMemoryBridge seed data with all required fields |
| `src/omnifocus_operator/simulator/_data.py` | VERIFIED | 569 lines, simulator snapshot with url, lifecycle, TagRef tags, no builtin |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| bridge.js ts() | Task.Status constants | === comparison | WIRED | Throws on unknown; 7 known values mapped |
| bridge.js ps() | Project.Status constants | === comparison | WIRED | Throws on unknown; 4 known values mapped |
| bridge.js gs() | Tag.Status constants | === comparison | WIRED | Throws on unknown; 3 known values mapped |
| bridge.js fs() | Folder.Status constants | === comparison | WIRED | Throws on unknown; 2 known values mapped |
| bridge.js rst() | Task.RepetitionScheduleType | === comparison | WIRED | Throws on unknown; 3 known values mapped |
| bridge.js adk() | Task.AnchorDateKey | === comparison | WIRED | Throws on unknown; 3 known values mapped |
| _enums.py ProjectStatus | _project.py Project.status | TYPE_CHECKING + model_rebuild | WIRED | `status: ProjectStatus` in Project; _ns dict includes ProjectStatus |
| _enums.py TagStatus | _tag.py Tag.status | TYPE_CHECKING + model_rebuild | WIRED | `status: TagStatus` in Tag; _ns dict includes TagStatus |
| _enums.py FolderStatus | _folder.py Folder.status | TYPE_CHECKING + model_rebuild | WIRED | `status: FolderStatus` in Folder; _ns dict includes FolderStatus |
| _common.py TagRef | _base.py ActionableEntity.tags | TYPE_CHECKING + model_rebuild | WIRED | `tags: list[TagRef]` in ActionableEntity; _ns dict includes TagRef |
| _common.py RepetitionRule | _base.py ActionableEntity.repetition_rule | TYPE_CHECKING + model_rebuild | WIRED | `repetition_rule: RepetitionRule` in ActionableEntity |
| __init__.py _ns dict | all model_rebuild() calls | _types_namespace parameter | WIRED | 7 model_rebuild calls using _ns: RepetitionRule, ActionableEntity, Task, Project, Tag, Folder, DatabaseSnapshot |
| conftest.py factories | model_validate() | bridge-format dicts | WIRED | All factory dicts validate successfully; 177 tests prove it |
| _factory.py seed data | DatabaseSnapshot.model_validate() | InMemoryBridge response | WIRED | Seed data round-trips through model validation; service and server tests prove it |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| B1-B14 | 08.2-01 | Bridge JS enum resolvers, field additions, output shape updates | SATISFIED | All bridge resolver functions exist, all entity mappings updated, Vitest green |
| P1-P17 | 08.2-02 | Python per-entity enums, model hierarchy, field types, namespace | SATISFIED | All models updated, EntityStatus deleted, all new types in namespace, ruff+mypy clean |
| T1-T11 | 08.2-03 | Test infrastructure: factories, test_models, seed data | SATISFIED | All factories updated, all tests pass (177 pytest + 26 Vitest), seed data validates |

**Note:** B1-B14, P1-P17, T1-T11 are phase-internal requirement IDs for tracking BRIDGE-SPEC alignment work. They are referenced in ROADMAP.md and plan frontmatter but are not formal IDs in the main REQUIREMENTS.md (which uses MODL-*, BRDG-*, etc. prefixes for v1 requirements). No orphaned requirements from REQUIREMENTS.md are mapped to this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | - |

No TODO, FIXME, PLACEHOLDER, HACK, or XXX markers found in any modified files. No empty implementations. No stub patterns detected. No EntityStatus or assignedContainer remnants anywhere in the codebase.

### Human Verification Required

### 1. Bridge output matches live OmniFocus data

**Test:** Run UAT scripts in `uat/` against a live OmniFocus database with RealBridge
**Expected:** Bridge output parses successfully through DatabaseSnapshot.model_validate() with real data; all enum values resolve; no "Unknown *Status" errors
**Why human:** SAFE-01/SAFE-02 -- RealBridge interaction is manual UAT only. Cannot be automated. The bridge changes (new resolvers, field access paths like p.task.*) need validation against actual OmniFocus runtime behavior.

### Gaps Summary

No gaps found. All 37 must-have truths verified across all 3 plans. The phase goal of full BRIDGE-SPEC alignment is achieved:

1. **Bridge JS (Plan 01):** All 8 enum resolvers implemented with fail-fast throws, all entity mappings updated (url, TagRef, project p.task.* fields, no assignedContainer, perspective id fix), 26 Vitest tests green.

2. **Python models (Plan 02):** EntityStatus replaced by 3 per-entity enums, ScheduleType + AnchorDateKey added, RepetitionRule redesigned to 4 required fields, TagRef model added, OmniFocusEntity holds shared lifecycle fields, all entity models updated, Perspective.builtin is computed, namespace rebuilt. ruff and mypy clean.

3. **Test infrastructure (Plan 03):** All factory functions produce correct bridge-format dicts, all model tests updated with new enums/field counts/TagRef format, InMemoryBridge and simulator seed data updated, 177 pytest tests passing at 98.43% coverage.

---

_Verified: 2026-03-06T02:17:00Z_
_Verifier: Claude (gsd-verifier)_
