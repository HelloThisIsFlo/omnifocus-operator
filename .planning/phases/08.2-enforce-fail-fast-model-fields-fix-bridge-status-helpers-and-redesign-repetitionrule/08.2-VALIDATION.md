---
phase: 8.2
slug: enforce-fail-fast-model-fields-fix-bridge-status-helpers-and-redesign-repetitionrule
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-06
---

# Phase 8.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (Python)** | pytest 9+ with pytest-asyncio |
| **Framework (JS)** | Vitest (bridge/vitest.config.js) |
| **Config file (Python)** | pyproject.toml `[tool.pytest.ini_options]` |
| **Config file (JS)** | bridge/vitest.config.js |
| **Quick run command (Python)** | `uv run pytest tests/test_models.py -x` |
| **Quick run command (JS)** | `cd bridge && npx vitest run` |
| **Full suite command** | `uv run pytest && cd bridge && npx vitest run` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_models.py -x` (Python) or `cd bridge && npx vitest run` (JS)
- **After every plan wave:** Run `uv run pytest && cd bridge && npx vitest run`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| B1-B5 | 01 | 1 | Bridge enum resolvers | unit (JS) | `cd bridge && npx vitest run` | Yes (needs update) | ⬜ pending |
| B6-B13 | 01 | 1 | Bridge output shape | unit (JS) | `cd bridge && npx vitest run` | Yes (needs update) | ⬜ pending |
| P1-P2 | 02 | 1 | Per-entity status enums | unit (Python) | `uv run pytest tests/test_models.py::TestProjectStatus -x` | No (Wave 0) | ⬜ pending |
| P3,P16,P17 | 02 | 1 | RepetitionRule + typed enums | unit (Python) | `uv run pytest tests/test_models.py::TestRepetitionRule -x` | Yes (needs update) | ⬜ pending |
| P4-P8 | 02 | 1 | Required fields reject None | unit (Python) | `uv run pytest tests/test_models.py -x` | Partially | ⬜ pending |
| P9-P13 | 02 | 1 | Entity models correct fields | unit (Python) | `uv run pytest tests/test_models.py -x` | Yes (needs update) | ⬜ pending |
| P14 | 02 | 1 | Perspective.builtin computed | unit (Python) | `uv run pytest tests/test_models.py::TestPerspectiveModel -x` | Yes (needs update) | ⬜ pending |
| P15 | 02 | 1 | TagRef model | unit (Python) | `uv run pytest tests/test_models.py -x` | No (Wave 0) | ⬜ pending |
| T11 | 03 | 2 | Seed data + integration | integration | `uv run pytest tests/test_service.py -x` | Yes (needs update) | ⬜ pending |
| Full | 03 | 2 | Pipeline round-trip | integration | `uv run pytest tests/ -x` | Yes (needs update) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New test classes for `ProjectStatus`, `TagStatus`, `FolderStatus` enums
- [ ] New test class for `ScheduleType`, `AnchorDateKey` enums
- [ ] New test for `TagRef` model
- [ ] Updated Vitest mocks for `Project.Status`, `Tag.Status`, `Folder.Status`, `Task.RepetitionScheduleType`, `Task.AnchorDateKey`
- [ ] Tests for `ps()`, `gs()`, `fs()`, `rst()`, `adk()` resolvers (known + unknown values)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Bridge output matches live OmniFocus | SAFE-01/02 | RealBridge = manual UAT only | Run UAT scripts in `uat/` against live OmniFocus |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
