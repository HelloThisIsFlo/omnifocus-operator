# Phase 35.2: Uniform Name-vs-ID Resolution — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-30
**Phase:** 35.2-uniform-name-vs-id-resolution-at-service-boundary-for-all-list-filters
**Areas discussed:** Scope vs Phase 37, Did-you-mean inclusion, RepoQuery field design, Query builder changes, Resolution code location, Simple pass-throughs

---

## Scope vs Phase 37

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal pipelines | 35.2 builds _ListTasksPipeline, _ListProjectsPipeline with resolution + bare delegation. Lifts NotImplementedError stubs. Phase 37 adds validation, defaults, shorthands on top. | ✓ |
| Full pipelines | 35.2 builds everything including validation/defaults. Phase 37 shrinks to nearly nothing. | |
| Resolution functions only | 35.2 only adds resolve_* functions and RepoQuery changes. Service stubs stay NotImplementedError. Phase 37 builds pipelines. | |

**User's choice:** Minimal pipelines
**Notes:** Mirrors how write pipelines evolved — pipeline class exists after 35.2, Phase 37 enriches it. Discipline rule: 35.2 does resolution + delegation only.

---

## Did-you-mean Inclusion

| Option | Description | Selected |
|--------|-------------|----------|
| Steps 1-2 + skip on no match | ID match → substring match → no match = skip filter. Phase 37 adds fuzzy + warnings. | |
| Include did-you-mean now | Pull ListResult.warnings forward, include fuzzy matching in 35.2. Full cascade ships complete. | ✓ |
| Steps 1-2, error on no match | Raise ValueError on resolution failure, like write-side TAG_NOT_FOUND. | |

**User's choice:** Include did-you-mean now
**Notes:** User asked "why not include it?" — the downside (adding warnings field to ListResult) is minimal. The cascade is one logical unit. INFRA-07 pulled forward from Phase 37. User initially concerned about a "dead" warnings field, but since the cascade uses it immediately, it's load-bearing on ship.

---

## RepoQuery Field Design

| Option | Description | Selected |
|--------|-------------|----------|
| Clean replacement | project→project_ids: list[str], folder→folder_ids: list[str], tags→tag_ids: list[str]. Old name fields removed. | ✓ |
| Dual fields (transitional) | Add ID fields alongside existing name fields. Incremental but muddies the contract. | |

**User's choice:** Clean replacement
**Notes:** Mirrors write-side precedent (AddTaskCommand.tags → AddTaskRepoPayload.tag_ids). Consistent with Structure Over Discipline.

---

## Query Builder Changes

| Option | Description | Selected |
|--------|-------------|----------|
| Full rewrite | LIKE→WHERE IN for project/folder. Existing tests rewritten. Substring matching moves to service tests. | ✓ |
| Minimal: keep LIKE, add IN path | Support both LIKE and IN in query builder. More complex but existing tests keep working initially. | |

**User's choice:** Full rewrite
**Notes:** Significant simplification for project filter (eliminates correlated subquery with two JOINs). All three entity filters become uniform WHERE IN.

---

## Resolution Code Location

| Option | Description | Selected |
|--------|-------------|----------|
| Extend Resolver class | Add resolve_filter methods to existing Resolver. Keeps all resolution in one place. | ✓ (lookup) |
| DomainLogic for fuzzy | Fuzzy matching / did-you-mean logic lives in DomainLogic alongside other behavioral policies. | ✓ (policy) |
| New ListFilterResolver | Separate class in resolve.py. Clean separation but over-splitting. | |
| Inline in pipeline | Pipeline calls difflib directly. Duplicates across pipelines. | |

**User's choice:** Split: Resolver handles lookup (ID match + substring), DomainLogic handles policy (fuzzy matching + warning construction)
**Notes:** User's intuition was "resolver tells you whether something is an ID or a name, fuzzy matching logic happens in the domain." This matches the existing write-side pattern: Resolver = "what is this?", DomainLogic = "what do we tell the agent?"

---

## Simple Pass-throughs

| Option | Description | Selected |
|--------|-------------|----------|
| Inline one-liners | Tags, folders, perspectives are 3-4 line pass-throughs in service. No pipeline classes. | ✓ |
| Minimal pipeline classes | Create _ListTagsPipeline etc. for consistency. More ceremony. | |

**User's choice:** Inline one-liners
**Notes:** Per Phase 37 SC#3: "without unnecessary pipeline overhead." No entity-reference filters to resolve for these types.

---

## Claude's Discretion

- Exact method signatures on Resolver (single generic method vs per-entity methods)
- Internal pipeline step organization
- Test organization for resolution cascade
- Warning message phrasing

## Deferred Ideas

None — discussion stayed within phase scope.
