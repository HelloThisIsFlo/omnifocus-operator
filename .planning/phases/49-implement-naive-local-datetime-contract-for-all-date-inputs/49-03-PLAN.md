---
phase: 49-implement-naive-local-datetime-contract-for-all-date-inputs
plan: 03
type: execute
wave: 3
depends_on: ["49-01", "49-02"]
files_modified:
  - tests/test_contracts_field_constraints.py
  - tests/test_date_filter_contracts.py
  - tests/test_date_filter_constants.py
  - tests/test_service.py
  - tests/test_service_payload.py
  - tests/test_service_domain.py
  - tests/test_resolve_dates.py
  - tests/test_output_schema.py
  - docs/architecture.md
autonomous: true
requirements:
  - LOCAL-08

must_haves:
  truths:
    - "Full test suite passes with all date inputs as strings"
    - "architecture.md documents naive-local principle with rationale"
    - "No test references AwareDatetime for command/filter date construction"
    - "Contract tests verify naive, aware, and date-only strings are all accepted"
    - "Schema test confirms no format: date-time in write tool schemas"
  artifacts:
    - path: "docs/architecture.md"
      provides: "Naive-local principle documentation section"
      contains: "naive local"
    - path: "tests/test_contracts_field_constraints.py"
      provides: "Updated date field constraint tests for str type"
    - path: "tests/test_resolve_dates.py"
      provides: "Updated resolver tests using str inputs"
  key_links:
    - from: "tests/test_service.py"
      to: "service/service.py"
      via: "integration tests using str date inputs"
      pattern: "due_date.*=.*\"2026"
    - from: "tests/test_output_schema.py"
      to: "contracts/use_cases/add/tasks.py"
      via: "schema validation ensuring no format: date-time"
      pattern: "date-time"
---

<objective>
Migrate all test fixtures from AwareDatetime objects to str date inputs, add architecture.md documentation, and verify the full test suite passes — completing the naive-local datetime contract implementation.

Purpose: Plans 01 and 02 changed the contract types and service implementation. This plan updates the test suite to use the new str-typed inputs, verifies nothing is broken, and documents the architectural decision for future maintainers.

Output: All tests green with str-typed dates, architecture.md documenting naive-local principle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-implement-naive-local-datetime-contract-for-all-date-inputs/49-CONTEXT.md
@.planning/phases/49-implement-naive-local-datetime-contract-for-all-date-inputs/49-RESEARCH.md
@.planning/phases/49-implement-naive-local-datetime-contract-for-all-date-inputs/49-01-SUMMARY.md
@.planning/phases/49-implement-naive-local-datetime-contract-for-all-date-inputs/49-02-SUMMARY.md

@docs/architecture.md

<interfaces>
<!-- After Plans 01+02, the new contract is: -->
<!-- Write side: str | None (add) or PatchOrClear[str] (edit) -->
<!-- Read side: _DateBound = Literal["now"] | str -->
<!-- Service: local_now() for all 'now' timestamps -->
<!-- Domain: normalize_date_input(value: str) -> str -->
<!-- Payload: passes str through (no .isoformat()) -->
<!-- Resolver: _parse_absolute_after/before accept str -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test fixtures from AwareDatetime to str + fix failing tests</name>
  <read_first>
    tests/test_contracts_field_constraints.py
    tests/test_date_filter_contracts.py
    tests/test_date_filter_constants.py
    tests/test_service.py
    tests/test_service_payload.py
    tests/test_service_domain.py
    tests/test_resolve_dates.py
    tests/test_output_schema.py
  </read_first>
  <files>
    tests/test_contracts_field_constraints.py
    tests/test_date_filter_contracts.py
    tests/test_date_filter_constants.py
    tests/test_service.py
    tests/test_service_payload.py
    tests/test_service_domain.py
    tests/test_resolve_dates.py
    tests/test_output_schema.py
  </files>
  <action>
Run `uv run pytest tests/ -x -q` first to identify all failing tests. Then systematically fix each category:

**Category 1: Contract tests (test_contracts_field_constraints.py)**
- Tests that construct `AddTaskCommand(due_date=datetime(2026, ..., tzinfo=UTC))` must change to `AddTaskCommand(due_date="2026-07-15T17:00:00Z")` or `AddTaskCommand(due_date="2026-07-15T17:00:00")` (naive preferred)
- Tests that assert `AwareDatetime` validation behavior (rejecting naive) should be REMOVED or INVERTED — naive is now accepted
- Add new tests: naive string accepted, aware string accepted, date-only string accepted, invalid string rejected

**Category 2: Date filter contracts (test_date_filter_contracts.py)**
- Tests for `_reject_naive_datetime` must be REMOVED (dead code removed in Plan 01)
- Tests constructing `AbsoluteRangeFilter(after=datetime(..., tzinfo=UTC))` must change to `AbsoluteRangeFilter(after="2026-07-15T17:00:00Z")` or `AbsoluteRangeFilter(after="2026-07-15T17:00:00")` (naive now preferred)
- Tests for bounds ordering with `date(2026, 7, 15)` objects must change to `"2026-07-15"` strings
- The constant `DATE_FILTER_NAIVE_DATETIME` reference in test_date_filter_constants.py must be removed

**Category 3: Service tests (test_service.py)**
- Integration tests that pass `due_date=datetime(2026, ..., tzinfo=UTC)` to commands via MCP tool calls must change to `due_date="2026-07-15T17:00:00"` (or `"...Z"`)
- Tests that mock `datetime.now(UTC)` may need to mock `local_now()` instead. Search for patches like `@patch('...datetime')` or `@patch.object(...)` that inject UTC timestamps
- The test fixture for `now` should be tz-aware local (e.g., `datetime(2026, 4, 7, 14, 0, 0, tzinfo=timezone(timedelta(hours=1)))`)

**Category 4: Payload tests (test_service_payload.py)**
- Tests that assert `.isoformat()` is called on date fields must be updated
- Tests that pass `datetime(2026, ..., tzinfo=UTC)` to command objects before payload build must pass `"2026-07-15T17:00:00"` strings instead
- Assertions on payload output like `assert payload.due_date == "2026-07-15T17:00:00+00:00"` should become `assert payload.due_date == "2026-07-15T17:00:00"`

**Category 5: Domain tests (test_service_domain.py)**
- The `_to_utc_ts` tests should verify naive strings work (add test)
- No-op detection tests that compare `"2026-07-15T17:00:00+00:00"` should compare naive-local strings

**Category 6: Resolve dates tests (test_resolve_dates.py)**
- Tests that pass `datetime(2026, 7, 15, tzinfo=UTC)` as filter bound values must pass `"2026-07-15T17:00:00Z"` or `"2026-07-15"` strings
- The NOW fixture should be tz-aware LOCAL (not UTC): `datetime(2026, 4, 7, 14, 0, 0, tzinfo=timezone(timedelta(hours=1)))` — but keep existing UTC fixtures if tests are asserting UTC-relative behavior (they should still pass since the math is tz-aware)
- Actually, keep NOW as UTC for test stability (tests already work with UTC now and the subtraction math is tz-aware). The important change is that _input values_ are strings.

**Category 7: Output schema test (test_output_schema.py)**
- If there's a test asserting `format: "date-time"` appears in write schemas, REMOVE or INVERT it
- Add assertion: `"date-time" not in json.dumps(AddTaskCommand.model_json_schema())`

**Process:**
1. Run `uv run pytest tests/ -x -q` to find first failure
2. Fix it
3. Repeat until all pass
4. Run `uv run pytest tests/ -q` for full suite
5. Ensure 0 failures

**Key gotcha**: Some tests may import `AwareDatetime` from pydantic to construct test data. Replace those imports with plain string construction. The test should never need `AwareDatetime` for constructing inputs (only for asserting output types, which haven't changed).

**Do NOT change**: Tests for read-side OUTPUT models (Task, Project models that output `AwareDatetime` in their date fields) — those haven't changed. Only INPUT contracts changed.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` exits 0 (all tests pass)
    - No test file imports `AwareDatetime` for constructing AddTaskCommand, EditTaskCommand, or AbsoluteRangeFilter inputs
    - No test references `_reject_naive_datetime` or `DATE_FILTER_NAIVE_DATETIME`
    - test_output_schema.py verifies no `format: "date-time"` in write tool schemas
    - test_contracts_field_constraints.py has tests for: naive string accepted, aware string accepted, date-only accepted, invalid string rejected
  </acceptance_criteria>
  <done>Full test suite passes with str-typed date inputs. No AwareDatetime used for input construction in tests. New tests verify the naive-preferred contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document naive-local principle in architecture.md</name>
  <read_first>
    docs/architecture.md
  </read_first>
  <files>
    docs/architecture.md
  </files>
  <action>
Add a new section to `docs/architecture.md`. Place it alongside the existing design philosophy sections (look for sections like "Product Decisions vs Plumbing" or "Write Pipeline"). Title: "Naive-Local DateTime Principle".

Content:

```markdown
## Naive-Local DateTime Principle

**All date inputs are local time. The API matches how OmniFocus thinks.**

### Why

OmniFocus stores every date as naive local time -- "5pm" means 5pm in whatever timezone you're in when you set it. The server runs on the same Mac as OmniFocus. Using local time in the API means:

- Agents never reason about timezone -- they pass the time as the user expressed it
- "Due at 5pm" becomes `"dueDate": "2026-07-15T17:00:00"` -- no offset, no conversion
- `format: "date-time"` (RFC 3339) requires timezone by spec, so we use `str` to avoid contradicting the contract
- Period boundaries ("today", "this week") align with the user's local calendar

### Evidence

The timezone deep-dive tested 430 tasks across BST and GMT, proving:
- OmniFocus uses floating naive-local storage (Core Foundation epoch + implicit local tz)
- The conversion formula `(cf_epoch_seconds + CF_EPOCH_OFFSET)` produces correct local timestamps
- Every input format (naive, UTC, offset, date-only) has known bridge behavior

See `.research/deep-dives/timezone-behavior/RESULTS.md` for full empirical data.

### Contract

| Input Format | Write Side | Read Side (filter bound) |
|--------------|-----------|--------------------------|
| Naive (`"17:00:00"`) | Preferred. Passed to bridge as-is. | Treated as local time. |
| Aware (`"17:00:00Z"`, `"+01:00"`) | Accepted. Converted to local, stripped. | Accepted. Used with offset for comparison. |
| Date-only (`"2026-07-15"`) | Midnight local appended (`T00:00:00`). | Start-of-day (after) or start-of-next-day (before). |

### Architecture

- **Contract layer**: `str` type + syntax validator (is it parseable ISO?). No transformation.
- **Domain layer**: `normalize_date_input()` -- product decision. Converts aware->local, appends midnight to date-only.
- **Payload builder**: Passes normalized string through to bridge.
- **Resolver**: `local_now()` provides local-anchored "now". Period boundaries align with local calendar.
- **`local_now()`**: `datetime.now().astimezone()` -- tz-aware local. Lives in `config.py`. CF epoch math works unchanged (tz-aware subtraction handles offset).

### Why not `NaiveDatetime` or `datetime`?

Pydantic's `NaiveDatetime`, `AwareDatetime`, and plain `datetime` all produce identical JSON Schema: `{"type": "string", "format": "date-time"}`. That format references RFC 3339 section 5.6 which **requires** a timezone offset. Only `str` drops the format constraint entirely.
```

Ensure the section integrates naturally with the existing document structure. If architecture.md has a table of contents or similar navigation, add an entry.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && grep -c "Naive-Local DateTime Principle" docs/architecture.md && grep -c "naive local" docs/architecture.md && grep -c "local_now" docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture.md` contains section "Naive-Local DateTime Principle"
    - Section explains: OmniFocus stores naive local, server co-located, format: date-time requires TZ
    - Section includes the input format table (naive/aware/date-only behavior)
    - Section mentions `local_now()` and `normalize_date_input()` by name
    - Section references the timezone deep-dive evidence
  </acceptance_criteria>
  <done>architecture.md documents the naive-local principle with rationale, evidence reference, contract table, and architecture breakdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is test migration and documentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06 | Information Disclosure | architecture.md | accept | Public repo documentation; no secrets exposed |
</threat_model>

<verification>
- `uv run pytest tests/ -q` exits 0 (full suite green)
- `grep "Naive-Local DateTime Principle" docs/architecture.md` returns a match
- `grep -r "AwareDatetime" tests/ | grep -i "AddTaskCommand\|EditTaskCommand\|AbsoluteRangeFilter"` returns no matches (no AwareDatetime used for input construction)
- `grep -r "_reject_naive_datetime\|DATE_FILTER_NAIVE_DATETIME" tests/` returns no matches
</verification>

<success_criteria>
- Full test suite passes (0 failures)
- architecture.md has naive-local principle section with rationale and evidence
- No dead references to removed code in test suite
- Test coverage for the new contract: naive accepted, aware accepted, date-only accepted, invalid rejected
</success_criteria>

<output>
After completion, create `.planning/phases/49-implement-naive-local-datetime-contract-for-all-date-inputs/49-03-SUMMARY.md`
</output>
