---
phase: 27-repository-contract-tests-for-behavioral-equivalence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/doubles/bridge.py
  - tests/test_stateful_bridge.py
  - tests/golden/__init__.py
  - tests/golden/normalize.py
  - tests/golden/README.md
autonomous: true
requirements: [INFRA-13, INFRA-14]

must_haves:
  truths:
    - "InMemoryBridge resolves parent to {type, id, name} dict when add_task has a parent param"
    - "InMemoryBridge resolves tag names from internal _tags list when edit_task adds tags"
    - "InMemoryBridge resolves tags from internal _tags list when add_task has tagIds param"
    - "normalize_for_comparison strips dynamic fields from task/project/tag dicts"
    - "filter_to_known_ids returns only entities matching provided ID sets"
  artifacts:
    - path: "tests/golden/__init__.py"
      provides: "Package init exporting normalize/filter helpers"
    - path: "tests/golden/normalize.py"
      provides: "normalize_for_comparison, normalize_response, filter_to_known_ids helpers"
      exports: ["normalize_for_comparison", "normalize_response", "filter_to_known_ids", "DYNAMIC_TASK_FIELDS", "DYNAMIC_PROJECT_FIELDS", "DYNAMIC_TAG_FIELDS"]
    - path: "tests/golden/README.md"
      provides: "Documentation for golden master directory"
  key_links:
    - from: "tests/golden/normalize.py"
      to: "tests/test_bridge_contract.py"
      via: "import in contract tests (Plan 02)"
      pattern: "from tests.golden.normalize import"
    - from: "tests/doubles/bridge.py"
      to: "tests/golden/"
      via: "InMemoryBridge behavioral parity enables golden master comparison"
      pattern: "_handle_add_task.*parent.*type.*id.*name"
---

<objective>
Fix InMemoryBridge behavioral gaps and create golden master normalization infrastructure.

Purpose: InMemoryBridge must behave identically to RealBridge for the golden master comparison to work. Three gaps exist: (1) add_task ignores parent resolution, (2) add_task ignores tagIds, (3) edit_task uses tag ID as tag name. Additionally, the normalization/filtering helpers are shared between the capture script and contract tests.

Output: Patched InMemoryBridge with parent+tag resolution, tests/golden/ package with normalize.py helpers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-repository-contract-tests-for-behavioral-equivalence/27-CONTEXT.md
@.planning/phases/27-repository-contract-tests-for-behavioral-equivalence/27-RESEARCH.md

@tests/doubles/bridge.py
@tests/test_stateful_bridge.py
@tests/conftest.py
@src/omnifocus_operator/bridge/adapter.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tests/doubles/bridge.py (InMemoryBridge._handle_add_task, lines 113-142):
- params dict contains camelCase keys: name, parent (str ID or None), tagIds (list[str] or absent), flagged, dueDate, deferDate, plannedDate, estimatedMinutes, note
- Currently sets parent=None always and tags=[] always -- GAPS to fix
- Returns {"id": task_id, "name": params["name"]}

From tests/doubles/bridge.py (InMemoryBridge._handle_edit_task, line 183):
- When adding tags: task["tags"].append({"id": tid, "name": tid}) -- uses tid as name, GAP to fix
- Should look up tag name from self._tags list

From src/omnifocus_operator/bridge/adapter.py:
- DYNAMIC fields to strip per D-15: id, url, added, modified, effectiveDueDate, effectiveDeferDate, effectiveCompletionDate, effectivePlannedDate, effectiveDropDate, repetitionRule
- For projects: also completionDate is dynamic when computed
- For tags: id, url, added, modified
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix InMemoryBridge parent resolution, tagIds handling, and tag name resolution</name>
  <files>tests/doubles/bridge.py, tests/test_stateful_bridge.py</files>
  <read_first>
    - tests/doubles/bridge.py (current InMemoryBridge implementation -- see _handle_add_task lines 113-142, _handle_edit_task lines 144-219)
    - tests/test_stateful_bridge.py (existing tests -- see TestAddTask class line 140, TestEditTask class line 242)
    - src/omnifocus_operator/contracts/use_cases/add_task.py (AddTaskRepoPayload -- tag_ids field, line 46)
  </read_first>
  <behavior>
    - Test: add_task with parent="proj-001" (project exists in seed data) sets task["parent"] to {"type": "project", "id": "proj-001", "name": "Test Project"}
    - Test: add_task with parent="task-001" (task exists in seed data, no matching project) sets task["parent"] to {"type": "task", "id": "task-001", "name": "Test Task"}
    - Test: add_task with parent="unknown-id" (no matching project or task) sets task["parent"] to {"type": "task", "id": "unknown-id", "name": "unknown-id"} (fallback)
    - Test: add_task with tagIds=["tag-001"] (tag exists in seed data) sets task["tags"] to [{"id": "tag-001", "name": "Test Tag"}]
    - Test: add_task with tagIds=["unknown-tag"] (tag not in seed data) sets task["tags"] to [{"id": "unknown-tag", "name": "unknown-tag"}] (fallback)
    - Test: edit_task addTagIds=["tag-001"] (tag exists in seed data) appends {"id": "tag-001", "name": "Test Tag"} (not {"id": "tag-001", "name": "tag-001"})
    - Test: edit_task addTagIds=["unknown-tag"] (tag not in seed data) appends {"id": "unknown-tag", "name": "unknown-tag"} (fallback, same as before)
  </behavior>
  <action>
    **In `tests/doubles/bridge.py`:**

    1. Add a private helper method `_resolve_tag_name(self, tag_id: str) -> str` that looks up the tag name from `self._tags`:
       ```python
       def _resolve_tag_name(self, tag_id: str) -> str:
           tag = next((t for t in self._tags if t["id"] == tag_id), None)
           return tag["name"] if tag is not None else tag_id
       ```

    2. In `_handle_add_task` (around line 133), replace `parent=None` with parent resolution logic:
       - If `params.get("parent")` is not None, resolve it the same way `_handle_edit_task` does for moveTo:
         - Check `self._projects` for matching ID -> `{"type": "project", "id": ..., "name": ...}`
         - Else check `self._tasks` for matching ID -> `{"type": "task", "id": ..., "name": ...}`
         - Else fallback -> `{"type": "task", "id": container_id, "name": container_id}`
       - If `params.get("parent")` is None, keep `parent=None`

    3. In `_handle_add_task`, replace `tags=[]` with tagIds resolution:
       - Read `tag_ids = params.get("tagIds", [])` (camelCase, as sent by BridgeWriteMixin with by_alias=True)
       - Build tags list: `[{"id": tid, "name": self._resolve_tag_name(tid)} for tid in tag_ids]`

    4. In `_handle_edit_task` (line 183), replace `task["tags"].append({"id": tid, "name": tid})` with:
       `task["tags"].append({"id": tid, "name": self._resolve_tag_name(tid)})`

    **In `tests/test_stateful_bridge.py`:**

    Add new tests in TestAddTask class:
    - `test_add_task_with_parent_resolves_project` -- add_task with parent="proj-001" where proj-001 exists in seed projects
    - `test_add_task_with_parent_resolves_task` -- add_task with parent="task-001" where task-001 exists in seed tasks but not projects
    - `test_add_task_with_parent_unknown_falls_back` -- add_task with parent="unknown" resolves to type=task, name=unknown
    - `test_add_task_with_tag_ids_resolves_names` -- add_task with tagIds=["tag-001"] resolves tag name from seed
    - `test_add_task_with_tag_ids_unknown_falls_back` -- add_task with tagIds=["no-such-tag"] uses ID as name

    Add new test in TestEditTask class:
    - `test_edit_task_add_tags_resolves_name_from_internal_tags` -- edit_task addTagIds=["tag-001"] resolves name from seed
    - Update existing `test_edit_task_add_tags` if it asserts `name == tid` (check first -- it may not assert on name)
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_stateful_bridge.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/doubles/bridge.py contains `def _resolve_tag_name(self, tag_id: str) -> str:`
    - tests/doubles/bridge.py _handle_add_task resolves parent to dict when params["parent"] is set (grep: `"type": "project"` or `"type": "task"` in _handle_add_task)
    - tests/doubles/bridge.py _handle_add_task builds tags list from tagIds param (grep: `params.get("tagIds"` in _handle_add_task)
    - tests/doubles/bridge.py _handle_edit_task uses `self._resolve_tag_name(tid)` instead of bare `tid` for tag name
    - tests/test_stateful_bridge.py contains `test_add_task_with_parent_resolves_project`
    - tests/test_stateful_bridge.py contains `test_add_task_with_tag_ids_resolves_names`
    - tests/test_stateful_bridge.py contains `test_edit_task_add_tags_resolves_name`
    - `uv run pytest tests/test_stateful_bridge.py -x` exits 0
  </acceptance_criteria>
  <done>InMemoryBridge resolves parent to {type, id, name} dict in add_task, resolves tagIds to [{id, name}] in add_task, and resolves tag names from _tags in edit_task. All new and existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/golden/ package with normalization and filtering helpers</name>
  <files>tests/golden/__init__.py, tests/golden/normalize.py, tests/golden/README.md</files>
  <read_first>
    - src/omnifocus_operator/bridge/adapter.py (field names and dead field lists -- lines 67-84 for reference on what fields exist)
    - .planning/phases/27-repository-contract-tests-for-behavioral-equivalence/27-RESEARCH.md (D-15 dynamic fields list, normalization helper shape)
    - tests/conftest.py (make_task_dict for full field list -- lines 17-59)
  </read_first>
  <action>
    **Create `tests/golden/__init__.py`:**
    ```python
    """Golden master fixtures and comparison helpers for bridge contract tests."""

    from tests.golden.normalize import (
        DYNAMIC_PROJECT_FIELDS,
        DYNAMIC_TAG_FIELDS,
        DYNAMIC_TASK_FIELDS,
        filter_to_known_ids,
        normalize_for_comparison,
        normalize_response,
        normalize_state,
    )

    __all__ = [
        "DYNAMIC_TASK_FIELDS",
        "DYNAMIC_PROJECT_FIELDS",
        "DYNAMIC_TAG_FIELDS",
        "filter_to_known_ids",
        "normalize_for_comparison",
        "normalize_response",
        "normalize_state",
    ]
    ```

    **Create `tests/golden/normalize.py`:**

    Define these constants (per D-15, D-18):
    ```python
    DYNAMIC_TASK_FIELDS = {
        "id", "url", "added", "modified",
        "effectiveDueDate", "effectiveDeferDate",
        "effectiveCompletionDate", "effectivePlannedDate",
        "effectiveDropDate", "repetitionRule",
    }

    DYNAMIC_PROJECT_FIELDS = {
        "id", "url", "added", "modified",
        "completionDate",
        "repetitionRule",
    }

    DYNAMIC_TAG_FIELDS = {
        "id", "url", "added", "modified",
    }
    ```

    Define these functions:

    `normalize_for_comparison(entity: dict, entity_type: str) -> dict` -- strips dynamic fields based on entity_type ("task", "project", "tag"). Returns new dict without dynamic fields. Uses the constants above to determine which fields to strip.

    `normalize_response(response: dict) -> dict` -- strips `id` from a write response dict (add_task/edit_task returns `{id, name}`, we only compare `name`). Returns new dict.

    `normalize_state(state: dict) -> dict` -- normalizes an entire state snapshot. For each entity list ("tasks", "projects", "tags"), applies `normalize_for_comparison` to each entity. Sorts each list by `name` for deterministic comparison. Returns new dict.

    `filter_to_known_ids(raw: dict, known_task_ids: set[str], known_project_ids: set[str], known_tag_ids: set[str]) -> dict` -- filters a full get_all response to only entities with IDs in the known sets. Returns new dict with filtered lists. Per D-09/D-17.

    **Create `tests/golden/README.md`:**
    Brief documentation:
    - What this directory contains (golden master fixture files + helpers)
    - How to regenerate: `uv run python uat/capture_golden_master.py`
    - Files: `initial_state.json` (seeded state), `scenario_NN_*.json` (captured scenarios)
    - Helpers: `normalize.py` (normalization/filtering for comparison)
    - Per GOLD-01: regenerate when bridge operations change
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run python -c "from tests.golden import normalize_for_comparison, normalize_response, normalize_state, filter_to_known_ids, DYNAMIC_TASK_FIELDS; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/golden/__init__.py exists and exports normalize_for_comparison, normalize_response, normalize_state, filter_to_known_ids
    - tests/golden/normalize.py contains `DYNAMIC_TASK_FIELDS = {` with "id", "url", "added", "modified", "effectiveDueDate", "effectiveDeferDate", "effectiveCompletionDate", "effectivePlannedDate", "effectiveDropDate", "repetitionRule"
    - tests/golden/normalize.py contains `DYNAMIC_PROJECT_FIELDS = {` with "id", "url", "added", "modified"
    - tests/golden/normalize.py contains `DYNAMIC_TAG_FIELDS = {` with "id", "url", "added", "modified"
    - tests/golden/normalize.py contains `def normalize_for_comparison(`
    - tests/golden/normalize.py contains `def normalize_response(`
    - tests/golden/normalize.py contains `def normalize_state(`
    - tests/golden/normalize.py contains `def filter_to_known_ids(`
    - tests/golden/README.md contains "capture_golden_master.py"
    - `uv run python -c "from tests.golden import normalize_for_comparison"` exits 0
    - `uv run pytest` full suite passes (no regressions)
  </acceptance_criteria>
  <done>tests/golden/ package exists with normalization helpers that both the capture script and contract tests can import. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_stateful_bridge.py -x -v` -- all InMemoryBridge tests pass including new gap-fix tests
- `uv run python -c "from tests.golden import normalize_for_comparison, filter_to_known_ids"` -- helpers importable
- `uv run pytest` -- full suite green, no regressions
</verification>

<success_criteria>
1. InMemoryBridge.add_task resolves parent param to {type, id, name} dict (not None)
2. InMemoryBridge.add_task resolves tagIds param to [{id, name}] list (not empty)
3. InMemoryBridge.edit_task resolves tag names from internal _tags list (not using ID as name)
4. tests/golden/normalize.py provides normalize_for_comparison, normalize_response, normalize_state, filter_to_known_ids
5. All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/27-repository-contract-tests-for-behavioral-equivalence/27-01-SUMMARY.md`
</output>
