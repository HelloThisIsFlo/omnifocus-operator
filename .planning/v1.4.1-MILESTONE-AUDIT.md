---
milestone: v1.4.1
milestone_name: Task Property Surface & Subtree Retrieval
audited: 2026-04-24T00:00:00Z
status: tech_debt
scores:
  requirements: 54/54 verified (51 fully_satisfied + 3 satisfied_evidence_only)
  phases: 2/2 verified
  integration: 6/6 findings info-level (no warnings, no critical)
  flows: 2/2 E2E flows verified
  nyquist:
    compliant_phases: 1
    partial_phases: 0
    missing_phases: 0
    expected_missing: 1  # Phase 56 (research skipped — no Wave-0 expected)
    overall: compliant
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 56-task-property-surface
    items:
      - "WR-01: Duplicate preferences warning in AddTaskResult — _normalize_dates and _resolve_type_defaults both drain preferences warnings; cosmetic only. service/service.py:584,597. Documented in 56-REVIEW.md."
      - "IN-01: HIER-05 ProjectType truth table open-coded in 3 locations (hybrid.py:474, adapter.py:253, service/domain.py:337). Coordinated drift is possible; not caught by existing tests."
      - "IN-03: _adapt_task_property_surface and _adapt_project_property_surface share 4 of 5 lines (adapter.py:228-263). Duplication acknowledged in 56-REVIEW.md."
  - phase: 57-parent-filter-filter-unification
    items:
      - "ROADMAP cosmetic: task_id_scope → candidate_task_ids rename mentioned in 57-04-SUMMARY (blocked by worktree protection). Documentation-only, no functional impact. Ambiguous scope — pending Flo decision."
  - phase: cross-phase
    items:
      - "GOLD-01 is referenced in 56-09-SUMMARY but never added to REQUIREMENTS.md traceability table — reverse orphan. Decide: promote to formal requirement or drop from summary. Ambiguous — pending Flo decision."
mechanical_hygiene_completed:
  - "REQUIREMENTS.md traceability: 53 checkboxes flipped [ ] → [x]; 54 rows flipped Pending → Satisfied (all 54 now Satisfied)"
  - "57-04-SUMMARY.md: requirements-completed backfilled with UNIFY-07, UNIFY-08, WARN-06 (gap-closure evidence was already in VERIFICATION.md)"
  - "Golden master fixtures verified committed: 8/8 JSON files present in tests/golden_master/snapshots/09-task-property-surface/ (commit 19906902)"
---

# Milestone v1.4.1 — Audit Report

**Milestone:** v1.4.1 — Task Property Surface & Subtree Retrieval
**Phases:** 56 (Task Property Surface), 57 (Parent Filter & Filter Unification)
**Audited:** 2026-04-24
**Status:** `tech_debt` — all requirements satisfied, no critical blockers, moderate accumulated tech debt needs review

## Milestone Definition of Done

Per ROADMAP.md:
- **Phase 56**: Agents can read and write the new task property surface (`completesWithChildren`, per-type `type`, presence flags, expanded `hierarchy` include group) end-to-end — cache-backed reads with no per-row bridge fallback; writes honor `Patch[bool]` / `Patch[TaskType]` semantics on tasks; omitted create-values resolve to explicit OmniFocus preference.
- **Phase 57**: Agents can fetch a task's full descendant subtree through `list_tasks` with a single call using the new `parent` filter — sharing one service-layer `get_tasks_subtree` helper and one repo-layer primitive with the existing `project` filter, with the full warning surface guiding correct usage.

Both phase goals **achieved**.

---

## Requirements Coverage — 3-Source Cross-Reference

**Total:** 54 requirements (31 Phase 56 + 23 Phase 57)

### Status Distribution (post-hygiene)

| Status | Count | % |
|--------|------:|---|
| Satisfied (all 3 sources agree) | 54 | 100% |
| Unsatisfied | 0 | 0% |
| Orphaned in traceability | 0 | 0% |
| Reverse-orphan (summary-only) | 1 (GOLD-01 — not enrolled) | — |

### Phase 56 (31 requirements) — all satisfied

| REQ | VERIFICATION | SUMMARY | Traceability | Status |
|-----|:-:|:-:|:-:|--------|
| PREFS-01..05 | passed | 56-01 ✓ | `[x]` | satisfied |
| CACHE-01..04 | passed | 56-02 ✓ | `[x]` | satisfied |
| HIER-01..05 | passed | 56-02, 56-03, 56-04 ✓ | `[x]` | satisfied |
| FLAG-01..08 | passed | 56-04, 56-05, 56-08 ✓ | `[x]` | satisfied |
| PROP-01..08 | passed | 56-04, 56-06, 56-07 ✓ | `[x]` | satisfied |
| STRIP-11 | passed | 56-04 ✓ | `[x]` | satisfied |

### Phase 57 (23 requirements) — all satisfied

| REQ | VERIFICATION | SUMMARY | Traceability | Status |
|-----|:-:|:-:|:-:|--------|
| PARENT-01..09 | passed | 57-01, 57-02, 57-04 ✓ | `[x]` | satisfied |
| UNIFY-01..06 | passed | 57-01, 57-02 ✓ | `[x]` | satisfied |
| UNIFY-07, UNIFY-08 | passed (gap-closure ev.) | 57-04 ✓ (backfilled in audit) | `[x]` | satisfied |
| WARN-01..05 | passed | 57-02, 57-03, 57-04, 57-05 ✓ | `[x]` | satisfied |
| WARN-06 | passed (gap-closure ev.) | 57-04 ✓ (backfilled in audit) | `[x]` | satisfied |

**Note:** UNIFY-07, UNIFY-08, WARN-06 were added to REQUIREMENTS.md on 2026-04-24 during UAT gap-closure *after* 57-04-SUMMARY was written. VERIFICATION.md's Gap Closure Evidence section explicitly proves them (G2 → WARN-06 at `warnings.py:204`; G2 → UNIFY-08 at `service.py:430,620,644-662`; G3 → UNIFY-07 at `warnings.py:160`). SUMMARY frontmatter was backfilled during this audit.

---

## Integration Check Results — PASS

Spawned gsd-integration-checker against the full codebase. Findings:

| # | Severity | Area | Finding |
|---|----------|------|---------|
| 1 | info | dependency | `get_tasks_subtree` cleanly reads only parent/project relationships from snapshot — minimal coupling to Phase 56's flag additions |
| 2 | info | field-surface | Phase 56 adds *response* fields only, not *filter* fields. Enrichment runs uniformly post-repo, so parent-filtered queries cannot distort flag projection |
| 3 | info | warnings | `_SUBTREE_PRUNING_FIELDS` correctly excludes Phase 56 flags (they are not filters). Drift guarded by `TestSubtreePruningFieldsDrift` |
| 4 | info | contract-parity | `candidate_task_ids` / `pinned_task_ids` consumed by both repos with cross-path equivalence tests passing |
| 5 | info | test-health | No single test combines `list_tasks(parent=..., include=["hierarchy"])` — decomposed coverage is sufficient; optional confirmatory test could lock the composition |
| 6 | info | flow | `_AddTaskPipeline._resolve_type_defaults` wires preference reads correctly; bridge never relies on OmniFocus implicit defaulting |

**Overall:** 6 info-level findings, 0 warnings, 0 critical. Architecture is composed by construction — not by integration glue. The phases interlock at three well-defined seams with no hidden coupling.

**Test suite:** 2558 passed / 2558 total (97.52% coverage).

---

## End-to-End Flows Verified

### Flow A: Write with explicit values → Read back with enriched flags
`add_tasks(name=..., completesWithChildren=true, type="sequential")` → `list_tasks(parent=<id>)` with enriched flags.
**Status: PASS** — `_AddTaskPipeline` honors explicit values via `is_set` short-circuit; `_ListTasksPipeline._delegate` applies `enrich_task_presence_flags` uniformly.

### Flow B: Write with omitted values → Preference-resolved defaults
`add_tasks(parent=<task>)` with omitted `completesWithChildren`/`type` → preference-resolved values written.
**Status: PASS** — `_resolve_type_defaults` reads `OmniFocusPreferences` when fields unset; factory defaults (`true`, `"parallel"`) applied when OF Setting store empty.

---

## Nyquist Compliance (workflow.nyquist_validation = true)

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|:-:|:-:|:-:|--------|
| 56-task-property-surface | N/A | — | — | **N/A (research phase skipped — no Wave-0 expected)** |
| 57-parent-filter-filter-unification | exists | true | true | **COMPLIANT** |

**Overall:** compliant (1 compliant, 1 not-expected). Phase 56 was confirmed by Flo to have skipped the research phase, so no Wave-0 validation baseline was created. Coverage is instead evidenced by 2424+ passing tests and closed human UAT items.

---

## Tech Debt by Phase

### Phase 56
- **WR-01** — Duplicate `SETTINGS_FALLBACK_WARNING` in `AddTaskResult.warnings` on bridge failure (`service.py:584,597`). Two pipeline steps drain preference warnings. Cosmetic. Documented in `56-REVIEW.md`.
- **IN-01** — HIER-05 `ProjectType` truth table open-coded in 3 locations (`hybrid.py:474`, `adapter.py:253`, `service/domain.py:337`). Domain method `assemble_project_type` defined but not called by either repo. Coordinated-drift risk; no current correctness issue.
- **IN-03** — `_adapt_task_property_surface` / `_adapt_project_property_surface` share 4 of 5 lines (`adapter.py:228-263`). Duplication acknowledged in `56-REVIEW.md`.

### Phase 57
- **ROADMAP cosmetic rename** — `task_id_scope → candidate_task_ids` rename blocked by worktree protection during 57-04 execution. Mentioned in 57-04-SUMMARY; zero functional impact. **Ambiguous — pending Flo decision** (see end of report).

### Cross-phase / Milestone hygiene
- **GOLD-01 reverse orphan** — claimed by 56-09-SUMMARY but never enrolled in REQUIREMENTS.md traceability table. **Ambiguous — pending Flo decision** (see end of report).

### Hygiene completed during this audit
- ✅ **REQUIREMENTS.md traceability** — 53 checkboxes flipped `[ ]` → `[x]`; all 54 rows now show `Satisfied`.
- ✅ **57-04-SUMMARY.md frontmatter** — `requirements-completed` backfilled with UNIFY-07, UNIFY-08, WARN-06.
- ✅ **Golden master fixtures verified** — 8/8 JSON files present in `tests/golden_master/snapshots/09-task-property-surface/` (commit `19906902`). Human UAT item 5 fully closed.

---

## Anti-Pattern Inventory

From Phase 56 `Anti-Patterns Found` in VERIFICATION.md (all severity `info` or `warning` — no `critical`):

| Severity | Area | Locations |
|----------|------|-----------|
| warning | Duplicate warning drain | `service/service.py:584,597` |
| info | HIER-05 truth table duplication | `hybrid.py:474`, `adapter.py:253`, `service/domain.py:337` |
| info | adapter code duplication | `adapter.py:228-263` |

Phase 57 VERIFICATION.md reports zero anti-patterns across all gap-closure files.

---

## Audit Summary

**Goal achievement:** Both phase goals achieved. All 54 requirements satisfied (51 fully, 3 via direct gap-closure evidence). Cross-phase integration is clean by construction — no wiring defects.

**Blockers to milestone close:** None.

**Tech debt before close:** Light — 3 items of code-level debt in Phase 56 (WR-01, IN-01, IN-03) + 2 ambiguous ledger items pending Flo decision. None are correctness risks. Mechanical hygiene (traceability checkboxes, summary frontmatter, golden master verification) completed during this audit.

---

## Ambiguous Items — Pending Flo Decision

### 1. ROADMAP.md `task_id_scope` → `candidate_task_ids` rename (3 mentions)

The 57-04 SUMMARY flagged this as a cosmetic update "blocked by worktree protection" but which mentions should actually be renamed is ambiguous:

| Line | Context | Rename? |
|------|---------|---------|
| 74 | Phase 57 success criterion #3 — "retiring the old `project_ids` field; renamed from `task_id_scope` in 57-04" | Likely **no** — this parenthetical records the rename lineage; removing it loses the why |
| 82 | `57-01-PLAN.md` description: "add `task_id_scope`" | Ambiguous — historical description of what 57-01 shipped (at that time it WAS `task_id_scope`); renaming here would make ROADMAP describe events that didn't happen |
| 85 | `57-04-PLAN.md` description: "rename `task_id_scope` → `candidate_task_ids`" | **No** — this is the rename itself being described |

**Recommendation:** Leave all three as-is. The ROADMAP is a historical record; renaming erases the lineage that explains why the contract exists the way it does. Code is already on `candidate_task_ids` everywhere.

### 2. GOLD-01 reverse orphan

`56-09-SUMMARY.md` claims `GOLD-01` in `requirements-completed` but `REQUIREMENTS.md` has no GOLD-01 entry. Two options:

**(a) Promote GOLD-01 to a formal requirement** — add it to the traceability table with a descriptive line like "Golden-master canonical scaffolding for Phase 56 field surface". This honors the work that was done and makes the phase-close math (54/55 or 55/55) reflect reality.

**(b) Drop GOLD-01 from the 56-09-SUMMARY frontmatter** — treat it as informal scaffolding work that doesn't deserve a REQ-ID. Keeps REQUIREMENTS.md clean but slightly devalues the work.

**Recommendation:** **(a)**. The golden master scaffolding is non-trivial work that gated human UAT; it deserves a traceable requirement. But this is a naming/formality call, not a correctness one.

### 3. Code-level debt items — status

| Item | Decision | Detail |
|------|----------|--------|
| **IN-03** — adapter code duplication | ✅ **Fixed during audit** | Extracted `_adapt_common_entity_property_surface` helper in `bridge_only/adapter.py`. 4 shared lines consolidated; type resolution stays in each entity-specific function. Full suite: 2558 passed. |
| **WR-01** — duplicate `SETTINGS_FALLBACK_WARNING` in `AddTaskResult` | ❌ **Dropped (not worth it)** | Fix is more complex than the edge case justifies. Only appears on bridge-failure path; cosmetic duplicate warning. Accepted as-is. |
| **IN-01** — HIER-05 truth table duplicated across 3 files | 🟡 **Pending decision** | Consolidation would wire `DomainLogic.assemble_project_type` into both repo adapters and remove open-coded logic from `hybrid.py:474` + `adapter.py:253`. Violates single-code-path rule (feedback: maintainability > micro-perf); domain method exists but is unused. ~1 hour. |

**Recommended path:** Flo's call on IN-01. Two reasonable options:

1. **Close as-is** → `/gsd-complete-milestone v1.4.1`. IN-01 as backlog item. Zero correctness risk today; drift risk only if OF gains a 4th project type.

2. **Fix IN-01 before close** → wire `DomainLogic.assemble_project_type` into both repos. Honors the single-code-path rule and removes the "defined but unused" domain method. ~1 hour.

---

*Audit generated: 2026-04-24*
*Auditor: Claude (gsd-audit-milestone)*
