---
phase: 53.1-true-inherited-fields
plan: 04
subsystem: service, domain
tags: [per-field-aggregation, inherited-fields, min-max-first-found, true-inheritance]
dependency_graph:
  requires: [computed-ancestor-values, soonest-date-semantics]
  provides: [per-field-aggregation, max-defer, first-found-planned, first-found-drop, first-found-completion]
  affects: [get_all, get_task, list_tasks]
tech_stack:
  added: []
  patterns: [strategy-constants-frozenset, three-way-branching, first-found-skip]
key_files:
  created: []
  modified:
    - src/omnifocus_operator/service/domain.py
    - tests/test_service_domain.py
decisions:
  - "Strategy constants (_MIN_FIELDS, _MAX_FIELDS, _FIRST_FOUND_FIELDS) as frozensets for O(1) lookup"
  - "Three-way branching in both task loop and project check -- same logic, no helper extraction needed"
  - "Default branch is min (due_date) -- safest fallback for unknown fields"
requirements_completed: [INHERIT-05, INHERIT-06, INHERIT-07, INHERIT-08, INHERIT-09, INHERIT-10]
metrics:
  duration: 4min
  completed: "2026-04-15T19:48:00Z"
  tasks: 2
  files_modified: 2
  files_created: 0
  tests_before: 2113
  tests_after: 2122
---

# Phase 53.1 Plan 04: Per-Field Aggregation Semantics Summary

**_walk_one uses per-field aggregation: min (due), max (defer), first-found (planned/drop/completion), any-True (flagged) -- fixes 4 bugs confirmed by empirical OmniFocus testing**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-15T19:43:44Z
- **Completed:** 2026-04-15T19:48:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Fixed inheritedDeferDate: now uses max (latest block wins) instead of min
- Fixed inheritedPlannedDate: now uses first-found (nearest ancestor) instead of min
- Fixed inheritedDropDate: now uses first-found (nearest ancestor) instead of min
- Fixed inheritedCompletionDate: now uses first-found (nearest ancestor) instead of min
- Added _MIN_FIELDS, _MAX_FIELDS, _FIRST_FOUND_FIELDS strategy constants
- Removed FIXME comments about incorrect date aggregation
- 9 new tests that distinguish min/max/first-found using multi-ancestor hierarchies

## Task Commits

Each task was committed atomically:

1. **Task 1: Add per-field aggregation tests (RED)** - `11770e2f` (test)
2. **Task 2: Fix _walk_one per-field aggregation and remove FIXME (GREEN)** - `b7ea19e6` (fix)

## Files Created/Modified

- `src/omnifocus_operator/service/domain.py` - Three strategy constants, three-way branching in both task loop and project check, FIXME removed, docstrings updated
- `tests/test_service_domain.py` - 9 new tests under "Per-field aggregation semantics" section: defer_max (x2), planned_first_found (x2), drop_first_found (x2), completion_first_found (x2), due_min_regression (x1)

## Decisions Made

- Strategy constants as frozensets for O(1) membership testing -- `_MIN_FIELDS`, `_MAX_FIELDS`, `_FIRST_FOUND_FIELDS`
- Three-way branching (`if first-found / elif max / else min`) in both the task ancestor loop and project check -- same logic duplicated rather than extracting a helper, matching the existing code style
- Default branch is min (due_date) -- if a new date field were added without updating the constants, it would get the safest behavior

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Additional test_aggregation_defer_max_with_project also fails**
- **Found during:** Task 1
- **Issue:** Plan predicted 4 failing tests, but 5 failed -- `test_aggregation_defer_max_with_project` also fails under min code because the project's defer date (2027) is later than the parent task's (2026), so min picks the wrong one
- **Fix:** Not a fix needed -- the test is correct and produces the expected RED failure. Plan's count was slightly off.
- **Impact:** None -- all tests pass GREEN after Task 2

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 53.1 (True Inherited Fields) is now complete: all 4 plans executed
- All 6 inherited fields have correct per-field aggregation semantics
- Ready to proceed to Phase 54 (Batch Processing)

## Self-Check: PASSED

- All 2 modified files verified present
- All 2 commits (11770e2f, b7ea19e6) verified in git log
- Full test suite: 2122 passed
