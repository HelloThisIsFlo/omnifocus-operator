---
phase: 53-response-shaping
plan: 05
type: execute
wave: 5
depends_on:
  - 53-04
files_modified:
  - src/omnifocus_operator/agent_messages/descriptions.py
  - tests/test_descriptions.py
  - tests/test_server.py
autonomous: true
requirements:
  - COUNT-01

must_haves:
  truths:
    - "limit: 0 returns {items: [], total: N, hasMore: total > 0} on list tools"
    - "LIMIT_DESC mentions count-only behavior"
    - "Tool descriptions use inherited* instead of effective*"
    - "Tool descriptions include stripping note"
    - "list_tasks tool description documents include groups with field contents"
    - "list_projects tool description documents include groups with field contents"
    - "All tool descriptions fit within 2048 byte limit"
  artifacts:
    - path: "src/omnifocus_operator/agent_messages/descriptions.py"
      provides: "Updated tool descriptions with inherited*, stripping, include groups, count-only"
      contains: "_STRIPPING_NOTE"
    - path: "tests/test_descriptions.py"
      provides: "Byte limit enforcement passing with new descriptions"
  key_links:
    - from: "src/omnifocus_operator/agent_messages/descriptions.py"
      to: "src/omnifocus_operator/server/handlers.py"
      via: "tool description constants imported by handlers"
      pattern: "LIST_TASKS_TOOL_DOC"
---

<objective>
Update all tool descriptions for the response-shaping features and verify count-only mode works end-to-end.

Purpose: Agents need updated tool descriptions to discover the new capabilities (include groups, stripping behavior, count-only mode, inherited field semantics). COUNT-01 requires no code changes but needs an integration test and LIMIT_DESC update.

Output: All tool descriptions updated per D-10/D-11/D-11b. LIMIT_DESC updated for count-only. Integration test for limit: 0. Byte limits verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-response-shaping/53-CONTEXT.md
@.planning/phases/53-response-shaping/53-RESEARCH.md
@.planning/phases/53-response-shaping/53-04-SUMMARY.md

@src/omnifocus_operator/agent_messages/descriptions.py
@tests/test_descriptions.py

<interfaces>
<!-- D-11 verbatim text for description constants (from CONTEXT.md) -->

INHERITED_FIELD_DESC = "Inherited from parent hierarchy when not set directly on this entity."

LIMIT_DESC = "Max items to return. Pass null to return all. Tip: pass 0 for count only."

_STRIPPING_NOTE = (
    "Response stripping: null values, empty arrays, empty strings, "
    "false booleans, and \"none\" urgency are omitted. Absent field = not set."
)

<!-- D-11b draft tool descriptions in CONTEXT.md specifics section -->
<!-- list_tasks and list_projects drafts are the target output -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all tool descriptions and description constants</name>
  <files>
    src/omnifocus_operator/agent_messages/descriptions.py
  </files>
  <read_first>
    src/omnifocus_operator/agent_messages/descriptions.py,
    .planning/phases/53-response-shaping/53-CONTEXT.md,
    tests/test_descriptions.py
  </read_first>
  <action>
Per D-10, D-11, D-11b: Update all agent-facing description constants.

**1. Update inherited field description constants (from Plan 01 rename)**

The constants were renamed from EFFECTIVE_* to INHERITED_* in Plan 01, but their TEXT content was kept as-is. Now update the text to the D-11 agreed verbatim:

Replace all 6 INHERITED_* constant values with:
```python
INHERITED_FLAGGED = "Inherited from parent hierarchy when not set directly on this entity."
INHERITED_DUE_DATE = "Inherited from parent hierarchy when not set directly on this entity."
INHERITED_DEFER_DATE = "Inherited from parent hierarchy when not set directly on this entity."
INHERITED_PLANNED_DATE = "Inherited from parent hierarchy when not set directly on this entity."
INHERITED_DROP_DATE = "Inherited from parent hierarchy when not set directly on this entity."
INHERITED_COMPLETION_DATE = "Inherited from parent hierarchy when not set directly on this entity."
```

Since all 6 have the same text per D-11, consider using one shared constant:
```python
INHERITED_FIELD_DESC = "Inherited from parent hierarchy when not set directly on this entity."
INHERITED_FLAGGED = INHERITED_FIELD_DESC
INHERITED_DUE_DATE = INHERITED_FIELD_DESC
# etc.
```

**2. Update LIMIT_DESC**

Replace:
```python
LIMIT_DESC = "Max items to return. Pass null to return all."
```
With (D-11 verbatim):
```python
LIMIT_DESC = "Max items to return. Pass null to return all. Tip: pass 0 for count only."
```

**3. Add _STRIPPING_NOTE reusable fragment**

```python
_STRIPPING_NOTE = (
    "Response stripping: null values, empty arrays, empty strings, "
    "false booleans, and \"none\" urgency are omitted. Absent field = not set."
)
```

**4. Rewrite LIST_TASKS_TOOL_DOC**

Replace the current `LIST_TASKS_TOOL_DOC` with the D-11b draft from CONTEXT.md. The draft is the target structure. Key changes from current:
- Remove `effective*` references, replace with `inherited*`
- Add `_STRIPPING_NOTE` paragraph
- Add `include:` group documentation (one line per group with field contents)
- Add default fields line
- Add count-only tip
- Add inherited* explanation paragraph
- Reorganize to match the draft structure in CONTEXT.md

Use reusable fragments where possible: `_STRIPPING_NOTE`, `_DATE_INPUT_NOTE` (already exists).

**5. Rewrite LIST_PROJECTS_TOOL_DOC**

Same treatment, using the D-11b projects draft from CONTEXT.md. Key differences from tasks:
- Hierarchy group has only `hasChildren` (no `parent`)
- Additional `review` group: nextReviewDate, reviewInterval, lastReviewDate, nextTask
- Default fields include `folder` instead of `order` and `project`
- Inherited explanation says "folder" instead of "parent task, project, folder"

**6. Update GET_TASK_TOOL_DOC, GET_PROJECT_TOOL_DOC**

- Replace `effective*` with `inherited*` in field lists and explanations
- Add `_STRIPPING_NOTE` paragraph
- Update "effective*: inherited from..." to "inherited* fields: value inherited from the hierarchy..."
- Keep existing structure, just update terminology

**7. Update GET_ALL_TOOL_DOC**

- Add `_STRIPPING_NOTE` paragraph
- Keep existing structure otherwise

**8. Update GET_TAG_TOOL_DOC, LIST_TAGS_TOOL_DOC, LIST_FOLDERS_TOOL_DOC, LIST_PERSPECTIVES_TOOL_DOC**

- Add `_STRIPPING_NOTE` paragraph to each
- These are simple tools with no include/only support, so no group documentation needed

**9. Extract reusable fragments**

Per D-11 principle: "Use reusable fragments wherever there is opportunity for reuse across tool descriptions."

Look for additional fragment extraction opportunities:
- `_INHERITED_EXPLANATION` — shared inherited field explanation paragraph
- `_DATE_INPUT_NOTE` — already exists, continue using
- Any other repeated text blocks

**CRITICAL per Pitfall 5:** After every description change, mentally estimate byte count. The existing `test_tool_descriptions_within_client_byte_limit` test in `test_descriptions.py` enforces 2048 bytes. Current LIST_TASKS_TOOL_DOC is ~1200 bytes. The draft is longer. Measure and compress if needed. Run `uv run pytest tests/test_descriptions.py -x -q -k byte_limit` after changes to verify.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_descriptions.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - descriptions.py contains `INHERITED_FIELD_DESC = "Inherited from parent hierarchy when not set directly on this entity."`
    - descriptions.py contains `_STRIPPING_NOTE` constant
    - descriptions.py `LIMIT_DESC` contains "Tip: pass 0 for count only"
    - `LIST_TASKS_TOOL_DOC` contains "inheritedDueDate" (not "effectiveDueDate")
    - `LIST_TASKS_TOOL_DOC` contains the stripping note text
    - `LIST_TASKS_TOOL_DOC` contains include group documentation ('"notes" — note')
    - `LIST_TASKS_TOOL_DOC` contains "Default fields (always returned):"
    - `LIST_TASKS_TOOL_DOC` contains "Count-only: use limit: 0"
    - `LIST_PROJECTS_TOOL_DOC` contains '"review" — nextReviewDate, reviewInterval, lastReviewDate, nextTask'
    - `GET_TASK_TOOL_DOC` contains "inherited*" (not "effective*")
    - `GET_TASK_TOOL_DOC` contains stripping note text
    - No tool description constant contains "effective*" (old name)
    - `uv run pytest tests/test_descriptions.py -x -q` exits 0 (byte limits pass)
  </acceptance_criteria>
  <done>All tool descriptions updated with inherited* naming, stripping note, include group documentation, count-only tip. Reusable fragments extracted. All within byte limits.</done>
</task>

<task type="auto">
  <name>Task 2: Count-only integration test and final suite verification</name>
  <files>
    tests/test_server.py
  </files>
  <read_first>
    tests/test_server.py,
    src/omnifocus_operator/contracts/use_cases/list/tasks.py,
    src/omnifocus_operator/contracts/use_cases/list/projects.py
  </read_first>
  <action>
Per D-08, COUNT-01: `limit: 0` passes through naturally. No special-casing. SQL `LIMIT 0` returns no rows. The repo count query still runs. Verify this works end-to-end.

**tests/test_server.py** — Add count-only integration test:

```python
# Test: limit: 0 returns count-only response
# Call list_tasks with limit=0
# Expect: {"items": [], "total": N, "hasMore": N > 0}
# Where N is the total count of matching tasks
```

The test should:
1. Set up some tasks in the InMemoryBridge/test fixtures
2. Call `list_tasks` with `{"limit": 0}`
3. Assert response `items` is an empty list
4. Assert response `total` is the expected count (number of matching tasks)
5. Assert response `hasMore` is `True` if total > 0, `False` if total == 0
6. Verify no other entity fields leak into the response

Also test `list_projects` with `limit: 0` for completeness.

**Verification:** Currently `limit` on ListTasksQuery has no `ge=1` constraint (verified in RESEARCH.md). So `limit: 0` already passes Pydantic validation. The SQL layer handles `LIMIT 0` naturally. The only thing to verify is that the shaping layer (shape_list_response) correctly handles an empty items list.

After adding the test, run the full test suite to confirm everything works end-to-end.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test_server.py contains a test for limit: 0 count-only mode
    - The count-only test asserts `items == []` and `total >= 0` and `hasMore` is boolean
    - `uv run pytest tests/test_server.py -x -q -k "limit"` exits 0
    - `uv run pytest tests/ -x -q` exits 0 (full suite green)
    - `uv run pytest tests/test_output_schema.py -x -q` exits 0
    - `uv run pytest tests/test_descriptions.py -x -q` exits 0
  </acceptance_criteria>
  <done>Count-only mode verified end-to-end. Full test suite green. Phase 53 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent input -> limit | limit: 0 is valid input; produces empty items array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-07 | Information Disclosure | count-only | accept | limit: 0 only reveals total count of matching items, which is already available in any list response. No new information exposure. |
| T-53-08 | Denial of Service | description byte limit | mitigate | Existing test_tool_descriptions_within_client_byte_limit enforces 2048 byte cap. Run after every description change. |
</threat_model>

<verification>
- `uv run pytest tests/ -q` — full suite green
- `uv run pytest tests/test_descriptions.py -x -q` — description enforcement passes
- `grep "effective" src/omnifocus_operator/agent_messages/descriptions.py` — no `effective*` field references remain (only generic English uses like "effective" in sentences)
- Count-only test passes
</verification>

<success_criteria>
All tool descriptions updated with inherited naming, stripping documentation, include group documentation, and count-only tip. Count-only mode tested end-to-end. All descriptions within byte limits. Full test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/53-response-shaping/53-05-SUMMARY.md`
</output>
