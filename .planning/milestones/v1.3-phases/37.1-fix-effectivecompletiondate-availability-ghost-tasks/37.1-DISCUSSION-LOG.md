# Phase 37.1: Fix effectiveCompletionDate availability ghost tasks - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-04
**Phase:** 37.1-fix-effectivecompletiondate-availability-ghost-tasks
**Areas discussed:** Availability mapping logic, Default query filtering, Hierarchy reconstruction

---

## Area Selection

All three identified gray areas were selected for discussion:
- Availability mapping logic
- Default query filtering
- Hierarchy reconstruction

## Research Phase

User requested codebase/spec research before answering questions. Three explore agents were spawned in parallel:

1. **Specs research** — Found BRIDGE-SPEC.md defines `effectiveCompletionDate` as inherited, FINDINGS.md shows 62 inherited instances beyond 203 direct
2. **Availability & hierarchy research** — Found `_map_task_availability` and `_TASK_AVAILABILITY_CLAUSES` only check own `dateCompleted`, not effective. No hierarchy reconstruction exists (flat lists with parent references).
3. **Prior decisions research** — Found Phase 28 discussion establishing "all or none" principle for effective fields. Found v1.3.2 spec explicitly using effective dates for all date filters.

### Key Finding: v1.3.2 Spec Pre-Answers Everything

User pointed to `.research/updated-spec/MILESTONE-v1.3.2.md` which explicitly states:
- Line 249: "All date filters use effective (inherited) values"
- Line 142: availability trimmed to available/blocked only
- Line 18: `completed` filter operates on "effective completion date"

This pre-determined the fix direction — no ambiguity remained.

## Confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| That's right, proceed | Write CONTEXT.md with effective dates as authoritative | ✓ |
| Mostly right, let me clarify | Additional context needed | |

**User's choice:** That's right, proceed
**Notes:** All decisions derived from existing specs (BRIDGE-SPEC, Phase 28 principle, v1.3.2 direction). No novel design decisions needed.

---

## Claude's Discretion

- Exact implementation approach (whether to use `effectiveDateCompleted` directly or `COALESCE(dateCompleted, effectiveDateCompleted)`)
- Test case design for effective-only completion scenarios
- Whether `_map_project_availability` needs the same fix (review during planning)

## Deferred Ideas

None — discussion stayed within phase scope.
