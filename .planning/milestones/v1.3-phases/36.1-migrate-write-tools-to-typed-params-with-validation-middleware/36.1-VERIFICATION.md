---
phase: 36.1-migrate-write-tools-to-typed-params-with-validation-middleware
verified: 2026-03-31T21:00:00Z
status: passed
score: 11/11 must-haves verified
---

# Phase 36.1: Migrate Write Tools to Typed Params with Validation Middleware — Verification Report

**Phase Goal:** Write tools expose rich inputSchema via typed params, with validation middleware reformatting errors into clean agent-friendly messages
**Verified:** 2026-03-31
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Plan 01)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Validation errors on write tools produce clean ToolError with "Task N: field" format | VERIFIED | `_format_validation_errors` in middleware.py prepends `f"Task {idx + 1}: "` when `idx is not None`; canary test asserts `"Task 1:"` in error message |
| 2 | UNSET sentinel noise never appears in agent-facing error messages | VERIFIED | `middleware.py:107` — `if e.get("ctx", {}).get("class") == "_Unset": continue`; `TestUnsetFiltering` confirms exclusion |
| 3 | Unknown fields produce "Unknown field <name>" without items.0. prefix | VERIFIED | `_strip_items_prefix` removes `("items", N, ...)` prefix before field name formatting; `test_extra_forbidden_produces_unknown_field` confirms |
| 4 | All previously valid add_tasks / edit_tasks inputs produce identical results | VERIFIED | 1372 tests pass including all pre-existing write tool tests; no regressions |
| 5 | Logging middleware sees the reformatted ToolError and logs timing | VERIFIED | LIFO order confirmed in `create_server()` — ValidationReformatting added first (innermost), ToolLogging added second (outermost); `TestLoggingIntegration` confirms error appears in logs |

### Observable Truths (Plan 02)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 6 | add_tasks inputSchema contains 52+ fields with types and enums | VERIFIED | `test_add_tasks_schema_has_rich_input_schema` asserts `len(schema_json) > 500` and checks for `name`, `dueDate`, `flagged`, `estimatedMinutes`, `repetitionRule`, `tags` |
| 7 | edit_tasks inputSchema contains 61+ fields with types and enums | VERIFIED | `test_edit_tasks_schema_has_rich_input_schema` asserts `len(schema_json) > 500` and checks for `id`, `name`, `dueDate`, `flagged`, `estimatedMinutes`, `repetitionRule`, `actions` |
| 8 | If FastMCP moves validation outside middleware chain, canary test fails with explanatory message | VERIFIED | `test_canary_middleware_catches_validation_error` exists with full docstring including "What to do if this fails" guidance |
| 9 | _format_validation_errors produces "Task N: field" format for list[Model] loc tuples | VERIFIED | `TestFormatValidationErrors` — tests for `items.0` → `"Task 1:"`, `items.2` → `"Task 3:"`, no-prefix case |
| 10 | UNSET sentinel errors are filtered out by ctx-class check, not string matching | VERIFIED | `TestUnsetFiltering.test_unset_in_msg_but_not_ctx_is_not_filtered` — error with `_Unset` in msg but no `ctx.class` survives; `test_unset_ctx_class_errors_are_filtered_out` — `ctx.class="_Unset"` filtered |
| 11 | Validation ToolErrors appear in captured logs via logging middleware | VERIFIED | `TestLoggingIntegration.test_validation_error_appears_in_logs` asserts ERROR-level log with `"add_tasks"` and `"FAILED"` |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/omnifocus_operator/middleware.py` | ValidationReformatterMiddleware + helpers | VERIFIED | Contains `class ValidationReformatterMiddleware(Middleware)`, `_format_validation_errors`, `_clean_loc`, `_strip_items_prefix`, `_extract_item_index`, D-08a ctx check |
| `src/omnifocus_operator/server.py` | Typed handler signatures, no try/except blocks | VERIFIED | `items: list[AddTaskCommand]` and `items: list[EditTaskCommand]`; no `_clean_loc`, `_format_validation_errors`, `except ValidationError`, or `INVALID_INPUT` present |
| `tests/conftest.py` | Server fixture with middleware registration | VERIFIED | Both `ValidationReformatterMiddleware()` and `ToolLoggingMiddleware(...)` registered in correct order |
| `tests/test_server.py` | Schema richness tests, canary test | VERIFIED | `test_add_tasks_schema_has_rich_input_schema`, `test_edit_tasks_schema_has_rich_input_schema`, `test_canary_middleware_catches_validation_error` all present and passing |
| `tests/test_middleware.py` | Unit tests for middleware helpers and logging integration | VERIFIED | 470-line file with `TestStripItemsPrefix`, `TestExtractItemIndex`, `TestCleanLoc`, `TestFormatValidationErrors`, `TestUnsetFiltering`, `TestLoggingIntegration` — 33 tests, all passing |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `middleware.py` | `fastmcp.server.middleware.Middleware` | `ValidationReformatterMiddleware` inherits `Middleware` | WIRED | `class ValidationReformatterMiddleware(Middleware):` at line 126 |
| `server.py` | `middleware.py` | `create_server` registers `ValidationReformatterMiddleware` | WIRED | `mcp.add_middleware(ValidationReformatterMiddleware())` at line 333, before `ToolLoggingMiddleware` |
| `conftest.py` | `middleware.py` | server fixture registers middleware | WIRED | Both middlewares registered at lines 422-423 |
| `test_server.py` | `middleware.py` | canary test verifies middleware catches `ValidationError` | WIRED | Test calls `add_tasks` with bogus field, asserts `ToolError` with `"Task 1:"` prefix |
| `test_middleware.py` | `middleware.py` | unit tests for helper functions | WIRED | `from omnifocus_operator.middleware import _clean_loc, _extract_item_index, _format_validation_errors, _strip_items_prefix` at lines 31-37 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Schema richness tests pass | `pytest -k "rich_input_schema or canary_middleware"` | 3 passed | PASS |
| Middleware unit tests pass | `pytest tests/test_middleware.py` | 33 passed | PASS |
| Full test suite passes | `pytest -x -q` | 1372 passed, 98.10% coverage | PASS |
| Removed items absent from server.py | grep for `_clean_loc`, `_format_validation`, `except ValidationError`, `INVALID_INPUT` | no output | PASS |
| Commits verified | `git log a932023 06e1294 2213332 ed2a1d8` | All 4 commits present | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| WRIT-01 | Plan 02 | `add_tasks` inputSchema exposes all `AddTaskCommand` fields with types, enums, constraints | SATISFIED | `test_add_tasks_schema_has_rich_input_schema` passes with schema > 500 chars and key camelCase fields present |
| WRIT-02 | Plan 02 | `edit_tasks` inputSchema exposes all `EditTaskCommand` fields with types, enums, constraints | SATISFIED | `test_edit_tasks_schema_has_rich_input_schema` passes with key fields including `actions` |
| WRIT-03 | Plan 01+02 | Schema field names use camelCase aliases, no snake_case leakage | SATISFIED | `_extract_all_property_keys` recursive check confirms no `due_date`, `defer_date`, `estimated_minutes`, `planned_date` in property keys |
| WRIT-04 | Plan 01+02 | Validation errors use `"Task N: field"` format | SATISFIED | `_format_validation_errors` with `f"Task {idx + 1}: "` prefix; `TestFormatValidationErrors` confirms for index 0 and 2 |
| WRIT-05 | Plan 01+02 | UNSET sentinel noise filtered from validation errors | SATISFIED | `e.get("ctx", {}).get("class") == "_Unset"` at middleware.py:107; `TestUnsetFiltering` with ctx-vs-string distinction |
| WRIT-06 | Plan 01 | Unknown fields produce `"Unknown field '<name>'"` error messages | SATISFIED | `UNKNOWN_FIELD.format(field=field)` in `_format_validation_errors`; `test_extra_forbidden_produces_unknown_field` passes |
| WRIT-07 | Plan 01 | Validation errors surface as `ToolError` with clean messages | SATISFIED | `ValidationReformatterMiddleware` catches `ValidationError` and raises `ToolError("; ".join(messages) or INVALID_INPUT)` |
| WRIT-08 | Plan 01 | All previously valid inputs produce identical results | SATISFIED | 1372 tests pass; pre-existing test_server.py write tests unchanged |
| WRIT-09 | Plan 01 | Error messages maintain functional parity; no internals leak | SATISFIED | No `model_validate`, `ValidationError`, or Pydantic internals in handler output; canary test asserts `"validation error"` not in error message |
| WRIT-10 | Plan 01+02 | Validation errors are logged by tool logger with timing | SATISFIED | LIFO order means `ToolLoggingMiddleware` sees reformatted `ToolError`; `TestLoggingIntegration` confirms `"FAILED"` in captured logs |
| WRIT-11 | Plan 02 | Canary test detects if FastMCP moves validation outside middleware chain | SATISFIED | `test_canary_middleware_catches_validation_error` with full docstring, `"Task 1:"` assertion, and "What to do if this fails" guidance |

**All 11 WRIT requirements verified and satisfied.**

### Anti-Patterns Found

None found. Spot checks:
- No `TODO/FIXME/PLACEHOLDER` in phase files
- No `return null` / `return {}` in middleware
- No hardcoded empty state passing to rendering
- `test_warnings.py` updated to include `middleware` in `_ERROR_CONSUMERS` — prevents silent loss of `INVALID_INPUT` and `UNKNOWN_FIELD` constants

### Human Verification Required

None. All behavioral contracts are fully verifiable through automated tests.

### Gaps Summary

No gaps. All 11 must-haves verified across both plans. Phase goal fully achieved.

- Write tools (`add_tasks`, `edit_tasks`) now use typed params (`list[AddTaskCommand]`, `list[EditTaskCommand]`), exposing rich inputSchema to agents.
- `ValidationReformatterMiddleware` catches Pydantic `ValidationError` and reformats to clean `ToolError` with `"Task N: field"` prefix.
- LIFO middleware order is correct — validation is innermost, logging is outermost, so the logging middleware sees the reformatted error.
- Snake_case leakage check uses property key extraction (not substring match) to avoid false positives from enum values.
- Full test suite: 1372 tests, 98.10% coverage.

---

_Verified: 2026-03-31_
_Verifier: Claude (gsd-verifier)_
