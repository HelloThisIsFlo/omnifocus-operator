---
phase: 35.2-uniform-name-vs-id-resolution-at-service-boundary-for-all-list-filters
verified: 2026-03-31T00:00:00Z
status: passed
score: 9/9 must-haves verified
---

# Phase 35.2: Uniform Name/ID Resolution at Service Boundary Verification Report

**Phase Goal:** Service layer resolves all entity references to IDs via a resolution cascade (ID match → substring → fuzzy "did you mean?"), RepoQuery becomes IDs-only, query builder uses WHERE IN uniformly
**Verified:** 2026-03-31
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                 | Status     | Evidence                                                                                         |
|----|---------------------------------------------------------------------------------------|------------|--------------------------------------------------------------------------------------------------|
| 1  | RepoQuery models use ID-list fields instead of name strings                           | VERIFIED   | `ListTasksRepoQuery.project_ids: list[str] \| None`, `tag_ids: list[str] \| None`; `ListProjectsRepoQuery.folder_ids: list[str] \| None` — no old `project`, `tags`, `folder` fields |
| 2  | Query builder produces WHERE IN with ID placeholders instead of LIKE subqueries       | VERIFIED   | `query_builder.py` line 127: `pi2.task IN ({placeholders})`, line 196: `pi.folder IN ({placeholders})` — zero `LIKE` in project/folder filters |
| 3  | ListResult has an optional warnings field for agent guidance                          | VERIFIED   | `common.py` line 18: `warnings: list[str] \| None = None` on `ListResult[T]`; `ListRepoResult[T]` has no warnings field (D-02d boundary respected) |
| 4  | All existing repo-level tests pass with ID-based field names                          | VERIFIED   | 1281 tests passing, 97.95% coverage |
| 5  | Service resolves name-based filter values to entity IDs before calling the repository | VERIFIED   | `_ListTasksPipeline._resolve_project()` calls `self._resolver.resolve_filter(query.project, all_data.projects)`; `_ListProjectsPipeline._resolve_folder()` equivalent |
| 6  | Agent can pass either a name or an ID in any entity-reference filter field            | VERIFIED   | `Resolver.resolve_filter()`: step 1 = exact ID match, step 2 = substring match — both paths wired and tested |
| 7  | Zero-result name filters produce did-you-mean warnings with close matches             | VERIFIED   | `_ReadPipeline._build_warning()` calls `self._domain.suggest_close_matches()` → `FILTER_DID_YOU_MEAN` or `FILTER_NO_MATCH`; `test_did_you_mean_warning` and `test_unresolved_project_skips_filter_with_warning` both pass |
| 8  | All 5 list methods are callable (not NotImplementedError)                             | VERIFIED   | `grep "NotImplementedError" service.py` returns nothing; `list_tasks` → `_ListTasksPipeline`, `list_projects` → `_ListProjectsPipeline`, `list_tags/folders/perspectives` → inline pass-throughs |
| 9  | Tags, folders, perspectives use inline pass-throughs without pipeline overhead        | VERIFIED   | `service.py` lines 177-192: 3-line pass-throughs for `list_tags`, `list_folders`, `list_perspectives`; no `_ReadPipeline` instantiation |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact                                                    | Expected                                               | Status     | Details                                                                |
|-------------------------------------------------------------|--------------------------------------------------------|------------|------------------------------------------------------------------------|
| `src/omnifocus_operator/contracts/use_cases/list/tasks.py`  | `ListTasksRepoQuery` with `project_ids` and `tag_ids` | VERIFIED   | Both fields present: `project_ids: list[str] \| None`, `tag_ids: list[str] \| None` |
| `src/omnifocus_operator/contracts/use_cases/list/projects.py` | `ListProjectsRepoQuery` with `folder_ids`            | VERIFIED   | `folder_ids: list[str] \| None` present                               |
| `src/omnifocus_operator/contracts/use_cases/list/common.py` | `ListResult` with `warnings` field                    | VERIFIED   | `warnings: list[str] \| None = None` present; `ListRepoResult` has no warnings |
| `src/omnifocus_operator/repository/query_builder.py`        | WHERE IN SQL for project and folder filters            | VERIFIED   | `project_ids` and `folder_ids` used; WHERE IN SQL confirmed            |
| `src/omnifocus_operator/service/resolve.py`                 | Read-side resolution cascade: ID match → substring    | VERIFIED   | `resolve_filter`, `resolve_filter_list`, `find_unresolved` all present |
| `src/omnifocus_operator/service/domain.py`                  | `suggest_close_matches` using difflib                 | VERIFIED   | Method present at line 106; `import difflib` at line 10                |
| `src/omnifocus_operator/service/service.py`                 | `_ListTasksPipeline`, `_ListProjectsPipeline`, pass-throughs | VERIFIED | All pipeline classes present; no NotImplementedError for list methods  |
| `src/omnifocus_operator/agent_messages/warnings.py`         | `FILTER_DID_YOU_MEAN`, `FILTER_NO_MATCH` constants    | VERIFIED   | Both defined at lines 117 and 122                                      |
| `tests/test_list_pipelines.py`                              | Integration tests (min 50 lines)                      | VERIFIED   | 274 lines, 15 integration tests covering name resolution, ID priority, warnings, pass-throughs, and callability |

---

### Key Link Verification

| From                         | To                                              | Via                                               | Status  | Details                                                          |
|------------------------------|-------------------------------------------------|---------------------------------------------------|---------|------------------------------------------------------------------|
| `query_builder.py`           | `contracts/.../list/tasks.py`                   | `query.project_ids` and `query.tag_ids` access    | WIRED   | Lines 122, 131 use `query.project_ids` and `query.tag_ids`      |
| `query_builder.py`           | `contracts/.../list/projects.py`                | `query.folder_ids` access                         | WIRED   | Line 194 uses `query.folder_ids`                                |
| `service.py`                 | `service/resolve.py`                            | `_ListTasksPipeline` calls `Resolver.resolve_filter()` | WIRED | Line 272: `self._resolver.resolve_filter(...)` confirmed        |
| `service.py`                 | `service/domain.py`                             | `_ReadPipeline` calls `DomainLogic.suggest_close_matches()` | WIRED | Line 233: `self._domain.suggest_close_matches(...)` confirmed   |
| `service.py`                 | `contracts/.../list/common.py`                  | `_result_from_repo` converts `ListRepoResult` to `ListResult` with warnings | WIRED | Lines 243-248: `ListResult(..., warnings=self._warnings or None)` |

---

### Data-Flow Trace (Level 4)

| Artifact                | Data Variable     | Source                                   | Produces Real Data | Status    |
|-------------------------|-------------------|------------------------------------------|--------------------|-----------|
| `_ListTasksPipeline`    | `self._all_data`  | `self._repository.get_all()` (async)    | Yes — live entity snapshot used for resolution | FLOWING |
| `_ListTasksPipeline`    | `self._project_ids` | `Resolver.resolve_filter()` against `all_data.projects` | Yes — filters resolved from real entity list | FLOWING |
| `_ReadPipeline`         | `self._warnings`  | `_build_warning()` → `FILTER_DID_YOU_MEAN`/`FILTER_NO_MATCH` | Yes — generated on actual failed resolutions | FLOWING |
| `list_tags` pass-through | `repo_result`    | `self._repository.list_tags(repo_query)` | Yes — direct repo delegation | FLOWING |

---

### Behavioral Spot-Checks

| Behavior                                         | Command                                                                                                                          | Result          | Status |
|--------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------|-----------------|--------|
| `ListTasksRepoQuery` accepts `project_ids`       | `uv run python -c "from omnifocus_operator.contracts.use_cases.list.tasks import ListTasksRepoQuery; q = ListTasksRepoQuery(project_ids=['a']); print(q.project_ids)"` | `['a']`         | PASS   |
| `ListResult` accepts `warnings`                  | `uv run python -c "from omnifocus_operator.contracts.use_cases.list.common import ListResult; r = ListResult(items=[], total=0, has_more=False, warnings=['test']); print(r.warnings)"` | `['test']`      | PASS   |
| No old field names in query_builder              | `grep -n "query\.project\b\|query\.tags\b\|query\.folder\b" src/omnifocus_operator/repository/query_builder.py`                 | (no output)     | PASS   |
| No `NotImplementedError` in service list methods | `grep -n "NotImplementedError" src/omnifocus_operator/service/service.py`                                                        | (no output)     | PASS   |
| All 4 phase commits verified in git history      | `git log --oneline \| grep -E "aa45f67\|9afe39b\|483ec43\|273f85d"`                                                              | All 4 found     | PASS   |
| Full test suite passes                           | `uv run pytest tests/ -x -q --no-header`                                                                                         | 1281 passed, 97.95% coverage | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                           | Status    | Evidence                                                                         |
|-------------|-------------|-------------------------------------------------------------------------------------------------------|-----------|----------------------------------------------------------------------------------|
| INFRA-14    | Plan 01     | RepoQuery models use ID-only fields (project_ids, folder_ids, tag_ids)                                | SATISFIED | `ListTasksRepoQuery.project_ids`, `tag_ids`; `ListProjectsRepoQuery.folder_ids` |
| INFRA-15    | Plan 01     | `ListResult[T]` includes optional `warnings: list[str] \| None` field                                | SATISFIED | `common.py` line 18                                                              |
| INFRA-07    | Plan 02     | Zero-result name filters emit "did you mean?" warning with close matches                              | SATISFIED | `_ReadPipeline._build_warning()` wired to `suggest_close_matches()`; integration test `test_did_you_mean_warning` passes |
| INFRA-12    | Plan 02     | Service resolves all name-based filter values to IDs before passing to repository                     | SATISFIED | `_ListTasksPipeline._resolve_project/tags()`, `_ListProjectsPipeline._resolve_folder()` |
| INFRA-13    | Plan 02     | Agent can pass name or ID — service resolves both uniformly (ID match priority)                       | SATISFIED | `Resolver.resolve_filter()` step 1 = ID, step 2 = substring; `test_id_match_takes_priority` passes |
| INFRA-16    | Plan 02     | All 5 service list methods callable without `NotImplementedError`                                     | SATISFIED | `list_tasks` + `list_projects` via pipelines; `list_tags`, `list_folders`, `list_perspectives` via pass-throughs; `test_no_not_implemented_error` tests all 5 |

All 6 required IDs satisfied. No orphaned requirements detected.

---

### Anti-Patterns Found

No blockers or warnings detected.

- Old field names (`query.project`, `query.tags`, `query.folder`) absent from `query_builder.py` — confirmed by grep.
- No `NotImplementedError` for list methods in `service.py` — confirmed by grep.
- `ListRepoResult` correctly excluded from `warnings` field — verified in `common.py`.
- No placeholder/stub patterns in key phase files.

---

### Human Verification Required

None. All phase behaviors are fully verifiable programmatically.

---

### Gaps Summary

No gaps. All 9 must-have truths pass all verification levels (exists, substantive, wired, data-flowing). All 6 requirement IDs satisfied. Full test suite green at 1281 tests, 97.95% coverage.

---

_Verified: 2026-03-31_
_Verifier: Claude (gsd-verifier)_
