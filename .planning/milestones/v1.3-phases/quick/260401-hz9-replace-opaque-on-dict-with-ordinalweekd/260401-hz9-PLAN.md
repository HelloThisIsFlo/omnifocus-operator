---
phase: quick-260401-hz9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/models/repetition_rule.py
  - src/omnifocus_operator/contracts/shared/repetition_rule.py
  - src/omnifocus_operator/agent_messages/errors.py
  - src/omnifocus_operator/rrule/parser.py
  - src/omnifocus_operator/rrule/builder.py
  - src/omnifocus_operator/service/domain.py
  - tests/test_rrule.py
  - tests/test_contracts_repetition_rule.py
  - tests/test_service_domain.py
  - tests/test_service.py
  - tests/test_validation_repetition.py
  - tests/test_output_schema.py
autonomous: true
requirements: [hz9-01]

must_haves:
  truths:
    - "JSON Schema for `on` field exposes 6 named ordinal fields with DayName enum, not opaque additionalProperties"
    - "Wire format unchanged: `{\"last\": \"friday\"}` round-trips through add/edit/read"
    - "Empty `on: {}` still flows through gracefully with warning (no crash)"
    - "At-most-one validator rejects `{\"first\": \"monday\", \"last\": \"friday\"}`"
    - "All existing tests pass after migration (no behavioral regression)"
  artifacts:
    - path: "src/omnifocus_operator/models/repetition_rule.py"
      provides: "OrdinalWeekday model, DayName type, normalize_day_name(), check_at_most_one_ordinal()"
      contains: "class OrdinalWeekday"
    - path: "src/omnifocus_operator/contracts/shared/repetition_rule.py"
      provides: "OrdinalWeekdaySpec with CommandModel base"
      contains: "class OrdinalWeekdaySpec"
  key_links:
    - from: "src/omnifocus_operator/rrule/parser.py"
      to: "models/repetition_rule.py"
      via: "Constructs OrdinalWeekday(last='friday') instead of dict"
      pattern: "OrdinalWeekday\\("
    - from: "src/omnifocus_operator/rrule/builder.py"
      to: "models/repetition_rule.py"
      via: "Extracts single set field from OrdinalWeekday"
      pattern: "on\\.first|on\\.last"
    - from: "src/omnifocus_operator/service/domain.py"
      to: "models/repetition_rule.py"
      via: "Empty OrdinalWeekday detection in normalize_empty_specialization_fields"
      pattern: "all.*None"
---

<objective>
Replace the opaque `on: dict[str, str] | None` field on `Frequency` / `FrequencyAddSpec` / `FrequencyEditSpec` with typed `OrdinalWeekday` / `OrdinalWeekdaySpec` models so JSON Schema exposes valid ordinals and day names as an enum.

Purpose: Agent sees structured schema with 6 optional ordinal fields and DayName enum instead of `additionalProperties: {type: string}`. No wire format change.
Output: Two new models, updated validators, updated consumers (parser/builder/domain), all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260401-hz9-replace-opaque-on-dict-with-ordinalweekd/260401-hz9-CONTEXT.md
@.planning/quick/260401-hz9-replace-opaque-on-dict-with-ordinalweekd/260401-hz9-RESEARCH.md
@docs/model-taxonomy.md

<interfaces>
<!-- Reference patterns executor needs -->

From contracts/shared/actions.py (MoveAction pattern -- reference for at-most-one validator):
```python
class MoveAction(CommandModel):
    beginning: PatchOrNone[str] = UNSET
    ending: PatchOrNone[str] = UNSET
    before: Patch[str] = UNSET
    after: Patch[str] = UNSET

    @model_validator(mode="after")
    def _exactly_one_key(self) -> MoveAction:
        keys_set = sum(1 for v in (self.beginning, self.ending, self.before, self.after) if is_set(v))
        if keys_set != 1:
            msg = MOVE_EXACTLY_ONE_KEY
            raise ValueError(msg)
        return self
```

From models/repetition_rule.py (current code to replace):
```python
FrequencyType = Literal["minutely", "hourly", "daily", "weekly", "monthly", "yearly"]
_VALID_ORDINALS = {"first", "second", "third", "fourth", "fifth", "last"}
_VALID_DAY_NAMES = {"monday", "tuesday", ..., "sunday", "weekday", "weekend_day"}

def normalize_on(value: dict[str, str] | None) -> dict[str, str] | None: ...

class Frequency(OmniFocusBaseModel):
    on: dict[str, str] | None = None

def check_frequency_cross_type_fields(type_, on_days, on: dict[str, str] | None, on_dates): ...
```

From contracts/shared/repetition_rule.py (current code to replace):
```python
class FrequencyAddSpec(CommandModel):
    on: dict[str, str] | None = None
    def _normalize_on(cls, value): return normalize_on(value)

class FrequencyEditSpec(CommandModel):
    on: PatchOrClear[dict[str, str]] = UNSET
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OrdinalWeekday/OrdinalWeekdaySpec models and shared validators</name>
  <files>
    src/omnifocus_operator/models/repetition_rule.py,
    src/omnifocus_operator/contracts/shared/repetition_rule.py,
    src/omnifocus_operator/agent_messages/errors.py,
    tests/test_validation_repetition.py,
    tests/test_contracts_repetition_rule.py,
    tests/test_output_schema.py
  </files>
  <behavior>
    - OrdinalWeekday(last="friday").model_dump(exclude_defaults=True) == {"last": "friday"}
    - OrdinalWeekday(first="MONDAY") normalizes to first="monday" via field_validator
    - OrdinalWeekday(first="monday", last="friday") raises ValueError (at-most-one)
    - OrdinalWeekday() with all None passes (at-most-one allows 0)
    - OrdinalWeekday(first="invalid_day") raises ValueError
    - OrdinalWeekdaySpec has same field structure but inherits CommandModel (extra="forbid")
    - OrdinalWeekdaySpec(unknown_field="x") raises ValidationError
    - Frequency(type="monthly", on={"last": "friday"}) creates OrdinalWeekday via Pydantic coercion
    - FrequencyAddSpec(type="monthly", on={"last": "friday"}) creates OrdinalWeekdaySpec via coercion
    - FrequencyEditSpec(on={"second": "tuesday"}) creates OrdinalWeekdaySpec via coercion
    - JSON Schema for Frequency.on shows 6 optional fields with DayName enum, not additionalProperties
    - test_output_schema.py passes with updated fixture and schema assertions
  </behavior>
  <action>
    **In models/repetition_rule.py:**
    1. Add `DayName = Literal["monday", "tuesday", "wednesday", "thursday", "friday", "saturday", "sunday", "weekday", "weekend_day"]`
    2. Create `OrdinalWeekday(OmniFocusBaseModel)` with 6 fields: `first`, `second`, `third`, `fourth`, `fifth`, `last` -- each `DayName | None = None`
    3. Add `@field_validator("first", "second", ..., mode="before")` for case normalization (`"Friday"` -> `"friday"`)
    4. Add `@model_validator(mode="after")` calling `check_at_most_one_ordinal()` -- rejects 2+ fields set, allows 0 or 1
    5. Create standalone `normalize_day_name(value: str) -> str` that lowercases and validates against DayName values, raising ValueError with `REPETITION_INVALID_DAY_NAME` message
    6. Create standalone `check_at_most_one_ordinal(model: OrdinalWeekday) -> OrdinalWeekday` that counts non-None fields -- if >1, raises ValueError with message from errors.py
    7. Remove `normalize_on()` function entirely
    8. Remove `_VALID_ORDINALS` and `_VALID_DAY_NAMES` sets (replaced by DayName Literal and field names)
    9. Update `Frequency.on` type from `dict[str, str] | None` to `OrdinalWeekday | None`
    10. Remove `Frequency._normalize_on` validator (model handles its own validation now)
    11. Update `check_frequency_cross_type_fields` param: `on: dict[str, str] | None` -> `on: OrdinalWeekday | None` (only uses `is not None`, safe change)
    12. Update `__all__`: remove `normalize_on`, add `OrdinalWeekday`, `DayName`, `normalize_day_name`, `check_at_most_one_ordinal`
    13. NO `@model_serializer` -- research confirmed `exclude_defaults=True` is recursive in Pydantic v2

    **In contracts/shared/repetition_rule.py:**
    1. Create `OrdinalWeekdaySpec(CommandModel)` with identical 6 fields (`DayName | None = None`), same field_validator and model_validator
    2. Update `FrequencyAddSpec.on` type to `OrdinalWeekdaySpec | None = None`
    3. Remove `FrequencyAddSpec._normalize_on` validator
    4. Update `FrequencyEditSpec.on` type to `PatchOrClear[OrdinalWeekdaySpec] = UNSET`
    5. Update `__all__` to export `OrdinalWeekdaySpec`

    **In agent_messages/errors.py:**
    1. Add `REPETITION_AT_MOST_ONE_ORDINAL = 'on must specify exactly one ordinal (e.g. {{"last": "friday"}}), got {count} keys'`

    **In tests/test_validation_repetition.py:**
    - Update all `on={...}` dict literals to work with new model (Pydantic coerces dicts to models, so most stay as dicts)
    - Add test for at-most-one validator (2 fields -> error)
    - Add test for 0 fields (empty dict / all None -> passes)
    - Add test for case normalization ("Friday" -> "friday")
    - Update invalid ordinal/day tests to match new validation behavior

    **In tests/test_contracts_repetition_rule.py:**
    - Update `on={...}` test data -- dicts still work via Pydantic coercion
    - Add OrdinalWeekdaySpec-specific tests (extra="forbid" rejects unknown fields)

    **In tests/test_output_schema.py:**
    - Update fixture at `"on": {"second": "tuesday"}` -- keep as dict (Pydantic coerces)
    - Update schema assertion: `on` property should now reference OrdinalWeekday schema with 6 fields, not `additionalProperties`
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/.claude/worktrees/agent-00781dfd && uv run pytest tests/test_validation_repetition.py tests/test_contracts_repetition_rule.py tests/test_output_schema.py -x -q</automated>
  </verify>
  <done>OrdinalWeekday and OrdinalWeekdaySpec models exist with at-most-one validator, DayName enum, case normalization. Frequency.on and FrequencyAddSpec.on/FrequencyEditSpec.on use new types. normalize_on() removed. JSON Schema shows structured fields. Output schema test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update consumers (parser, builder, domain) and remaining tests</name>
  <files>
    src/omnifocus_operator/rrule/parser.py,
    src/omnifocus_operator/rrule/builder.py,
    src/omnifocus_operator/service/domain.py,
    tests/test_rrule.py,
    tests/test_service_domain.py,
    tests/test_service.py
  </files>
  <action>
    **In rrule/parser.py (~L210, L244):**
    - Import `OrdinalWeekday` from models
    - Where parser constructs `on={ordinal: day_name}` dict, change to `on=OrdinalWeekday(**{ordinal: day_name})`
    - The `**{ordinal: day_name}` pattern works because ordinal strings match field names

    **In rrule/builder.py (~L95-96, L117-141):**
    - Update `_build_byday_positional` signature: `on: dict[str, str]` -> `on: OrdinalWeekday`
    - Replace dict iteration `next(iter(on.items()))` with field extraction:
      ```python
      ordinal, day_name = next(
          (name, val) for name, val in [
              ("first", on.first), ("second", on.second),
              ("third", on.third), ("fourth", on.fourth),
              ("fifth", on.fifth), ("last", on.last),
          ] if val is not None
      )
      ```
    - Replace `len(on) != 1` guard with equivalent check on non-None count (or trust the at-most-one validator -- builder is only called with valid models from parser)

    **In service/domain.py:**
    - `normalize_empty_specialization_fields()` (~L262-264): Change `len(frequency.on) == 0` to `all(getattr(frequency.on, f) is None for f in ["first", "second", "third", "fourth", "fifth", "last"])` -- or use a helper method
    - `merge_frequency()` (~L302-319): The loop iterates `("on_days", "on", "on_dates")` using `getattr`. When building `merged` dict, ensure `on` values go through `.model_dump()` before `Frequency.model_validate(merged)` to avoid Spec-vs-Core type mismatch (per CONTEXT.md decision and RESEARCH.md pitfall 2). Specifically: if the value being merged is an OrdinalWeekday/OrdinalWeekdaySpec instance, call `.model_dump(exclude_defaults=True)` on it before placing in `merged` dict.
    - Import `OrdinalWeekday` where needed

    **In service/service.py (~L701):**
    - `_build_frequency_from_edit_spec` iterates `("on_days", "on", "on_dates")` with `getattr` -- no structural change needed, but verify the `.model_dump()` boundary is applied (the existing `Frequency.model_validate(spec.frequency.model_dump())` at L461 handles the add path; verify the edit path in domain.py's merge_frequency is also safe)

    **In tests/test_rrule.py:**
    - Update assertions: `result.on == {"last": "friday"}` -> `result.on == OrdinalWeekday(last="friday")` (or compare via `.model_dump(exclude_defaults=True)`)
    - Update `Frequency(on={...})` constructions -- dicts still work via Pydantic coercion
    - Update `build_rrule` test inputs that pass `on=` to use OrdinalWeekday instances

    **In tests/test_service_domain.py:**
    - Update `Frequency(on={})` empty check tests to `Frequency(on=OrdinalWeekday())` or `Frequency(on={})` (Pydantic coerces)
    - Update merge test assertions for new model type
    - Verify empty-on normalization still produces warning

    **In tests/test_service.py:**
    - Update FrequencyAddSpec/FrequencyEditSpec `on=` values in test data -- dicts still coerce, but assertions on output need to check OrdinalWeekday instances
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/.claude/worktrees/agent-00781dfd && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>All consumers (parser, builder, domain) use OrdinalWeekday model. Parser constructs OrdinalWeekday instances. Builder extracts fields from model. Domain handles empty detection and merge with .model_dump() boundary. Full test suite passes (534+ tests).</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q` -- full suite passes
2. `uv run pytest tests/test_output_schema.py -x -q` -- output schema validates (no @model_serializer erasure)
3. JSON Schema check: `uv run python -c "from omnifocus_operator.models.repetition_rule import Frequency; import json; s = Frequency.model_json_schema(); print(json.dumps(s['$defs']['OrdinalWeekday'], indent=2))"` -- shows 6 optional fields with DayName enum
4. Wire format check: `uv run python -c "from omnifocus_operator.models.repetition_rule import Frequency; f = Frequency(type='monthly', on={'last': 'friday'}); print(f.model_dump(exclude_defaults=True))"` -- outputs `{'type': 'monthly', 'on': {'last': 'friday'}}`
5. `uv run mypy src/omnifocus_operator --strict` -- no type errors
</verification>

<success_criteria>
- JSON Schema for `on` field shows 6 named ordinal fields with DayName enum values (not opaque dict)
- Wire format `{"last": "friday"}` unchanged in both input and output
- At-most-one validator rejects multi-field `on`, accepts empty and single-field
- Empty `on: {}` flows through normalization with warning (no crash)
- `normalize_on()` function no longer exists
- Full test suite green, output schema test green, mypy clean
</success_criteria>

<output>
After completion, create `.planning/quick/260401-hz9-replace-opaque-on-dict-with-ordinalweekd/260401-hz9-SUMMARY.md`
</output>
