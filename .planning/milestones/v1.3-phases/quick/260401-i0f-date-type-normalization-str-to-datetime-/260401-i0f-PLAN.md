---
phase: quick-260401-i0f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/models/repetition_rule.py
  - src/omnifocus_operator/rrule/parser.py
  - src/omnifocus_operator/rrule/builder.py
  - src/omnifocus_operator/service/domain.py
  - tests/test_rrule.py
  - tests/test_service_domain.py
autonomous: true
requirements: [DATE-NORM]

must_haves:
  truths:
    - "EndByDate.date is typed as datetime.date, not str"
    - "Parser returns date object from RRULE UNTIL clause"
    - "Builder converts date object to RRULE UNTIL format"
    - "Domain warning compares date objects directly without manual ISO parsing"
    - "JSON output serializes as '2026-12-31' (no T00:00:00Z)"
    - "JSON Schema emits format: date for EndByDate.date"
  artifacts:
    - path: "src/omnifocus_operator/models/repetition_rule.py"
      provides: "EndByDate model with date: datetime.date"
      contains: "date: date"
    - path: "src/omnifocus_operator/rrule/parser.py"
      provides: "UNTIL parser returning date object"
      contains: "_convert_until_to_date"
    - path: "src/omnifocus_operator/rrule/builder.py"
      provides: "date-to-UNTIL converter"
      contains: "_convert_date_to_until"
  key_links:
    - from: "src/omnifocus_operator/rrule/parser.py"
      to: "src/omnifocus_operator/models/repetition_rule.py"
      via: "EndByDate(date=date_object)"
      pattern: "EndByDate\\(date=_convert_until_to_date"
    - from: "src/omnifocus_operator/rrule/builder.py"
      to: "src/omnifocus_operator/models/repetition_rule.py"
      via: "end.date passed to formatter"
      pattern: "_convert_date_to_until\\(end\\.date\\)"
    - from: "src/omnifocus_operator/service/domain.py"
      to: "src/omnifocus_operator/models/repetition_rule.py"
      via: "end.date compared directly as date"
      pattern: "end\\.date < date"
---

<objective>
Normalize EndByDate.date from str to datetime.date across model, parser, builder, domain, and tests.

Purpose: EndByDate.date is the only model-layer date field still stored as a bare string. OmniFocus "end repeat by" is a calendar date (no time component), so datetime.date is semantically correct. This produces clean JSON Schema (format: date) and eliminates manual fromisoformat parsing in domain code.
Output: All 4 production callsites updated, all test fixtures updated, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260401-i0f-date-type-normalization-str-to-datetime-/260401-i0f-CONTEXT.md
@.planning/quick/260401-i0f-date-type-normalization-str-to-datetime-/260401-i0f-RESEARCH.md

@src/omnifocus_operator/models/repetition_rule.py
@src/omnifocus_operator/rrule/parser.py
@src/omnifocus_operator/rrule/builder.py
@src/omnifocus_operator/service/domain.py
@tests/test_rrule.py
@tests/test_service_domain.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update model and all 4 production callsites</name>
  <files>
    src/omnifocus_operator/models/repetition_rule.py,
    src/omnifocus_operator/rrule/parser.py,
    src/omnifocus_operator/rrule/builder.py,
    src/omnifocus_operator/service/domain.py
  </files>
  <behavior>
    - EndByDate(date=date(2026, 12, 31)).model_dump() returns {"date": date(2026, 12, 31)}
    - EndByDate(date=date(2026, 12, 31)).model_dump(mode="json") returns {"date": "2026-12-31"}
    - Parser: _convert_until_to_date("20261231T000000Z") returns date(2026, 12, 31)
    - Builder: _convert_date_to_until(date(2026, 12, 31)) returns "20261231T000000Z"
    - Domain: past date triggers warning via direct date comparison (no fromisoformat)
  </behavior>
  <action>
    Four changes, all informed by RESEARCH.md callsite analysis:

    1. **Model** (`repetition_rule.py` ~line 177-180): Change `date: str` to `date: date` (import `from datetime import date as date_type` at top, use `date_type` as the field type to avoid shadowing). Keep the class docstring. Remove the `# ISO-8601` comment since the type is now self-documenting.

    2. **Parser** (`parser.py` ~line 265-274): Rename `_convert_until_to_iso` to `_convert_until_to_date`. Change return type from `str` to `date`. Instead of building an ISO string, return `date_type(int(y), int(m), int(d))` directly from the regex groups. Update the callsite at ~line 125 where EndByDate is constructed.

    3. **Builder** (`builder.py` ~line 144-152): Rename `_convert_iso_to_until` to `_convert_date_to_until`. Change parameter from `str` to `date_type`. Implementation: `d.strftime("%Y%m%dT000000Z")`. Update callsite at ~line 104.

    4. **Domain** (`domain.py` ~lines 206-208): Replace `datetime.fromisoformat(end.date.replace("Z", "+00:00"))` and the `datetime.now(UTC)` comparison with `end.date < date_type.today()`. Import `from datetime import date as date_type`. The warning message format string works unchanged since `str(date(2026,12,31))` produces `"2026-12-31"`.

    Use `from datetime import date as date_type` consistently across all files to avoid shadowing the `date` field name in EndByDate.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/.claude/worktrees/agent-7af73a89 && uv run python -c "from omnifocus_operator.models.repetition_rule import EndByDate; from datetime import date; e = EndByDate(date=date(2026,12,31)); assert isinstance(e.date, date); assert e.model_dump(mode='json') == {'date': '2026-12-31'}; print('OK')"</automated>
  </verify>
  <done>All 4 production files updated. EndByDate.date is datetime.date. Parser returns date objects. Builder accepts date objects. Domain uses direct date comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Update test fixtures and verify full suite</name>
  <files>
    tests/test_rrule.py,
    tests/test_service_domain.py
  </files>
  <action>
    Update all test fixtures that construct EndByDate or assert against its serialized form. Per RESEARCH.md:

    **test_rrule.py:**
    - ~Line 234: `EndByDate(date="2026-12-31T00:00:00Z")` -> `EndByDate(date=date(2026, 12, 31))`
    - ~Line 235: Update model_dump assertion from `{"date": "2026-12-31T00:00:00Z"}` to `{"date": date(2026, 12, 31)}` (model_dump returns Python objects)
    - ~Line 387: Same EndByDate constructor update
    - ~Line 543: Same EndByDate constructor update in build tests
    - ~Line 593-594: Round-trip test -- parser now returns date object, verify round-trip still works
    - Add `from datetime import date` import at top of file

    **test_service_domain.py:**
    - ~Line 513: `EndByDate(date="2020-01-01T00:00:00Z")` -> `EndByDate(date=date(2020, 1, 1))` (past date for warning test)
    - ~Line 519: `EndByDate(date="2099-12-31T00:00:00Z")` -> `EndByDate(date=date(2099, 12, 31))` (future date)
    - Add `from datetime import date` import

    **test_output_schema.py:** Per RESEARCH.md, fixture already uses `"date": "2025-12-31"` which Pydantic parses into a date object. No changes expected, but verify it passes.

    Search each file for ALL occurrences of `EndByDate(date=` and date string patterns like `"date": "20` to ensure no fixtures are missed.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/.claude/worktrees/agent-7af73a89 && uv run pytest tests/test_rrule.py tests/test_service_domain.py tests/test_output_schema.py -x -q</automated>
  </verify>
  <done>All EndByDate test fixtures use date objects. Full rrule, domain, and output schema test suites pass. No remaining string-based EndByDate.date usage in tests.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q` -- full test suite passes (534+ tests)
2. `uv run python -c "from omnifocus_operator.models.repetition_rule import EndByDate; print(EndByDate.model_json_schema())"` -- shows `"format": "date"` for the date field
3. No remaining `EndByDate(date="` string patterns in codebase (grep confirms)
</verification>

<success_criteria>
- EndByDate.date typed as datetime.date in model
- Parser, builder, domain all use date objects (no ISO string intermediary)
- JSON serialization outputs "2026-12-31" format (no T00:00:00Z)
- JSON Schema shows format: date
- All tests pass including output schema validation
</success_criteria>

<output>
After completion, create `.planning/quick/260401-i0f-date-type-normalization-str-to-datetime-/260401-i0f-SUMMARY.md`
</output>
