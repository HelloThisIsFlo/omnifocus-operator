---
phase: 35-sql-repository
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/repository/hybrid.py
  - tests/test_hybrid_repository.py
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, TASK-08, TASK-09, TASK-10, TASK-11, PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05, PROJ-06, PROJ-07, INFRA-02]

must_haves:
  truths:
    - "HybridRepository.list_tasks returns only tasks matching a single filter (inbox, flagged, project, tags, estimated_minutes_max, availability, search)"
    - "HybridRepository.list_tasks excludes completed/dropped tasks by default"
    - "HybridRepository.list_tasks supports pagination with limit/offset and has_more"
    - "HybridRepository.list_tasks combines multiple filters with AND logic"
    - "HybridRepository.list_projects returns projects matching status/folder/review_due_within/flagged filters"
    - "HybridRepository.list_projects defaults to remaining (available + blocked) projects"
    - "HybridRepository.list_projects supports pagination"
    - "Filtered SQL queries execute measurably faster than full get_all() snapshot"
  artifacts:
    - path: "src/omnifocus_operator/repository/hybrid.py"
      provides: "list_tasks and list_projects async methods + sync helpers"
      contains: "async def list_tasks"
    - path: "tests/test_hybrid_repository.py"
      provides: "Comprehensive tests for all task and project filters"
      contains: "class TestListTasks"
  key_links:
    - from: "src/omnifocus_operator/repository/hybrid.py"
      to: "src/omnifocus_operator/repository/query_builder.py"
      via: "build_list_tasks_sql and build_list_projects_sql calls"
      pattern: "build_list_tasks_sql\\(query\\)"
    - from: "src/omnifocus_operator/repository/hybrid.py"
      to: "src/omnifocus_operator/contracts/use_cases/list_entities.py"
      via: "ListTasksQuery, ListProjectsQuery, ListResult imports"
      pattern: "from omnifocus_operator.contracts.use_cases.list_entities import"
---

<objective>
Implement `list_tasks` and `list_projects` on HybridRepository -- the two complex list methods that use the Phase 34 query builder, full lookup tables, pagination, and ListResult construction.

Purpose: These are the core filtered read paths that give agents fast, parameterized access to tasks and projects from the SQLite cache.
Output: Two new async methods on HybridRepository with comprehensive test coverage for every filter combination.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-sql-repository/35-CONTEXT.md
@.planning/phases/35-sql-repository/35-RESEARCH.md
@.planning/phases/34-contracts-and-query-foundation/34-01-SUMMARY.md
@.planning/phases/34-contracts-and-query-foundation/34-02-SUMMARY.md
@src/omnifocus_operator/repository/hybrid.py
@src/omnifocus_operator/repository/query_builder.py
@src/omnifocus_operator/contracts/use_cases/list_entities.py
@src/omnifocus_operator/contracts/protocols.py
@tests/test_hybrid_repository.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/omnifocus_operator/repository/query_builder.py:
```python
class SqlQuery(NamedTuple):
    sql: str
    params: tuple[Any, ...]

def build_list_tasks_sql(query: ListTasksQuery) -> tuple[SqlQuery, SqlQuery]: ...
def build_list_projects_sql(query: ListProjectsQuery) -> tuple[SqlQuery, SqlQuery]: ...
```

From src/omnifocus_operator/contracts/use_cases/list_entities.py:
```python
class ListTasksQuery(QueryModel):
    in_inbox: bool | None = None
    flagged: bool | None = None
    project: str | None = None
    tags: list[str] | None = None
    estimated_minutes_max: int | None = None
    availability: list[Availability] = Field(default_factory=lambda: [Availability.AVAILABLE, Availability.BLOCKED])
    search: str | None = None
    limit: int | None = None
    offset: int | None = None

class ListProjectsQuery(QueryModel):
    availability: list[Availability] = Field(default_factory=lambda: [Availability.AVAILABLE, Availability.BLOCKED])
    folder: str | None = None
    review_due_within: str | None = None
    flagged: bool | None = None
    limit: int | None = None
    offset: int | None = None

class ListResult(OmniFocusBaseModel, Generic[T]):
    items: list[T]
    total: int
    has_more: bool
```

From src/omnifocus_operator/contracts/protocols.py (Repository):
```python
async def list_tasks(self, query: ListTasksQuery) -> ListResult[Task]: ...
async def list_projects(self, query: ListProjectsQuery) -> ListResult[Project]: ...
```

From src/omnifocus_operator/repository/hybrid.py (existing row mappers):
```python
def _map_task_row(row, tag_lookup, project_info_lookup, task_name_lookup) -> dict[str, Any]: ...
def _map_project_row(row, tag_lookup) -> dict[str, Any]: ...
# NOTE: _map_project_row takes only 2 params (row, tag_lookup), NOT 3
```

From tests/test_hybrid_repository.py (test fixture pattern):
```python
@pytest.mark.asyncio
@pytest.mark.hybrid_db(tasks=[...], tags=[...], task_tags=[...])
async def test_example(self, hybrid_repo: HybridRepository) -> None:
    result = await hybrid_repo.list_tasks(ListTasksQuery(flagged=True))
    assert len(result.items) == 1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared lookup helpers and implement list_tasks + list_projects on HybridRepository</name>
  <files>src/omnifocus_operator/repository/hybrid.py</files>
  <read_first>
    - src/omnifocus_operator/repository/hybrid.py (current state -- lines 526-592 for _read_all lookup pattern)
    - src/omnifocus_operator/repository/query_builder.py (build_list_tasks_sql, build_list_projects_sql signatures)
    - src/omnifocus_operator/contracts/use_cases/list_entities.py (ListTasksQuery, ListProjectsQuery, ListResult)
    - src/omnifocus_operator/contracts/protocols.py (Repository protocol list method signatures)
  </read_first>
  <behavior>
    list_tasks behavior:
    - list_tasks(ListTasksQuery()) returns ListResult[Task] with all non-completed/non-dropped tasks (default availability)
    - list_tasks(ListTasksQuery(flagged=True)) returns only flagged tasks
    - list_tasks(ListTasksQuery(in_inbox=True)) returns only inbox tasks
    - list_tasks(ListTasksQuery(project="Work")) returns tasks in projects matching "Work" (case-insensitive partial)
    - list_tasks(ListTasksQuery(tags=["tag-id"])) returns tasks with that tag (OR logic for multiple)
    - list_tasks(ListTasksQuery(estimated_minutes_max=30)) returns tasks with estimatedMinutes <= 30
    - list_tasks(ListTasksQuery(availability=[Availability.COMPLETED])) returns only completed tasks
    - list_tasks(ListTasksQuery(search="keyword")) returns tasks with "keyword" in name or notes (case-insensitive)
    - list_tasks(ListTasksQuery(limit=5)) returns at most 5 items with correct total and has_more
    - list_tasks(ListTasksQuery(limit=5, offset=3)) paginates correctly
    - list_tasks(ListTasksQuery(flagged=True, in_inbox=True)) combines filters with AND logic
    - Default query excludes completed/dropped tasks (availability=[available, blocked])

    list_projects behavior:
    - list_projects(ListProjectsQuery()) returns ListResult[Project] with remaining projects (available + blocked)
    - list_projects(ListProjectsQuery(availability=[Availability.COMPLETED])) returns only completed projects
    - list_projects(ListProjectsQuery(folder="Personal")) returns projects in matching folders (case-insensitive partial)
    - list_projects(ListProjectsQuery(review_due_within="2026-04-01T00:00:00+00:00")) returns projects with nextReviewDate <= that timestamp
    - list_projects(ListProjectsQuery(flagged=True)) returns only flagged projects
    - list_projects(ListProjectsQuery(limit=3)) paginates with correct total and has_more
    - Default query excludes completed/dropped projects
  </behavior>
  <action>
    1. **Extract 4 private lookup-builder functions** from the existing `_read_all()` logic (lines 534-567). These become module-level or class-level functions that both `_read_all` and the new list methods can call:
       - `_build_tag_name_lookup(conn: sqlite3.Connection) -> dict[str, str]` -- executes `_TAGS_SQL`, returns `{tag_id: tag_name}`
       - `_build_task_tag_map(conn: sqlite3.Connection, tag_name_lookup: dict[str, str]) -> dict[str, list[dict[str, str]]]` -- executes `_TASK_TO_TAG_SQL`, returns `{task_id: [{id, name}]}`
       - `_build_project_info_lookup(conn: sqlite3.Connection) -> dict[str, dict[str, str]]` -- executes ProjectInfo JOIN query, returns `{pi_pk: {id, name}}`
       - `_build_task_name_lookup(conn: sqlite3.Connection) -> dict[str, str]` -- executes `SELECT persistentIdentifier, name FROM Task`, returns `{task_id: name}`

    2. **Refactor `_read_all()`** to call the extracted helpers instead of inline code. Behavior must remain identical.

    3. **Add imports** at top of hybrid.py:
       ```python
       from omnifocus_operator.contracts.use_cases.list_entities import (
           ListProjectsQuery,
           ListResult,
           ListTasksQuery,
       )
       from omnifocus_operator.repository.query_builder import (
           build_list_projects_sql,
           build_list_tasks_sql,
       )
       ```
       Also import `Folder`, `Perspective` from models (needed for type annotations and Plan 02):
       ```python
       from omnifocus_operator.models.folder import Folder
       from omnifocus_operator.models.perspective import Perspective
       ```

    4. **Implement `_list_tasks_sync`** following the pattern from RESEARCH.md:
       - Open fresh read-only connection: `sqlite3.connect(f"file:{self._db_path}?mode=ro", uri=True)`
       - Set `conn.row_factory = sqlite3.Row`
       - Build all 4 lookups using extracted helpers (MUST build tag_name_lookup BEFORE task_tag_map)
       - Call `build_list_tasks_sql(query)` to get `(data_q, count_q)`
       - Execute `conn.execute(data_q.sql, data_q.params).fetchall()` for data rows
       - Execute `conn.execute(count_q.sql, count_q.params).fetchone()` for count
       - Map rows: `[Task.model_validate(_map_task_row(row, task_tag_map, project_info_lookup, task_name_lookup)) for row in data_rows]`
       - Compute: `total = count_row[0]`, `offset = query.offset or 0`, `has_more = (offset + len(tasks)) < total`
       - Return `ListResult(items=tasks, total=total, has_more=has_more)`
       - Close connection in `finally` block

    5. **Implement `async def list_tasks`**: `return await asyncio.to_thread(self._list_tasks_sync, query)`

    6. **Implement `_list_projects_sync`** same pattern:
       - Build only tag_name_lookup and task_tag_map (projects use `_map_project_row(row, tag_lookup)` -- only 2 params)
       - Call `build_list_projects_sql(query)` for `(data_q, count_q)`
       - Map rows: `[Project.model_validate(_map_project_row(row, task_tag_map)) for row in data_rows]`
       - Same ListResult construction as tasks

    7. **Implement `async def list_projects`**: `return await asyncio.to_thread(self._list_projects_sync, query)`

    CRITICAL: `_map_project_row` takes exactly 2 parameters `(row, tag_lookup)`, NOT 3. The CONTEXT.md incorrectly states 3 params. The actual code has 2 params. Use the actual signature.
  </action>
  <verify>
    <automated>uv run pytest tests/test_hybrid_repository.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - hybrid.py contains `def _build_tag_name_lookup(conn`
    - hybrid.py contains `def _build_task_tag_map(conn`
    - hybrid.py contains `def _build_project_info_lookup(conn`
    - hybrid.py contains `def _build_task_name_lookup(conn`
    - hybrid.py contains `async def list_tasks(self, query: ListTasksQuery) -> ListResult[Task]`
    - hybrid.py contains `def _list_tasks_sync(self, query: ListTasksQuery) -> ListResult[Task]`
    - hybrid.py contains `async def list_projects(self, query: ListProjectsQuery) -> ListResult[Project]`
    - hybrid.py contains `def _list_projects_sync(self, query: ListProjectsQuery) -> ListResult[Project]`
    - hybrid.py contains `build_list_tasks_sql(query)` call
    - hybrid.py contains `build_list_projects_sql(query)` call
    - `_read_all` still works (existing tests pass)
    - `uv run pytest tests/test_hybrid_repository.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    list_tasks and list_projects implemented on HybridRepository. Shared lookup helpers extracted. All existing tests still pass. Both methods follow the established async-wraps-sync pattern.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comprehensive tests for list_tasks and list_projects filters, pagination, and performance</name>
  <files>tests/test_hybrid_repository.py</files>
  <read_first>
    - tests/test_hybrid_repository.py (current state -- fixture patterns, _minimal_task, _minimal_project, _minimal_tag helpers, create_test_db)
    - src/omnifocus_operator/repository/hybrid.py (after Task 1 -- list_tasks, list_projects implementations)
    - src/omnifocus_operator/contracts/use_cases/list_entities.py (ListTasksQuery, ListProjectsQuery field definitions)
    - src/omnifocus_operator/models/enums.py (Availability enum values)
  </read_first>
  <behavior>
    TestListTasks:
    - TASK-01: flagged=True returns only flagged tasks; flagged=False returns only unflagged
    - TASK-01: in_inbox=True returns only inbox tasks; in_inbox=False returns only non-inbox
    - TASK-03: project="Work" returns tasks in project matching "Work" (case-insensitive, partial match)
    - TASK-04: tags=["tag-id-1"] returns tasks with that tag; tags=["id1","id2"] returns tasks with either (OR)
    - TASK-05: has_children is DROPPED -- no test needed (noted in requirements)
    - TASK-06: estimated_minutes_max=30 returns tasks with estimatedMinutes <= 30
    - TASK-07: availability=[Availability.COMPLETED] returns completed tasks; availability=[Availability.DROPPED] returns dropped
    - TASK-08: search="keyword" returns tasks matching in name; search also matches in plainTextNote; case-insensitive
    - TASK-09: limit=2 returns at most 2 items; limit=2,offset=1 skips first item; has_more is True when more exist; total reflects all matches not just page
    - TASK-10: flagged=True + in_inbox=True returns only tasks matching BOTH (AND logic); project + tags combined
    - TASK-11: Default query (no filters) returns available+blocked tasks, excludes completed/dropped

    TestListProjects:
    - PROJ-01: availability=[Availability.AVAILABLE] returns only available projects
    - PROJ-02: (replaced by PROJ-01 via availability enum -- no separate test)
    - PROJ-03: Default query returns remaining (available + blocked) projects, not completed/dropped
    - PROJ-04: folder="Personal" returns projects in matching folder (case-insensitive partial)
    - PROJ-05: review_due_within with a future timestamp returns projects with nextReviewDate <= that timestamp
    - PROJ-06: flagged=True returns only flagged projects
    - PROJ-07: limit=2 returns at most 2 items with correct has_more and total

    TestListPerformance:
    - INFRA-02: Filtered query executes faster than full get_all() with ~200 row seed database
  </behavior>
  <action>
    Add test classes at the end of `tests/test_hybrid_repository.py`:

    **1. `class TestListTasks:`** with these test methods (each uses `@pytest.mark.asyncio` and `@pytest.mark.hybrid_db(...)` to seed data):

    - `test_list_tasks_default_excludes_completed_dropped`: Seed 4 tasks: 1 available (blocked=0, no dateCompleted/dateHidden), 1 blocked (blocked=1), 1 completed (dateCompleted set), 1 dropped (dateHidden set). Default query returns 2 items (available + blocked). TASK-11.

    - `test_list_tasks_flagged_true`: Seed 2 tasks: one flagged=1, one flagged=0. `ListTasksQuery(flagged=True)` returns 1 item, flagged is True. TASK-02.

    - `test_list_tasks_flagged_false`: Seed 2 tasks. `ListTasksQuery(flagged=False)` returns only the unflagged task. TASK-02.

    - `test_list_tasks_in_inbox`: Seed 2 tasks: one inInbox=1, one inInbox=0. `ListTasksQuery(in_inbox=True)` returns 1 inbox task. TASK-01.

    - `test_list_tasks_project_filter`: Seed 1 project named "Work Stuff" with pk "pi-proj-001", and 2 tasks: one with containingProjectInfo="pi-proj-001", one without. Also seed a ProjectInfo row. `ListTasksQuery(project="work")` returns 1 task. TASK-03.

    - `test_list_tasks_tags_filter`: Seed 2 tasks and 2 tags. task_tags linking task-001 to tag-001, task-002 to tag-002. `ListTasksQuery(tags=["tag-001"])` returns 1 task. `ListTasksQuery(tags=["tag-001", "tag-002"])` returns 2 tasks (OR logic). TASK-04.

    - `test_list_tasks_estimated_minutes_max`: Seed 3 tasks: estimatedMinutes=15, 30, 60. `ListTasksQuery(estimated_minutes_max=30)` returns 2 tasks. TASK-06.

    - `test_list_tasks_availability_completed`: Seed 2 tasks: one completed (dateCompleted set), one available. `ListTasksQuery(availability=[Availability.COMPLETED])` returns 1 completed task. TASK-07.

    - `test_list_tasks_search_name`: Seed 2 tasks: "Buy groceries", "Write report". `ListTasksQuery(search="buy")` returns 1 task (case-insensitive). TASK-08.

    - `test_list_tasks_search_notes`: Seed 1 task with plainTextNote="Remember to call dentist". `ListTasksQuery(search="dentist")` returns 1 task. TASK-08.

    - `test_list_tasks_pagination_limit`: Seed 5 tasks with unique IDs. `ListTasksQuery(limit=2)` returns 2 items, total=5, has_more=True. TASK-09.

    - `test_list_tasks_pagination_offset`: Seed 5 tasks. `ListTasksQuery(limit=2, offset=3)` returns 2 items, total=5, has_more=False. TASK-09.

    - `test_list_tasks_combined_filters`: Seed 3 tasks: one flagged+inbox, one flagged+not-inbox, one not-flagged+inbox. `ListTasksQuery(flagged=True, in_inbox=True)` returns 1 task (AND logic). TASK-10.

    - `test_list_tasks_no_results`: Seed 1 available task. `ListTasksQuery(flagged=True)` with unflagged task returns 0 items, total=0, has_more=False.

    - `test_list_tasks_has_more_false_when_all_fit`: Seed 2 tasks. `ListTasksQuery(limit=10)` returns 2 items, has_more=False.

    **2. `class TestListProjects:`**

    - `test_list_projects_default_remaining`: Seed 3 projects: one active (effectiveStatus="active"), one inactive (effectiveStatus="inactive" -> blocked), one dropped (effectiveStatus="dropped"). Default query returns 2 (available + blocked). PROJ-03.

    - `test_list_projects_availability_completed`: Seed 2 projects: one completed (dateCompleted set, effectiveStatus="active"), one available. `ListProjectsQuery(availability=[Availability.COMPLETED])` returns 1. PROJ-01.

    - `test_list_projects_folder_filter`: Seed 1 folder "Personal Projects" and 2 projects: one with folder set to the folder ID, one without. `ListProjectsQuery(folder="personal")` returns 1 project. PROJ-04.

    - `test_list_projects_review_due_within`: Seed 2 projects: one with nextReviewDate in the past (should match), one with nextReviewDate far in the future. Use a threshold timestamp between them. `ListProjectsQuery(review_due_within=threshold)` returns 1 project. PROJ-05.

    - `test_list_projects_flagged`: Seed 2 projects: one flagged=1, one flagged=0. `ListProjectsQuery(flagged=True)` returns 1 project. PROJ-06.

    - `test_list_projects_pagination`: Seed 5 projects. `ListProjectsQuery(limit=2)` returns 2 items, total=5, has_more=True. PROJ-07.

    **3. `class TestListPerformance:`**

    - `test_filtered_faster_than_full_snapshot` (INFRA-02): Create a helper function `_generate_performance_seed(n_tasks=150, n_projects=30, n_tags=15, n_folders=5, n_perspectives=3)` that programmatically generates seed data dicts with varied attributes (some flagged, some completed, some with tags, some in projects, varied estimatedMinutes). Use `create_test_db(tmp_path, tasks=..., projects=..., tags=..., folders=..., perspectives=..., task_tags=...)` directly (not the marker, since the seed is too large). Create a HybridRepository manually. Time `await hybrid_repo.list_tasks(ListTasksQuery(flagged=True))` vs `await hybrid_repo.get_all()`. Assert filtered_time < full_time. Per D-03d: no absolute thresholds, only relative comparison.

    **Test data helpers**: Use existing `_minimal_task`, `_minimal_project`, `_minimal_tag`, `_minimal_folder` with overrides. Each test gives unique `persistentIdentifier` values to avoid collisions.

    **Import additions**: Add these imports to the test file:
    ```python
    from omnifocus_operator.contracts.use_cases.list_entities import (
        ListTasksQuery,
        ListProjectsQuery,
        ListResult,
    )
    from omnifocus_operator.models.enums import Availability
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_hybrid_repository.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_hybrid_repository.py contains `class TestListTasks`
    - test_hybrid_repository.py contains `class TestListProjects`
    - test_hybrid_repository.py contains `class TestListPerformance`
    - test_hybrid_repository.py contains `test_list_tasks_default_excludes_completed_dropped`
    - test_hybrid_repository.py contains `test_list_tasks_flagged_true`
    - test_hybrid_repository.py contains `test_list_tasks_in_inbox`
    - test_hybrid_repository.py contains `test_list_tasks_project_filter`
    - test_hybrid_repository.py contains `test_list_tasks_tags_filter`
    - test_hybrid_repository.py contains `test_list_tasks_estimated_minutes_max`
    - test_hybrid_repository.py contains `test_list_tasks_availability_completed`
    - test_hybrid_repository.py contains `test_list_tasks_search_name`
    - test_hybrid_repository.py contains `test_list_tasks_search_notes`
    - test_hybrid_repository.py contains `test_list_tasks_pagination_limit`
    - test_hybrid_repository.py contains `test_list_tasks_pagination_offset`
    - test_hybrid_repository.py contains `test_list_tasks_combined_filters`
    - test_hybrid_repository.py contains `test_list_projects_default_remaining`
    - test_hybrid_repository.py contains `test_list_projects_folder_filter`
    - test_hybrid_repository.py contains `test_list_projects_review_due_within`
    - test_hybrid_repository.py contains `test_list_projects_flagged`
    - test_hybrid_repository.py contains `test_list_projects_pagination`
    - test_hybrid_repository.py contains `test_filtered_faster_than_full_snapshot`
    - `uv run pytest tests/test_hybrid_repository.py -x -q` exits 0
    - `uv run pytest -x -q` exits 0 (full suite)
  </acceptance_criteria>
  <done>
    All task filter tests (TASK-01 through TASK-11 minus TASK-05 dropped), project filter tests (PROJ-01 through PROJ-07), and performance comparison test (INFRA-02) pass. Total test count increased by ~20+ tests. Full suite green.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_hybrid_repository.py -x -q` -- all existing + new tests pass
2. `uv run pytest tests/test_query_builder.py -x -q` -- query builder tests still pass (untouched)
3. `uv run pytest tests/test_output_schema.py -x -q` -- output schema validation still passes
4. `uv run pytest -x -q` -- full suite green
5. `uv run mypy src/omnifocus_operator/repository/hybrid.py --no-error-summary` -- type checks pass (except expected Protocol incomplete errors for list_tags/list_folders/list_perspectives which Plan 02 adds)
</verification>

<success_criteria>
- list_tasks returns filtered results for every individual filter field (inbox, flagged, project, tags, estimated_minutes_max, availability, search)
- list_tasks returns correct pagination (limit, offset, has_more, total)
- list_tasks combines multiple filters with AND logic
- list_tasks defaults exclude completed/dropped tasks
- list_projects returns filtered results for all project filters (availability, folder, review_due_within, flagged)
- list_projects defaults to remaining (available + blocked)
- list_projects supports pagination
- Filtered queries measurably faster than full snapshot (INFRA-02 comparative test)
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/35-sql-repository/35-01-SUMMARY.md`
</output>
