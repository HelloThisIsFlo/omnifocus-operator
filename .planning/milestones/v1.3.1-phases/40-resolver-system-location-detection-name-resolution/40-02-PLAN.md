---
phase: 40-resolver-system-location-detection-name-resolution
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/omnifocus_operator/service/service.py
  - src/omnifocus_operator/service/domain.py
  - tests/test_service.py
  - tests/test_service_domain.py
autonomous: true
requirements: [NRES-01, NRES-02, NRES-03]

must_haves:
  truths:
    - "add_tasks with parent='My Project' resolves to the project ID and creates the task there"
    - "edit_tasks with ending='My Project' resolves to the project ID and moves the task there"
    - "edit_tasks with before='Alpha' resolves to the task ID and positions before it"
    - "Name resolution works end-to-end through the full pipeline, not just at resolver unit level"
  artifacts:
    - path: "tests/test_service.py"
      provides: "Integration tests for name resolution through add_task and edit_task pipelines"
      contains: "name_resolution"
  key_links:
    - from: "src/omnifocus_operator/service/service.py"
      to: "src/omnifocus_operator/service/resolve.py"
      via: "_AddTaskPipeline calls resolve_container"
      pattern: "resolve_container"
    - from: "src/omnifocus_operator/service/domain.py"
      to: "src/omnifocus_operator/service/resolve.py"
      via: "_process_container_move calls resolve_container"
      pattern: "resolve_container"
    - from: "src/omnifocus_operator/service/domain.py"
      to: "src/omnifocus_operator/service/resolve.py"
      via: "_process_anchor_move calls resolve_anchor for before/after name resolution"
      pattern: "resolve_anchor"
---

<objective>
Wire name resolution into all write-field call sites and add integration tests proving end-to-end name resolution through add_task and edit_task pipelines.

Purpose: Plan 01 built the resolver cascade but left the old call sites using the old method names. Plan 01 Task 2 updated the call sites for rename compatibility, but the actual name resolution (passing a name string instead of an ID through the pipeline) needs integration testing. This plan adds the integration tests that prove NRES-01, NRES-02, NRES-03 work end-to-end.
Output: Integration tests confirming all 5 write fields accept entity names through the full pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-resolver-system-location-detection-name-resolution/40-CONTEXT.md
@.planning/phases/40-resolver-system-location-detection-name-resolution/40-RESEARCH.md
@.planning/phases/40-resolver-system-location-detection-name-resolution/40-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, Resolver has these public methods: -->
From src/omnifocus_operator/service/resolve.py (post Plan 01):
```python
class Resolver:
    async def resolve_container(self, value: str) -> str: ...  # $-prefix, substring, ID fallback
    async def resolve_anchor(self, value: str) -> str: ...     # $-prefix (error), substring, ID fallback (TASK only)
    async def resolve_tags(self, tag_names: list[str]) -> list[str]: ...  # substring matching
    async def lookup_task(self, task_id: str) -> Task: ...     # ID-only, returns full entity
    async def lookup_project(self, project_id: str) -> Project: ...
    async def lookup_tag(self, tag_id: str) -> Tag: ...
```

<!-- Pipeline call sites (service.py) after Plan 01 Task 2: -->
From src/omnifocus_operator/service/service.py:
```python
# _AddTaskPipeline._resolve_parent: calls self._resolver.resolve_container(parent)
# _EditTaskPipeline._verify_task_exists: calls self._resolver.lookup_task(id)
```

<!-- Domain call sites (domain.py) after Plan 01 Task 2: -->
From src/omnifocus_operator/service/domain.py:
```python
# _process_container_move: calls self._resolver.resolve_container(container_id)
# _process_anchor_move: calls self._resolver.lookup_task(anchor_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire resolve_anchor into _process_anchor_move and add integration tests for all 5 write fields</name>
  <files>
    src/omnifocus_operator/service/domain.py,
    tests/test_service.py,
    tests/test_service_domain.py
  </files>
  <read_first>
    src/omnifocus_operator/service/domain.py,
    src/omnifocus_operator/service/service.py,
    src/omnifocus_operator/service/resolve.py,
    tests/test_service.py,
    tests/test_service_domain.py
  </read_first>
  <behavior>
    - Test: `add_task(parent="Project One")` succeeds and creates task under proj-1
    - Test: `add_task(parent="$inbox")` succeeds (resolve_container handles $inbox)
    - Test: `add_task(parent="Nonexistent")` raises ValueError with fuzzy suggestions
    - Test: `edit_task(moveTo={ending: "Project One"})` succeeds and moves task
    - Test: `edit_task(moveTo={beginning: "Project One"})` succeeds and moves task
    - Test: `edit_task(moveTo={before: "Alpha"})` succeeds -- anchor resolved by name
    - Test: `edit_task(moveTo={after: "Alpha"})` succeeds -- anchor resolved by name
    - Test: `edit_task(moveTo={before: "Nonexistent"})` raises ValueError
    - Test: `edit_task(moveTo={ending: "$inbox"})` resolves without error
  </behavior>
  <action>
    **Step 1 -- Fix `_process_anchor_move` in domain.py:**
    Plan 01 Task 2 renamed `resolve_task` -> `lookup_task` at the anchor call site. But `_process_anchor_move` should actually use `resolve_anchor` (not `lookup_task`) so that name resolution works for `before`/`after` fields. Per D-08, `resolve_anchor` passes `[TASK]` to `_resolve`, enabling name-to-ID resolution.

    Change `_process_anchor_move` (around line 609):
    - FROM: `await self._resolver.lookup_task(anchor_id)` (or whatever Plan 01 left)
    - TO: `await self._resolver.resolve_anchor(anchor_id)`
    - The resolve_anchor method returns a str (the resolved ID), not a Task. Update the surrounding code: currently the return value is discarded (only checking existence), and the dict uses `anchor_id` directly. After the change, capture the resolved ID: `resolved_id = await self._resolver.resolve_anchor(anchor_id)` and use `resolved_id` in the return dict instead of `anchor_id`.

    Also verify `_process_container_move` uses the resolved ID from `resolve_container` (not the original input). Currently line 593 calls `resolve_parent`/`resolve_container` but discards the return value and uses the original `container_id`. Fix: `resolved_id = await self._resolver.resolve_container(container_id)` and use `resolved_id` in the return dict and for the cycle check.

    **Step 2 -- Add integration tests in `tests/test_service.py`:**
    Add a test class or section for name resolution through the full pipeline. Use InMemoryBridge with known entities (projects, tasks, tags with names). Test through `OperatorService.add_task` and `OperatorService.edit_task` -- not through Resolver directly (those are unit tests in Plan 01).

    Tests to add:
    - `test_add_task_parent_by_name`: add_task with `parent="Project One"` -- task created under proj-1
    - `test_add_task_parent_by_name_substring`: add_task with `parent="Proj"` -- substring matches "Project One"
    - `test_add_task_parent_system_location`: add_task with `parent="$inbox"` -- resolves to inbox
    - `test_add_task_parent_name_not_found`: add_task with `parent="Nonexistent"` -- ValueError
    - `test_edit_task_move_ending_by_name`: edit_task with moveTo ending="Project One" -- moves task
    - `test_edit_task_move_beginning_by_name`: edit_task with moveTo beginning="Project One" -- moves task
    - `test_edit_task_move_before_by_name`: edit_task with moveTo before="Alpha" -- positions task
    - `test_edit_task_move_after_by_name`: edit_task with moveTo after="Alpha" -- positions task
    - `test_edit_task_move_anchor_not_found`: edit_task with moveTo before="Nonexistent" -- ValueError
    - `test_edit_task_move_ending_system_location`: edit_task with moveTo ending="$inbox" -- resolves

    **Step 3 -- Run full test suite.**
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/.claude/worktrees/agent-59bcbc05 && uv run pytest tests/test_service.py tests/test_service_domain.py tests/test_service_resolve.py -x -q && uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/omnifocus_operator/service/domain.py` `_process_anchor_move` calls `resolve_anchor` (not `lookup_task`)
    - `src/omnifocus_operator/service/domain.py` `_process_container_move` uses the return value from `resolve_container` (resolved ID, not original input)
    - `tests/test_service.py` contains `test_add_task_parent_by_name`
    - `tests/test_service.py` contains `test_edit_task_move_ending_by_name`
    - `tests/test_service.py` contains `test_edit_task_move_before_by_name`
    - `uv run pytest -x -q` exits 0 with zero failures
  </acceptance_criteria>
  <done>
    All 5 write fields (parent, beginning, ending, before, after) accept entity names end-to-end through the full add_task and edit_task pipelines. Integration tests prove the wiring works. resolve_anchor used for before/after, resolve_container used for parent/beginning/ending. Full test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP protocol -> service layer | Agent-provided entity names flow through pipeline into resolver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Tampering | domain.py `_process_container_move` | mitigate | Use resolved ID (from resolve_container) in return dict, not raw user input. Prevents bypassing resolution by passing crafted strings that look like IDs but aren't validated. |
| T-40-06 | Tampering | domain.py `_process_anchor_move` | mitigate | Use resolved ID (from resolve_anchor) in return dict, not raw user input. Same principle as T-40-05. |
</threat_model>

<verification>
1. `uv run pytest tests/test_service.py -x -q -k name_resolution` -- integration tests pass
2. `uv run pytest -x -q` -- full suite green
3. Manual grep: `grep -n "resolve_container\|resolve_anchor" src/omnifocus_operator/service/domain.py` shows both methods called at the correct locations
</verification>

<success_criteria>
- `add_tasks` parent field accepts project/task names (NRES-01)
- `edit_tasks` beginning/ending accept container names (NRES-02)
- `edit_tasks` before/after accept task names (NRES-03)
- All resolution goes through the cascade: $-prefix, substring, ID fallback
- Integration tests prove end-to-end behavior through full pipelines
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/40-resolver-system-location-detection-name-resolution/40-02-SUMMARY.md`
</output>
