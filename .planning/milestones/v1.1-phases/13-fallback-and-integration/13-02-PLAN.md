---
phase: 13-fallback-and-integration
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - docs/configuration.md
  - tests/test_adapter.py
autonomous: true
requirements: [FALL-02]

must_haves:
  truths:
    - "Bridge mode produces urgency values correctly (fully populated)"
    - "Bridge mode availability is limited to available/completed/dropped (no blocked)"
    - "Configuration docs accurately describe OMNIFOCUS_REPOSITORY behavior as active (not 'Coming in Phase 13')"
    - "Configuration docs explain bridge mode as degraded fallback with availability limitation"
  artifacts:
    - path: "docs/configuration.md"
      provides: "Active configuration documentation for OMNIFOCUS_REPOSITORY"
      contains: "sqlite"
    - path: "tests/test_adapter.py"
      provides: "FALL-02 bridge availability assertion"
      contains: "blocked"
  key_links:
    - from: "tests/test_adapter.py"
      to: "src/omnifocus_operator/bridge/adapter.py"
      via: "adapt_snapshot call verifying availability values"
      pattern: "adapt_snapshot"
---

<objective>
Verify bridge availability limitation (FALL-02) and finalize configuration documentation.

Purpose: Ensure the bridge adapter's availability limitation is explicitly tested, and update docs to reflect Phase 13 completion.
Output: FALL-02 test assertion, updated configuration.md.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fallback-and-integration/13-CONTEXT.md
@.planning/phases/13-fallback-and-integration/13-01-SUMMARY.md

@docs/configuration.md
@tests/test_adapter.py
@src/omnifocus_operator/bridge/adapter.py

<interfaces>
<!-- The bridge adapter transforms raw bridge data to new model shape -->

From src/omnifocus_operator/bridge/adapter.py:
```python
def adapt_snapshot(raw: dict[str, Any]) -> dict[str, Any]:
    """Transform a raw bridge snapshot dict to the new model shape (in-place)."""
```

The adapter maps status values to urgency + availability.
For tasks/projects: availability maps to "available", "completed", "dropped" (never "blocked" -- bridge doesn't expose sequential/dependency info).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FALL-02 bridge availability assertion test</name>
  <files>tests/test_adapter.py</files>
  <behavior>
    - Bridge adapter never produces "blocked" availability for any task
    - Bridge adapter never produces "blocked" availability for any project
    - Bridge adapter produces valid availability values: available, completed, dropped only
    - Bridge adapter produces valid urgency values: overdue, due_soon, none
  </behavior>
  <action>
    Add a test class or test function in `tests/test_adapter.py` that explicitly asserts FALL-02:

    1. Read existing adapter tests to understand the test pattern and helpers used
    2. Add test(s) that exercise all bridge status mappings and assert:
       - No task availability is "blocked"
       - No project availability is "blocked"
       - All availability values are in {"available", "completed", "dropped"}
       - Urgency values are in {"overdue", "due_soon", "none"}
    3. Name the test clearly: e.g., `test_bridge_adapter_never_produces_blocked_availability`
    4. Test should cover every input status that the adapter handles, asserting none map to "blocked"

    This is a verification test -- the adapter already handles this correctly. The test makes the FALL-02 requirement explicit and prevents regression.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_adapter.py -x -q</automated>
  </verify>
  <done>
    - Explicit test asserts bridge adapter never produces "blocked" availability
    - Test covers all bridge status -> availability mappings
    - FALL-02 requirement is regression-protected
  </done>
</task>

<task type="auto">
  <name>Task 2: Update configuration.md for Phase 13 completion</name>
  <files>docs/configuration.md</files>
  <action>
    Update `docs/configuration.md`:

    1. Remove "**Status:** Coming in Phase 13. Currently all reads go through the bridge." line from `OMNIFOCUS_REPOSITORY` section
    2. The existing table and description are already accurate (written during Phase 12 context) -- verify and adjust if needed
    3. Ensure the `bridge` row clearly states: degraded availability (no `blocked` status)
    4. Ensure the docs match the user's decision: bridge is a "temporary workaround" not a permanent mode

    Add a new `OMNIFOCUS_SQLITE_PATH` section under "Rarely Changed":
    - Path to the OmniFocus SQLite database file
    - Auto-detected from default location
    - Only set if auto-detection fails (e.g., after OmniFocus update changes the path)
    - Show the default path value

    Keep the doc concise and scannable (per Flo's documentation style preference).
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && grep -c "Coming in Phase 13" docs/configuration.md | grep -q "^0$" && echo "PASS: Phase 13 placeholder removed" || echo "FAIL: Still contains placeholder"</automated>
  </verify>
  <done>
    - "Coming in Phase 13" placeholder removed
    - OMNIFOCUS_REPOSITORY documented as active with bridge degradation caveat
    - OMNIFOCUS_SQLITE_PATH documented under "Rarely Changed"
    - Documentation is concise and scannable
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q` -- full suite green
- `grep "Coming in Phase 13" docs/configuration.md` returns nothing
- `grep "OMNIFOCUS_SQLITE_PATH" docs/configuration.md` returns match
- `grep "blocked" tests/test_adapter.py` confirms FALL-02 regression test exists
</verification>

<success_criteria>
- FALL-02: Explicit test proves bridge adapter never produces "blocked" availability
- Configuration docs are accurate and active (no "coming soon" placeholders)
- OMNIFOCUS_SQLITE_PATH is documented
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/13-fallback-and-integration/13-02-SUMMARY.md`
</output>
