---
phase: 10-model-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/models/enums.py
  - src/omnifocus_operator/bridge/adapter.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03]

must_haves:
  truths:
    - "Urgency enum exists with values overdue, due_soon, none"
    - "Availability enum exists with values available, blocked, completed, dropped"
    - "Adapter module maps old bridge TaskStatus/ProjectStatus/TagStatus/FolderStatus strings to new enum values"
    - "All existing tests still pass (zero breakage -- new code is additive only)"
  artifacts:
    - path: "src/omnifocus_operator/models/enums.py"
      provides: "Urgency and Availability enums alongside existing enums"
      contains: "class Urgency"
    - path: "src/omnifocus_operator/bridge/adapter.py"
      provides: "Bridge-to-model snapshot transformation"
      exports: ["adapt_snapshot"]
  key_links:
    - from: "src/omnifocus_operator/bridge/adapter.py"
      to: "src/omnifocus_operator/models/enums.py"
      via: "imports Urgency, Availability enum values for mapping"
      pattern: "from omnifocus_operator.models"
---

<objective>
Add new Urgency and Availability enums and create the bridge adapter module that maps old bridge output format to new model shape.

Purpose: Lay the foundation for model migration without breaking any existing tests. The adapter and enums must exist before models can reference them.
Output: Two new/modified source files + adapter test file, all existing tests still green.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-model-overhaul/10-CONTEXT.md
@.planning/phases/10-model-overhaul/10-RESEARCH.md

@src/omnifocus_operator/models/enums.py
@src/omnifocus_operator/models/__init__.py
@src/omnifocus_operator/repository.py
@tests/conftest.py

<interfaces>
<!-- Current enums the adapter must map FROM -->
From src/omnifocus_operator/models/enums.py:
```python
class TaskStatus(StrEnum):
    AVAILABLE = "Available"
    BLOCKED = "Blocked"
    COMPLETED = "Completed"
    DROPPED = "Dropped"
    DUE_SOON = "DueSoon"
    NEXT = "Next"
    OVERDUE = "Overdue"

class ProjectStatus(StrEnum):
    ACTIVE = "Active"
    ON_HOLD = "OnHold"
    DONE = "Done"
    DROPPED = "Dropped"

class TagStatus(StrEnum):
    ACTIVE = "Active"
    ON_HOLD = "OnHold"
    DROPPED = "Dropped"

class FolderStatus(StrEnum):
    ACTIVE = "Active"
    DROPPED = "Dropped"

class ScheduleType(StrEnum):
    REGULARLY = "Regularly"
    FROM_COMPLETION = "FromCompletion"
    NONE = "None"

class AnchorDateKey(StrEnum):
    DUE_DATE = "DueDate"
    DEFER_DATE = "DeferDate"
    PLANNED_DATE = "PlannedDate"
```

From src/omnifocus_operator/repository.py (where adapter will be wired in Plan 03):
```python
async def _refresh(self, current_mtime: int) -> DatabaseSnapshot:
    raw: dict[str, Any] = await self._bridge.send_command("snapshot")
    snapshot = DatabaseSnapshot.model_validate(raw)
    self._last_mtime_ns = current_mtime
    return snapshot
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Urgency and Availability enums to enums.py</name>
  <files>src/omnifocus_operator/models/enums.py</files>
  <behavior>
    - Urgency enum has exactly 3 values: "overdue", "due_soon", "none" (all snake_case)
    - Availability enum has exactly 4 values: "available", "blocked", "completed", "dropped" (all snake_case)
    - Both are StrEnum subclasses
    - All existing enums (TaskStatus, ProjectStatus, TagStatus, FolderStatus, ScheduleType, AnchorDateKey) remain unchanged -- they are still needed by existing code
    - Importing Urgency and Availability works without error
  </behavior>
  <action>
Add `Urgency` and `Availability` StrEnum classes to `enums.py`. Place them ABOVE the existing enums (they are the new primary enums). Use snake_case string values per locked decision. Keep all existing enums exactly as-is -- they will be removed in Plan 02 after models stop referencing them.

Urgency docstring: "Time pressure axis -- is this task/project pressing?"
Availability docstring: "Work readiness axis -- can this be worked on?"
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_models.py -x -q</automated>
  </verify>
  <done>Urgency and Availability enums exist in enums.py with correct snake_case values. All existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bridge adapter module with snapshot transformation</name>
  <files>src/omnifocus_operator/bridge/adapter.py, tests/test_adapter.py</files>
  <behavior>
    - adapt_snapshot(raw) transforms a bridge-format snapshot dict in place and returns it
    - Task mapping: TaskStatus string -> (urgency, availability) tuple per mapping table in RESEARCH.md
    - Project mapping: ProjectStatus string -> availability, TaskStatus string -> urgency (from taskStatus field)
    - Tag mapping: TagStatus string -> snake_case value
    - Folder mapping: FolderStatus string -> snake_case value
    - ScheduleType mapping: old PascalCase -> new snake_case
    - AnchorDateKey mapping: old PascalCase -> new snake_case
    - Dead fields removed from each entity dict: active, effectiveActive, completed, completedByChildren, sequential, shouldUseFloatingTimeZone (from tasks/projects), containsSingletonActions (from projects), allowsNextAction (from tags)
    - Unknown status values raise ValueError (fail-fast, per project pattern)
    - adapt_snapshot handles empty collections gracefully
  </behavior>
  <action>
Create `src/omnifocus_operator/bridge/adapter.py` with:

1. Dict-based mapping tables (NOT if/elif chains -- per research "Don't Hand-Roll"):
   - `_TASK_STATUS_MAP: dict[str, tuple[str, str]]` mapping old TaskStatus strings to (urgency, availability) tuples
   - `_PROJECT_STATUS_MAP: dict[str, str]` mapping old ProjectStatus strings to availability values
   - `_TAG_STATUS_MAP: dict[str, str]` mapping old TagStatus strings to snake_case values
   - `_FOLDER_STATUS_MAP: dict[str, str]` mapping old FolderStatus strings to snake_case values
   - `_SCHEDULE_TYPE_MAP: dict[str, str]` mapping old ScheduleType strings to snake_case values
   - `_ANCHOR_DATE_KEY_MAP: dict[str, str]` mapping old AnchorDateKey strings to snake_case values

2. Per-entity adapter functions:
   - `_adapt_task(raw: dict)`: Pop `status`, look up in `_TASK_STATUS_MAP`, set `urgency` and `availability`. Pop dead fields (active, effectiveActive, completed, completedByChildren, sequential, shouldUseFloatingTimeZone). Use `raw.pop(key, None)` for dead fields. Raise `ValueError` for unknown status.
   - `_adapt_project(raw: dict)`: Pop `status` (ProjectStatus) -> look up availability. Pop `taskStatus` (TaskStatus) -> look up urgency from `_TASK_STATUS_MAP` (first element of tuple). Pop dead fields (same as task + containsSingletonActions). Raise `ValueError` for unknown status.
   - `_adapt_tag(raw: dict)`: Map `status` value in-place via `_TAG_STATUS_MAP`. Pop `allowsNextAction`. Pop `active`, `effectiveActive`. Raise `ValueError` for unknown status.
   - `_adapt_folder(raw: dict)`: Map `status` value in-place via `_FOLDER_STATUS_MAP`. Pop `active`, `effectiveActive`. Raise `ValueError` for unknown status.
   - Handle repetitionRule nested dicts: if `repetitionRule` is not None, map `scheduleType` and `anchorDateKey` values via their maps.

3. Public function:
   - `adapt_snapshot(raw: dict[str, Any]) -> dict[str, Any]`: Iterate tasks, projects, tags, folders lists and call respective adapter. Return the dict. This is NOT wired into repository yet (that's Plan 03).

Create `tests/test_adapter.py` with thorough tests:
- Test each status mapping (all 7 TaskStatus values, all 4 ProjectStatus values, all 3 TagStatus values, all 2 FolderStatus values)
- Test dead field removal (fields are gone from output)
- Test urgency + availability fields are set correctly
- Test unknown status raises ValueError
- Test full adapt_snapshot with a complete snapshot dict (use conftest factories)
- Test adapt_snapshot with empty collections
- Test repetitionRule enum value mapping (scheduleType, anchorDateKey)
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_adapter.py tests/test_models.py -x -q</automated>
  </verify>
  <done>Bridge adapter module exists with complete mapping tables for all entity types. All adapter tests pass. All existing tests still pass (adapter is not wired yet).</done>
</task>

</tasks>

<verification>
- `uv run pytest -x` passes (full suite green, zero breakage)
- `from omnifocus_operator.models.enums import Urgency, Availability` works
- `from omnifocus_operator.bridge.adapter import adapt_snapshot` works
- Adapter correctly maps all old status values to new enum values
</verification>

<success_criteria>
- Urgency enum: 3 values (overdue, due_soon, none) in snake_case
- Availability enum: 4 values (available, blocked, completed, dropped) in snake_case
- adapt_snapshot transforms bridge-format dicts to new model shape
- Dead fields removed by adapter (active, effectiveActive, completed, etc.)
- Unknown status values raise ValueError (fail-fast)
- All 182+ existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/10-model-overhaul/10-01-SUMMARY.md`
</output>
