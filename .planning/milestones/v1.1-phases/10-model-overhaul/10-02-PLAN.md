---
phase: 10-model-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/omnifocus_operator/models/base.py
  - src/omnifocus_operator/models/task.py
  - src/omnifocus_operator/models/project.py
  - src/omnifocus_operator/models/tag.py
  - src/omnifocus_operator/models/enums.py
  - src/omnifocus_operator/models/__init__.py
  - tests/conftest.py
  - tests/test_models.py
  - tests/test_server.py
  - tests/test_ipc_engine.py
autonomous: true
requirements: [MODEL-03, MODEL-04, MODEL-05, MODEL-06]

must_haves:
  truths:
    - "Task model has urgency and availability fields instead of status"
    - "Project model has urgency and availability fields instead of status, task_status, and contains_singleton_actions"
    - "Tag model no longer has allows_next_action field"
    - "OmniFocusEntity no longer has active or effective_active fields"
    - "ActionableEntity no longer has completed, completed_by_children, sequential, or should_use_floating_time_zone fields"
    - "TaskStatus and ProjectStatus enums no longer exist in the codebase"
    - "All existing enum values use snake_case (TagStatus, FolderStatus, ScheduleType, AnchorDateKey)"
    - "All Python tests pass with new model shape"
  artifacts:
    - path: "src/omnifocus_operator/models/base.py"
      provides: "Simplified OmniFocusEntity and ActionableEntity without deprecated fields"
      contains: "class ActionableEntity"
    - path: "src/omnifocus_operator/models/task.py"
      provides: "Task with urgency + availability instead of status"
      contains: "urgency: Urgency"
    - path: "src/omnifocus_operator/models/project.py"
      provides: "Project with urgency + availability instead of status + task_status"
      contains: "availability: Availability"
    - path: "src/omnifocus_operator/models/enums.py"
      provides: "Only new enums (TaskStatus and ProjectStatus deleted)"
    - path: "tests/conftest.py"
      provides: "Updated factories returning new-shape dicts"
  key_links:
    - from: "src/omnifocus_operator/models/task.py"
      to: "src/omnifocus_operator/models/enums.py"
      via: "TYPE_CHECKING import of Urgency and Availability"
      pattern: "from omnifocus_operator.models.enums import.*Urgency"
    - from: "src/omnifocus_operator/models/__init__.py"
      to: "src/omnifocus_operator/models/enums.py"
      via: "_ns dict and model_rebuild with Urgency/Availability"
      pattern: "Urgency.*_ns"
    - from: "tests/conftest.py"
      to: "src/omnifocus_operator/models/task.py"
      via: "Factory dicts match model field expectations"
      pattern: "urgency.*availability"
---

<objective>
Migrate all Pydantic models from old status enums to two-axis model (Urgency + Availability), remove deprecated fields, update all existing enum values to snake_case, delete old enums, and update all test factories and assertions.

Purpose: This is the core model overhaul -- after this plan, the entire Python model layer uses the new two-axis status model. Tests stay green at every commit via coordinated model+factory changes.
Output: All models use new shape, old enums deleted, all Python tests pass.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-model-overhaul/10-CONTEXT.md
@.planning/phases/10-model-overhaul/10-RESEARCH.md
@.planning/phases/10-model-overhaul/10-01-SUMMARY.md

@src/omnifocus_operator/models/base.py
@src/omnifocus_operator/models/task.py
@src/omnifocus_operator/models/project.py
@src/omnifocus_operator/models/tag.py
@src/omnifocus_operator/models/folder.py
@src/omnifocus_operator/models/enums.py
@src/omnifocus_operator/models/__init__.py
@tests/conftest.py
@tests/test_models.py
@tests/test_server.py
@tests/test_ipc_engine.py

<interfaces>
<!-- From Plan 01 -- new enums and adapter -->
From src/omnifocus_operator/models/enums.py (added in Plan 01):
```python
class Urgency(StrEnum):
    OVERDUE = "overdue"
    DUE_SOON = "due_soon"
    NONE = "none"

class Availability(StrEnum):
    AVAILABLE = "available"
    BLOCKED = "blocked"
    COMPLETED = "completed"
    DROPPED = "dropped"
```

<!-- Target model hierarchy (from RESEARCH.md) -->
Target hierarchy after this plan:
```
OmniFocusBaseModel (unchanged)
  +-- OmniFocusEntity (id, name, url, added, modified) -- active/effective_active REMOVED
      +-- ActionableEntity (urgency, availability, note, flagged, effective_flagged, dates, tags, estimated_minutes, has_children, repetition_rule)
          +-- Task (in_inbox, project, parent)
          +-- Project (last_review_date, next_review_date, review_interval, next_task, folder)
      +-- Tag (status: TagStatus, children_are_mutually_exclusive, parent)
      +-- Folder (status: FolderStatus, parent)
```

<!-- Field count targets (from RESEARCH.md) -->
Field counts after migration:
- Task: 25 fields (was 32, -7 removed +2 added)
- Project: 28 fields (was 36, -8 removed +2 added)
- Tag: 8 fields (was 11, -3 removed)
- Folder: 7 fields (was 9, -2 removed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update models -- remove deprecated fields, add urgency/availability, switch enum values to snake_case, delete old enums</name>
  <files>
    src/omnifocus_operator/models/enums.py,
    src/omnifocus_operator/models/base.py,
    src/omnifocus_operator/models/task.py,
    src/omnifocus_operator/models/project.py,
    src/omnifocus_operator/models/tag.py,
    src/omnifocus_operator/models/__init__.py
  </files>
  <action>
**CRITICAL: This task modifies models only. The next task updates factories and tests to match. Do NOT run tests between -- they will fail until factories are updated.**

Make the following changes incrementally, but commit them together with the factory/test updates in Task 2:

**enums.py:**
- Update `TagStatus` values to snake_case: `ACTIVE = "active"`, `ON_HOLD = "on_hold"`, `DROPPED = "dropped"`
- Update `FolderStatus` values to snake_case: `ACTIVE = "active"`, `DROPPED = "dropped"`
- Update `ScheduleType` values to snake_case: `REGULARLY = "regularly"`, `FROM_COMPLETION = "from_completion"`, `NONE = "none"`
- Update `AnchorDateKey` values to snake_case: `DUE_DATE = "due_date"`, `DEFER_DATE = "defer_date"`, `PLANNED_DATE = "planned_date"`
- DELETE `TaskStatus` class entirely
- DELETE `ProjectStatus` class entirely
- Keep `Urgency` and `Availability` (added in Plan 01)

**base.py:**
- `OmniFocusEntity`: Remove `active: bool` and `effective_active: bool` fields. Update docstring to reflect removed fields.
- `ActionableEntity`: Remove `completed: bool`, `completed_by_children: bool`, `sequential: bool`, `should_use_floating_time_zone: bool`. Add `urgency: Urgency` and `availability: Availability` fields (required, no default). Add TYPE_CHECKING import for Urgency and Availability from enums. Update docstring.

**task.py:**
- Remove `status: TaskStatus` field and its TYPE_CHECKING import
- Remove TaskStatus from TYPE_CHECKING import block (Urgency/Availability now come from parent class)
- `in_inbox`, `project`, `parent` remain
- Update docstring -- no more "status" mention

**project.py:**
- Remove `status: ProjectStatus`, `task_status: TaskStatus`, `contains_singleton_actions: bool` fields
- Remove TYPE_CHECKING imports for `ProjectStatus`, `TaskStatus`
- Keep: `last_review_date`, `next_review_date`, `review_interval`, `next_task`, `folder`
- Update docstring

**tag.py:**
- Remove `allows_next_action: bool` field
- Keep: `status: TagStatus`, `children_are_mutually_exclusive`, `parent`
- Update docstring

**__init__.py:**
- Remove `TaskStatus`, `ProjectStatus` from imports, `_ns` dict, and `__all__`
- Add `Urgency`, `Availability` to imports, `_ns` dict, and `__all__`
- model_rebuild calls stay the same (same models, different namespace contents)

**PITFALL WARNING:** Update `_ns` dict in the SAME change as removing old enums from models -- otherwise `PydanticUndefinedAnnotation` at import time (Pitfall 3 from research).
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run python -c "from omnifocus_operator.models import Task, Project, Tag, Folder, Urgency, Availability; print('imports OK')"</automated>
  </verify>
  <done>All model files updated to new shape. Imports work without PydanticUndefinedAnnotation. Old enums deleted. New enums in namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Update all test factories, test assertions, and inline fixtures to new model shape</name>
  <files>
    tests/conftest.py,
    tests/test_models.py,
    tests/test_server.py,
    tests/test_ipc_engine.py
  </files>
  <action>
Update all test infrastructure to match the new model shape from Task 1. **Tests MUST pass after this task.**

**conftest.py -- Update ALL factory functions:**

`make_task_dict()`: Remove keys: `active`, `effectiveActive`, `status`, `completed`, `completedByChildren`, `sequential`, `shouldUseFloatingTimeZone`. Add keys: `"urgency": "none"`, `"availability": "available"`. Update field count comment (32 -> 25). These factories now return NEW-shape dicts (what InMemoryBridge provides directly, NOT bridge format).

`make_project_dict()`: Remove keys: `active`, `effectiveActive`, `status`, `taskStatus`, `completed`, `completedByChildren`, `sequential`, `shouldUseFloatingTimeZone`, `containsSingletonActions`. Add keys: `"urgency": "none"`, `"availability": "available"`. Update field count comment (36 -> 28).

`make_tag_dict()`: Remove keys: `active`, `effectiveActive`, `allowsNextAction`. Update `"status": "Active"` to `"status": "active"` (snake_case). Update field count comment (11 -> 8).

`make_folder_dict()`: Remove keys: `active`, `effectiveActive`. Update `"status": "Active"` to `"status": "active"` (snake_case). Update field count comment (9 -> 7).

`make_snapshot_dict()`: No structural change needed (it delegates to entity factories).

**test_models.py -- Update all model tests (~53 occurrences):**

For every test that:
- References `active`, `effective_active` on any entity -> remove those assertions
- References `completed`, `completed_by_children`, `sequential`, `should_use_floating_time_zone` on Task/Project -> remove those assertions
- References `status` on Task (TaskStatus) -> change to `urgency`/`availability` assertions
- References `status`/`task_status` on Project -> change to `urgency`/`availability` assertions
- References `contains_singleton_actions` on Project -> remove assertion
- References `allows_next_action` on Tag -> remove assertion
- Has field count assertions (e.g., `len(Task.model_fields)`) -> update to new counts (Task: 25->count task model_fields, Project: 28->count, Tag: 8->count, Folder: 7->count)
- Imports TaskStatus or ProjectStatus -> change to Urgency/Availability
- Uses enum values like `TaskStatus.AVAILABLE` -> change to `Urgency.NONE` + `Availability.AVAILABLE`
- Uses `TagStatus.ACTIVE` (PascalCase "Active") -> now `TagStatus.ACTIVE` (snake_case "active"), same member name but string value changed

**test_server.py -- Update inline fixture data (~5 occurrences):**
- Any inline snapshot dicts must match new shape (same changes as factories)
- Any assertions on model fields must reference new field names

**test_ipc_engine.py -- Update if needed (~2 occurrences):**
- Likely minimal -- check for any enum imports or field references that changed

**Commit strategy:** Commit all model changes (Task 1) together with all test changes (Task 2) in a SINGLE commit. This ensures tests are never broken. The commit message should be `feat(10-02): migrate models to two-axis status model (urgency + availability)`.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest -x -q</automated>
  </verify>
  <done>All Python tests pass with new model shape. No test references old enums (TaskStatus, ProjectStatus) or removed fields (active, effectiveActive, completed, etc.). Field count assertions updated.</done>
</task>

</tasks>

<verification>
- `uv run pytest -x` passes (full Python suite green)
- `grep -r "TaskStatus\|ProjectStatus" src/omnifocus_operator/models/` returns nothing
- `grep -r "active.*bool\|completed.*bool\|sequential.*bool" src/omnifocus_operator/models/base.py` returns nothing
- `grep -r "allows_next_action" src/omnifocus_operator/models/tag.py` returns nothing
- `grep -r "contains_singleton_actions" src/omnifocus_operator/models/project.py` returns nothing
</verification>

<success_criteria>
- Task.urgency and Task.availability exist; Task.status does not
- Project.urgency and Project.availability exist; Project.status, Project.task_status, Project.contains_singleton_actions do not
- Tag.allows_next_action does not exist
- OmniFocusEntity has no active/effective_active fields
- ActionableEntity has no completed/completed_by_children/sequential/should_use_floating_time_zone fields
- TaskStatus and ProjectStatus classes deleted from enums.py
- TagStatus, FolderStatus, ScheduleType, AnchorDateKey use snake_case values
- All 182+ Python tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-model-overhaul/10-02-SUMMARY.md`
</output>
