---
phase: 11-datasource-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/repository/__init__.py
  - src/omnifocus_operator/repository/protocol.py
  - src/omnifocus_operator/repository/bridge.py
  - src/omnifocus_operator/repository/in_memory.py
  - src/omnifocus_operator/bridge/mtime.py
  - src/omnifocus_operator/bridge/__init__.py
  - src/omnifocus_operator/repository.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03

must_haves:
  truths:
    - "Repository protocol exists with get_snapshot() method"
    - "BridgeRepository wraps Bridge + MtimeSource + adapter with caching"
    - "InMemoryRepository returns pre-built DatabaseSnapshot without bridge"
    - "MtimeSource, FileMtimeSource, ConstantMtimeSource live in bridge/mtime.py"
    - "Old repository.py file is deleted"
  artifacts:
    - path: "src/omnifocus_operator/repository/protocol.py"
      provides: "Repository protocol definition"
      exports: ["Repository"]
    - path: "src/omnifocus_operator/repository/bridge.py"
      provides: "BridgeRepository implementation"
      exports: ["BridgeRepository"]
    - path: "src/omnifocus_operator/repository/in_memory.py"
      provides: "InMemoryRepository for testing"
      exports: ["InMemoryRepository"]
    - path: "src/omnifocus_operator/bridge/mtime.py"
      provides: "MtimeSource protocol and implementations"
      exports: ["MtimeSource", "FileMtimeSource", "ConstantMtimeSource"]
  key_links:
    - from: "src/omnifocus_operator/repository/bridge.py"
      to: "src/omnifocus_operator/bridge/mtime.py"
      via: "imports MtimeSource"
      pattern: "from omnifocus_operator\\.bridge\\.mtime import"
    - from: "src/omnifocus_operator/repository/bridge.py"
      to: "src/omnifocus_operator/bridge/adapter.py"
      via: "imports adapt_snapshot"
      pattern: "from omnifocus_operator\\.bridge\\.adapter import adapt_snapshot"
    - from: "src/omnifocus_operator/repository/__init__.py"
      to: "repository/protocol.py, repository/bridge.py, repository/in_memory.py"
      via: "re-exports all public names"
      pattern: "from omnifocus_operator\\.repository\\.(protocol|bridge|in_memory) import"
---

<objective>
Create the repository package structure with protocol, implementations, and relocated MtimeSource.

Purpose: Establish the Repository protocol abstraction (ARCH-01) and InMemoryRepository (ARCH-03) so consumers can be wired in Plan 02. Also relocates MtimeSource into bridge package where it belongs (bridge-internal concern).

Output: `repository/` package with 3 modules, `bridge/mtime.py`, old `repository.py` deleted.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-datasource-protocol/11-CONTEXT.md
@.planning/phases/11-datasource-protocol/11-RESEARCH.md

@src/omnifocus_operator/repository.py
@src/omnifocus_operator/bridge/__init__.py
@src/omnifocus_operator/bridge/protocol.py
@src/omnifocus_operator/bridge/adapter.py
@src/omnifocus_operator/models/snapshot.py

<interfaces>
<!-- Current repository.py exports that must be preserved in new locations -->
From src/omnifocus_operator/repository.py:
```python
@runtime_checkable
class MtimeSource(Protocol):
    async def get_mtime_ns(self) -> int: ...

class FileMtimeSource:
    def __init__(self, path: str) -> None: ...
    async def get_mtime_ns(self) -> int: ...

class ConstantMtimeSource:
    async def get_mtime_ns(self) -> int: ...  # always returns 0

class OmniFocusRepository:
    def __init__(self, bridge: Bridge, mtime_source: MtimeSource) -> None: ...
    async def get_snapshot(self) -> DatabaseSnapshot: ...
```

From src/omnifocus_operator/bridge/protocol.py:
```python
@runtime_checkable
class Bridge(Protocol):
    async def send_command(self, operation: str, payload: dict[str, Any] | None = None) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bridge/mtime.py and repository/ package</name>
  <files>
    src/omnifocus_operator/bridge/mtime.py,
    src/omnifocus_operator/bridge/__init__.py,
    src/omnifocus_operator/repository/__init__.py,
    src/omnifocus_operator/repository/protocol.py,
    src/omnifocus_operator/repository/bridge.py,
    src/omnifocus_operator/repository/in_memory.py
  </files>
  <action>
    **Step 1: Create `bridge/mtime.py`** -- Move `MtimeSource` protocol, `FileMtimeSource`, and `ConstantMtimeSource` from `repository.py` into this new file. Keep exact same logic, docstrings, and imports. Use `from __future__ import annotations` and `TYPE_CHECKING` pattern consistent with project conventions.

    **Step 2: Update `bridge/__init__.py`** -- Add exports for `MtimeSource`, `FileMtimeSource`, `ConstantMtimeSource` from `bridge.mtime`. Add them to `__all__`.

    **Step 3: Create `repository/protocol.py`** -- Define `Repository` protocol with single method `async def get_snapshot(self) -> DatabaseSnapshot`. Use `@runtime_checkable`, `from __future__ import annotations`, `TYPE_CHECKING` for DatabaseSnapshot import, project docstring style. See RESEARCH.md Pattern 1.

    **Step 4: Create `repository/bridge.py`** -- Create `BridgeRepository` class. This is a copy of the current `OmniFocusRepository` from `repository.py` with these changes:
    - Rename class to `BridgeRepository`
    - Import `MtimeSource` from `omnifocus_operator.bridge.mtime` (not from repository)
    - Import `Bridge` from `omnifocus_operator.bridge.protocol`
    - Import `adapt_snapshot` from `omnifocus_operator.bridge.adapter`
    - Import `DatabaseSnapshot` from `omnifocus_operator.models.snapshot`
    - Keep all caching logic, asyncio.Lock, _refresh method identical
    - Dependency direction: repository/bridge.py -> bridge/ (never the reverse)

    **Step 5: Create `repository/in_memory.py`** -- Create `InMemoryRepository` class:
    - Constructor accepts `snapshot: DatabaseSnapshot`
    - `get_snapshot()` returns `self._snapshot` directly (no caching, no locking, no adapter)
    - Use `from __future__ import annotations` and `TYPE_CHECKING` for DatabaseSnapshot

    **Step 6: Create `repository/__init__.py`** -- Export `Repository`, `BridgeRepository`, `InMemoryRepository` from their submodules. Also re-export `MtimeSource`, `FileMtimeSource`, `ConstantMtimeSource` from `omnifocus_operator.bridge.mtime` for backward compatibility during migration (Plan 02 will update all import sites, but this prevents breakage in the same commit). Include `__all__`.

    CRITICAL: Do NOT delete `repository.py` yet in this task. Both `repository.py` (file) and `repository/` (package) cannot coexist. Instead, the deletion happens atomically: delete `repository.py`, create `repository/` package in the same git operation. To achieve this, first create all new files in a temporary holding area or create the package files directly and delete `repository.py` in the same task before running tests.

    Actually, the correct approach: Delete `repository.py` FIRST, then create `repository/__init__.py` (and other package files). Python cannot have both a `repository.py` module and a `repository/` package. The `__init__.py` re-exports ensure nothing breaks.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - `repository.py` deleted, `repository/` package exists with protocol.py, bridge.py, in_memory.py, __init__.py
    - `bridge/mtime.py` exists with MtimeSource, FileMtimeSource, ConstantMtimeSource
    - `bridge/__init__.py` exports mtime classes
    - `repository/__init__.py` re-exports everything (including mtime classes for backward compat)
    - All 233 tests pass (imports resolve through re-exports)
    - `isinstance(InMemoryRepository(snapshot), Repository)` works (runtime_checkable)
    - `isinstance(BridgeRepository(bridge, mtime), Repository)` works
  </done>
</task>

</tasks>

<verification>
- `uv run python -m pytest tests/ -x -q` -- all 233 tests green
- `uv run python -c "from omnifocus_operator.repository import Repository, BridgeRepository, InMemoryRepository; print('imports OK')"` -- clean imports
- `uv run python -c "from omnifocus_operator.bridge.mtime import MtimeSource, FileMtimeSource, ConstantMtimeSource; print('mtime imports OK')"` -- mtime relocated
- No file `src/omnifocus_operator/repository.py` exists (only `repository/` directory)
</verification>

<success_criteria>
Repository package created with protocol + two implementations. MtimeSource relocated to bridge package. All tests pass through re-exported imports. Old repository.py deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/11-datasource-protocol/11-01-SUMMARY.md`
</output>
