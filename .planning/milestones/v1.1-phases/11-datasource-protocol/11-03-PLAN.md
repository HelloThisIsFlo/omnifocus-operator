---
phase: 11-datasource-protocol
plan: 03
type: execute
wave: 1
depends_on: ["11-01", "11-02"]
files_modified:
  - src/omnifocus_operator/models/snapshot.py
  - src/omnifocus_operator/models/__init__.py
  - src/omnifocus_operator/repository/protocol.py
  - src/omnifocus_operator/repository/bridge.py
  - src/omnifocus_operator/repository/in_memory.py
  - src/omnifocus_operator/service.py
  - src/omnifocus_operator/server.py
  - tests/conftest.py
  - tests/test_models.py
  - tests/test_repository.py
  - tests/test_service.py
  - uat/README.md
  - uat/test_read_only.py
  - uat/test_model_overhaul.py
  - docs/architecture.md
autonomous: true
gap_closure: true
requirements: [ARCH-01, ARCH-02, ARCH-03]

must_haves:
  truths:
    - "Repository protocol method is get_all() not get_snapshot()"
    - "Return type is AllEntities not DatabaseSnapshot"
    - "All 236+ tests pass with new names"
    - "No references to DatabaseSnapshot or get_snapshot remain in src/ or tests/"
  artifacts:
    - path: "src/omnifocus_operator/models/snapshot.py"
      provides: "AllEntities model class (renamed from DatabaseSnapshot)"
      contains: "class AllEntities"
    - path: "src/omnifocus_operator/repository/protocol.py"
      provides: "Repository protocol with get_all() method"
      contains: "async def get_all"
  key_links:
    - from: "src/omnifocus_operator/repository/protocol.py"
      to: "src/omnifocus_operator/models/snapshot.py"
      via: "TYPE_CHECKING import of AllEntities"
      pattern: "AllEntities"
    - from: "src/omnifocus_operator/service.py"
      to: "src/omnifocus_operator/repository/protocol.py"
      via: "calls repository.get_all()"
      pattern: "get_all\\(\\)"
---

<objective>
Rename DatabaseSnapshot to AllEntities and get_snapshot() to get_all() across the entire codebase.

Purpose: The snapshot metaphor no longer applies at the protocol boundary now that caching moved inside BridgeRepository. The new naming establishes a convention: `get_*` for structured multi-type containers, `list_*` for flat filtered collections (future v1.2).

Output: All references updated, all tests green, zero leftover references to old names.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-datasource-protocol/11-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename DatabaseSnapshot to AllEntities in models</name>
  <files>
    src/omnifocus_operator/models/snapshot.py
    src/omnifocus_operator/models/__init__.py
  </files>
  <action>
In `models/snapshot.py`:
- Rename class `DatabaseSnapshot` to `AllEntities`
- Update module docstring: replace "DatabaseSnapshot model" with "AllEntities model" and update description to say "aggregates all OmniFocus entity collections" (remove "snapshot" language)
- Update class docstring: replace snapshot references with "all OmniFocus entities" language

In `models/__init__.py`:
- Update import: `from omnifocus_operator.models.snapshot import AllEntities` (was DatabaseSnapshot)
- Update `model_rebuild` call: `AllEntities.model_rebuild(_types_namespace=_ns)`
- Update `__all__` list: replace "DatabaseSnapshot" with "AllEntities"

Note: Do NOT rename the file `snapshot.py` itself -- just rename the class within it. The file name is an internal detail.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run python -c "from omnifocus_operator.models import AllEntities; print('AllEntities imported OK')" && uv run python -c "from omnifocus_operator.models import DatabaseSnapshot" 2>&1 | grep -q "cannot import" && echo "DatabaseSnapshot correctly removed"</automated>
  </verify>
  <done>AllEntities class exists in models, DatabaseSnapshot does not, model_rebuild works</done>
</task>

<task type="auto">
  <name>Task 2: Rename get_snapshot to get_all and update all references</name>
  <files>
    src/omnifocus_operator/repository/protocol.py
    src/omnifocus_operator/repository/bridge.py
    src/omnifocus_operator/repository/in_memory.py
    src/omnifocus_operator/service.py
    src/omnifocus_operator/server.py
    tests/conftest.py
    tests/test_models.py
    tests/test_repository.py
    tests/test_service.py
    uat/README.md
    uat/test_read_only.py
    uat/test_model_overhaul.py
    docs/architecture.md
  </files>
  <action>
**Repository package** (rename method + update type refs):
- `repository/protocol.py`: Rename `get_snapshot` to `get_all`, update TYPE_CHECKING import to `AllEntities`, update docstrings
- `repository/bridge.py`: Rename `get_snapshot` to `get_all`, rename internal `_snapshot` field to `_cached` (or keep as-is since it's internal -- Claude's discretion), update all `DatabaseSnapshot` refs to `AllEntities`, update docstrings
- `repository/in_memory.py`: Rename `get_snapshot` to `get_all`, update TYPE_CHECKING import to `AllEntities`, update docstrings

**Service layer:**
- `service.py`: Change `get_snapshot()` call to `get_all()`, update TYPE_CHECKING import to `AllEntities`, update return type annotation and docstrings

**Server:**
- `server.py`: Update runtime import from `DatabaseSnapshot` to `AllEntities`, update type annotation on `list_all` return type

**Tests:**
- `tests/conftest.py`: Update import to `AllEntities`, update `make_snapshot` helper return type annotation and docstring (keep function name `make_snapshot` or rename to `make_all_entities` -- Claude's discretion, but stay consistent)
- `tests/test_models.py`: Replace all `DatabaseSnapshot` refs with `AllEntities`, rename test class `TestDatabaseSnapshot` to `TestAllEntities`
- `tests/test_repository.py`: Replace all `get_snapshot` calls with `get_all`
- `tests/test_service.py`: Replace `get_snapshot` mock references with `get_all`

**UAT scripts:**
- `uat/README.md`: Replace `DatabaseSnapshot` references with `AllEntities`
- `uat/test_read_only.py`: Update import and all references from `DatabaseSnapshot` to `AllEntities`
- `uat/test_model_overhaul.py`: Update import and all references from `DatabaseSnapshot` to `AllEntities`

**Docs:**
- `docs/architecture.md`: Update any remaining `DatabaseSnapshot` references. Note: line 39 already mentions the rename should happen -- update to reflect it IS `AllEntities` now (not "renamed to").

After all changes, run a final grep to confirm zero remaining references:
- `grep -r "DatabaseSnapshot" src/ tests/` should return nothing
- `grep -r "get_snapshot" src/ tests/` should return nothing
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run python -m pytest tests/ -x -q && echo "---" && grep -rn "DatabaseSnapshot\|get_snapshot" src/ tests/ | grep -v __pycache__ | head -5 || echo "No old references found"</automated>
  </verify>
  <done>All 236+ tests pass, zero references to DatabaseSnapshot or get_snapshot in src/ or tests/, get_all()/AllEntities used everywhere</done>
</task>

</tasks>

<verification>
- `uv run python -m pytest tests/ -q` -- all tests pass
- `grep -rn "DatabaseSnapshot" src/ tests/ docs/` -- zero matches
- `grep -rn "get_snapshot" src/ tests/` -- zero matches
- `uv run python -c "from omnifocus_operator.models import AllEntities; print(AllEntities.__name__)"` -- prints "AllEntities"
- `uv run python -c "from omnifocus_operator.repository import Repository; print(Repository.get_all)"` -- method exists
</verification>

<success_criteria>
- AllEntities class replaces DatabaseSnapshot everywhere
- get_all() method replaces get_snapshot() everywhere
- All tests pass (236+)
- Zero leftover references to old names in src/, tests/, docs/
- UAT scripts updated to use new names
</success_criteria>

<output>
After completion, create `.planning/phases/11-datasource-protocol/11-03-SUMMARY.md`
</output>
