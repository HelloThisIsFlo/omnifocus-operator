---
phase: 11-datasource-protocol
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/omnifocus_operator/service.py
  - src/omnifocus_operator/server.py
  - src/omnifocus_operator/repository/__init__.py
  - tests/conftest.py
  - tests/test_repository.py
  - tests/test_service.py
  - tests/test_server.py
  - docs/architecture.md
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03

must_haves:
  truths:
    - "Service layer accepts Repository protocol, not OmniFocusRepository concrete class"
    - "server.py creates BridgeRepository directly (not OmniFocusRepository)"
    - "Repository-level tests use InMemoryRepository, not InMemoryBridge + MtimeSource"
    - "Service-level tests use InMemoryRepository, not InMemoryBridge + MtimeSource"
    - "No import of OmniFocusRepository anywhere in the codebase"
    - "Architecture doc exists at docs/architecture.md"
  artifacts:
    - path: "src/omnifocus_operator/service.py"
      provides: "Service with Repository protocol type hint"
      contains: "Repository"
    - path: "src/omnifocus_operator/server.py"
      provides: "Server wiring with BridgeRepository"
      contains: "BridgeRepository"
    - path: "tests/conftest.py"
      provides: "make_snapshot() helper returning DatabaseSnapshot"
      exports: ["make_snapshot"]
    - path: "docs/architecture.md"
      provides: "Architecture overview document"
  key_links:
    - from: "src/omnifocus_operator/service.py"
      to: "src/omnifocus_operator/repository/protocol.py"
      via: "TYPE_CHECKING import of Repository"
      pattern: "from omnifocus_operator\\.repository import Repository"
    - from: "src/omnifocus_operator/server.py"
      to: "src/omnifocus_operator/repository/bridge.py"
      via: "imports BridgeRepository"
      pattern: "from omnifocus_operator\\.repository import BridgeRepository"
    - from: "src/omnifocus_operator/server.py"
      to: "src/omnifocus_operator/bridge/mtime.py"
      via: "imports MtimeSource implementations"
      pattern: "from omnifocus_operator\\.bridge\\.mtime import"
    - from: "tests/test_service.py"
      to: "src/omnifocus_operator/repository/in_memory.py"
      via: "uses InMemoryRepository"
      pattern: "InMemoryRepository"
---

<objective>
Update all consumers to use the new Repository protocol and migrate tests to InMemoryRepository. Create architecture documentation.

Purpose: Complete the refactoring by updating service.py, server.py, and all test files to use the new abstractions (ARCH-02). Migrate repository and service tests to InMemoryRepository (ARCH-03). Remove all backward-compat re-exports. Create architecture overview doc.

Output: Clean codebase with no references to OmniFocusRepository, all tests using InMemoryRepository, architecture doc.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-datasource-protocol/11-CONTEXT.md
@.planning/phases/11-datasource-protocol/11-RESEARCH.md
@.planning/phases/11-datasource-protocol/11-01-SUMMARY.md

@src/omnifocus_operator/service.py
@src/omnifocus_operator/server.py
@tests/conftest.py
@tests/test_repository.py
@tests/test_service.py
@tests/test_server.py

<interfaces>
<!-- From Plan 01 outputs -->
From src/omnifocus_operator/repository/protocol.py:
```python
@runtime_checkable
class Repository(Protocol):
    async def get_snapshot(self) -> DatabaseSnapshot: ...
```

From src/omnifocus_operator/repository/bridge.py:
```python
class BridgeRepository:
    def __init__(self, bridge: Bridge, mtime_source: MtimeSource) -> None: ...
    async def get_snapshot(self) -> DatabaseSnapshot: ...
```

From src/omnifocus_operator/repository/in_memory.py:
```python
class InMemoryRepository:
    def __init__(self, snapshot: DatabaseSnapshot) -> None: ...
    async def get_snapshot(self) -> DatabaseSnapshot: ...
```

From src/omnifocus_operator/bridge/mtime.py:
```python
class MtimeSource(Protocol):
    async def get_mtime_ns(self) -> int: ...
class FileMtimeSource:
    def __init__(self, path: str) -> None: ...
class ConstantMtimeSource:
    async def get_mtime_ns(self) -> int: ...  # always 0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update service.py, server.py, and migrate all tests</name>
  <files>
    src/omnifocus_operator/service.py,
    src/omnifocus_operator/server.py,
    tests/conftest.py,
    tests/test_repository.py,
    tests/test_service.py,
    tests/test_server.py
  </files>
  <action>
    **Step 1: Update `service.py`:**
    - Change TYPE_CHECKING import from `from omnifocus_operator.repository import OmniFocusRepository` to `from omnifocus_operator.repository import Repository`
    - Update `OperatorService.__init__` type hint: `repository: Repository` (not `OmniFocusRepository`)
    - Update docstrings to reference `Repository` protocol instead of `OmniFocusRepository`
    - `ErrorOperatorService` needs no changes (bypasses __init__)

    **Step 2: Update `server.py`:**
    - Change imports inside `app_lifespan()`:
      - `from omnifocus_operator.repository import OmniFocusRepository` -> `from omnifocus_operator.repository import BridgeRepository`
      - `from omnifocus_operator.repository import ConstantMtimeSource, MtimeSource` -> `from omnifocus_operator.bridge.mtime import ConstantMtimeSource, MtimeSource`
      - `from omnifocus_operator.repository import FileMtimeSource` -> `from omnifocus_operator.bridge.mtime import FileMtimeSource`
    - Change `repository = OmniFocusRepository(bridge=bridge, mtime_source=mtime_source)` to `repository = BridgeRepository(bridge=bridge, mtime_source=mtime_source)`
    - Update module docstring to reference `Repository` protocol
    - IMPORTANT: These are lazy imports inside `app_lifespan()` function body. Grep carefully.

    **Step 3: Add `make_snapshot()` helper to `tests/conftest.py`:**
    - Add function: `def make_snapshot(**overrides) -> DatabaseSnapshot:` that calls `DatabaseSnapshot.model_validate(make_snapshot_dict(**overrides))`
    - Import `DatabaseSnapshot` from `omnifocus_operator.models.snapshot`

    **Step 4: Migrate `tests/test_repository.py`:**
    - Keep BridgeRepository-specific tests (caching, mtime refresh, concurrency, error propagation) -- these test BridgeRepository behavior specifically. Update imports:
      - `from omnifocus_operator.repository import OmniFocusRepository` -> `from omnifocus_operator.repository import BridgeRepository`
      - `from omnifocus_operator.repository import FileMtimeSource` -> `from omnifocus_operator.bridge.mtime import FileMtimeSource`
    - Replace all `OmniFocusRepository` references with `BridgeRepository` in fixtures and test classes
    - Add new tests for InMemoryRepository:
      - `test_satisfies_repository_protocol`: `isinstance(InMemoryRepository(snapshot), Repository)` is True
      - `test_returns_snapshot`: returns the exact snapshot passed to constructor
      - `test_bridge_repository_satisfies_protocol`: `isinstance(BridgeRepository(bridge, mtime), Repository)` is True

    **Step 5: Migrate `tests/test_service.py`:**
    - Replace the `InMemoryBridge + FakeMtimeSource + OmniFocusRepository` pattern with `InMemoryRepository`:
      ```python
      # Before:
      bridge = InMemoryBridge(data=make_snapshot_dict())
      mtime = FakeMtimeSource()
      repo = OmniFocusRepository(bridge=bridge, mtime_source=mtime)

      # After:
      from omnifocus_operator.repository import InMemoryRepository
      snapshot = make_snapshot()  # from conftest
      repo = InMemoryRepository(snapshot=snapshot)
      ```
    - Remove `FakeMtimeSource` class (no longer needed in this file)
    - Remove imports of `InMemoryBridge`, `OmniFocusRepository` from this file
    - The `TestOperatorService.test_get_all_data_delegates_to_repository` test needs rethinking: it currently checks `bridge.call_count`. With InMemoryRepository, verify the snapshot is returned (delegation is implicit). Replace with a test that confirms the snapshot returned by service matches the one given to InMemoryRepository.
    - `TestOperatorService.test_get_all_data_propagates_errors`: With InMemoryRepository there's no bridge to error. This test belongs in test_repository.py (BridgeRepository tests). Remove from test_service.py OR replace with a test that the service propagates a repository error (e.g. mock repository that raises).
    - Keep `TestConstantMtimeSource` tests -- update imports from `omnifocus_operator.bridge.mtime` instead of `omnifocus_operator.repository`
    - Keep `TestCreateBridge`, `TestErrorOperatorService` tests unchanged
    - Update `test_satisfies_mtime_protocol` import: `from omnifocus_operator.bridge.mtime import MtimeSource`

    **Step 6: Migrate `tests/test_server.py`:**
    - Update TYPE_CHECKING imports: `OmniFocusRepository` -> `BridgeRepository`, `from omnifocus_operator.repository import ...` -> `from omnifocus_operator.repository import BridgeRepository`
    - Update `_build_patched_server` parameter type: `repo: BridgeRepository` (or just `repo: Repository`)
    - In `test_list_all_structured_content_is_camelcase`: replace `OmniFocusRepository(bridge=bridge, mtime_source=ConstantMtimeSource())` with `BridgeRepository(bridge=bridge, mtime_source=ConstantMtimeSource())`. Import `ConstantMtimeSource` from `omnifocus_operator.bridge.mtime`.

    **Step 7: Clean up `repository/__init__.py`:**
    - Remove backward-compat re-exports of `MtimeSource`, `FileMtimeSource`, `ConstantMtimeSource` (if Plan 01 added them). These should only be importable from `omnifocus_operator.bridge.mtime` or `omnifocus_operator.bridge`.
    - Remove any `OmniFocusRepository` alias.
    - Final exports: `Repository`, `BridgeRepository`, `InMemoryRepository` only.

    **Step 8: Final grep validation:**
    - `grep -r "OmniFocusRepository" src/ tests/` must return ZERO results
    - `grep -r "from omnifocus_operator.repository import.*MtimeSource" src/ tests/` must return ZERO results (mtime imports come from bridge)
    - `grep -r "from omnifocus_operator.repository import.*ConstantMtimeSource" src/ tests/` must return ZERO results
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run python -m pytest tests/ -x -q && grep -r "OmniFocusRepository" src/ tests/ && echo "FAIL: OmniFocusRepository still referenced" || echo "CLEAN: No OmniFocusRepository references"</automated>
  </verify>
  <done>
    - service.py type hint is `Repository` (protocol)
    - server.py creates `BridgeRepository`, imports mtime from `bridge.mtime`
    - test_service.py uses InMemoryRepository (no InMemoryBridge/FakeMtimeSource)
    - test_repository.py uses BridgeRepository name, has InMemoryRepository protocol tests
    - test_server.py updated imports
    - Zero references to `OmniFocusRepository` in src/ or tests/
    - Zero imports of MtimeSource from `omnifocus_operator.repository`
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create architecture overview document</name>
  <files>docs/architecture.md</files>
  <action>
    Create `docs/architecture.md` at project root. Keep it concise and scannable (per user's documentation style preference: bullet points over prose, outline density).

    Content should cover:
    - **Architecture diagram** (ASCII): `MCP Tool -> Service -> Repository (protocol) -> {BridgeRepository, InMemoryRepository, SQLiteRepository (Phase 12)}`
    - **Why Repository, not DataSource**: Repository implies querying/filtering (v1.2 `get_tasks(filters)`), DataSource implies raw data access. Repository is the richer abstraction.
    - **Why flat packages (bridge/ and repository/ as peers)**: Bridge is a general OmniFocus communication channel (not just data access). It will be used directly for non-data operations in future milestones (perspective switching, UI actions).
    - **Caching strategy**: No caching in primary path (SQLite ~46ms). BridgeRepository has mtime-based caching for fallback mode. InMemoryRepository and future SQLiteRepository have no caching.
    - **Deferred decisions**: Whether writes go through Repository or Bridge directly (future milestone). SQLiteRepository naming may change.
    - **Package dependency direction**: `repository/ -> bridge/` (never reverse). Service -> Repository protocol (never concrete). Server wires concrete implementations.

    Keep it under 80 lines. No prose paragraphs -- structured outline with headers and bullets.
  </action>
  <verify>
    <automated>test -f /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/docs/architecture.md && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>
    - `docs/architecture.md` exists with architecture overview
    - Covers: diagram, Repository vs DataSource rationale, flat packages rationale, caching strategy, deferred decisions
    - Concise (under 80 lines, bullet-point style)
  </done>
</task>

</tasks>

<verification>
- `uv run python -m pytest tests/ -q` -- full test suite green (233+ tests)
- `grep -r "OmniFocusRepository" src/ tests/` -- zero results
- `grep -r "from omnifocus_operator.repository import.*MtimeSource" src/ tests/` -- zero results
- `docs/architecture.md` exists
- `uv run python -c "from omnifocus_operator.repository import Repository, BridgeRepository, InMemoryRepository; print('OK')"` -- clean imports
- `uv run python -c "from omnifocus_operator.bridge.mtime import MtimeSource, FileMtimeSource, ConstantMtimeSource; print('OK')"` -- clean imports
</verification>

<success_criteria>
All consumers use Repository protocol. All tests use InMemoryRepository where appropriate. Zero references to OmniFocusRepository. Architecture doc exists. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-datasource-protocol/11-02-SUMMARY.md`
</output>
