---
phase: 28-expand-golden-master-coverage-and-improve-field-normalization
plan: 03
type: execute
wave: 2
depends_on: [28-01, 28-02]
files_modified:
  - tests/golden_master/snapshots/initial_state.json
  - tests/golden_master/snapshots/01-add/*.json
  - tests/golden_master/snapshots/02-edit/*.json
  - tests/golden_master/snapshots/03-move/*.json
  - tests/golden_master/snapshots/04-tags/*.json
  - tests/golden_master/snapshots/05-lifecycle/*.json
  - tests/golden_master/snapshots/06-combined/*.json
  - tests/golden_master/snapshots/07-inheritance/*.json
autonomous: false
requirements: [GOLD-01, GOLD-02]

user_setup:
  - service: omnifocus
    why: "Golden master capture requires running against live OmniFocus database (SAFE-01/02)"
    dashboard_config:
      - task: "Create project: 🧪 GM-TestProject (if not already present)"
        location: "OmniFocus -> Projects"
      - task: "Create project: 🧪 GM-TestProject2 (for cross-project moves)"
        location: "OmniFocus -> Projects"
      - task: "Create project: 🧪 GM-TestProject-Dated with dueDate, deferDate set, flagged=true"
        location: "OmniFocus -> Projects -> Inspector"
      - task: "Create tag: 🧪 GM-Tag1 (if not already present)"
        location: "OmniFocus -> Tags"
      - task: "Create tag: 🧪 GM-Tag2 (if not already present)"
        location: "OmniFocus -> Tags"

must_haves:
  truths:
    - "Golden master fixtures captured from live OmniFocus in subfolder layout"
    - "~43 scenario JSON files exist across 7 subfolders"
    - "initial_state.json includes 3 projects and 2 tags"
    - "Old flat scenario files removed (clean slate per D-03)"
  artifacts:
    - path: "tests/golden_master/snapshots/initial_state.json"
      provides: "Seed state with 3 projects and 2 tags"
    - path: "tests/golden_master/snapshots/01-add/"
      provides: "6 add_task scenario fixtures"
    - path: "tests/golden_master/snapshots/07-inheritance/"
      provides: "Inheritance scenario fixtures"
  key_links:
    - from: "tests/golden_master/snapshots/"
      to: "tests/test_bridge_contract.py"
      via: "subfolder discovery in _load_scenarios"
      pattern: "SNAPSHOTS_DIR"
---

<objective>
Human runs the capture script against live OmniFocus to produce golden master fixtures in the new subfolder layout.

Purpose: Capture real OmniFocus bridge behavior as the source of truth for contract tests. This is a human-only step (SAFE-01/02).
Output: ~43 JSON fixture files in numbered subfolders under tests/golden_master/snapshots/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-expand-golden-master-coverage-and-improve-field-normalization/28-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run golden master capture against live OmniFocus</name>
  <files>tests/golden_master/snapshots/</files>
  <action>
Human runs the capture script (SAFE-01/02 -- agent cannot do this).

Plan 01 updated InMemoryBridge with inheritance helpers, graduated 9 fields from VOLATILE/UNCOMPUTED, and updated contract test discovery for subfolders.
Plan 02 rewrote the capture script with ~43 scenarios across 7 categories.
Now the golden master needs to be re-captured from live OmniFocus.

Steps:
1. Ensure OmniFocus is running with bridge script installed
2. Run: `uv run python uat/capture_golden_master.py`
3. Follow interactive prompts (create/verify 3 projects + 2 tags, clean leftover tasks, confirm preview)
4. For GM-TestProject-Dated: set dueDate, deferDate, flagged=true in OmniFocus Inspector
5. Wait for ~43 scenarios to execute
6. Verify subfolder structure: `ls tests/golden_master/snapshots/`
7. Verify count: `find tests/golden_master/snapshots -name "*.json" -not -name "initial_state.json" | wc -l`
8. Clean up in OmniFocus: delete the GM-Cleanup task from inbox
9. Commit fixtures:
   ```
   git add tests/golden_master/snapshots/
   git rm tests/golden_master/snapshots/scenario_*.json 2>/dev/null || true
   git commit -m "test(28): re-capture golden master with 43 scenarios in subfolder layout"
   ```
  </action>
  <verify>
    <automated>ls /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/tests/golden_master/snapshots/01-add/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>Golden master fixtures captured in numbered subfolders. ~43 JSON files across 7 categories. Old flat files removed. Committed to git.</done>
</task>

</tasks>

<verification>
1. `ls tests/golden_master/snapshots/01-add/` shows 6 JSON files
2. `ls tests/golden_master/snapshots/07-inheritance/` shows scenario files
3. `cat tests/golden_master/snapshots/initial_state.json | python -c "import json,sys; d=json.load(sys.stdin); print(len(d['projects']), 'projects,', len(d['tags']), 'tags')"` shows 3 projects, 2 tags
4. No flat `scenario_*.json` files remain in snapshots root
</verification>

<success_criteria>
- Golden master fixtures captured in numbered subfolders
- ~43 scenario files across 7 categories
- initial_state.json includes 3 projects and 2 tags
- Old flat scenario files removed
- Fixtures committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/28-expand-golden-master-coverage-and-improve-field-normalization/28-03-SUMMARY.md`
</output>
