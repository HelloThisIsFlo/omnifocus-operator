---
phase: 28-expand-golden-master-coverage-and-improve-field-normalization
plan: 04
type: execute
wave: 3
depends_on: [28-03]
files_modified:
  - tests/doubles/bridge.py
  - tests/test_bridge_contract.py
  - tests/golden_master/normalize.py
autonomous: false
requirements: [GOLD-03, NORM-01, NORM-02, NORM-03, NORM-04]

must_haves:
  truths:
    - "All ~43 contract tests pass against InMemoryBridge"
    - "Presence-check fields show correct sentinel normalization"
    - "Inheritance scenarios verify effectiveDueDate/effectiveDeferDate/effectiveFlagged propagation"
    - "Full test suite passes"
  artifacts:
    - path: "tests/doubles/bridge.py"
      provides: "InMemoryBridge with any behavioral fixes discovered during triage"
    - path: "tests/test_bridge_contract.py"
      provides: "Contract tests passing for all scenarios"
  key_links:
    - from: "tests/test_bridge_contract.py"
      to: "tests/golden_master/snapshots/"
      via: "subfolder discovery and sequential replay"
      pattern: "SNAPSHOTS_DIR"
    - from: "tests/doubles/bridge.py"
      to: "tests/test_bridge_contract.py"
      via: "InMemoryBridge.send_command replayed by contract test"
      pattern: "InMemoryBridge"
---

<objective>
Interactive triage session: run contract tests against new golden master, diagnose failures, fix InMemoryBridge mismatches with user input on whether to match OmniFocus behavior or document as known divergence.

Purpose: Close the loop -- ensure InMemoryBridge produces equivalent output to real OmniFocus for all 43 scenarios. This is explicitly interactive (D-19): straightforward fixes done immediately, surprising behavior discussed before deciding.
Output: All contract tests passing, InMemoryBridge behavior verified against real OmniFocus output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-expand-golden-master-coverage-and-improve-field-normalization/28-CONTEXT.md
@.planning/phases/28-expand-golden-master-coverage-and-improve-field-normalization/28-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run contract tests and triage failures interactively</name>
  <files>tests/doubles/bridge.py, tests/test_bridge_contract.py, tests/golden_master/normalize.py</files>
  <action>
Interactive session (per D-19). Plans 01-03 established:
- InMemoryBridge with inheritance helpers
- Graduated field normalization
- Capture script with ~43 scenarios
- Freshly captured golden master fixtures

Run contract tests and triage:
1. Run: `uv run pytest tests/test_bridge_contract.py -x -v`
2. If all pass: skip to step 5
3. If failures: review the diff output together
   - Straightforward InMemoryBridge fixes (e.g., missing field computation, wrong default): fix immediately
   - Surprising OmniFocus behavior (e.g., unexpected field values, inheritance edge cases): discuss whether to match in InMemoryBridge or document as known divergence (add to UNCOMPUTED/VOLATILE)
4. After fixing, re-run: `uv run pytest tests/test_bridge_contract.py -x -v`
   Iterate until all pass
5. Run full suite: `uv run pytest`
6. All tests green = phase complete
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_bridge_contract.py -x -v --timeout=30 2>&1 | tail -5</automated>
  </verify>
  <done>All ~43 contract tests pass. Full test suite green. Any InMemoryBridge fixes committed. Any known divergences documented.</done>
</task>

</tasks>

<verification>
1. Contract tests: `uv run pytest tests/test_bridge_contract.py -x -v` -- all pass
2. Full suite: `uv run pytest` -- all pass
3. Presence-check scenarios: lifecycle/complete and lifecycle/drop scenarios pass with sentinel
4. Inheritance scenarios: 07-inheritance/* scenarios pass with correct effective field propagation
</verification>

<success_criteria>
- All ~43 contract tests pass
- All existing tests pass (no regressions)
- Any InMemoryBridge fixes committed with clear descriptions
- Any known divergences documented (added to UNCOMPUTED or commented)
</success_criteria>

<output>
After completion, create `.planning/phases/28-expand-golden-master-coverage-and-improve-field-normalization/28-04-SUMMARY.md`
</output>
