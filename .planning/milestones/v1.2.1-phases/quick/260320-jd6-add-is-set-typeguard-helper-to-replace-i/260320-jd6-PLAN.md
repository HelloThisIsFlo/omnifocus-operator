---
phase: quick-260320-jd6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/contracts/base.py
  - src/omnifocus_operator/contracts/common.py
  - src/omnifocus_operator/service/service.py
  - src/omnifocus_operator/service/domain.py
  - src/omnifocus_operator/service/payload.py
  - src/omnifocus_operator/service/validate.py
autonomous: true
requirements: [TODO-is-set-typeguard]

must_haves:
  truths:
    - "is_set() helper exists in contracts/base.py and is exported"
    - "All 21 isinstance(_Unset) checks replaced with is_set()"
    - "mypy passes with no new errors (and potentially fewer type: ignore comments)"
    - "All 534 tests pass unchanged"
  artifacts:
    - path: "src/omnifocus_operator/contracts/base.py"
      provides: "is_set TypeGuard helper function"
      exports: ["is_set"]
      contains: "TypeGuard"
    - path: "src/omnifocus_operator/contracts/common.py"
      provides: "TagAction/MoveAction validators using is_set"
      contains: "is_set("
    - path: "src/omnifocus_operator/service/service.py"
      provides: "edit_task orchestration using is_set"
      contains: "is_set("
    - path: "src/omnifocus_operator/service/domain.py"
      provides: "domain logic using is_set"
      contains: "is_set("
    - path: "src/omnifocus_operator/service/payload.py"
      provides: "payload builder using is_set"
      contains: "is_set("
    - path: "src/omnifocus_operator/service/validate.py"
      provides: "validate_task_name_if_set using is_set"
      contains: "is_set("
  key_links:
    - from: "src/omnifocus_operator/contracts/base.py"
      to: "all 5 consuming files"
      via: "is_set import"
      pattern: "from omnifocus_operator\\.contracts\\.base import.*is_set"
---

<objective>
Add `is_set()` TypeGuard helper to `contracts/base.py` and replace all 21 `isinstance(..., _Unset)` checks across 5 files.

Purpose: Improve readability (`is_set(x)` vs `not isinstance(x, _Unset)`) and gain mypy type narrowing via TypeGuard (may eliminate some `# type: ignore` comments in service.py).
Output: Cleaner sentinel checks across the codebase, no behavior changes.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/omnifocus_operator/contracts/base.py
@src/omnifocus_operator/contracts/common.py
@src/omnifocus_operator/service/service.py
@src/omnifocus_operator/service/domain.py
@src/omnifocus_operator/service/payload.py
@src/omnifocus_operator/service/validate.py

<interfaces>
<!-- Current _Unset sentinel from contracts/base.py -->
From src/omnifocus_operator/contracts/base.py:
```python
class _Unset: ...
UNSET = _Unset()
__all__ = ["UNSET", "CommandModel", "_Unset"]
```

<!-- Current import pattern used by all consumers -->
```python
from omnifocus_operator.contracts.base import _Unset
# or
from omnifocus_operator.contracts.base import UNSET, CommandModel, _Unset
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_set TypeGuard helper to contracts/base.py</name>
  <files>src/omnifocus_operator/contracts/base.py</files>
  <action>
Add the `is_set` TypeGuard helper function to `contracts/base.py`:

```python
from typing import TypeGuard, TypeVar

T = TypeVar("T")

def is_set(value: T | _Unset) -> TypeGuard[T]:
    """True if value was explicitly provided (not UNSET)."""
    return not isinstance(value, _Unset)
```

Place it immediately after the `UNSET = _Unset()` line.

Update `__all__` to include `is_set`:
```python
__all__ = ["UNSET", "CommandModel", "_Unset", "is_set"]
```

Note: Import `TypeGuard` from `typing` (Python 3.12+, no typing_extensions needed). Import `TypeVar` which is also in `typing`. The `from __future__ import annotations` is already present.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && python -c "from omnifocus_operator.contracts.base import is_set, UNSET, _Unset; assert is_set('hello'); assert not is_set(UNSET); print('OK')"</automated>
  </verify>
  <done>is_set function exported from contracts/base.py, returns True for real values and False for UNSET</done>
</task>

<task type="auto">
  <name>Task 2: Replace all isinstance _Unset checks with is_set across 5 files</name>
  <files>
    src/omnifocus_operator/contracts/common.py
    src/omnifocus_operator/service/service.py
    src/omnifocus_operator/service/domain.py
    src/omnifocus_operator/service/payload.py
    src/omnifocus_operator/service/validate.py
  </files>
  <action>
Replace all 21 `isinstance(..., _Unset)` patterns with `is_set()` across 5 files. The mapping:

- `not isinstance(x, _Unset)` becomes `is_set(x)`
- `isinstance(x, _Unset)` becomes `not is_set(x)`

**contracts/common.py** (4 occurrences):
- Update import: add `is_set` to the import from `contracts.base` (keep `_Unset` for type annotations on fields)
- TagAction validator (lines 27-29): `has_replace = is_set(self.replace)`, `has_add = is_set(self.add)`, `has_remove = is_set(self.remove)`
- MoveAction validator (line 63): `if is_set(v)` instead of `if not isinstance(v, _Unset)`

**service/service.py** (8 occurrences):
- Change import from inline `from omnifocus_operator.contracts.base import _Unset` to `from omnifocus_operator.contracts.base import is_set` (move to top-level imports since it's not TYPE_CHECKING-only -- `is_set` is called at runtime)
- Lines 149-163: Replace all the has_actions/has_lifecycle/has_tag_actions/has_move checks:
  ```python
  has_actions = is_set(command.actions)
  has_lifecycle = has_actions and is_set(command.actions.lifecycle)
  has_tag_actions = has_actions and is_set(command.actions.tags)
  has_move = has_actions and is_set(command.actions.move)
  ```
  Note: With TypeGuard narrowing, after `is_set(command.actions)` is True, mypy knows `command.actions` is `ActionsGroup` (not `_Unset`). So the redundant `not isinstance(command.actions, _Unset)` checks in the compound conditions go away. Additionally, check if the `# type: ignore[union-attr,assignment]` comments on lines 170, 184, 198 can now be REMOVED -- TypeGuard should narrow the type after `is_set()` succeeds. If mypy still complains (due to the bool variable indirection not propagating narrowing), keep the comments.

**service/domain.py** (7 occurrences):
- normalize_clear_intents (lines 85-94): Replace inline `from omnifocus_operator.contracts.base import _Unset` with `from omnifocus_operator.contracts.base import is_set`. Replace 3 checks: `if is_set(command.note) and command.note is None`, `if is_set(command.actions) and is_set(command.actions.tags)`, `if is_set(tag_actions.replace) and tag_actions.replace is None`
- compute_tag_diff (lines 158-164): Replace inline import, replace 3 checks: `has_replace = is_set(tag_actions.replace)`, `has_add = is_set(tag_actions.add)`, `has_remove = is_set(tag_actions.remove)`
- _extract_move_target (lines 324-328): Replace inline import, replace 1 check: `if is_set(value)`

**service/payload.py** (2 occurrences):
- _add_if_set (lines 97-101): Replace inline import, replace check: `if is_set(value)`
- _add_dates_if_set (lines 106-110): Replace inline import, replace check: `if is_set(value)`
  Both methods use inline imports -- consolidate to a single import at the top of the `_add_if_set` method or move `is_set` to a top-level import (preferred, since `is_set` is a lightweight function, not a heavy circular-dep risk like model classes). Check if existing inline imports are there to avoid circular imports. If `_Unset` was imported inline, `is_set` should be safe at top-level since it's just a function -- but follow the existing pattern for safety.

**service/validate.py** (1 occurrence):
- validate_task_name_if_set (line 21-23): Replace inline import, replace `if isinstance(name, _Unset)` with `if not is_set(name)`.

**Important:** Keep `_Unset` imports wherever `_Unset` is still used for type annotations (field defaults like `field: T | _Unset = UNSET` in common.py). Only remove `_Unset` from imports where it was ONLY used for isinstance checks.

After all replacements, run mypy to check if any `# type: ignore` comments in service.py (lines 170, 184, 198) can be removed. TypeGuard narrowing through boolean variables is limited -- mypy may not propagate narrowing from `has_actions = is_set(command.actions)` into a later `if has_actions:` block. If mypy still complains, keep the type: ignore comments (this is a known mypy limitation, not a regression).
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && python -m mypy src/omnifocus_operator/contracts/base.py src/omnifocus_operator/contracts/common.py src/omnifocus_operator/service/service.py src/omnifocus_operator/service/domain.py src/omnifocus_operator/service/payload.py src/omnifocus_operator/service/validate.py && python -m pytest tests/ -x -q --timeout=60</automated>
  </verify>
  <done>All 21 isinstance checks replaced with is_set(). Zero remaining `isinstance(*, _Unset)` in those 5 files (except type annotations). mypy clean, all tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -rn "isinstance.*_Unset" src/omnifocus_operator/` should return ZERO results
2. `grep -rn "is_set(" src/omnifocus_operator/` should show usage in all 5 consumer files + definition in base.py
3. `python -m mypy src/omnifocus_operator/` passes cleanly
4. `python -m pytest tests/ -x -q` all 534 tests pass
5. `python -m ruff check src/omnifocus_operator/` no lint errors
</verification>

<success_criteria>
- is_set TypeGuard helper defined and exported from contracts/base.py
- All 21 isinstance _Unset checks replaced across 5 files
- Zero remaining isinstance _Unset patterns in production code
- mypy, ruff, and full test suite pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/quick/260320-jd6-add-is-set-typeguard-helper-to-replace-i/260320-jd6-SUMMARY.md`
</output>
