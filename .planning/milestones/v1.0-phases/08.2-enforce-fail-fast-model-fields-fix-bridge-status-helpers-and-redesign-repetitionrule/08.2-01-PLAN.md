---
phase: 08.2-enforce-fail-fast-model-fields-fix-bridge-status-helpers-and-redesign-repetitionrule
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/bridge/bridge.js
  - bridge/tests/bridge.test.js
autonomous: true
requirements:
  - B1-B14

must_haves:
  truths:
    - "Bridge ts() throws on unknown TaskStatus instead of returning null"
    - "Bridge ps(), gs(), fs() resolvers return correct strings for all known values and throw on unknown"
    - "Bridge rst(), adk() resolvers return correct strings for all RepetitionScheduleType and AnchorDateKey values and throw on unknown"
    - "Bridge rr() extracts all 4 RepetitionRule fields (ruleString, scheduleType, anchorDateKey, catchUpAutomatically)"
    - "Bridge serializes tags as {id, name} objects instead of bare name strings"
    - "Bridge adds url field to all 4 entity types (tasks, projects, tags, folders)"
    - "Bridge adds active, effectiveActive, added, modified to projects via p.task.*"
    - "Bridge adds childrenAreMutuallyExclusive to tags"
    - "Bridge removes assignedContainer from tasks"
    - "Bridge perspective uses p.id.primaryKey instead of p.identifier, removes builtin"
    - "Bridge uses p.task.taskStatus for project taskStatus"
    - "All Vitest tests pass with updated mocks and assertions"
  artifacts:
    - path: "src/omnifocus_operator/bridge/bridge.js"
      provides: "Updated bridge with per-entity resolvers, field changes, TagRef format"
      exports: ["handleSnapshot", "dispatch", "ts", "ps", "gs", "fs", "rst", "adk", "rr"]
    - path: "bridge/tests/bridge.test.js"
      provides: "Updated Vitest tests covering all new resolvers and output shape changes"
  key_links:
    - from: "bridge.js ts()"
      to: "Task.Status constants"
      via: "=== comparison"
      pattern: "throw new Error.*Unknown TaskStatus"
    - from: "bridge.js ps()"
      to: "Project.Status constants"
      via: "=== comparison"
      pattern: "throw new Error.*Unknown ProjectStatus"
    - from: "bridge.js handleSnapshot()"
      to: "entity mappings"
      via: "map functions"
      pattern: "url:.*\\.toString\\(\\)"
---

<objective>
Update the bridge.js script to align with the BRIDGE-SPEC: add per-entity status resolvers (ps, gs, fs), RepetitionRule resolvers (rst, adk), fix ts() fallback to throw, update all entity field mappings (url, TagRef, project fields from p.task.*, remove assignedContainer, perspective fix), and update all Vitest tests.

Purpose: The bridge.js is the authoritative data extraction layer running inside OmniFocus. Fixing it first ensures the JS output matches BRIDGE-SPEC before Python models try to parse it.
Output: Updated bridge.js and passing Vitest test suite.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-enforce-fail-fast-model-fields-fix-bridge-status-helpers-and-redesign-repetitionrule/08.2-CONTEXT.md
@.planning/phases/08.2-enforce-fail-fast-model-fields-fix-bridge-status-helpers-and-redesign-repetitionrule/08.2-RESEARCH.md

CRITICAL: Read `.research/deep-dives/omnifocus-api-ground-truth/BRIDGE-SPEC.md` for authoritative field specs.

<interfaces>
<!-- Current bridge.js exports (will be extended) -->
From src/omnifocus_operator/bridge/bridge.js:
```javascript
module.exports = {
    readRequest, writeResponse, handleSnapshot, dispatch,
    d, pk, rr, ri, ts,
};
```

<!-- Current Vitest mock globals (need updating) -->
From bridge/tests/bridge.test.js:
- Task.Status mock: 7 values (Available through Overdue) -- keep as-is
- Need NEW mocks: Project.Status (4 values), Tag.Status (3 values), Folder.Status (2 values)
- Need NEW mocks: Task.RepetitionScheduleType (3 values), Task.AnchorDateKey (3 values)
- flattenedTasks mock: needs url, tags as TagRef objects, remove assignedContainer
- flattenedProjects mock: needs task sub-object for p.task.*, url, tags as TagRef
- flattenedTags mock: needs url, childrenAreMutuallyExclusive, Project.Status/Tag.Status constants
- flattenedFolders mock: needs url, Folder.Status constants
- Perspective mock: change identifier to id with primaryKey sub-object
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-entity status resolvers and RepetitionRule resolvers to bridge.js</name>
  <files>src/omnifocus_operator/bridge/bridge.js</files>
  <action>
Read the BRIDGE-SPEC at `.research/deep-dives/omnifocus-api-ground-truth/BRIDGE-SPEC.md` for authoritative enum values and field access paths.

Add 5 new resolver functions after the existing `ts()` function:

1. `ps(s)` -- Project status resolver using `===` against `Project.Status.Active`, `.OnHold`, `.Done`, `.Dropped`. Throw `new Error("Unknown ProjectStatus: " + String(s))` on unknown.

2. `gs(s)` -- Tag status resolver using `===` against `Tag.Status.Active`, `.OnHold`, `.Dropped`. Throw `new Error("Unknown TagStatus: " + String(s))` on unknown.

3. `fs(s)` -- Folder status resolver using `===` against `Folder.Status.Active`, `.Dropped`. Throw `new Error("Unknown FolderStatus: " + String(s))` on unknown.

4. `rst(s)` -- RepetitionScheduleType resolver using `===` against `Task.RepetitionScheduleType.Regularly`, `.FromCompletion`, `.None`. Throw on unknown.

5. `adk(s)` -- AnchorDateKey resolver using `===` against `Task.AnchorDateKey.DueDate`, `.DeferDate`, `.PlannedDate`. Throw on unknown.

Fix `ts()`: Change line 47 `return null;` to `throw new Error("Unknown TaskStatus: " + String(s));`

Update `rr(v)`: Replace the current 2-field implementation with 4 fields:
```javascript
function rr(v) {
    if (!v) return null;
    return {
        ruleString: v.ruleString,
        scheduleType: rst(v.scheduleType),
        anchorDateKey: adk(v.anchorDateKey),
        catchUpAutomatically: v.catchUpAutomatically,
    };
}
```

Update `handleSnapshot()` entity mappings:

**Tasks:**
- Add `url: t.url.toString()` after name
- Change tags from `g.name` to `{ id: g.id.primaryKey, name: g.name }`
- Remove `assignedContainer: pk(t.assignedContainer)` line entirely

**Projects:**
- Add `url: p.url.toString()` after name
- Replace `status: p.status ? p.status.name : null` with `status: ps(p.status)`
- Replace `taskStatus: ts(p.taskStatus)` with `taskStatus: ts(p.task.taskStatus)`
- Add 4 new fields: `active: p.task.active`, `effectiveActive: p.task.effectiveActive`, `added: d(p.task.added)`, `modified: d(p.task.modified)`
- Change tags from `g.name` to `{ id: g.id.primaryKey, name: g.name }`

**Tags:**
- Add `url: g.url.toString()` after name
- Replace `status: g.status ? g.status.name : null` with `status: gs(g.status)`
- Add `childrenAreMutuallyExclusive: g.childrenAreMutuallyExclusive`

**Folders:**
- Add `url: f.url.toString()` after name
- Replace `status: f.status ? f.status.name : null` with `status: fs(f.status)`

**Perspectives:**
- Replace `id: p.identifier || null` with `id: p.id ? p.id.primaryKey : null`
- Remove `builtin: !p.identifier` line entirely

Update the test-entry module.exports to include new functions: `ps`, `gs`, `fs`, `rst`, `adk`.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/bridge && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>bridge.js has all 5 new resolvers (ps, gs, fs, rst, adk), ts() throws on unknown, rr() extracts 4 fields, all entity mappings updated per BRIDGE-SPEC, module.exports includes new functions. NOTE: Vitest will FAIL at this point because mocks are not yet updated -- that is expected and fixed in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Update Vitest mocks and tests for all bridge.js changes</name>
  <files>bridge/tests/bridge.test.js</files>
  <action>
Update all Vitest mocks and test assertions to match the new bridge.js output shape.

**New mock globals to add:**

Add `Project.Status`, `Tag.Status`, `Folder.Status` as properties on new `Project`, `Tag`, `Folder` mock globals:
```javascript
vi.stubGlobal("Project", {
    Status: { Active: "PS_Active", OnHold: "PS_OnHold", Done: "PS_Done", Dropped: "PS_Dropped" },
});
vi.stubGlobal("Tag", {
    Status: { Active: "TS_Active", OnHold: "TS_OnHold", Dropped: "TS_Dropped" },
});
vi.stubGlobal("Folder", {
    Status: { Active: "FS_Active", Dropped: "FS_Dropped" },
});
```

Extend the existing `Task` mock global to add RepetitionScheduleType and AnchorDateKey:
```javascript
vi.stubGlobal("Task", {
    Status: { Available: "Available", Blocked: "Blocked", ... },  // keep existing
    RepetitionScheduleType: { Regularly: "RST_Regularly", FromCompletion: "RST_FromCompletion", None: "RST_None" },
    AnchorDateKey: { DueDate: "ADK_DueDate", DeferDate: "ADK_DeferDate", PlannedDate: "ADK_PlannedDate" },
});
```

Note: The mock values (e.g. "PS_Active") are opaque -- they simulate OmniFocus's opaque enum objects. The resolvers use `===` comparison, so the mock values just need to be consistent between the mock globals and the mock entity data.

**Update flattenedTasks mock:**
- Add `url: { toString: function() { return "omnifocus:///task/task-001"; } }`
- Change tags from `[{ name: "tag1" }]` to `[{ id: { primaryKey: "tag-001" }, name: "tag1" }]`
- Remove `assignedContainer` field
- Update `taskStatus` to use the Task.Status mock value: `taskStatus: "Available"` (this still works since ts() compares against Task.Status.Available which is the string "Available" in mock)

**Update flattenedProjects mock:**
- Add `url: { toString: function() { return "omnifocus:///project/proj-001"; } }`
- Replace `status: { name: "Active" }` with `status: "PS_Active"` (matching Project.Status.Active mock)
- Replace `taskStatus: "Available"` with a `task` sub-object: `task: { taskStatus: "Available", active: true, effectiveActive: true, added: new Date("2026-01-01T00:00:00Z"), modified: new Date("2026-01-02T00:00:00Z") }`
- Remove the top-level `taskStatus` field (it's now accessed via `p.task.taskStatus`)
- Change tags from `[]` to format `[{ id: { primaryKey: "tag-001" }, name: "tag1" }]` (or keep empty but format must be consistent)

**Update flattenedTags mock:**
- Add `url: { toString: function() { return "omnifocus:///tag/tag-001"; } }`
- Replace `status: { name: "Active" }` with `status: "TS_Active"` (matching Tag.Status.Active mock)
- Add `childrenAreMutuallyExclusive: false`

**Update flattenedFolders mock:**
- Add `url: { toString: function() { return "omnifocus:///folder/folder-001"; } }`
- Replace `status: { name: "Active" }` with `status: "FS_Active"` (matching Folder.Status.Active mock)

**Update Perspective mock:**
- Replace `{ identifier: "perspective-001", name: "Forecast" }` with `{ id: { primaryKey: "perspective-001" }, name: "Forecast" }`

**New test cases to add:**

In a new `describe("per-entity status resolvers")` block:
- `ps()` returns correct strings for all 4 Project.Status values
- `ps()` throws on unknown value
- `gs()` returns correct strings for all 3 Tag.Status values
- `gs()` throws on unknown value
- `fs()` returns correct strings for both Folder.Status values
- `fs()` throws on unknown value

In a new `describe("repetition resolvers")` block:
- `rst()` returns correct strings for all 3 RepetitionScheduleType values
- `rst()` throws on unknown value
- `adk()` returns correct strings for all 3 AnchorDateKey values
- `adk()` throws on unknown value

**Update existing test cases:**

- `ts() returns null for unknown status` -- change to: `ts() throws on unknown status` (expect `toThrow`)
- `rr() extracts repetition rule` -- update to test all 4 fields with mock that has scheduleType, anchorDateKey, catchUpAutomatically
- `rr() handles missing scheduleType gracefully` and `rr() handles scheduleType without name property` -- REMOVE these tests (no longer applicable; rst() throws on invalid input)

**Update handleSnapshot assertions:**
- Task tags assertion: change from `["tag1"]` to `[{ id: "tag-001", name: "tag1" }]`
- Task should NOT have `assignedContainer` in output
- Task should have `url` field
- Project should have `url`, `active`, `effectiveActive`, `added`, `modified` fields
- Project status should be "Active" string (resolved by ps())
- Tag should have `url`, `childrenAreMutuallyExclusive` fields
- Folder should have `url` field
- Perspective should NOT have `builtin` field
- Perspective `id` should still be "perspective-001" (now from p.id.primaryKey)
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/bridge && npx vitest run</automated>
  </verify>
  <done>All Vitest tests pass. New resolver tests cover all known values + unknown-throws. handleSnapshot output shape matches BRIDGE-SPEC: tags as TagRef objects, url on all entities, no assignedContainer on tasks, no builtin on perspectives, projects have active/effectiveActive/added/modified, tags have childrenAreMutuallyExclusive.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator/bridge && npx vitest run
```
All tests pass. Bridge output shape matches BRIDGE-SPEC for all entity types.
</verification>

<success_criteria>
- bridge.js has 5 new resolver functions (ps, gs, fs, rst, adk) all using === comparison and throwing on unknown
- ts() throws instead of returning null on unknown TaskStatus
- rr() extracts 4 fields (ruleString, scheduleType, anchorDateKey, catchUpAutomatically)
- All entity mappings updated: url added, tags as TagRef, assignedContainer removed, perspective id/builtin fixed, project fields from p.task.*
- All Vitest tests pass with comprehensive coverage of new resolvers and output shape
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-enforce-fail-fast-model-fields-fix-bridge-status-helpers-and-redesign-repetitionrule/08.2-01-SUMMARY.md`
</output>
