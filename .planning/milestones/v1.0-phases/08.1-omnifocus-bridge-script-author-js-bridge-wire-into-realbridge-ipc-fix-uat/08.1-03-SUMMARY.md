---
phase: 08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat
plan: 03
subsystem: bridge
tags: [ipc, protocol, importlib-resources, url-scheme, envelope]

# Dependency graph
requires:
  - phase: 08.1-01
    provides: Canonical bridge.js script for OmniFocus Omni Automation
  - phase: 08.1-02
    provides: dump_all -> snapshot rename across codebase
provides:
  - "New {operation, params} request envelope replacing {dispatch} format"
  - "bridge.js loaded and cached via importlib.resources at RealBridge init"
  - "_trigger_omnifocus() passes full script via script= and file prefix via arg="
  - "Simulator parses new envelope format"
affects: [08.1-04]

# Tech tracking
tech-stack:
  added: [importlib.resources]
  patterns:
    - "Template injection: __IPC_DIR__ replaced at RealBridge init time"
    - "File prefix pattern: pid_uuid as trigger argument instead of dispatch string"
    - "Request envelope: {operation, params} instead of {dispatch: uuid::::op}"

key-files:
  created: []
  modified:
    - src/omnifocus_operator/bridge/_real.py
    - src/omnifocus_operator/bridge/_simulator.py
    - src/omnifocus_operator/simulator/__main__.py
    - tests/test_ipc_engine.py
    - tests/test_simulator_bridge.py

key-decisions:
  - "bridge.js loaded once at __init__ time via importlib.resources -- PyPI-compatible and avoids repeated disk reads"
  - "_trigger_omnifocus operation= set to 'trigger' string literal since dispatch string no longer carries operation name"
  - "Simulator extracts operation from envelope but prefixes with _ (unused) -- validates format without routing"

patterns-established:
  - "importlib.resources.files('omnifocus_operator.bridge').joinpath('bridge.js') for package resource access"
  - "file_prefix = f'{pid}_{uuid}' as the canonical IPC identifier passed to trigger"

requirements-completed: [IPC-PROTO-01, IPC-PROTO-02, IPC-PROTO-03, SIM-UPDATE-01, PKG-01]

# Metrics
duration: 4min
completed: 2026-03-02
---

# Phase 08.1 Plan 03: IPC Protocol Overhaul Summary

**New {operation, params} request envelope with bridge.js loaded via importlib.resources and script= URL scheme trigger**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-02T22:33:26Z
- **Completed:** 2026-03-02T22:37:41Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Replaced `{dispatch: "uuid::::op"}` envelope with clean `{operation, params}` format across all production code, simulator, and tests
- RealBridge loads and caches bridge.js via importlib.resources with `__IPC_DIR__` template injection at init
- `_trigger_omnifocus()` now passes full script inline via `script=` URL parameter and file prefix via `arg=`
- Zero `::::` or `"dispatch"` references remain in codebase
- All 166 tests pass with 98% coverage

## Task Commits

Each task was committed atomically:

1. **Task 1: Update RealBridge IPC protocol and bridge.js loading** - `58d75ca` (feat)
2. **Task 2: Update simulator and all IPC/protocol tests** - `106c56a` (feat)

## Files Created/Modified
- `src/omnifocus_operator/bridge/_real.py` - importlib.resources loading, new envelope, script= trigger
- `src/omnifocus_operator/bridge/_simulator.py` - Updated trigger signature to file_prefix
- `src/omnifocus_operator/simulator/__main__.py` - Parse new {operation, params} envelope
- `tests/test_ipc_engine.py` - Reframed dispatch tests to envelope tests, updated all signatures
- `tests/test_simulator_bridge.py` - Updated trigger call to use file_prefix

## Decisions Made
- bridge.js loaded once at `__init__` time via `importlib.resources` -- PyPI-compatible, avoids repeated disk reads
- `_trigger_omnifocus` `operation=` parameter set to literal `"trigger"` since dispatch string no longer carries operation name
- Simulator extracts `_operation` from envelope but doesn't route on it -- validates format without implementation

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed ruff F841 unused variable in simulator**
- **Found during:** Task 2
- **Issue:** `operation: str = envelope["operation"]` flagged as unused by ruff
- **Fix:** Prefixed with underscore: `_operation` to match existing `_params` convention
- **Files modified:** `src/omnifocus_operator/simulator/__main__.py`
- **Verification:** ruff check passes, pre-commit hook succeeds
- **Committed in:** 106c56a (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Minor naming fix for linter compliance. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- IPC protocol fully updated -- end-to-end pipeline is now functional
- Ready for Plan 04: UAT script update and live OmniFocus validation
- bridge.js + Python IPC + simulator all use consistent {operation, params} envelope

## Self-Check: PASSED

All files exist, all commits verified.

---
*Phase: 08.1-omnifocus-bridge-script-author-js-bridge-wire-into-realbridge-ipc-fix-uat*
*Completed: 2026-03-02*
