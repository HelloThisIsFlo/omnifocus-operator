---
phase: 05-service-layer-and-mcp-server
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/bridge/_factory.py
autonomous: true
requirements:
  - TOOL-01
gap_closure: true

must_haves:
  truths:
    - "list_all response contains at least one item in each collection"
    - "camelCase field names (dueDate, deferDate, inInbox, taskStatus, etc.) are visible in the list_all JSON response"
    - "All existing tests continue to pass"
  artifacts:
    - path: "src/omnifocus_operator/bridge/_factory.py"
      provides: "InMemoryBridge seeded with realistic sample data (1 item per collection)"
      contains: "dueDate|deferDate|inInbox|taskStatus|allowsNextAction"
  key_links:
    - from: "src/omnifocus_operator/bridge/_factory.py"
      to: "src/omnifocus_operator/bridge/_in_memory.py"
      via: "InMemoryBridge(data={...})"
      pattern: "InMemoryBridge\\(data="
---

<objective>
Seed the InMemoryBridge factory with one realistic sample item per collection so that camelCase field names are visible in list_all responses.

Purpose: UAT test 4 could not verify camelCase serialization because the factory returns empty lists. This gap closure adds minimal but complete seed data so field names appear in the MCP response.

Output: Updated _factory.py where the "inmemory" case passes a dict with 1 Task, 1 Project, 1 Tag, 1 Folder, and 1 Perspective — all using camelCase keys matching bridge script output format.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/omnifocus_operator/bridge/_factory.py
@src/omnifocus_operator/bridge/_in_memory.py
@src/omnifocus_operator/models/_base.py
@src/omnifocus_operator/models/_task.py
@src/omnifocus_operator/models/_project.py
@src/omnifocus_operator/models/_tag.py
@src/omnifocus_operator/models/_folder.py
@src/omnifocus_operator/models/_perspective.py
@src/omnifocus_operator/models/_snapshot.py
@src/omnifocus_operator/models/_enums.py
@src/omnifocus_operator/models/_common.py

<interfaces>
<!-- Key contracts the executor needs. -->

From src/omnifocus_operator/bridge/_in_memory.py:
  InMemoryBridge(data: dict[str, Any] | None = None)
  -- data is returned verbatim by send_command() for any operation

From src/omnifocus_operator/models/_base.py:
  OmniFocusBaseModel: alias_generator=to_camel, validate_by_name=True, validate_by_alias=True
  -- models accept EITHER snake_case OR camelCase keys during construction

From src/omnifocus_operator/models/_enums.py:
  TaskStatus: "Available" | "Blocked" | "Completed" | "Dropped" | "DueSoon" | "Next" | "Overdue"
  EntityStatus: "Active" | "Done" | "Dropped"

Required non-optional fields by model:
  Task:        id, name, note, completed, completedByChildren, flagged, effectiveFlagged,
               sequential, hasChildren, shouldUseFloatingTimeZone, active, effectiveActive,
               status (TaskStatus string), inInbox
  Project:     id, name, note, completed, completedByChildren, flagged, effectiveFlagged,
               sequential, hasChildren, shouldUseFloatingTimeZone, taskStatus (TaskStatus string),
               containsSingletonActions
  Tag:         id, name, active, effectiveActive, allowsNextAction
  Folder:      id, name, active, effectiveActive
  Perspective: id (nullable), name, builtin
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed InMemoryBridge factory with realistic sample data</name>
  <files>src/omnifocus_operator/bridge/_factory.py</files>
  <action>
In the `create_bridge` function, replace the empty-list dict with a dict containing
one realistic sample item per collection. Use camelCase keys throughout (matching
bridge script JSON output format). The data must pass DatabaseSnapshot.model_validate()
which the service layer calls when processing a dump_all response.

Seed data requirements:
- tasks: 1 Task with all required fields. Include at least one optional date field
  (dueDate) to make camelCase naming obvious in the output.
- projects: 1 Project with all required fields. Include taskStatus (camelCase visible).
- tags: 1 Tag with all required fields. Include allowsNextAction (camelCase visible).
- folders: 1 Folder with all required fields.
- perspectives: 1 Perspective — use id=null (builtin perspective) and builtin=true.

Use these exact camelCase keys (matching bridge script output and Pydantic aliases):
  Task:    "id", "name", "note", "completed", "completedByChildren", "flagged",
           "effectiveFlagged", "sequential", "hasChildren", "shouldUseFloatingTimeZone",
           "active", "effectiveActive", "status", "inInbox", "dueDate", "tags"
  Project: "id", "name", "note", "completed", "completedByChildren", "flagged",
           "effectiveFlagged", "sequential", "hasChildren", "shouldUseFloatingTimeZone",
           "taskStatus", "containsSingletonActions", "status"
  Tag:     "id", "name", "active", "effectiveActive", "status", "allowsNextAction"
  Folder:  "id", "name", "active", "effectiveActive", "status"
  Perspective: "id", "name", "builtin"

Example values (realistic but minimal):
  Task: id="task-001", name="Sample Task", note="", completed=False,
        completedByChildren=False, flagged=False, effectiveFlagged=False,
        sequential=False, hasChildren=False, shouldUseFloatingTimeZone=False,
        active=True, effectiveActive=True, status="Available", inInbox=False,
        dueDate=None, tags=[]
  Project: id="proj-001", name="Sample Project", note="", completed=False,
           completedByChildren=False, flagged=False, effectiveFlagged=False,
           sequential=False, hasChildren=True, shouldUseFloatingTimeZone=False,
           taskStatus="Available", containsSingletonActions=False, status="Active"
  Tag: id="tag-001", name="work", active=True, effectiveActive=True,
       status="Active", allowsNextAction=True
  Folder: id="folder-001", name="Work", active=True, effectiveActive=True,
          status="Active"
  Perspective: id=None, name="Inbox", builtin=True

No new imports are needed — only the data dict literal changes.
The docstring for create_bridge does NOT need updating (still returns empty-ish data
for test isolation; this seed is for development/demonstration via factory only).
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
- All existing tests pass (pytest exits 0)
- Running `OMNIFOCUS_BRIDGE=inmemory uv run python -c "import asyncio; from omnifocus_operator.bridge._factory import create_bridge; b = create_bridge('inmemory'); result = asyncio.run(b.send_command('dump_all')); print(list(result.keys())); print(result['tasks'])"` prints a non-empty tasks list with camelCase keys visible
- The response contains dueDate, inInbox, taskStatus, allowsNextAction keys visible in the data
  </done>
</task>

</tasks>

<verification>
Run the full test suite after the change:

```bash
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest -x -q
```

Confirm the factory produces visible camelCase keys:

```bash
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && OMNIFOCUS_BRIDGE=inmemory uv run python -c "
import asyncio, json
from omnifocus_operator.bridge._factory import create_bridge
b = create_bridge('inmemory')
data = asyncio.run(b.send_command('dump_all'))
print('tasks count:', len(data['tasks']))
print('task keys:', list(data['tasks'][0].keys()) if data['tasks'] else 'EMPTY')
print('camelCase present:', any(k in str(data) for k in ['dueDate', 'inInbox', 'taskStatus', 'allowsNextAction']))
"
```

Expected: tasks count >= 1, camelCase keys visible, all tests pass.
</verification>

<success_criteria>
- `uv run pytest` exits 0 with all tests passing
- list_all response (via InMemoryBridge) contains 5 non-empty collections
- camelCase field names (dueDate, inInbox, taskStatus, allowsNextAction) are present and visible in the response dict
- No new tests required (this is a 1-line data change, existing integration test coverage is sufficient)
</success_criteria>

<output>
After completion, create `.planning/phases/05-service-layer-and-mcp-server/05-03-SUMMARY.md`
</output>
