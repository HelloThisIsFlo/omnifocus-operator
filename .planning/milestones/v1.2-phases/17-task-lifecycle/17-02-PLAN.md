---
phase: 17-task-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/omnifocus_operator/server.py
  - tests/test_server.py
  - .claude/skills/test-edit-operations/SKILL.md
autonomous: false
requirements: [LIFE-01, LIFE-02, LIFE-05]

must_haves:
  truths:
    - "edit_tasks docstring documents lifecycle actions (complete and drop)"
    - "Server-level tests verify lifecycle flows through to service layer"
    - "UAT skill includes lifecycle test cases for manual verification"
  artifacts:
    - path: "src/omnifocus_operator/server.py"
      provides: "Updated edit_tasks docstring with lifecycle documentation"
      contains: "actions.lifecycle"
    - path: "tests/test_server.py"
      provides: "Server integration tests for lifecycle"
      contains: "lifecycle"
    - path: ".claude/skills/test-edit-operations/SKILL.md"
      provides: "UAT test cases for complete and drop"
      contains: "lifecycle"
  key_links:
    - from: "src/omnifocus_operator/server.py"
      to: "src/omnifocus_operator/service.py"
      via: "edit_task delegation with lifecycle in spec"
      pattern: "service\\.edit_task"
---

<objective>
Update server docstring, add server-level tests, and extend UAT skill with lifecycle test cases. Then run UAT to verify against live OmniFocus.

Purpose: Complete the user-facing documentation and verification for lifecycle actions.
Output: Updated server docstring, server tests, UAT skill with lifecycle coverage.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-task-lifecycle/17-CONTEXT.md
@.planning/phases/17-task-lifecycle/17-01-SUMMARY.md

<interfaces>
From src/omnifocus_operator/server.py (edit_tasks docstring, line 254):
```python
- actions.lifecycle: Reserved (not yet implemented)  # <-- update this
```

From src/omnifocus_operator/server.py (edit_tasks handler, lines 258-279):
```python
# Validates spec via TaskEditSpec.model_validate, delegates to service.edit_task
# No server-level changes needed -- lifecycle flows through existing pipeline
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server docstring + server tests + UAT skill update</name>
  <files>
    src/omnifocus_operator/server.py,
    tests/test_server.py,
    .claude/skills/test-edit-operations/SKILL.md
  </files>
  <behavior>
    - Server edit_tasks with lifecycle="complete" returns success result
    - Server edit_tasks with lifecycle="drop" returns success result
    - Server edit_tasks with lifecycle="invalid" returns clean error (no Pydantic internals)
  </behavior>
  <action>
    1. **Server docstring** (`server.py`): Replace line 254 (`- actions.lifecycle: Reserved (not yet implemented)`) with:
       ```
       - actions.lifecycle: Task lifecycle action
         - "complete" -- mark task as complete
         - "drop" -- drop/skip task
       ```

    2. **Server tests** (`test_server.py`): Add lifecycle tests covering:
       - `lifecycle="complete"` on a normal task -> success
       - `lifecycle="drop"` on a normal task -> success
       - `lifecycle="invalid"` -> clean error message (no Pydantic internals like "type=" or "input_value")
       - `lifecycle="complete"` on already-completed task -> success with no-op warning

    3. **UAT skill** (`.claude/skills/test-edit-operations/SKILL.md`): Add a new "Section G -- Lifecycle" after Section F with these test cases:
       - Test 12a: Complete a task -- `actions: { lifecycle: "complete" }` on a test task, verify success, verify task shows as completed via get_task
       - Test 12b: Drop a task -- `actions: { lifecycle: "drop" }` on a different test task, verify success
       - Test 12c: No-op complete -- complete an already-completed task, PASS if warning about "already complete"
       - Test 12d: No-op drop -- drop an already-dropped task, PASS if warning about "already dropped"
       - Test 12e: Cross-state -- complete a dropped task (T6a from setup), PASS if success with warning about prior state
       - Test 12f: Lifecycle + field edit -- `name: "T-Lifecycle-Renamed", actions: { lifecycle: "complete" }`, PASS if both applied
       - Test 12g: Invalid lifecycle -- `actions: { lifecycle: "reopen" }`, PASS if clean error (no Pydantic internals)
       - Update Step 1.2 to add 2 more test tasks: T10-LifecycleA, T10-LifecycleB under UAT-Regression
       - Update Step 1.3 to note that T6a is already dropped (reuse for cross-state test)
       - Update the Phase 3 report table to include all lifecycle test rows (12a-12g)
       - Note: repeating task lifecycle (12h) is SKIP unless user has a repeating test task -- mention this
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_server.py -x -q -k "lifecycle"</automated>
  </verify>
  <done>
    Server docstring documents lifecycle. Server tests pass for complete/drop/invalid. UAT skill has lifecycle section (12a-12g) ready for manual testing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify lifecycle works end-to-end</name>
  <what-built>Complete task lifecycle support -- agents can now complete and drop tasks via edit_tasks with actions.lifecycle</what-built>
  <how-to-verify>
    1. Run the full test suite: `cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/ -q`
    2. Run mypy: `uv run mypy src/`
    3. Optionally run the UAT skill ("test edit operations") to verify against live OmniFocus
    4. Quick manual check: use Claude Desktop to call edit_tasks with `{id: "<some-task>", actions: {lifecycle: "complete"}}` and verify the task is completed in OmniFocus
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Full test suite passes:
```bash
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/ -x -q
```

Type checking passes:
```bash
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run mypy src/
```
</verification>

<success_criteria>
- Server docstring no longer says "Reserved (not yet implemented)" for lifecycle
- Server-level tests cover happy path and error cases
- UAT skill includes lifecycle section with 7 test cases
- Full test suite green, mypy clean
</success_criteria>

<output>
After completion, create `.planning/phases/17-task-lifecycle/17-02-SUMMARY.md`
</output>
