---
phase: 16.1
slug: introduce-actions-grouping-to-edit-tasks
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-09
validated: 2026-03-09
---

# Phase 16.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest with pytest-asyncio |
| **Config file** | `pyproject.toml` ([tool.pytest.ini_options]) |
| **Quick run command** | `uv run python -m pytest tests/test_service.py::TestEditTask -x` |
| **Full suite command** | `uv run python -m pytest` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run python -m pytest tests/test_service.py tests/test_server.py -x`
- **After every plan wave:** Run `uv run python -m pytest`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 16.1-01-01 | 01 | 1 | ActionsSpec model validation | unit | `uv run python -m pytest tests/test_service.py -x -k "tag or empty_actions"` | Yes (4 tests) | ✅ green |
| 16.1-01-02 | 01 | 1 | TagActionSpec replace mode | unit | `uv run python -m pytest tests/test_service.py -x -k "tag_replace"` | Yes (1 test + server) | ✅ green |
| 16.1-01-03 | 01 | 1 | TagActionSpec add/remove mode | unit | `uv run python -m pytest tests/test_service.py -x -k "tag_add or tag_remove or tag_mutual"` | Yes (6 tests) | ✅ green |
| 16.1-01-04 | 01 | 1 | MoveToSpec under actions.move | unit | `uv run python -m pytest tests/test_service.py -x -k "move"` | Yes (8 tests) | ✅ green |
| 16.1-01-05 | 01 | 1 | Lifecycle rejection | unit | `uv run python -m pytest tests/test_service.py -x -k "lifecycle"` | Yes (1 test) | ✅ green |
| 16.1-01-06 | 01 | 1 | Service translation to bridge | unit | `uv run python -m pytest tests/test_service.py -x -k "edit"` | Yes (51 tests) | ✅ green |
| 16.1-01-07 | 01 | 1 | Empty actions block | unit | `uv run python -m pytest tests/test_service.py -x -k "empty_actions"` | Yes (1 test) | ✅ green |
| 16.1-01-08 | 01 | 1 | Server tool schema clean | integration | `uv run python -m pytest tests/test_server.py -x -k "edit"` | Yes (13 tests) | ✅ green |
| 16.1-01-09 | 01 | 1 | No-op detection with actions | unit | `uv run python -m pytest tests/test_service.py -x -k "noop or warning_empty"` | Yes (3 tests) | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Rewrite existing edit tests to new `actions` shape in `tests/test_service.py`
- [x] Rewrite existing edit tests to new `actions` shape in `tests/test_server.py`
- [x] New unit test for `TagActionSpec` model validation (mutual exclusivity, empty tags block)
- [x] New unit test for lifecycle rejection
- [x] New unit test for empty `actions: {}` handling

*All Wave 0 requirements satisfied by Plans 02 and 03.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| UAT edit operations | End-to-end OmniFocus integration | Requires live OmniFocus database (SAFE-01/02) | Run updated UAT skill against live OmniFocus |

---

## Validation Audit 2026-03-09

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

All 9 requirements have automated coverage across 64 tests (51 service + 13 server). No gaps identified.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** complete
