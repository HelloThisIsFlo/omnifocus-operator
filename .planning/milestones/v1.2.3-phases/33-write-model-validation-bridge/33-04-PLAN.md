---
phase: 33-write-model-validation-bridge
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_warnings.py
  - src/omnifocus_operator/agent_messages/errors.py
  - src/omnifocus_operator/service/validate.py
  - src/omnifocus_operator/service/service.py
  - tests/test_validation_repetition.py
autonomous: true
requirements: [VALID-01]
gap_closure: true

must_haves:
  truths:
    - "test_warnings.py consolidation test catches unwired constants without exclusion bypass"
    - "No dead/unused error constants in agent_messages"
    - "All error-constant-using modules registered in _ERROR_CONSUMERS"
  artifacts:
    - path: "tests/test_warnings.py"
      provides: "Clean consolidation test without forward-declaration scaffolding"
      contains: "_ERROR_CONSUMERS"
    - path: "src/omnifocus_operator/agent_messages/errors.py"
      provides: "All error constants, no dead code"
    - path: "src/omnifocus_operator/service/validate.py"
      provides: "Task name validation using imported constants"
  key_links:
    - from: "src/omnifocus_operator/service/validate.py"
      to: "src/omnifocus_operator/agent_messages/errors.py"
      via: "imported constants for task name validation"
      pattern: "err\\.TASK_NAME"
    - from: "tests/test_warnings.py"
      to: "src/omnifocus_operator/service/validate.py"
      via: "validate module in _ERROR_CONSUMERS"
      pattern: "validate"
---

<objective>
Remove parallel-execution scaffolding and dead code from Phase 33, extract inline error strings to constants, and register validate.py in the consolidation test.

Purpose: Close 3 code-review gaps found during human UAT after Phase 33 execution. All fixes are mechanical cleanup -- no behavioral changes.
Output: Clean consolidation test, no dead constants, validate.py fully covered by error enforcement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-write-model-validation-bridge/33-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean scaffolding, extract constants, register validate module</name>
  <files>tests/test_warnings.py, src/omnifocus_operator/agent_messages/errors.py, src/omnifocus_operator/service/validate.py, src/omnifocus_operator/service/service.py, tests/test_validation_repetition.py</files>
  <action>
All changes are mechanical cleanup with no behavioral changes. Order matters -- register validate.py and extract its inline strings BEFORE removing exclusion sets, so the consolidation test passes at every intermediate step.

**A) Extract inline msg strings to constants in errors.py (UAT gap #6):**

Add two new constants to `errors.py` in a new `# --- Validation: Task Name ---` section (place it before the existing `# --- Validation: TagAction ---` section):

```python
TASK_NAME_REQUIRED = "Task name is required"
TASK_NAME_EMPTY = "Task name cannot be empty"
```

**B) Update validate.py to use imported constants (UAT gap #6):**

In `validate_task_name` (line 30-34): replace inline `msg = "Task name is required"` + `raise ValueError(msg)` with `raise ValueError(err.TASK_NAME_REQUIRED)`.

In `validate_task_name_if_set` (line 37-47): replace inline `msg = "Task name cannot be empty"` + `raise ValueError(msg)` with `raise ValueError(err.TASK_NAME_EMPTY)`.

The `import ... as err` already exists at line 7 of validate.py.

**C) Register validate module in test_warnings.py (UAT gap #6):**

Add `from omnifocus_operator.service import validate as service_validate` to the imports at the top (after the existing service imports).

Add `service_validate` to the `_ERROR_CONSUMERS` list (line 119).

**D) Remove exclusion sets from test_warnings.py (UAT gap #4):**

Now that validate.py is registered, it is safe to remove the exclusion bypass:
- Delete the `_FORWARD_DECLARED_WARNINGS` set (lines 33-38) and its comment (lines 31-32)
- Delete the `_FORWARD_DECLARED_ERRORS` set (lines 43-52) and its comment (lines 40-42)
- Remove the subtraction line `unreferenced -= _FORWARD_DECLARED_WARNINGS` (line 68)
- Remove the subtraction line `unreferenced -= _FORWARD_DECLARED_ERRORS` (line 130)

After removal, the consolidation test directly checks ALL constants against consumers -- no bypass.

**E) Remove dead REPETITION_TYPE_CHANGE_INCOMPLETE constant (UAT gap #5):**
- Delete the `REPETITION_TYPE_CHANGE_INCOMPLETE` constant definition from `errors.py` (lines 56-60)
- In `tests/test_validation_repetition.py`, method `test_error_constants_exist`: remove the import of `REPETITION_TYPE_CHANGE_INCOMPLETE` (line 213) and the assertion `assert "type" in REPETITION_TYPE_CHANGE_INCOMPLETE` (line 216)

This constant was forward-declared for Phase 33.1 (flat frequency model). It is not used in any production code. It will be re-created when Phase 33.1 is implemented.

**F) Clean stale comment on service.py import (UAT gap #5 secondary):**
- Line 18: remove the comment `# forward-declared for Phase 33.1 flat frequency model` from the `REPETITION_NO_EXISTING_RULE` import. This constant IS actively used (line 340) -- the comment is stale.
  </action>
  <verify>
    <automated>uv run pytest tests/test_warnings.py tests/test_validation_repetition.py tests/test_add_tasks.py tests/test_edit_tasks.py -x -q</automated>
  </verify>
  <done>
- _FORWARD_DECLARED_WARNINGS and _FORWARD_DECLARED_ERRORS sets deleted from test_warnings.py
- No subtraction lines remain in the consolidation test
- errors.py contains TASK_NAME_REQUIRED and TASK_NAME_EMPTY constants
- validate.py uses imported constants instead of inline strings
- validate module is in _ERROR_CONSUMERS list
- REPETITION_TYPE_CHANGE_INCOMPLETE removed from errors.py and test_validation_repetition.py
- service.py import has no stale comment
- All consolidation tests pass (no previously-excluded constant is now flagged as unreferenced)
- Existing task name validation tests still pass (error messages unchanged)
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:

```bash
uv run pytest -x -q
```

All tests pass, no new failures introduced. The consolidation test now covers validate.py and has no exclusion bypass sets.
</verification>

<success_criteria>
- test_warnings.py has zero forward-declaration scaffolding (no _FORWARD_DECLARED_* sets, no subtraction lines)
- REPETITION_TYPE_CHANGE_INCOMPLETE does not exist in any source file under src/
- validate.py has zero inline msg= string assignments followed by raise ValueError
- validate module appears in _ERROR_CONSUMERS
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/33-write-model-validation-bridge/33-04-SUMMARY.md`
</output>
