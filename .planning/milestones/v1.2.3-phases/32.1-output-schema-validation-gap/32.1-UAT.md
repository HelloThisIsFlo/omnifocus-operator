---
status: complete
phase: 32.1-output-schema-validation-gap
source: 32.1-01-SUMMARY.md
started: 2026-03-28T15:00:00Z
updated: 2026-03-28T17:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Schema validation tests pass for all 6 MCP tools
expected: Run `uv run pytest tests/test_output_schema.py -v --no-cov` — all 16 tests pass. Look for `TestSchemaValidation` passing for get_all, get_task, get_project, get_tag, add_tasks, edit_tasks.
result: pass

### 2. Union regression guard catches schema erasure
expected: In the same test output, `TestUnionRegressionGuard` has 3 passing tests: one for Frequency (8 branches with const discriminator), one for EndCondition (2 branches), and one broad catch-all ensuring no $defs entry is erased to `{type:object, additionalProperties:true}`.
result: pass

### 3. Naming convention enforcement
expected: `TestNamingConvention` has 2 passing tests: one verifying models/ classes have no write-side suffixes (Command, Result, etc.), one verifying contracts/ classes use recognized suffixes. Both use live package scanning.
result: pass

### 4. CLAUDE.md agent rules
expected: Open `CLAUDE.md` and find a `## Model Conventions` section that directs agents to (1) read `docs/architecture.md` naming taxonomy before creating models, and (2) run `uv run pytest tests/test_output_schema.py` after modifying output models.
result: pass

### 5. Full test suite — no regressions
expected: Run `uv run pytest -x -q --no-cov` — entire suite passes with no failures. Test count should be 846+.
result: pass

### 6. WeeklyFrequency omits onDays when unset (re-test after fix)
expected: Run `uv run pytest tests/test_output_schema.py tests/test_rrule.py -v --no-cov` and confirm: (1) WeeklyFrequency bare serializes as {"interval": 1, "type": "weekly"} with NO onDays key, (2) WeeklyOnDaysFrequency uses type "weekly_on_days" with required on_days, (3) Frequency union regression guard now asserts 9 branches.
result: pass
previous_result: issue
previous_reported: "WeeklyFrequency emits onDays: null when no specific days are selected."

### 7. from_completion with catchUpAutomatically=true maps correctly (re-test after fix)
expected: Run `uv run pytest tests/test_rrule.py::TestDeriveSchedule tests/test_adapter.py -v --no-cov` and confirm: (1) from_completion + catchUpAutomatically=true returns "from_completion" without error, (2) derive_schedule lives in a single shared module (rrule/schedule.py), not duplicated.
result: pass
previous_result: issue
previous_reported: "from_completion + catchUpAutomatically=true crashes instead of mapping."

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "A bare weekly repetition rule serializes without onDays field"
  status: closed
  reason: "User reported: WeeklyFrequency emits onDays: null when no specific days are selected. Should split into WeeklyFrequency (bare) and WeeklyOnDaysFrequency (type: weekly_on_days, on_days required) following the monthly pattern."
  severity: major
  test: 6
  root_cause: "WeeklyFrequency has on_days: list[str] | None = None as single model. Pydantic serializes None fields. Monthly pattern already solved this by splitting into separate types."
  fix_plan: "32.1-03"
  closed_by: "make test green after plan 32.1-03 execution"

- truth: "from_completion repetition rule with catchUpAutomatically=true maps to from_completion schedule without error"
  status: closed
  reason: "User reported: from_completion + catchUpAutomatically=true crashes instead of mapping. Server throws data corruption error. This is normal OmniFocus behavior — the flag has no effect in from_completion context. Originally a deliberate fail-fast decision, but OmniFocus UI can produce this state."
  severity: blocker
  test: 7
  root_cause: "_derive_schedule() in adapter.py:97-101 and hybrid.py:219-223 raises ValueError('from_completion + catchUpAutomatically=true is an impossible state') — but this state is legitimately produced by OmniFocus UI"
  fix_plan: "32.1-02"
  closed_by: "make test green after plan 32.1-02 execution"
