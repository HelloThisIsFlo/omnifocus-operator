---
phase: 32.1-output-schema-validation-gap
plan: 01
subsystem: testing
tags: [jsonschema, pydantic, fastmcp, mcp-output-schema, naming-convention]

# Dependency graph
requires:
  - phase: 32-read-model-rewrite
    provides: Structured RepetitionRule with Frequency union and EndCondition types
provides:
  - Schema-vs-data validation tests for all 6 MCP tools
  - Union regression guard (Frequency 8 branches, EndCondition 2 branches)
  - Naming convention enforcement (models/ vs contracts/ suffix rules)
  - CLAUDE.md agent rules for naming taxonomy and schema test running
  - jsonschema as explicit dev dependency
affects: [any phase modifying models/, contracts/, or tool return types]

# Tech tracking
tech-stack:
  added: [jsonschema>=4.23.0]
  patterns: [TypeAdapter.json_schema(mode=serialization) + compress_schema + pydantic_core.to_jsonable_python + jsonschema.validate, pkgutil.walk_packages + inspect.getmembers for class scanning]

key-files:
  created: [tests/test_output_schema.py]
  modified: [pyproject.toml, tests/test_models.py, CLAUDE.md]

key-decisions:
  - "Used pydantic_core.to_jsonable_python (matching FastMCP exactly) over model_dump for serialization"
  - "Inline Pydantic model construction (not InMemoryBridge) for pure model-layer schema tests"
  - "Exempt lists for naming convention: MODELS_EXEMPT={OmniFocusBaseModel, OmniFocusEntity, ActionableEntity}, CONTRACTS_EXEMPT={CommandModel, EditTaskActions}"

patterns-established:
  - "Schema validation pattern: TypeAdapter.json_schema(mode=serialization) + compress_schema(prune_titles=True) + jsonschema.validate -- reproduces FastMCP pipeline exactly"
  - "_WrappedResult pattern for list-returning tools (add_tasks, edit_tasks) -- wrap schema and data in {result: [...]}"
  - "Package scanning pattern for naming enforcement via pkgutil.walk_packages + inspect.getmembers"

requirements-completed: [SC-1, SC-2, SC-3, SC-4, SC-5]

# Metrics
duration: 5min
completed: 2026-03-28
---

# Phase 32.1 Plan 01: Output Schema Validation Gap Summary

**16 regression guard tests: jsonschema validates all 6 MCP tools' serialized output against outputSchema, union branches checked for erasure, naming conventions enforced programmatically**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-28T13:58:43Z
- **Completed:** 2026-03-28T14:03:53Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- All 6 MCP tools (get_all, get_task, get_project, get_tag, add_tasks, edit_tasks) have schema-vs-data validation via jsonschema
- Fixtures cover DailyFrequency, WeeklyFrequency(onDays), MonthlyDayOfWeekFrequency(on), MonthlyDayInMonthFrequency(onDates), EndByDate, EndByOccurrences
- Union regression guard covers Frequency (8 branches with const discriminator) and EndCondition (2 branches with properties)
- Broad catch-all: no $defs entry in any tool schema may be the erased {type:object, additionalProperties:true} form
- Naming convention enforcement scans models/ and contracts/ packages programmatically
- CLAUDE.md now directs agents to read naming taxonomy and run schema tests after model changes

## Task Commits

Each task was committed atomically:

1. **Task 1: Schema-vs-data validation + union regression guard + dependency + migration** - `fb7760d` (feat)
2. **Task 2: Naming convention enforcement test + CLAUDE.md agent rules** - `51b6a44` (feat)

## Files Created/Modified
- `tests/test_output_schema.py` - 16 tests: schema validation, union regression guard, naming conventions
- `pyproject.toml` - Added jsonschema>=4.23.0 dev dependency
- `tests/test_models.py` - Removed TestSerializationSchema (migrated), cleaned unused imports
- `CLAUDE.md` - Added Model Conventions section with agent rules

## Decisions Made
- Used inline Pydantic model construction (via conftest factories) rather than InMemoryBridge for schema tests -- these are pure model-layer tests, no bridge needed
- Used pydantic_core.to_jsonable_python() to match FastMCP's exact serialization path
- Exempt lists kept minimal: only base/infrastructure classes that structurally cannot follow suffix conventions

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all tests are fully wired with real schema generation and validation.

## Next Phase Readiness
- Schema validation infrastructure in place for any future model changes
- Any new MCP tool or model change will be caught by these regression tests
- Naming convention tests will flag violations when new models are added

---
*Phase: 32.1-output-schema-validation-gap*
*Completed: 2026-03-28*
