---
status: resolved
phase: 33.1-refactor-frequency-to-flat-model-with-type-optional-edits
source: 33.1-01-SUMMARY.md, 33.1-02-SUMMARY.md, 33.1-03-SUMMARY.md, repetition-rules-uat-regression
started: 2026-03-29T14:00:00Z
updated: 2026-03-29T15:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Add task with flat frequency model (6 types)
expected: Using add_tasks, create a task with a repetition rule using the flat frequency model (e.g. type "weekly" with on_days). Task creates successfully and reads back with correct frequency fields.
result: pass
source: regression tests 1a-1e (all 6 types verified)

### 2. Type-optional edit (infer type from existing)
expected: Edit a task's frequency fields WITHOUT specifying "type" — e.g. just change interval or on_days. The edit should succeed, inferring the type from the existing task's frequency. Reading back should show the updated field with original type preserved.
result: pass
source: regression tests 3a-3d (interval, onDays, basedOn, end — all preserved type)

### 3. Type change on edit (full replacement)
expected: Edit a task's frequency by specifying a DIFFERENT type (e.g. change from "weekly" to "daily"). The edit should replace the entire frequency with defaults for the new type, plus any fields you specified. Old specialization fields (like on_days) should be gone.
result: pass
source: regression test 4a (daily to weekly, full replacement)

### 4. Monthly mutual exclusion auto-clear (on → on_dates)
expected: Create or edit a monthly task with "on" set (day-of-week). Then edit to set "on_dates" instead. The "on" field should be auto-cleared with a warning.
result: pass
source: regression test 8c (on→onDates direction, auto-clear warning present)

### 5. Invalid frequency type rejection
expected: Try to add a task with an invalid frequency type. Should get a clear validation error mentioning the valid types (daily, weekly, monthly, yearly, hourly, minutely).
result: pass
source: regression test 10a (missing type field, no pydantic internals leaked)

### 6. Error: interval=0 leaks pydantic internals
expected: interval=0 should return clean error like "Interval must be >= 1"
result: issue
reported: "Raw pydantic validation error: 1 validation error for Frequency / interval / Input should be greater than or equal to 1 [type=greater_than_equal, input_value=0, input_type=int] with pydantic docs URL. Leaks pydantic internals."
severity: major

### 7. Error: end occurrences=0 confusing message
expected: occurrences=0 should return clean error like "End occurrences must be >= 1"
result: issue
reported: "Field required; Input should be greater than or equal to 1 — no field name context, confusing 'Field required' prefix, doesn't match expected message format"
severity: major

### 8. Interval=1 suppression in output
expected: Create a task with a daily/weekly frequency and interval=1 (or omit interval). When reading the task back, the "interval" field should NOT appear in the frequency output (suppressed as default).
result: pass
source: manual verification — frequency serialized as {"type": "daily"} with no interval key

### 9. Reverse auto-clear (on_dates → on direction)
expected: Create a monthly task with on_dates set. Then edit to set "on" instead. The "on_dates" field should be auto-cleared with a warning. (Reverse direction of test 4.)
result: pass
source: manual verification — auto-clear warning present, on_dates absent after edit

### 10. Tool descriptions show flat model docs
expected: The add_tasks and edit_tasks tool descriptions should document the 6 frequency types, type-optional edit semantics, and mutual exclusion rules.
result: pass
source: manual verification — all 6 types listed, type-optional semantics documented, mutual exclusion rules present

### 11. Empty end object gives useless error
expected: end: {} should return a clear error explaining which fields are needed (e.g. "end requires either 'date' or 'occurrences'")
result: issue
reported: "Returns 'Field required; Field required' — technically clean (no pydantic internals) but gives zero guidance about which fields are needed"
severity: minor

### 12. No anchor date warning missing
expected: Creating a task with basedOn set to any anchor (due_date, defer_date, planned_date) but without the corresponding date field set should warn — e.g. "basedOn is due_date but no dueDate is set — OmniFocus will fall back to creation date as anchor. Set a dueDate for predictable repetition."
result: issue
reported: "Silent success for all anchor date types. OmniFocus falls back to creation date as anchor when the specified date isn't set. Works but not what the agent likely intends — should warn to confirm with user."
severity: minor

## Summary

total: 12
passed: 8
issues: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "interval=0 returns clean error without pydantic internals"
  status: resolved
  reason: "User reported: Raw pydantic greater_than_equal error with type=, input_value=, input_type=, URL leaked"
  severity: major
  test: 6
  root_cause: "Frequency.interval uses Field(ge=1) producing pydantic's built-in greater_than_equal error. _format_validation_errors only handles extra_forbidden and literal_error — catch-all else emits raw msg. Other validators use @field_validator with custom ValueError."
  artifacts:
    - path: "src/omnifocus_operator/models/repetition_rule.py"
      issue: "interval uses Field(ge=1) with no custom validator"
    - path: "src/omnifocus_operator/server.py"
      issue: "catch-all else passes raw pydantic msg for greater_than_equal"
  missing:
    - "Add @field_validator on interval raising ValueError with clean message"
    - "Add REPETITION_INVALID_INTERVAL constant to errors.py"

- truth: "end.occurrences=0 returns clean error with field context"
  status: resolved
  reason: "User reported: 'Field required; Input should be >= 1' — no field name, confusing prefix"
  severity: major
  test: 7
  root_cause: "EndCondition = EndByDate | EndByOccurrences is untagged union. Pydantic tries both branches: EndByDate fails with 'Field required' (noise), EndByOccurrences fails with 'Input should be >= 1'. Both hit catch-all else, joined with '; '."
  artifacts:
    - path: "src/omnifocus_operator/models/repetition_rule.py"
      issue: "EndCondition untagged union — pydantic tries both branches"
    - path: "src/omnifocus_operator/server.py"
      issue: "No logic to filter union-branch noise errors"
  missing:
    - "Filter union-branch 'missing' errors when constraint error from matching branch exists"
    - "Add @field_validator on EndByOccurrences.occurrences with clean message"

- truth: "end: {} returns actionable error explaining required fields"
  status: resolved
  reason: "User reported: 'Field required; Field required' — zero guidance on which fields needed"
  severity: minor
  test: 11
  root_cause: "Same untagged EndCondition union. Empty {} fails both branches with 'Field required'. Loc tuples containing field names (EndByDate.date, EndByOccurrences.occurrences) are discarded by catch-all."
  artifacts:
    - path: "src/omnifocus_operator/server.py"
      issue: "_format_validation_errors discards loc context for union 'missing' errors"
  missing:
    - "Add special case for end union errors: 'end requires either date (ISO-8601) or occurrences (integer >= 1)'"

- truth: "basedOn set without corresponding anchor date should warn about creation-date fallback"
  status: resolved
  reason: "User reported: Silent success for all anchor types (due_date, defer_date, planned_date). OmniFocus falls back to creation date — works but not what agent likely intends. Should warn to confirm with user."
  severity: minor
  test: 12
  root_cause: "No check for basedOn/date-field mismatch. basedOn passes through to bridge without validating anchor date exists. For add: check command date fields. For edit: resolve effective date (command if set, else existing task)."
  artifacts:
    - path: "src/omnifocus_operator/service/service.py"
      issue: "Add and edit pipelines don't check anchor date existence"
    - path: "src/omnifocus_operator/agent_messages/warnings.py"
      issue: "Missing REPETITION_ANCHOR_DATE_MISSING constant"
  missing:
    - "Add REPETITION_ANCHOR_DATE_MISSING warning to warnings.py"
    - "Add check_anchor_date_warning() to domain.py DomainLogic"
    - "Call from both add and edit pipelines in service.py"
