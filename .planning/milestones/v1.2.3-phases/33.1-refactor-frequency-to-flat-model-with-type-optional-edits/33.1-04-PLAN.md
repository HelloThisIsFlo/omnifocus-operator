---
phase: 33.1-refactor-frequency-to-flat-model-with-type-optional-edits
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/models/repetition_rule.py
  - src/omnifocus_operator/contracts/use_cases/repetition_rule.py
  - src/omnifocus_operator/agent_messages/errors.py
  - src/omnifocus_operator/server.py
  - tests/test_validation_repetition.py
  - tests/test_server.py
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03]
gap_closure: true

must_haves:
  truths:
    - "interval=0 returns clean error 'Interval must be >= 1' without pydantic internals"
    - "end occurrences=0 returns clean error with field context, no 'Field required' noise"
    - "end: {} returns actionable error explaining 'end requires either date or occurrences'"
  artifacts:
    - path: "src/omnifocus_operator/models/repetition_rule.py"
      provides: "@field_validator on interval with clean ValueError"
      contains: "REPETITION_INVALID_INTERVAL"
    - path: "src/omnifocus_operator/contracts/use_cases/repetition_rule.py"
      provides: "@field_validator on FrequencyAddSpec.interval (mirrors Frequency)"
    - path: "src/omnifocus_operator/agent_messages/errors.py"
      provides: "REPETITION_INVALID_INTERVAL and REPETITION_INVALID_END_OCCURRENCES constants"
    - path: "src/omnifocus_operator/server.py"
      provides: "Union error filtering and end:{} special-case in _format_validation_errors"
  key_links:
    - from: "src/omnifocus_operator/models/repetition_rule.py"
      to: "src/omnifocus_operator/agent_messages/errors.py"
      via: "import REPETITION_INVALID_INTERVAL"
      pattern: "REPETITION_INVALID_INTERVAL"
    - from: "src/omnifocus_operator/server.py"
      to: "agent_messages/errors.py"
      via: "import REPETITION_INVALID_END_* for end:{} special case"
      pattern: "REPETITION_INVALID_END"
---

<objective>
Fix 3 validation error quality gaps found in UAT: interval=0 leaking pydantic internals (major), end occurrences=0 confusing message (major), end:{} useless error (minor).

Purpose: Agent-facing errors must be clean, educational, and free of pydantic internals -- consistent with the existing error quality bar.
Output: Custom validators on interval/occurrences, improved _format_validation_errors with union error filtering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/omnifocus_operator/models/repetition_rule.py
@src/omnifocus_operator/contracts/use_cases/repetition_rule.py
@src/omnifocus_operator/agent_messages/errors.py
@src/omnifocus_operator/server.py
@tests/test_validation_repetition.py
@tests/test_server.py

<interfaces>
From src/omnifocus_operator/models/repetition_rule.py:
```python
class Frequency(OmniFocusBaseModel):
    type: FrequencyType
    interval: int = Field(default=1, ge=1)  # <-- gap: no @field_validator

class EndByOccurrences(OmniFocusBaseModel):
    occurrences: int = Field(ge=1)  # <-- gap: no @field_validator

EndCondition = EndByDate | EndByOccurrences  # untagged union
```

From src/omnifocus_operator/contracts/use_cases/repetition_rule.py:
```python
class FrequencyAddSpec(CommandModel):
    interval: int = Field(default=1, ge=1)  # <-- same gap, mirrors Frequency
```

From src/omnifocus_operator/server.py:
```python
def _format_validation_errors(exc: ValidationError) -> list[str]:
    # handles: extra_forbidden, literal_error, _Unset suppression
    # catch-all else: messages.append(e["msg"])  <-- leaks raw pydantic msgs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add custom validators for interval and occurrences, add error constants</name>
  <files>
    src/omnifocus_operator/agent_messages/errors.py,
    src/omnifocus_operator/models/repetition_rule.py,
    src/omnifocus_operator/contracts/use_cases/repetition_rule.py,
    tests/test_validation_repetition.py
  </files>
  <behavior>
    - Frequency(type="daily", interval=0) raises ValueError matching "Interval must be"
    - Frequency(type="daily", interval=-1) raises ValueError matching "Interval must be"
    - Frequency(type="daily", interval=0) error does NOT contain "greater_than_equal" or "type=" or "input_value="
    - EndByOccurrences(occurrences=0) raises ValueError matching "occurrences must be"
    - EndByOccurrences(occurrences=-1) raises ValueError matching "occurrences must be"
    - EndByOccurrences(occurrences=0) error does NOT contain "greater_than_equal" or "type=" or "input_value="
    - FrequencyAddSpec(type="daily", interval=0) raises ValueError matching "Interval must be" (same message as Frequency)
    - Valid values still work: Frequency(type="daily", interval=1), EndByOccurrences(occurrences=5)
  </behavior>
  <action>
    1. Add to errors.py:
       - REPETITION_INVALID_INTERVAL = "Interval must be >= 1 (got {value})"
       - REPETITION_INVALID_END_OCCURRENCES = "End occurrences must be >= 1 (got {value})"

    2. In repetition_rule.py Frequency class:
       - Replace `interval: int = Field(default=1, ge=1)` with `interval: int = Field(default=1)`
       - Add @field_validator("interval", mode="before") that raises ValueError(REPETITION_INVALID_INTERVAL.format(value=v)) when v < 1
       - Keep default=1 so interval=1 suppress logic still works

    3. In repetition_rule.py EndByOccurrences class:
       - Replace `occurrences: int = Field(ge=1)` with `occurrences: int`
       - Add @field_validator("occurrences", mode="before") that raises ValueError(REPETITION_INVALID_END_OCCURRENCES.format(value=v)) when v < 1

    4. In contracts/use_cases/repetition_rule.py FrequencyAddSpec class:
       - Mirror the same change: replace Field(default=1, ge=1) with Field(default=1) + @field_validator

    5. Update tests/test_validation_repetition.py:
       - Update test_interval_zero_rejected and test_interval_negative_rejected to match the new message ("Interval must be")
       - Update test_occurrences_zero_rejected to match "occurrences must be"
       - Add assertions that error messages do NOT contain "greater_than_equal", "type=", "input_value="
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_validation_repetition.py -x -q</automated>
  </verify>
  <done>
    - interval=0 and interval=-1 produce "Interval must be >= 1 (got N)" -- no pydantic internals
    - occurrences=0 and occurrences=-1 produce "End occurrences must be >= 1 (got N)" -- no pydantic internals
    - FrequencyAddSpec mirrors the same behavior
    - All existing validation tests pass with updated match patterns
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix _format_validation_errors for union error filtering and end:{} special case</name>
  <files>
    src/omnifocus_operator/server.py,
    src/omnifocus_operator/agent_messages/errors.py,
    tests/test_server.py
  </files>
  <behavior>
    - add_tasks with frequency interval=0 returns clean error matching "Interval must be", NOT containing "type=" or "pydantic" or "input_value"
    - add_tasks with end: {occurrences: 0} returns clean error matching "occurrences must be", NOT containing "Field required"
    - add_tasks with end: {} returns error matching "end requires either" with "date" and "occurrences" mentioned
    - Existing error tests (unknown field, invalid frequency type, lifecycle invalid) still pass
  </behavior>
  <action>
    1. Add to errors.py:
       - REPETITION_INVALID_END_EMPTY = "end requires either 'date' (ISO-8601 string) or 'occurrences' (integer >= 1)"

    2. In server.py _format_validation_errors, enhance the logic:

       a. Add union error filtering: when processing errors for an EndCondition union, if there's a constraint error (like "occurrences must be >= 1") from the matching branch AND a "missing" error from the non-matching branch, suppress the "missing" error. Heuristic: if error list has both "missing" (type="missing") and a value_error for fields under the same union loc prefix (e.g. "end"), keep only the value_error.

       b. Add special case for end:{}: if ALL errors under the "end" loc are "missing" type (both union branches failed with "Field required"), replace them all with the single REPETITION_INVALID_END_EMPTY message.

       c. Keep the catch-all `else: messages.append(e["msg"])` -- the custom validators on interval/occurrences now produce clean messages, so the catch-all passes them through cleanly.

       Implementation approach: collect errors into a list first, then post-process before returning. Group errors by whether their loc contains "end". For the "end" group: if all are type="missing", replace with REPETITION_INVALID_END_EMPTY. If mix of "missing" and value_error, keep only non-"missing" ones.

    3. Add server-level tests in tests/test_server.py (in TestAddTasksRepetitionRule or a new class):
       - test_add_tasks_interval_zero_clean_error: sends interval=0, asserts "Interval must be" in error, asserts no pydantic internals ("type=", "pydantic", "input_value" absent)
       - test_add_tasks_end_occurrences_zero_clean_error: sends end={occurrences: 0}, asserts "occurrences must be" in error, asserts "Field required" NOT in error
       - test_add_tasks_end_empty_object_clean_error: sends end={}, asserts "end requires either" in error, asserts "date" and "occurrences" both mentioned
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_server.py::TestAddTasksRepetitionRule -x -q && uv run pytest tests/test_server.py -x -q -k "clean_error"</automated>
  </verify>
  <done>
    - interval=0 through MCP tool returns "Interval must be >= 1" -- clean, no pydantic internals
    - end:{occurrences: 0} returns "occurrences must be >= 1" -- no "Field required" noise
    - end:{} returns "end requires either 'date' or 'occurrences'" -- actionable guidance
    - All existing server tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator
uv run pytest tests/test_validation_repetition.py tests/test_server.py -x -q
uv run pytest tests/test_output_schema.py -x -q
```
</verification>

<success_criteria>
- All 3 UAT gaps (tests 6, 7, 11) produce clean, educational errors without pydantic internals
- No regressions in existing validation or server tests
- Output schema tests still pass (model changes don't affect serialization)
</success_criteria>

<output>
After completion, create `.planning/phases/33.1-refactor-frequency-to-flat-model-with-type-optional-edits/33.1-04-SUMMARY.md`
</output>
