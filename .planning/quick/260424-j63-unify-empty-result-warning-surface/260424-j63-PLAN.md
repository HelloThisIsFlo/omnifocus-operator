---
phase: 260424-j63
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/agent_messages/warnings.py
  - src/omnifocus_operator/service/service.py
  - src/omnifocus_operator/service/domain.py
  - tests/test_list_pipelines.py
  - tests/test_service_domain.py
  - .planning/phases/57-parent-filter-filter-unification/57-UAT.md
autonomous: true
requirements:
  - J63-01  # Retire EMPTY_SCOPE_INTERSECTION_WARNING + FILTER_NO_MATCH (CONTEXT.md Exact retirements)
  - J63-02  # Add unified EMPTY_RESULT_WARNING with alphabetical camelCase filter names (CONTEXT.md Exact additions + Decisions)
  - J63-03  # Reword FILTER_DID_YOU_MEAN to stand alone (CONTEXT.md Decisions: DYM reword)
  - J63-04  # Single emit point covering short-circuit + _delegate paths (CONTEXT.md Decisions: Emit site)
  - J63-05  # Test matrix coverage: 8 scenarios + alphabetical ordering lock (CONTEXT.md Test matrix)
  - J63-06  # UAT audit trail supersession entry linking back to this quick task

must_haves:
  truths:
    - "Empty `list_tasks` result with any non-default filter produces exactly one unified warning."
    - "Filter names in the warning are in alphabetical order, using the exact camelCase aliases the agent sent (e.g., `inInbox`, `estimatedMinutesMax`)."
    - "Empty result with zero active filters produces NO warning."
    - "Default-valued fields (e.g., `availability=[REMAINING]`) never count as active filters."
    - "No-match name with fuzzy candidates produces BOTH the unified empty-result warning AND the reworded DYM warning."
    - "The short-circuit path (is_empty_scope_query) and the delegated path share the same emit site for the unified warning."
    - "The retired constants EMPTY_SCOPE_INTERSECTION_WARNING and FILTER_NO_MATCH are gone from the codebase (zero remaining references)."
    - "57-UAT.md Post-Resolution Gap Audit section records G2's supersession with a link back to quick task 260424-j63."
  artifacts:
    - path: "src/omnifocus_operator/agent_messages/warnings.py"
      provides: "EMPTY_RESULT_WARNING constant + reworded FILTER_DID_YOU_MEAN; retires 2 constants"
      contains: "EMPTY_RESULT_WARNING"
    - path: "src/omnifocus_operator/service/service.py"
      provides: "Unified emit in _ListTasksPipeline.execute() covering both exit paths; _active_filter_names helper; _emit_empty_intersection_warning_if_applicable deleted"
      contains: "_active_filter_names"
    - path: "src/omnifocus_operator/service/domain.py"
      provides: "check_filter_resolution simplified — no-match-no-suggestions branch returns []"
      contains: "check_filter_resolution"
    - path: "tests/test_list_pipelines.py"
      provides: "TestEmptyResultWarning class covering 8-case matrix + alphabetical ordering lock; TestEmptyScopeShortCircuit + TestFilterNoMatchWarningText retired"
      contains: "class TestEmptyResultWarning"
    - path: "tests/test_service_domain.py"
      provides: "test_no_match_* cluster updated — no-match-with-suggestion returns DYM only; no-match-no-suggestion returns []"
    - path: ".planning/phases/57-parent-filter-filter-unification/57-UAT.md"
      provides: "Supersession entry under Post-Resolution Gap Audit (2026-04-24) linking to this quick task"
      contains: "260424-j63"
  key_links:
    - from: "src/omnifocus_operator/service/service.py (_ListTasksPipeline.execute)"
      to: "src/omnifocus_operator/agent_messages/warnings.py (EMPTY_RESULT_WARNING)"
      via: "import + .format(filters=...) at single emit point"
      pattern: "EMPTY_RESULT_WARNING"
    - from: "src/omnifocus_operator/service/service.py (_active_filter_names helper)"
      to: "src/omnifocus_operator/contracts/base.py (is_non_default)"
      via: "iteration over ListTasksQuery.model_fields calling is_non_default(self._query, field_name)"
      pattern: "is_non_default"
    - from: "tests/test_list_pipelines.py (TestEmptyResultWarning)"
      to: "src/omnifocus_operator/service/service.py (emit site)"
      via: "assert EMPTY_RESULT_WARNING text appears in ListResult.warnings for empty+active-filter scenarios"
      pattern: "resolved to zero tasks"
---

<objective>
Unify the empty-result warning surface in `list_tasks` into a single parameterized warning. Retire `EMPTY_SCOPE_INTERSECTION_WARNING` (narrow G2 fix for `project ∩ parent` disjoint case) and `FILTER_NO_MATCH` (subsumed by unified warning + reworded DYM). Implement the design in `.planning/todos/pending/2026-04-24-unify-empty-result-warning-surface.md` verbatim, with all implementation decisions locked in `260424-j63-CONTEXT.md`.

Purpose: Today the single-scope empty case (e.g., `list_tasks(parent="EmptyProject")`) returns `items=[], warnings=None` — the agent can't distinguish "filter matched nothing" from "filter matched an empty entity". Instead of adding yet another narrow sibling warning, collapse the entire empty-result surface into one rule: `items == [] AND any non-default filter → one warning, parameterized by the active-filter names the agent sent`.

Output: Unified `EMPTY_RESULT_WARNING` emitted at a single site covering both the short-circuit and delegated paths, 8-case test matrix in `TestEmptyResultWarning`, alphabetical ordering invariant locked in a dedicated test, 57-UAT.md audit-trail entry recording the supersession.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260424-j63-unify-empty-result-warning-surface/260424-j63-CONTEXT.md
@.planning/todos/pending/2026-04-24-unify-empty-result-warning-surface.md
@src/omnifocus_operator/service/service.py
@src/omnifocus_operator/service/domain.py
@src/omnifocus_operator/agent_messages/warnings.py
@src/omnifocus_operator/contracts/base.py
@src/omnifocus_operator/contracts/use_cases/list/tasks.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase so the executor -->
<!-- does not need to re-explore to understand the shape. -->

From src/omnifocus_operator/contracts/base.py:
```python
def is_non_default(model: BaseModel, field_name: str) -> bool:
    """True if the named field has a value differing from its declared default.
    - Patch[T] field → equivalent to is_set (non-UNSET = non-default).
    - Regular field with concrete default → getattr != model_fields[name].default.
    - Regular field with default_factory → factory() fresh baseline comparison.
    """
```

From src/omnifocus_operator/contracts/use_cases/list/tasks.py (ListTasksQuery fields):
```python
# Patch[...] fields (UNSET = default, any value = non-default):
in_inbox, flagged, project, parent, tags, estimated_minutes_max, search,
due, defer, planned, completed, dropped, added, modified

# Regular fields with defaults (must be non-default to count):
availability    # default=[AvailabilityFilter.REMAINING]
include         # default=[]
only            # default=[]
limit           # default=DEFAULT_LIST_LIMIT
offset          # default=0
```

alias_generator:
```python
# src/omnifocus_operator/models/base.py:25 — alias_generator=to_camel is inherited by QueryModel.
# This means ListTasksQuery.model_fields["in_inbox"].alias == "inInbox"
# and ListTasksQuery.model_fields["estimated_minutes_max"].alias == "estimatedMinutesMax".
# Fields with no case shift (project, parent, flagged, due, defer, etc.) come through unchanged.
# Always prefer .alias when present; fall back to the raw field name when alias is None.
```

From src/omnifocus_operator/service/service.py (_ListTasksPipeline.execute, ~line 415-442):
```python
# Two exit points in execute():
#   1. Short-circuit return (lines 430-440) when _is_empty_scope_query() is True.
#   2. Post-_delegate() return (line 442).
# The unified emit must cover BOTH. CONTEXT.md §Emit site says the natural place is:
#   - Build active_filters = self._active_filter_names() after resolution.
#   - Append EMPTY_RESULT_WARNING just before each return when result.items == [] and active_filters.
# Or: extract a common tail that both paths flow through. Either is fine — implementer's discretion.
```

From src/omnifocus_operator/service/domain.py (check_filter_resolution, lines 556-594):
```python
# Current: returns [FILTER_NO_MATCH] when no match + no suggestions.
# New: returns [] when no match + no suggestions (the unified warning handles it).
# Drop the fallback branch at line 593; update the docstring at lines 565-568.
```

From src/omnifocus_operator/agent_messages/warnings.py (existing style reference):
```python
# Single-quote style around user-provided values (line 172):
LIST_TASKS_INBOX_PROJECT_WARNING = """\
The 'project="{value}"' filter also matches the OmniFocus Inbox by name, ..."""
# Match this style for EMPTY_RESULT_WARNING (single quotes around each filter name).
```
</interfaces>

<existing_imports_to_prune>
- service.py:26 — remove `EMPTY_SCOPE_INTERSECTION_WARNING` from the import block; add `EMPTY_RESULT_WARNING`.
- domain.py:38 — remove `FILTER_NO_MATCH` from the import block; `FILTER_DID_YOU_MEAN` stays.
</existing_imports_to_prune>

<existing_test_references_to_update>
- tests/test_list_pipelines.py:2192 — `class TestEmptyScopeShortCircuit` → retire (superseded by TestEmptyResultWarning).
- tests/test_list_pipelines.py:2230 — comment `G2 new: EMPTY_SCOPE_INTERSECTION_WARNING fires alongside` → dead, delete with parent test.
- tests/test_list_pipelines.py:2282 — comment about EMPTY_SCOPE_INTERSECTION_WARNING not firing → dead, delete.
- tests/test_list_pipelines.py:2296 — `class TestFilterNoMatchWarningText` → retire.
- tests/test_list_pipelines.py:2522 — docstring reference `(EMPTY_SCOPE_INTERSECTION_WARNING fires)` → update to describe the unified warning or remove if the test is subsumed.
- tests/test_service_domain.py:1117 — `test_no_match_with_suggestion` → keep, assert DYM-only return (no prefix).
- tests/test_service_domain.py:1125 — `test_no_match_no_suggestion` → flip to assert `[]` return.
</existing_test_references_to_update>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Warnings + service + domain — unified emit</name>
  <files>
    - src/omnifocus_operator/agent_messages/warnings.py
    - src/omnifocus_operator/service/service.py
    - src/omnifocus_operator/service/domain.py
  </files>
  <behavior>
    Source-side changes that make the 8-case test matrix in Task 2 pass. All behavior locked in CONTEXT.md; do not improvise.

    warnings.py:
    - Delete `FILTER_NO_MATCH` (line 160).
    - Delete `EMPTY_SCOPE_INTERSECTION_WARNING` (lines 200-206 including the comment block above).
    - Reword `FILTER_DID_YOU_MEAN` (lines 222-224) verbatim per CONTEXT.md Decisions §DYM reword:
      `FILTER_DID_YOU_MEAN = "Did you mean: {suggestions}? (no {entity_type} matched '{value}')"`
    - Add `EMPTY_RESULT_WARNING` near the other list_tasks warnings (logical grouping with `LIST_TASKS_INBOX_PROJECT_WARNING` / `PARENT_PROJECT_COMBINED_WARNING`) with two templates selected by active-filter count:
      - 1 filter:  `"The 'X' filter resolved to zero tasks. No results."`
      - 2+ filters: `"The combination of filters 'X', 'Y', 'Z' resolved to zero tasks. No results."`
      Implementer's discretion: single constant with a branching format helper, or two constants. CONTEXT.md example text is authoritative; preserve single-quote wrapping and plain comma separation (no Oxford comma, no "and" conjunction — matches existing warning style).

    service.py:
    - Delete `_emit_empty_intersection_warning_if_applicable` method (lines 642-660) AND the call site at line 425.
    - Add `_active_filter_names(self) -> list[str]` pipeline method (CONTEXT.md §Claude's Discretion: method, not free function) that:
      - Iterates `ListTasksQuery.model_fields`.
      - Filters by `is_non_default(self._query, field_name)`.
      - Maps each kept field to its camelCase alias: `ListTasksQuery.model_fields[name].alias or name`.
      - Returns the list sorted alphabetically (stable `sorted()`).
    - Add a single emit step that runs before BOTH the short-circuit `return` (line 435-440) AND the `return await self._delegate()` (line 442). Two acceptable shapes:
      (a) Extract a common tail (`_finalize(result)`) that both paths call — preferred if it keeps execute() readable.
      (b) Inline the emit check at both return sites.
    - The emit predicate: `result.items == [] and active_filters`. When true, append the formatted warning to `self._warnings` (or to the result's warnings — whatever matches the existing pattern for the short-circuit path).
    - Update the service.py:26 import block: drop `EMPTY_SCOPE_INTERSECTION_WARNING`, add `EMPTY_RESULT_WARNING` (and any helper constant you introduced).

    domain.py:
    - In `check_filter_resolution` (lines 556-594): delete the `return [FILTER_NO_MATCH.format(...)]` branch at line 593. When `len(resolved_ids) == 0` and no fuzzy suggestions exist, return `[]`. Update the docstring at lines 565-568 to remove the "No match, no suggestions → FILTER_NO_MATCH" bullet.
    - Drop `FILTER_NO_MATCH` from the imports at line 38. Keep `FILTER_DID_YOU_MEAN`.

    Scope guard (CONTEXT.md §Task Boundary + Flo's steer "change should be pretty minimal"):
    - Do NOT refactor adjacent warnings, helpers, or domain methods.
    - Do NOT introduce additional helpers beyond `_active_filter_names`.
    - Do NOT special-case `$inbox`, `[]` tag sets, or any sentinel value — `is_non_default` handles all cases uniformly (CONTEXT.md §`$inbox` zero-result behavior).
    - If you encounter something you think should be cleaned up that's outside this scope, raise a checkpoint — do not silently expand scope.

    Verification lens for this task specifically: `ripgrep -n "EMPTY_SCOPE_INTERSECTION_WARNING\|FILTER_NO_MATCH" src/` should return zero hits after changes.
  </behavior>
  <action>
    Execute the behavior block above. Order within the task:
    1. `warnings.py` first — retire the two constants, reword DYM, add `EMPTY_RESULT_WARNING`.
    2. `domain.py` next — drop `FILTER_NO_MATCH` usage + import + docstring bullet.
    3. `service.py` last — delete helper + call site, add `_active_filter_names`, wire unified emit covering both exit paths, update imports.

    Per CONTEXT.md Decisions:
    - Filter names in warning text are **alphabetically sorted camelCase aliases** (e.g., `due, flagged, inInbox, parent`).
    - **Single-quote each name**, **comma-separate**, **no conjunction** between the last two.
    - **Zero-filter empty result**: skip emission entirely (no warning).
    - **No special casing for `$inbox`** — it's just a non-default `project` value.

    Reference implementation sketch for `_active_filter_names` (implementer may adjust idiomatically):
    ```python
    def _active_filter_names(self) -> list[str]:
        query = self._query
        names = [
            (ListTasksQuery.model_fields[name].alias or name)
            for name in ListTasksQuery.model_fields
            if is_non_default(query, name)
        ]
        return sorted(names)
    ```

    Rerun `mypy` + `ruff` gates before marking done — project uses strict mypy and these changes touch imports and warning constants.
  </action>
  <verify>
    <automated>uv run ruff check src/omnifocus_operator/agent_messages/warnings.py src/omnifocus_operator/service/service.py src/omnifocus_operator/service/domain.py &amp;&amp; uv run mypy src/omnifocus_operator/service/service.py src/omnifocus_operator/service/domain.py &amp;&amp; rg -n "EMPTY_SCOPE_INTERSECTION_WARNING|FILTER_NO_MATCH" src/ tests/ ; test $? -eq 1</automated>
  </verify>
  <done>
    - `EMPTY_RESULT_WARNING` constant present in `warnings.py` (with `{filters}` placeholder or equivalent) following CONTEXT.md text templates exactly.
    - `FILTER_NO_MATCH` and `EMPTY_SCOPE_INTERSECTION_WARNING` deleted from `warnings.py` — zero remaining references in `src/`.
    - `FILTER_DID_YOU_MEAN` reworded verbatim per CONTEXT.md.
    - `_active_filter_names` pipeline method exists on `_ListTasksPipeline`, iterates `ListTasksQuery.model_fields`, filters via `is_non_default`, maps to camelCase alias, returns sorted list.
    - `_emit_empty_intersection_warning_if_applicable` method + its call site at line 425 deleted.
    - Single emit point in `execute()` covers both the short-circuit return AND the `_delegate()` return paths.
    - `check_filter_resolution` in `domain.py` returns `[]` when no match + no suggestions (FILTER_NO_MATCH branch deleted).
    - `ruff check` + `mypy` pass clean on touched files.
    - Existing tests may be failing (expected until Task 2 updates them); that does not block this task's done state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test suite — retire dead tests, add TestEmptyResultWarning + alphabetical lock</name>
  <files>
    - tests/test_list_pipelines.py
    - tests/test_service_domain.py
  </files>
  <behavior>
    Test surface must match the new two-layer model and cover CONTEXT.md's 8-case matrix + the alphabetical ordering invariant.

    tests/test_list_pipelines.py:
    - Delete `class TestEmptyScopeShortCircuit` (starts line 2192) — its behavior is subsumed by the unified warning tests.
    - Delete `class TestFilterNoMatchWarningText` (starts line 2296) — `FILTER_NO_MATCH` no longer exists.
    - Any other tests referencing `EMPTY_SCOPE_INTERSECTION_WARNING` (e.g., the comment/docstring at line 2522): update the assertion to expect `EMPTY_RESULT_WARNING` text (or remove the assertion if it's redundant with the new matrix).
    - Add `class TestEmptyResultWarning` with exactly the 8 cases from CONTEXT.md §Test matrix (mirror existing test organization in the file — use InMemoryBridge fixtures, match existing async patterns):
      1. `test_scope_empty_fires_single_filter_warning` — project resolves to a real-but-empty project (no tasks). Assert result.items == [] AND warnings contains `"The 'project' filter resolved to zero tasks. No results."`.
      2. `test_intersection_empty_fires_two_filter_warning` — project ∩ parent disjoint. Assert warnings contains `"The combination of filters 'parent', 'project' resolved to zero tasks. No results."` (alphabetical) alongside the existing `PARENT_PROJECT_COMBINED_WARNING`.
      3. `test_pruning_empty_fires_single_filter_warning` — `flagged=true` with no flagged tasks in the bridge. Assert single-filter `"The 'flagged' filter resolved to zero tasks. No results."`.
      4. `test_no_match_name_fires_single_filter_warning` — `project="NonExistent"`. Assert unified warning fires AND DYM fires when a suggestion exists (test both sub-cases if practical; or split into two tests).
      5. `test_three_plus_active_filters_alphabetical` — set e.g. `flagged=true, inInbox=true, parent="X", due="today"` with empty result. Assert warning text is `"The combination of filters 'due', 'flagged', 'inInbox', 'parent' resolved to zero tasks. No results."` (alphabetical camelCase). **This is the alphabetical ordering lock test** (CONTEXT.md §Alphabetical ordering lock).
      6. `test_default_availability_does_not_count_as_filter` — only `availability=[REMAINING]` is set (the default), and the result is empty (synthesized bridge state). Assert NO unified warning. This locks the `is_non_default` semantic for regular fields.
      7. `test_zero_filters_empty_result_no_warning` — synthesize an empty result with every query field at its default. Assert NO unified warning (per CONTEXT.md §Zero-filter case).
      8. `test_dym_standalone_with_unified_warning` — name no-match with fuzzy candidates. Assert BOTH the unified empty-result warning AND the reworded `FILTER_DID_YOU_MEAN` text (`"Did you mean: ...? (no project matched '...')"`) appear in warnings.

    tests/test_service_domain.py (lines 1117-1131):
    - Update `test_no_match_with_suggestion` (line 1117): assertion stays — returns one warning. Verify the returned string matches the **reworded** `FILTER_DID_YOU_MEAN` template: `"Did you mean: ...? (no {entity_type} matched '{value}')"`. Do not assert the old "No ... found matching" prefix.
    - Update `test_no_match_no_suggestion` (line 1125): flip the assertion — `check_filter_resolution(...)` now returns `[]` for no-match-no-suggestions. Delete the Phase 57-04 G3 comment at line 1131 referencing `FILTER_NO_MATCH`.

    Style:
    - Mirror existing test-class organization in the file (async fixtures, `pytest.mark.asyncio` if used, InMemoryBridge setup helpers).
    - Use the alphabetical ordering lock test (case 5) as the canonical regression guard — its failure signals either an ordering drift or an alias-generator regression.
    - Match the camelCase alias surface literally: `'inInbox'` (not `'in_inbox'`), `'estimatedMinutesMax'` (if used in any synthetic case).
  </behavior>
  <action>
    Execute the behavior block above. Order:
    1. Retire the two dead test classes in `test_list_pipelines.py` (delete, don't comment out).
    2. Update the two domain tests in `test_service_domain.py`.
    3. Add `class TestEmptyResultWarning` with all 8 cases + alphabetical lock.
    4. Run the full test suite. Every existing green test except the ones deleted/updated should stay green — if other tests fail, read the failure and determine whether it's a legitimate semantic regression (bug in Task 1) or a test that was depending on `FILTER_NO_MATCH` / `EMPTY_SCOPE_INTERSECTION_WARNING` text that needs rewording. Fix in-place; do not widen scope.

    Guardrails:
    - SAFE-01/02: use `InMemoryBridge` only. No `RealBridge`, no UAT scripts.
    - Do NOT re-snapshot golden masters if any are touched — golden-master capture is human-only (GOLD-01).
    - If a test outside this task's scope fails for reasons unrelated to the warning surface, stop and raise a checkpoint rather than patching unrelated code.
  </action>
  <verify>
    <automated>uv run pytest tests/test_list_pipelines.py::TestEmptyResultWarning -x -q &amp;&amp; uv run pytest tests/test_service_domain.py -k "no_match" -x -q &amp;&amp; uv run pytest tests/test_list_pipelines.py tests/test_service_domain.py -x -q</automated>
  </verify>
  <done>
    - `TestEmptyScopeShortCircuit` and `TestFilterNoMatchWarningText` deleted from `test_list_pipelines.py`.
    - `TestEmptyResultWarning` class exists in `test_list_pipelines.py` with all 8 CONTEXT.md matrix cases implemented.
    - Alphabetical ordering lock test present and passing (case 5 — verifies filter names are sorted camelCase aliases).
    - `test_no_match_with_suggestion` + `test_no_match_no_suggestion` in `test_service_domain.py` updated to the new contract (DYM reworded; no-suggestion path returns `[]`).
    - `uv run pytest tests/test_list_pipelines.py tests/test_service_domain.py -x -q` is fully green.
    - Zero remaining references to `FILTER_NO_MATCH` or `EMPTY_SCOPE_INTERSECTION_WARNING` anywhere in `tests/`.
  </done>
</task>

<task type="auto">
  <name>Task 3: UAT audit trail — record G2 supersession in 57-UAT.md</name>
  <files>
    - .planning/phases/57-parent-filter-filter-unification/57-UAT.md
  </files>
  <action>
    Append a new subsection at the end of the existing `## Post-Resolution Gap Audit (2026-04-24)` section (after the G1 discussion ending at line 348). Do NOT modify any existing content above — only append.

    Add a `### G2 — Empty-scope intersection warning — SUPERSEDED (2026-04-24)` subsection that records:
    - The G2 fix shipped in 57-04 introduced `EMPTY_SCOPE_INTERSECTION_WARNING`, firing only for the `project ∩ parent` disjoint case.
    - The single-scope-empty and no-match cases remained silent — `list_tasks(parent="EmptyProject")` returned `items=[], warnings=None` with no signal to the agent.
    - Decision (2026-04-24): unify the entire empty-result warning surface into one parameterized `EMPTY_RESULT_WARNING`, retire `EMPTY_SCOPE_INTERSECTION_WARNING` AND `FILTER_NO_MATCH`.
    - Implementation tracked under quick task `260424-j63` — link: `.planning/quick/260424-j63-unify-empty-result-warning-surface/`.
    - Two-layer model (Layer 1 = unified empty-result; Layer 2 = reworded DYM on top).
    - Scope guard: service-layer only; no contract or repo changes.

    Keep the prose tight and scannable — bullets over paragraphs, per Flo's doc style preference. Match the tone of the existing G1 discussion (concrete, decision-first, with file/line references where useful).

    Do NOT edit 57-UAT.md frontmatter (status: resolved must stay). Do NOT touch any other phase 57 artifacts — CONTEXT.md explicitly lists phase-57 frozen historical documents that must not be altered.
  </action>
  <verify>
    <automated>rg -n "260424-j63" .planning/phases/57-parent-filter-filter-unification/57-UAT.md &amp;&amp; rg -n "SUPERSEDED" .planning/phases/57-parent-filter-filter-unification/57-UAT.md</automated>
  </verify>
  <done>
    - A new `### G2 — ... SUPERSEDED (2026-04-24)` subsection appended under `## Post-Resolution Gap Audit (2026-04-24)`.
    - The subsection references quick task `260424-j63` with a path link.
    - The subsection records: what G2 shipped, why it was incomplete, the 2026-04-24 decision to unify, and what was retired.
    - No other content in 57-UAT.md is modified; frontmatter `status: resolved` unchanged.
    - `rg -n "260424-j63" .planning/phases/57-parent-filter-filter-unification/57-UAT.md` returns at least one match.
  </done>
</task>

</tasks>

<verification>
End-to-end check that the unified warning surface is live and consistent:

1. Full test suite: `uv run pytest -x -q` — must be fully green.
2. Strict gates: `uv run ruff check src tests` and `uv run mypy src` — clean.
3. Retirement audit: `rg -n "EMPTY_SCOPE_INTERSECTION_WARNING|FILTER_NO_MATCH" src/ tests/` — exits with code 1 (no matches).
4. New constant audit: `rg -n "EMPTY_RESULT_WARNING" src/` — appears in `warnings.py` definition and `service.py` import + emit site.
5. Alphabetical lock test: `uv run pytest tests/test_list_pipelines.py::TestEmptyResultWarning -k "alphabetical" -x -q` — green.
6. Audit trail: `rg -n "260424-j63" .planning/phases/57-parent-filter-filter-unification/57-UAT.md` — one match.
7. Smoke (manual-read): open `warnings.py`, confirm `FILTER_DID_YOU_MEAN` reads `"Did you mean: {suggestions}? (no {entity_type} matched '{value}')"` verbatim.

No real-OmniFocus interaction required — all verification uses InMemoryBridge (SAFE-01/02 preserved).
</verification>

<success_criteria>
- A `list_tasks` call with `items == []` and any non-default filter returns a `ListResult.warnings` list containing exactly one `"... resolved to zero tasks. No results."` warning, with alphabetically sorted camelCase filter names.
- A `list_tasks` call with `items == []` and zero non-default filters returns NO unified warning.
- A `list_tasks` call with `project="NonExistentSimilarName"` where fuzzy candidates exist returns BOTH the unified warning AND the reworded DYM.
- The retired constants (`EMPTY_SCOPE_INTERSECTION_WARNING`, `FILTER_NO_MATCH`) are absent from `src/` and `tests/`.
- The alphabetical ordering invariant is locked by a dedicated test.
- `57-UAT.md` records the G2 supersession linking back to quick task `260424-j63`.
- Full `pytest` + `ruff` + `mypy` gates are green.
</success_criteria>

<output>
After completion, create `.planning/quick/260424-j63-unify-empty-result-warning-surface/260424-j63-SUMMARY.md` recording:
- Final diff stats (files changed, LOC added/removed — expected ~200 LOC per todo).
- The test case count actually delivered in `TestEmptyResultWarning` (target: 8).
- Confirmation that `EMPTY_SCOPE_INTERSECTION_WARNING` and `FILTER_NO_MATCH` are retired (zero grep hits).
- Link back to the todo (`.planning/todos/pending/2026-04-24-unify-empty-result-warning-surface.md`) and indicate it should be moved to `.planning/todos/completed/` after this task closes.
</output>
