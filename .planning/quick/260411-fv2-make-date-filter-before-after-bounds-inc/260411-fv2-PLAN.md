---
phase: quick-260411-fv2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/service/resolve_dates.py
  - tests/test_resolve_dates.py
autonomous: true
requirements: [DATETIME-BEFORE-INCLUSIVE]

must_haves:
  truths:
    - "Agent filtering with before: '2026-04-14T18:00:00' includes tasks due at exactly 18:00:00"
    - "Date-only before bounds still bump by +1 day (existing behavior unchanged)"
    - "Aware datetime before bounds get +1 minute bump"
    - "Naive datetime before bounds get +1 minute bump"
    - "'now' literal before bounds remain unbumped (exact snapshot semantics)"
  artifacts:
    - path: "src/omnifocus_operator/service/resolve_dates.py"
      provides: "+1 minute bump for datetime before bounds"
      contains: "timedelta(minutes=1)"
    - path: "tests/test_resolve_dates.py"
      provides: "Updated assertions for datetime before bounds"
  key_links:
    - from: "resolve_dates.py::_parse_absolute_before"
      to: "query_builder.py / bridge_only.py"
      via: "ResolvedDateBounds.before used with < comparison"
      pattern: "< before"
---

<objective>
Bump datetime `before` bounds by +1 minute in `_parse_absolute_before` so the half-open SQL `<` comparison includes the agent's boundary value. Date-only bounds already use +1 day for this purpose; this extends the pattern to datetime strings.

Purpose: Fix inclusive-bound contract violation where `before: "2026-04-14T18:00:00"` excluded tasks due at exactly 18:00.
Output: Updated `resolve_dates.py` and corresponding test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/omnifocus_operator/service/resolve_dates.py
@tests/test_resolve_dates.py
@docs/architecture.md (ResolvedDateBounds contract section)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add +1 minute bump for datetime before bounds</name>
  <files>src/omnifocus_operator/service/resolve_dates.py, tests/test_resolve_dates.py</files>
  <behavior>
    - test_before_datetime (aware): AbsoluteRangeFilter(before="2026-04-14T18:00:00Z") resolves to datetime(2026, 4, 14, 18, 1, 0, tzinfo=UTC) — bumped by 1 minute
    - test_before_datetime_naive: AbsoluteRangeFilter(before="2026-04-14T18:00:00") with NOW resolves to datetime(2026, 4, 14, 18, 1, 0, tzinfo=NOW.tzinfo) — naive inherits tzinfo, then bumped
    - test_before_date_only (unchanged): AbsoluteRangeFilter(before="2026-04-14") still resolves to datetime(2026, 4, 15, 0, 0, 0) — +1 day, no change
    - test_before_now (unchanged): AbsoluteRangeFilter(before="now") still resolves to NOW exactly — no bump
    - test_both_datetime: AbsoluteRangeFilter(after="2026-04-01T09:00:00Z", before="2026-04-14T18:00:00Z") — before resolves to 18:01, after unchanged at 09:00
  </behavior>
  <action>
In `_parse_absolute_before` in `resolve_dates.py`:

1. The two datetime branches (naive and aware) currently return the datetime as-is. Add `+ timedelta(minutes=1)` to both return statements so that `< before` includes the agent's boundary value.

Specifically:
- Line ~276 (naive branch): change `return dt.replace(tzinfo=now.tzinfo)` to `return dt.replace(tzinfo=now.tzinfo) + timedelta(minutes=1)`
- Line ~279 (aware branch): change `return dt` to `return dt + timedelta(minutes=1)`

The date-only branch (line ~272) and the "now" literal (line ~266) remain unchanged.

2. Update existing test assertions in `tests/test_resolve_dates.py`:
- `TestAbsoluteBefore::test_before_datetime`: expect `datetime(2026, 4, 14, 18, 1, 0, tzinfo=UTC)` (was 18:00)
- `TestAbsoluteBoth::test_both_datetime`: expect `before == datetime(2026, 4, 14, 18, 1, 0, tzinfo=UTC)` (was 18:00), `after` unchanged

3. Add a new test to `TestAbsoluteBefore`:
- `test_before_datetime_naive`: Input `AbsoluteRangeFilter(before="2026-04-14T18:00:00")` with NOW. Assert `resolved.before == datetime(2026, 4, 14, 18, 1, 0, tzinfo=UTC)` — naive gets tzinfo from NOW, then gets +1 minute bump.

4. Update the docstring on `test_before_datetime` from "Full datetime before -> exact value" to "Full datetime before -> +1 minute (inclusive bound for half-open < comparison)".
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_resolve_dates.py -x -q</automated>
  </verify>
  <done>
    - Aware datetime before bounds bumped by +1 minute
    - Naive datetime before bounds bumped by +1 minute (with tzinfo inheritance)
    - Date-only before bounds still use +1 day (unchanged)
    - "now" literal returns exact NOW (unchanged)
    - All test_resolve_dates.py tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no downstream test breakage</name>
  <files></files>
  <action>
Run the full test suite to confirm no downstream tests (cross-path equivalence, list pipelines, query builder, etc.) break from the +1 minute bump. No code changes expected — this is a verification-only task.

If any tests fail, investigate and fix the assertions to match the new inclusive-bound behavior (the bump is the correct behavior; any test asserting the old exact-value is wrong).

Also update `docs/architecture.md` if needed — check the `_parse_absolute_before()` description near line 267. Currently it only mentions date-only bumps. Add a note about datetime bumps:
- `_parse_absolute_before()` bumps date-only values by +1 day and datetime values by +1 minute
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest -x -q</automated>
  </verify>
  <done>
    - Full test suite passes (534+ tests)
    - Architecture docs updated to mention datetime +1 minute bump
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this is a pure internal resolution logic change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fv2-01 | T (Tampering) | resolve_dates._parse_absolute_before | accept | No external input reaches this function directly; contract layer validates strings upstream |
</threat_model>

<verification>
- `uv run pytest tests/test_resolve_dates.py -x -q` — all date resolution tests pass
- `uv run pytest -x -q` — full suite passes, no downstream breakage
- Spot-check: `before: "2026-04-14T18:00:00Z"` resolves to `2026-04-14T18:01:00Z` (not 18:00)
</verification>

<success_criteria>
- Datetime `before` bounds are bumped by +1 minute, making the agent-facing contract truly inclusive-inclusive
- Date-only `before` bounds unchanged (+1 day)
- "now" literal unchanged (exact snapshot)
- All tests pass including new naive datetime test
- Architecture docs reflect the datetime bump
</success_criteria>

<output>
After completion, create `.planning/quick/260411-fv2-make-date-filter-before-after-bounds-inc/260411-fv2-SUMMARY.md`
</output>
