---
phase: quick
plan: 260331-v9q
type: execute
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/contracts/use_cases/add/tasks.py
  - src/omnifocus_operator/contracts/use_cases/edit/tasks.py
  - tests/test_contracts_field_constraints.py
  - tests/test_service_payload.py
autonomous: true
requirements: [TODO-10]

must_haves:
  truths:
    - "AddTaskCommand(name='') raises ValidationError at model level"
    - "AddTaskCommand(name='   ') raises ValidationError at model level"
    - "EditTaskCommand(id='x', name='') raises ValidationError at model level"
    - "AddTaskCommand(flagged=...) defaults to False, not None"
    - "AddTaskCommand() without flagged always sends flagged=False to repo payload"
  artifacts:
    - path: "src/omnifocus_operator/contracts/use_cases/add/tasks.py"
      provides: "Tightened AddTaskCommand"
      contains: "bool = False"
    - path: "tests/test_contracts_field_constraints.py"
      provides: "Constraint validation tests"
  key_links:
    - from: "AddTaskCommand.flagged"
      to: "payload builder"
      via: "flagged is now always bool, never None"
      pattern: "flagged.*bool.*False"
---

<objective>
Tighten two loose field constraints on write-side command models.

Purpose: Prevent empty task names at the schema boundary (not just service validation) and ensure `flagged` always has an explicit boolean on creation.
Output: Updated command models + tests proving constraints hold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/omnifocus_operator/contracts/base.py
@src/omnifocus_operator/contracts/use_cases/add/tasks.py
@src/omnifocus_operator/contracts/use_cases/edit/tasks.py
@src/omnifocus_operator/service/payload.py
@src/omnifocus_operator/service/validate.py
@tests/test_service_payload.py
@tests/test_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add field constraint tests</name>
  <files>tests/test_contracts_field_constraints.py</files>
  <behavior>
    - AddTaskCommand(name="") raises ValidationError (min_length=1)
    - AddTaskCommand(name="   ") raises ValidationError (whitespace-only, use field_validator with strip)
    - EditTaskCommand(id="x", name="") raises ValidationError
    - EditTaskCommand(id="x", name="   ") raises ValidationError
    - AddTaskCommand(name="Valid") has flagged=False by default (not None)
    - AddTaskCommand(name="Valid", flagged=True) works
    - AddTaskCommand(name="Valid", flagged=None) raises ValidationError (bool, not Optional)
    - AddTaskCommand(name="x") — single char valid (boundary)
  </behavior>
  <action>
    Create a focused test file for the new field constraints. Use pytest + pydantic ValidationError.
    Group into two classes: TestNameConstraints and TestFlaggedDefault.
    For EditTaskCommand.name, the Patch[str] union means UNSET is still the default — only test explicit empty/whitespace values.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_contracts_field_constraints.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All constraint tests exist and FAIL (red phase — models not yet changed)</done>
</task>

<task type="auto">
  <name>Task 2: Tighten model constraints and update payload builder</name>
  <files>
    src/omnifocus_operator/contracts/use_cases/add/tasks.py
    src/omnifocus_operator/contracts/use_cases/edit/tasks.py
    src/omnifocus_operator/service/payload.py
    tests/test_service_payload.py
  </files>
  <action>
    **AddTaskCommand changes (add/tasks.py):**
    1. Change `flagged: bool | None = None` to `flagged: bool = False`
    2. Add `min_length=1` to `name` field: `name: str = Field(min_length=1)`
    3. Add a `field_validator("name", mode="before")` that strips whitespace before min_length check — this catches `"   "` (whitespace-only). The validator should `return v.strip() if isinstance(v, str) else v`.

    **EditTaskCommand changes (edit/tasks.py):**
    1. Add a `field_validator("name", mode="before")` that strips and validates non-empty when the value is a string (not _Unset). Pattern: `if isinstance(v, str): v = v.strip(); if not v: raise ValueError("Task name cannot be empty")`. Return `v` otherwise (handles _Unset passthrough).
    NOTE: Cannot use `Field(min_length=1)` on `Patch[str]` because the union includes `_Unset`. Use a field_validator instead.

    **Payload builder (payload.py):**
    1. In `_build_add_payload`, the `if command.flagged is not None` guard is now dead code — `flagged` is always a bool. Change to always include: `kwargs["flagged"] = command.flagged`. This ensures `False` is explicitly sent to bridge (the whole point of the change).

    **Test updates (test_service_payload.py):**
    1. Find the test for minimal add-task payload (name-only). It currently asserts `flagged` is absent from payload. Update to assert `flagged=False` is present.

    After all changes, run the full test suite to confirm nothing breaks.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_contracts_field_constraints.py tests/test_service_payload.py tests/test_service.py tests/test_models.py tests/test_output_schema.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All new constraint tests pass (green)
    - All existing tests pass (no regressions)
    - AddTaskCommand.flagged defaults to False
    - Empty/whitespace names rejected at model level for both Add and Edit commands
    - Payload builder always sends flagged to repo
  </done>
</task>

</tasks>

<verification>
Run full suite: `uv run pytest -x -q`
Verify schema output: `uv run pytest tests/test_output_schema.py -x -q`
</verification>

<success_criteria>
- `AddTaskCommand(name="")` and `AddTaskCommand(name="   ")` raise ValidationError
- `EditTaskCommand(id="x", name="")` raises ValidationError
- `AddTaskCommand(name="Valid").flagged` is `False` (not `None`)
- All 534+ tests pass
- Output schema tests pass (no JSON schema regressions)
</success_criteria>

<output>
After completion, create `.planning/quick/260331-v9q-tighten-schema-field-constraints-flagged/260331-v9q-SUMMARY.md`
</output>
