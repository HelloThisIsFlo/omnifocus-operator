---
phase: 260328-sh9
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/omnifocus_operator/rrule/parser.py
  - src/omnifocus_operator/rrule/builder.py
  - tests/test_rrule.py
  - .planning/phases/32-read-model-rewrite/32-CONTEXT.md
autonomous: true
requirements: [BYSETPOS-FIX]

must_haves:
  truths:
    - "BYSETPOS rules with weekday group (MO,TU,WE,TH,FR) parse to MonthlyDayOfWeekFrequency with on={'Nth': 'weekday'}"
    - "BYSETPOS rules with weekend_day group (SU,SA or SA,SU) parse to MonthlyDayOfWeekFrequency with on={'Nth': 'weekend_day'}"
    - "All 6 ordinals (1st-5th, last) work for both day groups"
    - "Unknown multi-day BYSETPOS combos raise educational ValueError"
    - "BYSETPOS rules with non-MONTHLY freq still raise ValueError"
    - "Builder round-trips weekday/weekend_day values back to BYSETPOS form"
    - "Single-day prefix parsing unchanged (no regression)"
  artifacts:
    - path: "src/omnifocus_operator/rrule/parser.py"
      provides: "_parse_monthly_bysetpos() helper, _DAY_GROUP_TO_NAME mapping"
      contains: "_parse_monthly_bysetpos"
    - path: "src/omnifocus_operator/rrule/builder.py"
      provides: "BYSETPOS output for weekday/weekend_day day names"
      contains: "BYSETPOS"
    - path: "tests/test_rrule.py"
      provides: "BYSETPOS parse, build, round-trip, and error tests"
      contains: "bysetpos"
  key_links:
    - from: "src/omnifocus_operator/rrule/parser.py"
      to: "MonthlyDayOfWeekFrequency"
      via: "_parse_monthly_bysetpos returns MonthlyDayOfWeekFrequency with day group name"
      pattern: "MonthlyDayOfWeekFrequency.*weekday|weekend_day"
    - from: "src/omnifocus_operator/rrule/builder.py"
      to: "src/omnifocus_operator/rrule/parser.py"
      via: "round-trip validation — build_rrule calls parse_rrule on output"
      pattern: "parse_rrule\\(result\\)"
---

<objective>
Fix the BYSETPOS rejection in the rrule parser so multi-day positional BYDAY rules parse correctly.

Purpose: OmniFocus produces `BYDAY=MO,TU,WE,TH,FR;BYSETPOS=N` (weekday) and `BYDAY=SU,SA;BYSETPOS=N` (weekend day) for the monthly repeat picker. The blanket `_validate_no_bysetpos()` rejection crashes reads on any database containing these rules. This blocked UAT.

Output: Parser handles the 12 BYSETPOS combinations (2 day groups x 6 ordinals), builder round-trips them, all existing tests unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/omnifocus_operator/rrule/parser.py
@src/omnifocus_operator/rrule/builder.py
@tests/test_rrule.py
@src/omnifocus_operator/models/repetition_rule.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/omnifocus_operator/models/repetition_rule.py:
```python
class MonthlyDayOfWeekFrequency(_FrequencyBase):
    type: Literal["monthly_day_of_week"] = "monthly_day_of_week"
    on: dict[str, str] | None = None  # {"second": "tuesday"} or {"first": "weekday"}
```

From src/omnifocus_operator/rrule/parser.py:
```python
_POS_TO_ORDINAL: dict[int, str] = {1: "first", 2: "second", 3: "third", 4: "fourth", 5: "fifth", -1: "last"}
_DAY_CODE_TO_NAME: dict[str, str] = {"MO": "monday", "TU": "tuesday", ...}
```

From src/omnifocus_operator/rrule/builder.py:
```python
_ORDINAL_TO_POS: dict[str, int] = {"first": 1, "second": 2, ...}
_NAME_TO_DAY_CODE: dict[str, str] = {"monday": "MO", "tuesday": "TU", ...}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BYSETPOS parsing and building with tests</name>
  <files>
    src/omnifocus_operator/rrule/parser.py,
    src/omnifocus_operator/rrule/builder.py,
    tests/test_rrule.py
  </files>
  <behavior>
    **Parser — happy path:**
    - `parse_rrule("FREQ=MONTHLY;BYDAY=SU,SA;BYSETPOS=1")` -> `MonthlyDayOfWeekFrequency(on={"first": "weekend_day"})`
    - `parse_rrule("FREQ=MONTHLY;BYDAY=MO,TU,WE,TH,FR;BYSETPOS=2")` -> `MonthlyDayOfWeekFrequency(on={"second": "weekday"})`
    - `parse_rrule("FREQ=MONTHLY;BYDAY=SA,SU;BYSETPOS=-1")` -> `MonthlyDayOfWeekFrequency(on={"last": "weekend_day"})` (SA,SU order)
    - `parse_rrule("FREQ=MONTHLY;BYDAY=MO,TU,WE,TH,FR;BYSETPOS=5")` -> `MonthlyDayOfWeekFrequency(on={"fifth": "weekday"})`
    - With interval: `parse_rrule("FREQ=MONTHLY;INTERVAL=2;BYDAY=SU,SA;BYSETPOS=1")` -> interval=2

    **Parser — errors:**
    - `parse_rrule("FREQ=MONTHLY;BYDAY=MO,WE;BYSETPOS=1")` -> ValueError mentioning unknown day group
    - `parse_rrule("FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYSETPOS=2")` -> ValueError (BYSETPOS only valid for MONTHLY)

    **Builder — weekday/weekend_day values:**
    - `build_rrule(MonthlyDayOfWeekFrequency(on={"first": "weekend_day"}))` -> `"FREQ=MONTHLY;BYDAY=SU,SA;BYSETPOS=1"`
    - `build_rrule(MonthlyDayOfWeekFrequency(on={"second": "weekday"}))` -> `"FREQ=MONTHLY;BYDAY=MO,TU,WE,TH,FR;BYSETPOS=2"`
    - `build_rrule(MonthlyDayOfWeekFrequency(on={"last": "weekend_day"}))` -> `"FREQ=MONTHLY;BYDAY=SU,SA;BYSETPOS=-1"`

    **Round-trip:**
    - Parse BYSETPOS rule -> build -> parse again -> same model
    - Parametrized over representative combinations

    **Regression:**
    - Existing `test_bysetpos_rejected` test REMOVED (was testing the now-incorrect blanket rejection)
    - All other existing tests pass unchanged
    - Single-day prefix parsing still works (e.g., `BYDAY=2TU`, `BYDAY=-1FR`)
  </behavior>
  <action>
    **In parser.py:**

    1. Add a day-group lookup table near the existing mapping tables:
       ```python
       _DAY_GROUP_TO_NAME: dict[frozenset[str], str] = {
           frozenset({"MO", "TU", "WE", "TH", "FR"}): "weekday",
           frozenset({"SU", "SA"}): "weekend_day",
       }
       ```

    2. Replace `_validate_no_bysetpos()` with a narrower check: BYSETPOS is only valid when FREQ=MONTHLY. If BYSETPOS present with non-MONTHLY freq, raise ValueError with educational message.

    3. In `_parse_monthly()`, when BOTH `BYDAY` and `BYSETPOS` are in parts, route to a new `_parse_monthly_bysetpos(byday_value, bysetpos_value, interval)` helper instead of `_parse_monthly_byday`.

    4. Implement `_parse_monthly_bysetpos()`:
       - Split BYDAY value on commas to get day codes
       - Look up the frozenset of day codes in `_DAY_GROUP_TO_NAME`
       - If not found, raise ValueError: "Unknown BYDAY day group: {codes}. Supported groups: weekday (MO-FR), weekend_day (SA,SU). Please report this rule string."
       - Parse BYSETPOS as int, look up in `_POS_TO_ORDINAL`
       - Return `MonthlyDayOfWeekFrequency(interval=interval, on={ordinal: day_group_name})`

    **In builder.py:**

    5. Add reverse mapping near existing tables:
       ```python
       _DAY_GROUP_BYDAY: dict[str, str] = {
           "weekday": "MO,TU,WE,TH,FR",
           "weekend_day": "SU,SA",
       }
       ```

    6. In `_build_byday_positional()`, check if the day_name is in `_DAY_GROUP_BYDAY`. If yes, emit `BYDAY={group};BYSETPOS={pos}` instead of `BYDAY={pos}{day_code}`. Otherwise fall through to existing single-day prefix logic.

    **In tests/test_rrule.py:**

    7. Remove `test_bysetpos_rejected` from `TestParseRruleErrors` — the blanket rejection no longer applies.

    8. Add `TestParseRruleBysetpos` class with tests for both day groups, representative ordinals (first, last, a middle one), interval passthrough, and error cases (unknown group, non-MONTHLY freq).

    9. Add `TestBuildRruleBysetpos` class with tests for building weekday and weekend_day rules.

    10. Add BYSETPOS round-trip cases to the `TestRoundTrip.test_round_trip_frequency` parametrize list:
        - `"FREQ=MONTHLY;BYDAY=SU,SA;BYSETPOS=1"`
        - `"FREQ=MONTHLY;BYDAY=MO,TU,WE,TH,FR;BYSETPOS=2"`
        - `"FREQ=MONTHLY;BYDAY=SU,SA;BYSETPOS=-1"`
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_rrule.py -x -q</automated>
  </verify>
  <done>
    - BYSETPOS weekday/weekend_day rules parse to correct MonthlyDayOfWeekFrequency
    - Builder emits BYSETPOS form for day group values
    - Round-trip: parse -> build -> parse is identity for all BYSETPOS combos
    - Unknown day groups and non-MONTHLY BYSETPOS raise educational errors
    - All pre-existing tests pass (minus the removed blanket rejection test)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update D-05 decision documentation</name>
  <files>.planning/phases/32-read-model-rewrite/32-CONTEXT.md</files>
  <behavior>
    The D-05 decision in 32-CONTEXT.md currently says "BYSETPOS as separate parameter is not supported" — this is now partially wrong. Update it to reflect the narrowed scope: BYSETPOS is supported for multi-day groups (weekday/weekend_day), single-day rules still use prefix form only.
  </behavior>
  <action>
    In `.planning/phases/32-read-model-rewrite/32-CONTEXT.md`, update the D-05 line to:
    ```
    - **D-05:** BYDAY positional prefix form for single-day rules (`BYDAY=2TU`, `BYDAY=-1FR`). Multi-day day-group rules use BYSETPOS form (`BYDAY=SU,SA;BYSETPOS=1` for "first weekend day", `BYDAY=MO,TU,WE,TH,FR;BYSETPOS=2` for "second weekday"). See quick task 260328-sh9 for root cause and fix.
    ```
  </action>
  <verify>
    <automated>grep "D-05" .planning/phases/32-read-model-rewrite/32-CONTEXT.md</automated>
  </verify>
  <done>
    - D-05 decision updated to reflect BYSETPOS is now supported for multi-day groups
    - Future readers understand this was a deliberate narrowing, not an oversight
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify output schema and full test suite</name>
  <files>tests/test_output_schema.py</files>
  <action>
    Run the output schema validation tests per CLAUDE.md convention (model was not changed, but verify no regression). Then run the full test suite to confirm no breakage outside rrule tests.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/MCP/omnifocus-operator && uv run pytest tests/test_output_schema.py tests/test_rrule.py -x -q && uv run pytest --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Output schema tests pass (no model serialization regression)
    - Full test suite green
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_rrule.py -x -q` — all rrule tests pass including new BYSETPOS cases
- `uv run pytest tests/test_output_schema.py -x -q` — output schema validation passes
- `uv run pytest -q` — full suite green
</verification>

<success_criteria>
- `parse_rrule("FREQ=MONTHLY;BYDAY=SU,SA;BYSETPOS=1")` returns `MonthlyDayOfWeekFrequency(on={"first": "weekend_day"})`
- `parse_rrule("FREQ=MONTHLY;BYDAY=MO,TU,WE,TH,FR;BYSETPOS=2")` returns `MonthlyDayOfWeekFrequency(on={"second": "weekday"})`
- `build_rrule(MonthlyDayOfWeekFrequency(on={"first": "weekend_day"}))` returns `"FREQ=MONTHLY;BYDAY=SU,SA;BYSETPOS=1"`
- Round-trip identity for all BYSETPOS rules
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/quick/260328-sh9-fix-bysetpos-repetition-rule-parsing-bug/260328-sh9-SUMMARY.md`
</output>
