---
session: repetition-rule-validation-failure
status: in_progress
last_updated: 2026-03-03T14:20:56.594Z
---

<current_state>
Debugging RepetitionRule schema mismatch between bridge.js and OmniFocus 4.7+ API.
The debugger agent applied a "make schedule_type optional" fix, but the user investigated
the actual OmniFocus API and decided on a different approach: **target 4.7+ only, no null fallbacks**.

The user is mid-investigation in the OmniFocus automation console, exploring how the
Omni Automation enum objects actually work. Key discovery: enum objects are opaque native
objects where `.name` returns `undefined` — the value is only accessible via `String()` coercion
or comparison against known constants.
</current_state>

<completed_work>

- Debug session opened: root cause identified — bridge.js `rr()` uses `.name` on OmniFocus
  enum objects, which returns `undefined`; `JSON.stringify` silently drops undefined values
- Debugger agent applied a quick fix (4 files) making `schedule_type` optional — but user
  wants a different approach
- User researched full 4.7+ RepetitionRule API: `ruleString`, `scheduleType`, `anchorDateKey`,
  `catchUpAutomatically`, `method` (deprecated)
- Console diagnostic confirmed: enum objects are opaque (`typeof` = object, `Object.keys()` = [],
  `JSON.stringify` = `"{}"`), but `String()` coercion reveals the value:
  - `String(r.scheduleType)` → `"[object Task.RepetitionScheduleType: Regularly]"`
  - `String(r.anchorDateKey)` → `"[object Task.AnchorDateKey: DueDate]"`
- `catchUpAutomatically` works fine as a plain boolean
- Accessing deprecated `method` triggers a runtime warning:
  `"The property 'method' on instances of 'Task.RepetitionRule' is deprecated. Use scheduleType instead."`
</completed_work>

<remaining_work>

- Decide how to extract enum values in bridge.js: `String()` parsing vs constant comparison
  (user still exploring OmniFocus docs for the "intended" way)
- Update bridge.js `rr()` to extract all 4.7+ fields: `ruleString`, `scheduleType`,
  `anchorDateKey`, `catchUpAutomatically` (NOT deprecated `method`)
- Update Pydantic `RepetitionRule` model to match 4.7+ shape — all fields required (no None defaults),
  since on a migrated DB they're guaranteed present when `repetitionRule` is non-null
- Update all tests (Python + JS) to match new schema
- Capture TODO for future phase: smart error detection for unmigrated databases
  (feature-detect on first repeating task, throw human-readable migration prompt)
- Run UAT `uat/test_read_only.py` against live OmniFocus to confirm
</remaining_work>

<decisions_made>

- **4.7+ only**: No support for pre-4.7 OmniFocus database format. User's DB is migrated.
- **No null fallbacks on inner properties**: When `repetitionRule` is non-null on 4.7+, all
  properties are guaranteed populated. Model fields should be required, not optional.
- **Skip deprecated `method` property**: Use `scheduleType` + `anchorDateKey` (the 4.7+ replacements)
- **Future phase for migration detection**: Nice error message for unmigrated DBs will come later.
  `app.version` alone is not sufficient — a user can be on 4.8 with an unmigrated DB. Feature
  detection against real data is the reliable approach.
- **Debugger's quick fix will be replaced**: The `schedule_type: str | None = None` approach
  is wrong for our target. We want required fields matching the real 4.7+ shape.
</decisions_made>

<blockers>

- Need to determine the right way to extract enum values from OmniFocus runtime objects.
  Two options identified: (1) parse `String()` coercion, (2) compare against known constants.
  User is checking official docs for the intended approach.
</blockers>

<context>
The user wants to learn and understand the OmniFocus API before finalizing the fix.
They're actively exploring in the automation console and reading docs. Don't rush to
implement — wait for their findings on enum extraction before updating the bridge script.

The 4 files currently modified by the debugger's quick fix need to be reworked, not committed as-is.

Enum values to support:
- Task.RepetitionScheduleType: None, Regularly, FromCompletion
- Task.AnchorDateKey: DueDate, DeferDate, PlannedDate
</context>

<next_action>
Wait for user's findings on OmniFocus enum extraction (docs + console experiments).
Then update bridge.js `rr()` and Pydantic `RepetitionRule` to match the full 4.7+ shape.
</next_action>
