---
context: default
phase: null
task: null
total_tasks: null
status: complete
last_updated: 2026-05-13T16:28:23.932Z
---

# Session Handoff — Renumber Complete, v1.4.2 Hotfix Pending

This is a **milestone-edge handoff**, not a paused phase. The renumber work is fully shipped (pushed to `origin/main`). The next session starts a separate work block: the v1.4.2 UNTIL-serialization hotfix.

<current_state>
Working tree clean. `main` at `9515f099`, pushed and synced with origin. No `.planning/phases/` active. OmniFocus parent task `mm_b-pw4bYq` ("[Bug] UNTIL repetitionRule serialization — pre-flight + fix") is `sequential` with one of its two subtasks now complete:
- ✅ `cvk9dw989E3` — Renumber future milestones to clean semver (this session's work)
- ⏳ `hJqHF2GJAG3` — Fix "UNTIL must match" bug → ship as v1.4.2 (next session)

The sequential parent means the hotfix subtask is now unblocked.
</current_state>

<completed_work>

This session: 20 atomic commits implementing the milestone renumber plan. Plus full check-all green and pushed.

| # | Commit | Summary |
|---|--------|---------|
| 1 | `16f6b44f` | docs: add versioning policy |
| 2 | `a0938427` | chore: pure rename MILESTONE-v1.7.md → v1.8.md |
| 3 | `66095097` | chore: pure rename MILESTONE-v1.6.1.md → v1.7.md |
| 4 | `7854feb8` | docs: update MILESTONE-v1.8.md heading |
| 5 | `973af3c8` | docs: update MILESTONE-v1.7.md heading |
| 6 | `8e63b740` | docs: update ROADMAP.md |
| 7 | `3641fe55` | docs: update PROJECT.md |
| 8 | `46785f7c` | docs: update STATE.md |
| 9 | `54b4e6cd` | docs: update MILESTONES.md |
| 10 | `dbda0af1` | docs: update README roadmap |
| 11 | `690e38db` | docs: update PROJECT-BRIEF |
| 12 | `832d17de` | docs: sync trigger scope in v1.6 + postpone-review Q in v1.5 |
| 13 | `d350db87` | docs: update seeds |
| 14 | `39ca9a68` | refactor: PROP-07 'deferred to v1.7' → v1.5 (correct domain) |
| 15 | `0a147570` | docs: strikethrough Phase 56 archive (7 files) |
| 16 | `19dfa646` | docs: strikethrough v1.4-era archive (mixed v1.5/v1.8) |
| 17 | `92318cdc` | docs: update pending todos |
| 18 | `d57b12e7` | docs: free v1.4.2 slot |
| 19 | `8ef71aa0` | docs: 'run plugin action' as MAYBE-IDEA |
| 20 | `9515f099` | docs: post-audit cleanup — docs/index.html |

Verification: `pytest 2558 passed (97.52%)`, `vitest 85 passed`, `ruff clean`, `mypy clean`. Subagent audits (investigator + reviewer) clean.

Pushed: `6aa2c382..9515f099` on main.
</completed_work>

<remaining_work>

**Not part of this work block — captured for the NEXT session:**

- Fix `repetitionRule.end.date` serialization bug. Error symptom: `"UNTIL must match YYYYMMDDTHHMMSSZ format, got '20260516'"`. The `end.date` field serializes as date-only instead of full UTC datetime when constructing the UNTIL RRULE component.
- Ship the fix as **v1.4.2** (slot is reserved and empty, ready for it).
- OmniFocus task `hJqHF2GJAG3` has full bug context.

This is **not** the immediate-next-action for this session — the session is over. It's the starting point for the next session.
</remaining_work>

<decisions_made>

- **Strict semver from v1.5 onward.** `X.Y.0` for features, `X.Y.Z` reserved for true patches only, `X.0.0` for breaking changes. Pre-v1.5 history not retroactively renumbered.
- **Shipped milestones (v1.0..v1.4.1) are sacred.** Git tags, CHANGELOG, PyPI history stay as-shipped.
- **PROP-07 test refs corrected to v1.5, not v1.8.** Plan said mechanically `v1.7 → v1.8` but project writes land at v1.5 in the new ordering. User confirmed correct domain. Lesson: mechanical rename plans need a domain-check pass before execution.
- **Pure rename commits contain ZERO content edits.** Preserves git's 50%-similarity rename heuristic. Verified R100 on both renames; `git log --follow` traces history cleanly.
- **Sync Trigger folded into v1.6** (same bridge surface as UI ops). Not a standalone milestone.
- **Smart Perspective demoted to v1.7** (after v1.6 UI). Two-step (show_perspective then list_tasks) is sufficient for review workflow.
- **Postpone-review API shape** captured as open design question in v1.5 spec (actions block vs. editable `nextReviewDate` field). Resolve during v1.5 planning, not now.
- **Versioning policy in two places**: canonical in `CONTRIBUTING.md § Versioning`, one-liner pointer in `CLAUDE.md` for agent discoverability.
- **TaskPaper Output stays in `MAYBE-IDEAS.md` with no version slot.** v1.4.2 slot freed for the hotfix.
- **Audit trail via markdown strikethrough** in archives: `~~v1.7~~ v1.5` or `~~v1.7~~ v1.8` per domain. Active forward-looking material gets plain rewrites.
</decisions_made>

<blockers>
None. Working tree clean, branch pushed, OmniFocus task chain advanced.
</blockers>

## Required Reading (when resuming for v1.4.2 hotfix)

1. **OmniFocus task `hJqHF2GJAG3`** — full bug context lives there (symptom, repro, expected behavior).
2. `.research/updated-spec/MILESTONE-v1.5.md` — postpone-review open question may surface during planning the eventual v1.5; not relevant for the hotfix itself.
3. `CONTRIBUTING.md § Versioning` — confirms `X.Y.Z` slot is patch-only; hotfix qualifies.
4. `src/omnifocus_operator/repository/rrule/serialize.py` — likely location of the UNTIL serialization bug.
5. `src/omnifocus_operator/repository/rrule/builder.py` and `rrule/parser.py` — adjacent serialization surface.

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Mechanical rename without domain check | Plan instructed `v1.7 → v1.8` substitution everywhere, but PROP-07 tests defer **project writes** which land at v1.5, not v1.8. Blind substitution would have planted false signals. | advisory | Before any sweeping rename, verify the domain the reference belongs to. Tests with phase-coupled assertions need per-ref domain analysis. |
| `git mv` + content edit in same commit | Collapses git's rename-detection heuristic → file appears as `delete + add`, breaking `git log --follow` and PR diffs. | advisory | Pure rename commits contain ZERO content edits. Heading/body updates land in a follow-up commit only after the rename commit is in. Verify with `git log --name-status -M` showing `R100`. |
| Subagent direction confusion | `cavecrew-investigator` flagged PROJECT.md and ROADMAP.md as wrong because it had the renumber direction backwards. Trust-but-verify cuts both ways. | advisory | Investigator findings are signals, not verdicts. Cross-check before acting. |

## Infrastructure State

- Git: branch `main`, HEAD `9515f099`, working tree clean, in sync with `origin/main`.
- Tests: 2558 pytest + 85 vitest passing. Coverage 97.52%.
- OmniFocus: parent task `mm_b-pw4bYq` is sequential. Renumber subtask `cvk9dw989E3` complete; hotfix subtask `hJqHF2GJAG3` unblocked and ready.
- PyPI: no new release in this session (renumber is docs/scope only, no git tag, no `_version.py` change).

## Milestone State (post-renumber)

| Slot | Status | Notes |
|------|--------|-------|
| v1.4.2 | **RESERVED** | for UNTIL-serialization hotfix (next session) |
| v1.5 | Planned | Project Writes — postpone-review API shape is the live design question |
| v1.6 | Planned | UI & Perspectives + Sync Trigger folded scope |
| v1.7 | Rough intention | Smart Perspective Content |
| v1.8 | Planned | Production Hardening |

<context>
This session executed a long, multi-phase plan (provided by the user in plan-mode form). The plan was clean and well-structured but had one mechanical error (PROP-07 v1.7 → v1.8 substitution) that I caught by reading the test domain. User-confirmed correction. Otherwise execution was straightforward: 11 phases, 20 atomic commits, two subagent audits, full check-all green, pushed.

The visual-explainer skill rendered a before/after page at `~/.agent/diagrams/milestone-renumber-before-after.html` for big-picture review.

Caveman mode (full) was active throughout. Stayed in caveman for prose updates; normal language for code/commits/security/UAT confirmations as the mode allows.
</context>

<next_action>
**For the resuming agent / next session:**

1. Read OmniFocus task `hJqHF2GJAG3` for full bug context.
2. Reproduce the bug locally:
   ```
   add_tasks with repetitionRule containing end.date = "2026-05-16" (date-only)
   ```
   Expected serialization to produce `UNTIL=20260516T000000Z` (full UTC datetime). Actual: `UNTIL=20260516` (date-only), which OmniFocus rejects with `"UNTIL must match YYYYMMDDTHHMMSSZ format"`.
3. Locate the serialization defect in `src/omnifocus_operator/repository/rrule/serialize.py` (most likely) or adjacent files.
4. Fix + test + ship as v1.4.2 (clean patch slot, semver-compliant). Bump version, tag, release on PyPI.
5. Mark OmniFocus subtask `hJqHF2GJAG3` complete when shipped.

The v1.4.2 slot is empty and reserved — no scope conflict. Pure patch.
</next_action>
